# Voice desk — design loop

**Skill:** `ingest-is-steps` `DESIGN: yes` · `cinematic-recipe` · `slice-build`  
**Tapes:** `whIp1SOahOM` (caption LEARNED) · `ud7wzdiM0gk` (caption-only this sit) · `YK-qJwmwjVc` (caption-only · Fable operate-never)  
**Surface:** existing `obsidianOS`, not a circuit-board clone.

## Bible

void `#08090c` · paper `#e9e4d8` · cream `#f0eadd` · amber `#e0a24a` · rose `#c4674f` · live `#7dcea0` · Newsreader + Outfit + IBM Plex Mono.

## Cinematic refs (named)

1. **Our desk** `obsidianOS/index.html` — cloud glass, wisps, star dust, cream glance. WHY: same OS, not a second home.
2. **Vale 4840** stacking desks / FAQ lock — WHY: motion has a job (lock the ask card).
3. **Live screen as hero video** (`ud7wzdiM0gk` “how Jarvis sees my screen”) — WHY: hero is real shared pixels, not fake 3D / WebGL.

CTA before scroll: **Share screen · talk** sits on the hero.

## Two distinct, then pick

1. **Cloud glass (PICK)** — live screen hero, hold-to-talk, bus card, amber ask lock. Ships as `voice.html`.
2. **Editorial paper (REJECT)** — cream daylight full-bleed type, no glass, no live video. Readable, not the land. Same reject as command-center pick 4.

Glowing HUD / Jarvis chrome stay off. Desk is the product name, never glass.
Hands are a desk control (pill + amber armed banner), not a second skin.

## Four pieces / three doors

- persona: hive desk (Evens’s OS)
- voice: local SpeechSynthesis (ElevenLabs ASK)
- knowledge: adopted vault + repo + session brief
- tools: hive skills + file sandbox + queued browse/watch + ASK-armed mouse
- door this sitting: **test UI** on `127.0.0.1:4018`. Widget / phone parked. Hands wired after Yes.
