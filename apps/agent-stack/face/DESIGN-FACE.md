# Agent-stack face — design loop

**Skill:** `cinematic-recipe` · `ingest-is-steps` `DESIGN: yes` · `observe-pane`  
**Surface:** `apps/agent-stack/face/pane.html` via `127.0.0.1:4018`  
**Not:** evenslouis.ca · a second hive · Jarvis chrome · WebGL · vendored `ai-visualizer`

## Bible (same desk)

void `#08090c` · paper `#e9e4d8` · cream `#f0eadd` · amber `#e0a24a` · rose `#c4674f` · live `#7dcea0` · Newsreader + Outfit + IBM Plex Mono.

## Three named refs

1. **Our desk** `obsidianOS` — cloud glass, wisps, star dust. WHY: same OS, not a second home.
2. **Vale 4840** stacking desks / FAQ lock — WHY: yellow ASK has a job (lock the card).
3. **Live bus as hero** (fullstack-agent Phase 4: the voice writes notes; the face reads them) — WHY: the presence *is* the bus phase, not a fake 3D mesh.

CTA before scroll: **LIVE / MUTE** (always-on). Type is fallback. Observe rows stay secondary.

## Two distinct, then pick

1. **Cloud-glass presence (PICK, doubled-down)** — large living canvas wisps + breathing orb react to idle / listen / think / speak. Observe-pane is a second card, not the hero. Ships as `pane.html`.
2. **Editorial paper (REJECT)** — cream daylight full-bleed type, no glass, no live bus. Same reject as command-center pick 4.

Hands stay parked. Desk is the product name, never glass. No WebGL default.
