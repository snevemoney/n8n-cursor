---
name: assume-it-will-touch
description: >-
  Permissions are tools, not vibes. If it can read it, assume it will.
  Bypass / allow-all on the lead flows downhill. Use when spawning
  desks, assigning MCP/tools, or writing “don’t” in a prompt.
  Cursor plus Grok Bot.
---

# Assume it will touch (Cursor)

Load `scripts/hive/grok-skills/assume-it-will-touch.md` and follow it.

**Card:** `ALLOW` · `DENY` · `TERRITORY` · `MAX-TURNS` · `BYPASS: none`.  
**Wired job:** `hive-spawn-desks` / tape-self-teach — write only the named take file.  
**SSOT:** `AGENT_TOOL_INVENTORY.json`. Prompt “don’t” is not a lock.

If Send is on the list, assume it fires → `send-removed`. Instance MCP stays NEVER.  
Hard step: send / pay / deploy / book / publish stay Evens.

Grok `/` copy: `~/.grokbot/skills/assume-it-will-touch/SKILL.md`.
