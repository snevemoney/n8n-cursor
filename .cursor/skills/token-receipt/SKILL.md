---
name: token-receipt
description: >-
  A billed run writes tokens + duration + correctness. Tape $ stays
  UNVERIFIED. Use on coverage-loop, money-desk PASS/HOLD, or any
  metered agent run. Cursor plus Grok Bot.
---

# Token receipt (Cursor)

Load `scripts/hive/grok-skills/token-receipt.md` and follow it.

**Card:** `TOKENS` · `DURATION` · `CORRECTNESS` — COST without this is a vibe.  
**Wired job:** `coverage-loop` + Money Desk PASS/HOLD.  
**CLI:** `python3 scripts/hive/hive-state.py receipt --tokens unknown --duration "this session" --correctness untested`

Tape $ UNVERIFIED. Do not fill unit-econ from a reel.  
Hard step: pay / refund / fee stay Evens.

Grok `/` copy: `~/.grokbot/skills/token-receipt/SKILL.md`.
