---
name: ingest-is-steps
description: >-
  Ingest (especially a build tape) is a numbered procedure to walk.
  If they built a UI and used design skills, run that process on
  Cursor + Grok. Use when studying a build tape, stealing a UI, or
  the operator says ingest is steps / steal their design process.
---

# Ingest is steps (Cursor)

Load `scripts/hive/grok-skills/ingest-is-steps.md` and follow it.

**Card:** `TAPE-KIND` · `D-STEPS` · `DESIGN` · `HIVE-HOST: Cursor + Grok` · `CAPTION: spoken steps only`.  
**Parents:** `deep-video-learning` (write numbered D) · `slice-build` (walk D before implement).  
**UI design on tape:** refs + N mockups + pick + double-down. Floor = **two distinctly different** variants (`FFWtxjvW2ts`). Do not skip to one HTML. Site bite cites D1–D7.  
**Design lane (2026-08-16 · business-operations-and-beautiful-webapps):** UI/design signal → same packet / atoms / LEARNED plus a DESIGN block (refs → N mockups → pick → double-down). Not a second skill.  
**HOW THEY BUILT (every build tape):** product/tool/workflow/website · steps they showed · tools on-tape vs ours · factory emit. See `SIGNAL-BUILDS.md`. DESIGN still required when UI.  
**Demonstration LEARNED requires** a capability card + ACTION TRACE (timestamps / state transitions / UNKNOWN blockers). UNKNOWN allowed. Do not invent clicks.

Hard step: send / pay / deploy / book / publish stay Evens. Claude Design / Claude Code stay on-tape.

Grok `/` copy: `~/.grokbot/skills/ingest-is-steps/SKILL.md`.
