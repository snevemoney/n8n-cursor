---
name: deep-video-learning
description: >-
  Reconstruct a speaker’s knowledge from a full video transcript (A–K),
  then steal the machine. Use when studying a tape, tape-self-teach,
  channel-walk teach, coverage-loop teach, or when the operator says
  deep video learning / study this video / learn this tape. Not a
  summary. Not understand-only. Not steal-first.
---

# Deep video learning (Cursor)

Load `scripts/hive/grok-skills/deep-video-learning.md` and follow it.

**In:** one `full.txt` (packet `{video_id}`). Complete transcript. Role does not filter what you learn.

**Order:** A–K globally → **Steal / Operate-never** (informed by A–K) → L last.

**Write:**
- Desk: `docs/hive/outer-heaven/CONTENT/job-cards/takes/{video_id}/{slug}.md` — header + A–L + Steal / Operate-never
- Global: Researcher + Librarian merge A–K **and** stolen machines into `docs/hive/outer-heaven/CONTENT/watch-later/packets/{id}/LEARNED.md` (no L; keep disagreements)
- Atoms: Researcher emits `docs/hive/outer-heaven/CONTENT/knowledge/atoms/by-video/{id}.jsonl` (structured extract, not a `full.txt` replacement)

A–L also ask: shown, clicked, sequenced, failed, retried, implicit, speech≠behavior. Caption-only: no invented clicks (`transcript-implied` / `unobserved` / `UNKNOWN`). Multimodal protocol: `multimodal-youtube-learning`. After steal, atoms go to layer 2, then `capability-acquisition` last-mile: distinct SKILL.md **or** labeled merge; one named workflow; reproduce card; THINK/BEHAVE/TRICKS/USE. Remap-as-done forbidden. Not 147→1.

Steal the machine even from ugly tapes. Operate ≠ learn. Hard steps HITL. Clients parked. Tape $ UNVERIFIED. Stack Cursor + Grok. Do not re-walk the 82. Do not dump the 146.

**Never:** understand-only · steal-first / skip transcript · one-line skim steal · merge LESSONS · invent clicks from captions · Frankenstein workflow · send / pay / deploy / book / publish
