---
name: api-macro-vision
description: >-
  API first (fast/cheap/consistent) → deterministic pixel macro if the
  path never changes → headed vision last. Use before a vision or
  computer-use agent, or when Forge opens a browser. Cursor plus Grok Bot.
---

# API → macro → vision (Cursor)

Load `scripts/hive/grok-skills/api-macro-vision.md` and follow it.

**Card:** `LADDER` · `WHY` · `WATCH` — write before a vision agent.  
**Wired job:** Forge `slice-build` / `click-live-site` intake.  
**After vision:** `verify-after-browser`. No Codex.

Headed browse: IF Cursor → `cursor-ide-browser`. IF Grok Bot → Grok Bot’s web browser (do not call Cursor MCP). Not Playwright / Chrome / browser-use.  
X-browser-despite-API stays a labeled exception. No headed send/pay/publish.  
Hard step: send / pay / deploy / book / publish stay Evens.

Grok `/` copy: `~/.grokbot/skills/api-macro-vision/SKILL.md`.
