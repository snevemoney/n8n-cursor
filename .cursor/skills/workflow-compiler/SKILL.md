---
name: workflow-compiler
description: >-
  Compile the smallest project workflow from the hive knowledge
  system without blending sources. Use when the operator names a
  project and asks to compile a workflow, or after a coverage map
  exists. Classify and decompose before retrieve. Then three audits.
  Not a dump of 146 tapes. Cursor plus Grok only.
---

# Workflow compiler (Cursor)

Load `scripts/hive/grok-skills/workflow-compiler.md` and follow it.

**In:** a named project (outcome + constraints) **or** one tape machine after A–K. One tape → one (or two) workflows. Not a parked ICP card. Not 147→1.  
**Order:** classify → decompose (no retrieve) → coverage map → retrieve patterns then atoms then dissent then examples → apply conditions → local `IF` synthesize → smallest workflow → `knowledge-audit`.  
**Write:** `docs/hive/outer-heaven/CONTENT/knowledge/workflows/{project_id}.md`  
**Provenance:** WORKFLOW → PATTERN → ATOMS → TRANSCRIPT.

Do not blend. Do not average incompatible tactics. Clients parked. Hard steps HITL.
