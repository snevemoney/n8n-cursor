---
name: side-effect-not-essay
description: >-
  Chat-refine can leave the system prompt unchanged. OAuth connected ≠
  tool called. Ship check = external side effect + prompt diff.
  Use on Watchdog smoke and any wrapper that says it learned.
  Cursor plus Grok Bot.
---

# Side effect, not essay (Cursor)

Load `scripts/hive/grok-skills/side-effect-not-essay.md` and follow it.

**Card:** `CLAIM` · `SIDE-EFFECT` · `DIFF` — essay without an artifact is a fail.  
**Wired job:** Watchdog smoke on `golden-test-loop` / `click-live-site`.  
**Verifier:** Watchdog fills this card, not Forge (`separate-verifier`).

OAuth connected ≠ tool called. Chat-refine can lie.  
Hard step: send / pay / deploy / book / publish stay Evens.

Grok `/` copy: `~/.grokbot/skills/side-effect-not-essay/SKILL.md`.
