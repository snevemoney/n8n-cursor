---
name: coverage-loop
description: >-
  Coverage loop: teach from a tape or score a business-model row, then
  spawn / extract / score / wire ONE system / dry-run / pick next. Use when
  the operator says loop the agents, coverage loop, until they can do them
  all, or drops a new YouTube URL with that goal. A channel handle or
  “new YouTube URL / channel” starts at `channel-walk` (one tape), then
  this loop. 24/7 means learn + dry-run + score, not auto-money.
  Cursor plus Grok Bot.
---

# Coverage loop (Cursor)

Load `scripts/hive/grok-skills/coverage-loop.md` and follow it.  
Load `checkable-stop` first. Write DONE-CHECK + CAP + COST before stages. Cap = 1 tape or 1 model row this session.  
If a desk clicks this session, load `verify-after-browser` and write ACT / EXPECTED / OBSERVED / COMPARE / NEXT. IF Cursor → `cursor-ide-browser`. IF Grok Bot → Grok Bot web browser. Caption-only: do not invent clicks.  
After the iteration: `state-json` log-run + `token-receipt` on last_run. `/loop` needs `hosted-neq-scheduled` (WAKE=human-run unless Evens names the interval). Watchdog grades any ship (`separate-verifier`).

**In:** a BUSINESS-MODEL-FIT row **or** a new YouTube tape. Channel / new YouTube URL as a *queue* → `channel-walk` first (one tape).  
**Stages:** Teach (`deep-video-learning` A–K) → Spawn (parent; `hive-spawn-desks --video-id`) → Steal (informed by A–K) → Atoms (layer 2) → Capability + last-mile (distinct SKILL.md **or** labeled merge; one named workflow; reproduce card; THINK/BEHAVE/TRICKS/USE) → Score → Wire **ONE** → Dry-run (draft only) → Loop. Remap-as-done forbidden. Not 147→1.  
**Hard step:** send / pay / deploy / book / publish stay Evens.

**Pick next:** clients parked (Evens skipped 2026-08-14) — no Path A this week. Lowest Our-now among Yes/Adjacent, or next `@nateherk` tape via `channel-walk`. Park only if Evens stars it. Do not unpark Normand.

**Cursor `/loop`:** may run this skill on an interval Evens sets. Do not arm unless he names the interval.

Grok `/` copy: `~/.grokbot/skills/coverage-loop/SKILL.md`.
