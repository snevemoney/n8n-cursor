---
name: website-emit-from-ref
description: >-
  Run the emit walk. Q4 detector decides clone / reconstruct-from-trace /
  file-a-reference — 1, 2, or all 3 can fire in one sitting for different
  jobs. Website emits land in website-assets. Not any-repo / any-skill → landing.
---

# Website emit from ref (Cursor)

Load `scripts/hive/grok-skills/website-emit-from-ref.md` and follow it.

**Owner:** Forge (clone + file) · Creative Studio (identity + verticals).  
**Status:** WIRED 2026-08-17 from `86HM0RUWhCk` + grill Q1–Q9. Q4 detector locked same day (`Tliio-33w4g`). Not accepted forever.  
**Landing repo:** `docs/hive/outer-heaven/CONTENT/creative/website-assets/` (monorepo folder — no nested `git init`).  
**Always-on:** `cinematic-recipe` on a website bite only. Q4 **detector** (not ASK-then-pick-ONE). Install only if ASK + necessary-to-implement. Watchdog GRADE is a later sitting. TESTING ≠ PROVEN. `website-assets` is for **website emits only** (buyable options). Tools → `tool-captures/`. Skills → `skill-candidates/`.

**HOLD (`FFWtxjvW2ts`):** clone-competitor-feel stays parked until Evens names a Path A client.

Hard step: send / pay / deploy / book / publish stay Evens. Claude Code / Claude Design stay on-tape.

Grok `/` copy: `~/.grokbot/skills/website-emit-from-ref/SKILL.md`.
