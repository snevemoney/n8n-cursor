---
name: hosted-neq-scheduled
description: >-
  Cloud sandbox ≠ always-on. Classify the wake primitive (event /
  cadence / human-run) before approving 24/7 or a new host. Use when
  someone proposes overnight, laptop-off, managed agents, or cron.
  Cursor plus Grok Bot.
---

# Hosted ≠ scheduled (Cursor)

Load `scripts/hive/grok-skills/hosted-neq-scheduled.md` and follow it.

**Card:** `WAKE` · `HOST` · `SCHEDULE` · `RUN-NOW` — default no new host.  
**Wired job:** Big Boss intake + Day Planner cadence + coverage-loop `/loop`.  
**Refuse:** Anthropic routines / managed agents / Trigger.dev.

Cloud ≠ 24/7. Run-now before schedule. `/loop` only if Evens names the interval.  
Hard step: send / pay / deploy / book / publish stay Evens.

Grok `/` copy: `~/.grokbot/skills/hosted-neq-scheduled/SKILL.md`.
