{
  "name": "Hive Golden Path Smoke Notify",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "hive-golden-path-smoke",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook-trigger",
      "name": "Golden Path Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [200, 400],
      "webhookId": "hive-golden-path-smoke",
      "notes": "G3 Scorpion/smoke still matters. JSON SSOT path is hive-golden-path-smoke (not hive-smoke-notify). Canonical https://evenslouis.ca/webhook/hive-golden-path-smoke — never n8ncloud.tech."
    },
    {
      "parameters": {
        "jsCode": "// G3 golden-path smoke webhook. Scorpion register-outcome is optional audit.\n// Daily OS = Grok Bot + Cursor. n8n is legacy webhook glue, not a second daily OS.\n// Lane: hive-os. G1 Telegram is soft/legacy — this workflow does not send Telegram.\nconst body = $json.body || $json;\n\nconst correlationId = body.correlationId || body.correlation_id || `gp_${Date.now()}`;\nconst message = body.message || body.summary || 'Golden path smoke';\nconst serviceName = body.serviceName || body.service_name || body.name || 'hive-golden-path-smoke';\n\nconst registerPayload = {\n  correlationId,\n  jobType: body.jobType || body.job_type || 'hive.golden_path.smoke',\n  goal: body.goal || 'G3 smoke-test registration',\n  source: body.source || 'hive-golden-path',\n  status: body.status || 'healthy',\n  registerTo: 'scorpion',\n  summary: message,\n  metadata: {\n    lane: 'hive-os',\n    serviceName,\n    version: body.version || '1.0.0',\n    host: 'evenslouis.ca',\n    triggeredAt: new Date().toISOString(),\n    telegramRequired: 'false',\n    g1Telegram: 'soft-legacy',\n    g3Smoke: 'required-legacy'\n  }\n};\n\nreturn [{\n  json: {\n    registerPayload,\n    originalInput: body\n  }\n}];"
      },
      "id": "build-register-payload",
      "name": "Build Register Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [450, 400]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://evenslouis.ca/scorpion/api/hive/register",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "X-Hive-Secret",
              "value": "={{ $env.HIVE_WEBHOOK_SECRET }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "json",
        "jsonBody": "={{ JSON.stringify($json.registerPayload) }}",
        "options": {
          "timeout": 10000
        }
      },
      "id": "register-scorpion",
      "name": "Register Scorpion",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [700, 400],
      "continueOnFail": true,
      "notes": "Legacy optional audit (Scorpion register-outcome). Daily notify is Grok Watchdog / Grok chat. Not required. continueOnFail. No new secret."
    },
    {
      "parameters": {
        "jsCode": "// Scorpion register-outcome is legacy optional audit — do not fail G3 smoke on 401/5xx.\n// Daily notify is Grok Watchdog / Grok chat. Telegram is not used here.\nconst result = $input.first().json;\nconst statusCode = result.statusCode || result.code || 200;\nconst success = statusCode >= 200 && statusCode < 300;\nconst sent = $('Build Register Payload').first().json.registerPayload;\n\nreturn [{\n  json: {\n    ok: true,\n    webhook: 'hive-golden-path-smoke',\n    notify: 'grok-watchdog-chat',\n    registered: !!success,\n    auditOptional: true,\n    statusCode,\n    correlationId: sent.correlationId,\n    message: success ? (result.data?.message || result.message || 'OK') : (result.message || result.error || 'Scorpion audit skipped/failed — not a notify failure'),\n    hint: '401 on Scorpion register-outcome is operator HITL only if you care about audit. Do not invent credentials. Do not treat Scorpion as OS.'\n  }\n}];"
      },
      "id": "evaluate-result",
      "name": "Evaluate Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [950, 400]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $json }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond-webhook",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1200, 400]
    }
  ],
  "connections": {
    "Golden Path Webhook": {
      "main": [
        [
          { "node": "Build Register Payload", "type": "main", "index": 0 }
        ]
      ]
    },
    "Build Register Payload": {
      "main": [
        [
          { "node": "Register Scorpion", "type": "main", "index": 0 }
        ]
      ]
    },
    "Register Scorpion": {
      "main": [
        [
          { "node": "Evaluate Result", "type": "main", "index": 0 }
        ]
      ]
    },
    "Evaluate Result": {
      "main": [
        [
          { "node": "Respond", "type": "main", "index": 0 }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "id": "TyxDfyLVDtxgqHfC",
  "meta": {
    "instanceId": "hive-ops",
    "notes": "G3 smoke webhook. Daily notify = Grok Watchdog / Grok chat. Scorpion register-outcome is optional audit. Telegram unused. Never n8ncloud.tech."
  },
  "tags": [
    { "name": "hive" },
    { "name": "golden-path" },
    { "name": "hive-os" },
    { "name": "g3-smoke" },
    { "name": "legacy-fallback" }
  ]
}
