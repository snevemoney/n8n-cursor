---
tags: [os, factory, hitl, automation, doctrine]
at: 2026-08-25
desk: hitl-operator
machine: dark-factory
status: filed · lock · unpublished
send: removed
clock: parked
---

# Cloud host is git

Evens: “not thinking about the context and the long term was your mistake.”

The first Hive daily TRAIN run (PR 47) halted correctly and then invented `factory-os-train-plane` because we designed six Cloud Automations as if they inherit this Mac chat, untracked hive drafts, and `docs/hive/.../dry-runs/` leftovers. A Cloud run is a **cold checkout of `origin/main`** + GitHub persist. No Cursor transcripts. No Grok `/workspace`. No untracked pack. Inventing a slug was the predictable failure.

This file is the lock. Paste memories and the six job pastes must quote it. Do not treat a path as a one-off hack.

## Locks (forever)

1. **HOST = git.** If a desk must read it, it is on `origin/main` at a **single SSOT path**. No split that drifts.
2. **Context = committed briefs + memories**, not live chats. Cloud will never see sitting JSONL (`01a6b95f` or any other). `hitl/briefs/INDEX.md` + **one** brief per job is the long-term store. Grow that. Do not dump transcripts.
3. **Pick is a real packet id** already on `main` (`full.txt` + LEARNED `## ACTION TRACE`) or **TRIAGE with no invented slug**. Never mint `factory-os-train-plane`.
4. **Tools match the host.** GitHub = persist. Slack never. `browser-use` = Mac stdio (`uvx`) only — never the Cloud web plan. Do not attach tools that only exist on this laptop.
5. **Actuate order forever:** files on `main` → then Automation. Never the reverse. Pack / pastes / job cards / retrieve / board / pick / the one TRAIN packet land **before** the next daily run is expected to work.
6. **Merge ≠ ship.** Close invent PRs. A fix lands as its own PR to `main`. Evens merges.

## SSOT (the only living desk tree)

```
desk-missions-now/          ← repo root. Cloud checkout = git root. Six jobs read/write here.
```

| Job | Reads / writes |
|-----|----------------|
| Hive daily TRAIN | `researcher/NEXT-TRAIN-PICK.md` · `watchdog/hold-outs/` · `forge/SIGNAL-TRAIN-*.md` |
| Watchdog GRADE sitting | `separate-verifier.md` · job card · `watchdog/hold-outs/` · `watchdog/{id}-GRADE.md` |
| Researcher next-row pick | `researcher/NEXT-TRAIN-PICK.md` · `scripts/hive/os/signal-retrieve.py` |
| Goal/gap board refresh | `hitl/GOAL-GAP-BOARD.md` |
| Factory-OS reminder | `forge/FACTORY-OS-NEXT.md` |
| HITL inbox | `hitl/INBOX.md` |

Also on `main` because desks cite them:

- `docs/hive/outer-heaven/CONTENT/job-cards/{forge,watchdog,researcher,hitl-operator}.md`
- `scripts/hive/os/signal-retrieve.py`
- `scripts/hive/grok-skills/checkable-stop.md`
- `scripts/hive/grok-skills/separate-verifier.md`
- one TRAIN packet under `docs/hive/outer-heaven/CONTENT/watch-later/packets/{id}/` (`full.txt` + `LEARNED.md` with ACTION TRACE)

**Not SSOT:** `docs/hive/outer-heaven/CONTENT/knowledge/workflows/dry-runs/desk-missions-now/`. That folder is leftover / archive. One pointer README only. Do not grow a second pack there. Do not create a third tree.

## Missing path

Missing brief / packet / script on this checkout → **TRIAGE** + `EVENS:` leftover + stop. Do not invent `full.txt`, `/workspace` trees, live chats, or a walkable id.

## Retired

`factory-os-train-plane` is not a packet, not a YouTube id, not a TRAIN row. PR 47 invented it. Do not merge that pick. Do not write hold-outs for it.

## This stretch (Evens 2026-08-25)

This stretch = **build the system** (Factory OS / dark-factory process). **Not** a sold product, not Path C emit, not a buyer site, not a virtual-CTO SKU.
Grill already: this week Factory OS, no buyer surface. Do not drift into Path A/C / buyer surface / website SKU.
Next work = host hygiene (Mine one-of-each GitHub jobs, files on `origin/main`, Grok reader on Mac, TRAIN only if Evens names an id). Not a new public surface.

## Yellow

`grokbot_orphans` = **8**. Named, continue. Do not restore.

[[HIVE-AUTOMATION-PACK]] · [[memories]] · [[NEXT-TRAIN-PICK]]
