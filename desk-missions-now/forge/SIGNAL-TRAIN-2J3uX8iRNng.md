---
tags: [os, factory, forge, signal-train]
at: 2026-08-29
desk: forge
machine: dark-factory
class: signal-train
id: 2J3uX8iRNng
status: filed · Forge attempt · OBSERVED only · unpublished · not PROVEN · waiting Watchdog GRADE
---

# SIGNAL-TRAIN-2J3uX8iRNng — Forge attempt (OBSERVED)

**Desk:** Forge · sitting 2026-08-29 (Hive daily TRAIN, Cloud)  
**Packet:** `2J3uX8iRNng` · https://www.youtube.com/watch?v=2J3uX8iRNng  
**Theme (retrieve):** `eval-harness`  
**Spoken machine:** `same-prompt-bench`  
**TASK:** Same prompt, two labeled runs, write the stop first, referee the patch; if wrong blame context then tokens then model. Steal the stop, not a Claude/Fable bakeoff.  
**Forge does not fill GRADE.** Watchdog hold-outs already exist as [PR 89](https://github.com/snevemoney/n8n-cursor/pull/89) (`desk-missions-now/watchdog/hold-outs/2J3uX8iRNng.md`). This file is not that exam and was not scored against it. Do not remint. Clock parked.

```
DONE-CHECK: TASK named from the packet · attempt file on desk-missions-now/forge/ · retrieve ran (theme or NONE) · cheap trusted check exited · no remint · no live send/POST · no paper-PROVEN
CAP: 1
COST: this sitting
STOP-KIND: cap + done-check
```

```
ALLOW: this attempt · signal-retrieve · ≤3 local refs · existing golden-path fixture / product-state / checkable-stop · OBSERVED
DENY: send / pay / deploy / book / publish · self-GRADE · paper-PROVEN · remint 325 · rewrite hold-outs · invent clicks · billed Whisper · Claude / Fable / n8n-as-host · flip COVERAGE_LEDGER.json
BYPASS: none
```

Yellow (named, continue): `drift.grokbot_orphans` = **8**. Unrelated. Clock parked.

---

## OBSERVE (spoken machine only)

Caption-only packet. `watch.json` = **ABSENT**. Visual / click / on-screen prompts = **UNKNOWN**. Did not invent clicks. Did not watch headed. Did not bill Whisper.

`full.txt` is real speech (opens on “All right, so Claude Opus 5 is here…”), **6825** words. `LEARNED.md` has **ACTION TRACE** at line **150**. Gate = **TRAIN**.

Skipped: `kwSVtQ7dziU` (TRAIN-1 PASS) · `X80ljdCPM_U` (TRAIN-2 PASS) · `karpathy-wiki-nate-herk` (no ACTION TRACE) · 1803 walk · `/workspace` · 4823 restyle · invented `factory-os-train-plane`.

On-tape vendors (Claude Opus / Fable / Claude Code / Codex referee / unattended `/goal`) stay on-tape. Hive remap already named: `same-prompt-bench` (merge into `golden-test-loop`, do not mint). Skill file **OK** on this checkout.

Tape $ / minutes / token counts / bench charts = **UNVERIFIED**. Not quoted as FACT.

---

## RECONSTRUCT

| Field | This sitting |
|-------|----------------|
| Macro-action | Reproduce the spoken Nate machine **once**: label the run → write the stop → cheap referee → if wrong, files/context first, then tokens, then model. |
| Metric | Evens’ DONE-CHECK (above). Cap = 1. Explicit **no unsupervised loop**. Same-prompt model crown is refuse. |
| Named workers | **Forge** = attempt + OBSERVED. **Watchdog** = hold-outs on PR 89 + later GRADE (other session). **HITL** = hard step DENY. Researcher already named the id (PRs 97/98 today; memories 2026-08-28). |
| Trusted check | Existing cheap eval only. No live webhook POST. No Claude/Fable call. |
| Few bits | Below. Not a lecture rewrite. |
| Deny-list | Mail / money / identity / send-pay-deploy-book-publish ungranted. Claude/Fable as hive. Quote the sheet. |

Retrieve (TASK matched `eval-harness`; ran on this checkout):

```
THEME: eval-harness
1. Karpathy · Macro-action + metric → named workers → trusted check → human keeps the few bits. · docs/hive/outer-heaven/CONTENT/watch-later/packets/kwSVtQ7dziU/LEARNED.md
2. Nate eval · Same prompt, two brains, a score, then wrap tools. Native node can lie. · docs/hive/outer-heaven/CONTENT/watch-later/packets/X80ljdCPM_U/LEARNED.md
3. Same-prompt tape · Verify-until is the story. Your stop condition is the product. · docs/hive/outer-heaven/CONTENT/watch-later/packets/2J3uX8iRNng/LEARNED.md
```

≤3 refs. Opened those three LEARNED paths only (ACTION TRACE headings at lines 275 / 149 / 150). No SIGNAL_INDEX dump. No `/workspace` path. `scripts/hive/grok-skills/golden-test-loop.md` and `eval-then-wrap-tools.md` exist on this checkout; not opened as extra retrieve rows.

Hold-outs file **exists** at `desk-missions-now/watchdog/hold-outs/2J3uX8iRNng.md` on [PR 89](https://github.com/snevemoney/n8n-cursor/pull/89) (name only). **ABSENT** on this cold `origin/main` worktree (HEAD `a7b920377`). Forge did not fetch or read the exam as a scorecard and did not rewrite it.

---

## ATTEMPT (existing host tools — wrap, do not install)

Bite = same-prompt-bench **remap**, not a site, not a skill remint, not a model bakeoff.

**Label the run:** Cloud TRAIN 2026-08-29 · host = git · model = this Cloud Agent (not Opus, not Fable) · packet `2J3uX8iRNng`.

**Write the stop first:** checkable-stop card above. Cap = 1. Until-satisfied absent.

**Referee (cheap, existing):** parse the golden-path fixture + `product-state --validate` + retrieve theme. Not Codex. Not a second billed brain.

**If wrong, blame order:** (1) context/files — hold-outs live on unmerged PR 89, not `origin/main`; (2) tokens — this Cloud run only; (3) model — do not swap vendors.

**Wrap (1–3 on this host):**

1. `scripts/hive/os/signal-retrieve.py` — theme match only.  
2. `workflows/hive/golden-path-smoke-notify.json` — cheap trusted parse (golden-test-loop fixture).  
3. `python3 scripts/hive/product-state.py --validate` + `scripts/hive/grok-skills/checkable-stop.md`.

**Pile denied:** Claude / Fable install · Codex referee buy · unattended `/goal` · quote tape $ · remint `same-prompt-bench` / `golden-test-loop` · auto-send.

Commands this sitting on the `origin/main` worktree (HEAD `a7b920377`):

| Check | Result |
|-------|--------|
| `wc -w` `packets/2J3uX8iRNng/full.txt` | **6825** |
| `head` first line | real speech, not a title stub |
| `rg ^## ACTION TRACE` on this LEARNED | **present** (line 150) |
| `test -f` `watch.json` | **ABSENT** |
| `test -f` hold-outs on this checkout | **ABSENT** (PR 89 only) |
| `python3 scripts/hive/os/signal-retrieve.py --prompt "same-prompt bench verify-until stop condition cheap check"` | THEME **eval-harness** · **3** local files · third ref = this packet |
| `json.loads` `workflows/hive/golden-path-smoke-notify.json` | name Hive Golden Path Smoke Notify · webhook path `hive-golden-path-smoke` · `options.responseMode=responseNode` · code nodes Build Register Payload, Evaluate Result · 5 nodes |
| `python3 scripts/hive/product-state.py --validate` | **OK (6 projects)** |
| `scripts/hive/grok-skills/*.md` count on this checkout | **90** |
| wrap skill files on this checkout | **OK** `golden-test-loop` · `eval-then-wrap-tools` · `checkable-stop` · `ask-principal` · `context-docs` · `send-removed` · `separate-verifier` |

Did **not** POST `/webhook/hive-golden-path-smoke`. Did not install Claude / Fable / Codex. Did not mint. Did not flip `COVERAGE_LEDGER.json`. Did not send mail. Did not write GRADE.

`click-live-site` not used: no living UI on this spoken-machine bite. Visual stays UNKNOWN.

`hive-state.py get --key last_run` (filter one key, not a dump): id 18 · job `login-primitive-slice` · 2026-08-15. Unrelated leftover. Did not log a new run (no factory bite that ships).

---

## Few bits (what the agent cannot invent)

1. Write the stop before you start. Verify-until is the story; the stop is the product.
2. Same prompt ≠ same run. Label the model / host / date or you compare ghosts.
3. Referee the patch with a cheap check, not a vibe.
4. If output is wrong: files/context first, then tokens, then model.
5. Half-price/token ≠ cheaper job. Tape $ stays UNVERIFIED.
6. Steal the stop. Do not crown Claude vs Fable. Hive is Cursor + Grok.
7. Caption-only TRAIN is the spoken machine only. Clicks stay UNKNOWN.

---

## BLOCKED

**none** for the spoken TASK on this host.

Hold-outs **ABSENT** on `origin/main` is a host/context leftover (PR 89 unmerged), not a missing vendor. Workaround = attempt cites the PR path by name only; Watchdog GRADE sitting reads the exam. Do not rewrite hold-outs. Do not remint to close that hole.

UNKNOWN (not blocking): timestamps · visual / click sequence · on-screen prompt bodies · tape $ · Watchdog hold-out contents (unread).

---

## Paths

| Role | Path |
|------|------|
| Hold-outs (Watchdog, unread) | `desk-missions-now/watchdog/hold-outs/2J3uX8iRNng.md` on PR 89; **ABSENT** this checkout |
| Attempt (this file) | `desk-missions-now/forge/SIGNAL-TRAIN-2J3uX8iRNng.md` |
| Packet | `docs/hive/outer-heaven/CONTENT/watch-later/packets/2J3uX8iRNng/` |

**WAITING** for Watchdog GRADE (separate sitting). Not this file.
