---
tags: [os, factory, forge, reminder]
at: 2026-09-15
desk: forge
status: REMINDER · not started on Cloud
send: removed
clock: parked
---

# FACTORY-OS-NEXT

**Status:** REMINDER · not started on Cloud  
**Date:** 2026-09-15  
**Lead:** Forge (draft only). Watchdog: no GRADE on a reminder.  
**SSOT:** `desk-missions-now/forge/FACTORY-OS-NEXT.md` (this file). Doctrine: `desk-missions-now/CLOUD-HOST.md`.

LANE: hive-os. File named: this path. Five components on this bite: people = Evens HITL + Forge reminder; process = Mac HOST `list` → one id → `read`; data = Grok persistence (Mac-local, unseen here); hardware = Mac; software = existing `grok-chat-sessions` sibling. No new host.

## Named gap

First Factory OS bite = hive CLI to list/read Grok desk threads (sibling of `cursor-chat-sessions`). Not a remint of `cursor-chat-sessions`. `skill-from-session` stays the mint gate. New slug only after PROVEN.

**Next human-run sitting:** Grok chat reader CLI on the Mac.

**Sibling shape (Cursor store only on this checkout):** `python3 scripts/hive/os/cursor-chat-sessions.py list` then `read --id`. Same card: STORE · FILTER · ROWS.

**Already on git (do not stack a second SKU):** `scripts/hive/os/grok-chat-sessions.py` + `scripts/hive/grok-skills/grok-chat-sessions.md`. Mac command when Evens names the sitting: `python3 scripts/hive/os/grok-chat-sessions.py list --limit 5` then `read --id`. Cloud did not run it.

## Why Cloud must not implement

Grok persistence is Mac-local (`~/Library/Application Support/Grok Bot/`). This checkout cannot see it. This run did not list threads. This run did not invent a reader. No SSH. No `/workspace` copy. Reminder job stays reminder-only.

## Never this sitting

Buyer surface · Path A book-door · 4823 restyle · walkthrough orb · `/workspace` copy · invoke all five scoreboard slugs · mint · send / pay / deploy / book / publish · remint `cursor-chat-sessions` · implement the reader on Cloud · `/loop`.

## Checkable-stop

```
DONE-CHECK: this file exists with 2026-09-15 + REMINDER + EVENS leftover
CAP: one brief. No code. No mint. No /loop.
COST: this Cloud Agent run only.
STOP-KIND: metric + cap.
```

**EVENS leftover:** name a human-run sitting on the Mac when ready (run the existing Grok reader CLI; do not remint).

Yellow: `grokbot_orphans` = 8. Named, continue. Do not restore.

Halt.
