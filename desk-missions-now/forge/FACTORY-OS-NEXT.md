---
tags: [os, factory, forge, reminder]
at: 2026-09-21
desk: forge
status: REMINDER · not started on Cloud
send: removed
clock: parked
---

# FACTORY-OS-NEXT

**Status:** REMINDER · not started on Cloud  
**Date:** 2026-09-21  
**SSOT:** `desk-missions-now/forge/FACTORY-OS-NEXT.md` (this file). Doctrine: `desk-missions-now/CLOUD-HOST.md`.

LANE: `hive-os`. One idea: Factory OS is people + process + Mac-local data + this Mac + Cursor/Grok software. Cloud is not the data host. Do not invent a second SKU.

## Named gap

First Factory OS bite = hive CLI to list/read Grok desk threads (sibling of `cursor-chat-sessions`). Same shape: `list` then one `--id` then `read`. Not a remint of `cursor-chat-sessions`.

**On git already (do not stack a second SKU):** `scripts/hive/os/grok-chat-sessions.py` · `scripts/hive/grok-skills/grok-chat-sessions.md`. New slug only after PROVEN (`skill-from-session`). This Cloud run did not execute the CLI and did not list threads.

**Next human-run sitting:** Grok chat reader CLI on the Mac — `python3 scripts/hive/os/grok-chat-sessions.py list --limit 20` then `read --id <uuid>`. Persistence is Mac-local. Copy `/workspace` stays Evens flag.

## Why Cloud must not implement

Grok persistence is Mac-local (`~/Library/Application Support/Grok Bot/`). This checkout cannot see it. Do not pretend you listed threads. Do not invent a reader. Do not SSH. Do not copy `/workspace` remotion trees. Reminder job stays reminder-only.

Sibling shape (Cursor store only, for the Mac sitting to copy): `python3 scripts/hive/os/cursor-chat-sessions.py list` then `read --id`.

## Never this sitting

Buyer surface · Path A book-door · 4823 restyle · walkthrough orb · `/workspace` copy · invoke all five scoreboard slugs · mint · send / pay / deploy / book / publish · `/loop` · Cole 24/7 · default-17.

## Halt roles (named, not invoked)

Watchdog — no GRADE on a reminder. HITL Operator. Researcher. Communications Manager (send-removed).

## Checkable-stop (this Cloud run)

```
DONE-CHECK: FACTORY-OS-NEXT.md exists with 2026-09-21 + REMINDER + EVENS leftover
CAP: one brief. No code. No mint. No /loop.
COST: this Cloud Agent run only
STOP-KIND: metric + cap
```

**EVENS leftover:** name a human-run sitting on the Mac when ready (Grok chat reader CLI: list → one id → read). Until then this file stays REMINDER.

Yellow: `grokbot_orphans` = 8. Named, continue. Do not restore.  
Yellow: `docs/hive/outer-heaven/CONTENT/os/AUDIT.json` missing on this checkout. Named, continue.

Forge OBSERVED only. Halt.
