---
tags: [os, factory, forge, reminder]
at: 2026-09-01
desk: forge
status: REMINDER · not started on Cloud
send: removed
clock: parked
---

# FACTORY-OS-NEXT

**Status:** REMINDER · not started on Cloud  
**Date:** 2026-09-01  
**Lead:** Forge (draft only). Watchdog: no GRADE on a reminder.  
**SSOT:** `desk-missions-now/forge/FACTORY-OS-NEXT.md` (this file). Doctrine: `desk-missions-now/CLOUD-HOST.md`.

## Named gap

First Factory OS bite = hive CLI to list/read Grok desk threads (sibling of `cursor-chat-sessions`). Not a remint of `cursor-chat-sessions`. Chats already on the Mac. Copy `/workspace` stays Evens flag.

**Sibling shape (Cursor store only — do not remint):** `python3 scripts/hive/os/cursor-chat-sessions.py list` then `read --id`. Same card: STORE · FILTER · ROWS.

**Already on git (do not stack a second SKU):** `scripts/hive/os/grok-chat-sessions.py` · `scripts/hive/grok-skills/grok-chat-sessions.md`. New slug only after PROVEN. `skill-from-session` stays the mint gate.

**Next human-run sitting:** Grok chat reader CLI on the Mac — `list --limit 20` → pick **one** id → `read --id`. This Cloud Agent did not list threads.

## Why Cloud must not implement

Grok persistence is Mac-local (`~/Library/Application Support/Grok Bot/sand-client-persistence/`). This checkout cannot see it. Do not pretend you listed threads. Do not invent a reader. Do not SSH. Do not copy `/workspace` remotion trees.

This checkout’s prior brief was still dated 2026-08-25. Do not invent a second SKU from that drift.

## Never this sitting

Buyer surface · Path A book-door · 4823 restyle · walkthrough orb · `/workspace` copy · invoke all five scoreboard slugs · mint · send / pay / deploy / book / publish · Slack · implement the reader on Cloud.

## CHECKABLE-STOP

```
DONE-CHECK: FACTORY-OS-NEXT.md exists with today's date + REMINDER + EVENS leftover
CAP: one brief. No code. No mint. No /loop.
COST: this Cloud Agent run only.
STOP-KIND: metric + cap
```

**EVENS leftover:** name a human-run sitting on the Mac when ready.

Yellow orphans = 8. Named, continue. Do not restore.

Inventory `AGENT_TOOL_INVENTORY.json` has no `forge` key on this checkout → TRIAGE, continue. Do not invent use/never.
