---
tags: [os, factory, forge, reminder]
at: 2026-09-04
desk: forge
status: REMINDER · not started on Cloud
send: removed
clock: parked
---

# FACTORY-OS-NEXT

**Status:** REMINDER · not started on Cloud  
**Date:** 2026-09-04  
**SSOT:** `desk-missions-now/forge/FACTORY-OS-NEXT.md` (this file). Doctrine: `desk-missions-now/CLOUD-HOST.md`.  
**Lead:** Forge (this brief). Watchdog: no GRADE on a reminder. HITL Operator · Researcher · Communications Manager (send-removed) named halt only.

## Named gap

First Factory OS bite = hive CLI to list/read Grok desk threads (sibling of `cursor-chat-sessions`). Shape = **list → one id → read**. Not a remint of `cursor-chat-sessions`. Chats already on the Mac. Copy `/workspace` stays Evens flag.

**Next human-run sitting:** Grok chat reader CLI on the Mac.  
`python3 scripts/hive/os/grok-chat-sessions.py list --limit 5` then `read --id`.  
Skill already on git: `scripts/hive/grok-skills/grok-chat-sessions.md`. Sibling card: STORE / FILTER / ROWS. New slug only after PROVEN. Do not stack a second SKU.

**OBSERVED (Cloud 2026-09-04):** checkout has the sibling files. This run did not list threads. Persistence is Mac-local. Cloud cannot see `~/Library/Application Support/Grok Bot/`.

## Why Cloud must not implement

Grok persistence is Mac-local (`~/Library/Application Support/Grok Bot/sand-client-persistence/`). This Cloud Agent cannot see it. Do not pretend you listed threads. Do not invent a reader. Do not SSH. Do not copy `/workspace` remotion trees. Reminder job stays reminder-only. `/loop` only if Evens types the interval.

## Never this sitting

Buyer surface · Path A book-door · 4823 restyle · walkthrough orb · `/workspace` copy · invoke all five scoreboard slugs · mint · remint `cursor-chat-sessions` · implement the reader on Cloud · send / pay / deploy / book / publish · Slack · Cole 24/7 · default-17.

**EVENS leftover:** name a human-run sitting on the Mac when ready. Sit at the Mac, run `list` then one `read --id`. Cloud will not do that sitting.

## Checkable-stop

```
DONE-CHECK: FACTORY-OS-NEXT.md exists with 2026-09-04 + REMINDER + EVENS leftover
CAP: one brief. No code. No mint. No /loop.
COST: this Cloud Agent run only.
STOP-KIND: metric + cap
```

Yellow orphans = 8. Named, continue. Do not restore.  
Skip already-PASS TRAIN-1 `kwSVtQ7dziU`. 1803 is a count, not a to-do.  
lightningflow.online and n8ncloud.tech are dead. No mint. Clock parked.

Halt.
