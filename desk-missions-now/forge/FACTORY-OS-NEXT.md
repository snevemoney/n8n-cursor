---
tags: [os, factory, forge, reminder]
at: 2026-09-17
desk: forge
status: REMINDER · not started on Cloud
send: removed
clock: parked
---

# FACTORY-OS-NEXT

**Status:** REMINDER · not started on Cloud  
**Date:** 2026-09-17  
**SSOT:** `desk-missions-now/forge/FACTORY-OS-NEXT.md` (this file). Doctrine: `desk-missions-now/CLOUD-HOST.md`.  
**Lead:** Forge (draft only). Watchdog: no GRADE on a reminder. HITL Operator · Researcher · Communications Manager: send-removed.

## Named gap

First Factory OS bite = hive CLI to **list/read Grok desk threads**. Sibling of `cursor-chat-sessions` (`list` → one id → `read`). Not a remint. New slug only after PROVEN (`skill-from-session`). Do not stack a second SKU this sitting.

**Next human-run sitting (Mac):** Grok chat reader CLI against live persistence.

```
STORE: ~/Library/Application Support/Grok Bot/sand-client-persistence
FILTER: --id <uuid> | --desk <name> --limit ≤20
ROWS: 1 thread
```

Shape already on git (do not remint): `scripts/hive/os/grok-chat-sessions.py` · `scripts/hive/grok-skills/grok-chat-sessions.md`. Sibling card: `cursor-chat-sessions` (`python3 scripts/hive/os/cursor-chat-sessions.py list` then `read --id`). Cloud did not run either CLI. Cloud did not list threads.

## Why Cloud must not implement

Grok persistence is Mac-local. This Cloud Agent cannot see `~/Library/Application Support/Grok Bot/`. Do not pretend you listed threads. Do not invent a reader. Do not SSH. Do not copy `/workspace` remotion trees. Reminder job stays reminder-only.

## Never this sitting

Buyer surface · Path A book-door · 4823 restyle · walkthrough orb · `/workspace` copy · invoke all five scoreboard slugs · mint · send / pay / deploy / book / publish · Slack · `/loop` · Cole 24/7 · default-17.

## Checkable-stop (this Cloud run)

```
DONE-CHECK: FACTORY-OS-NEXT.md exists with 2026-09-17 + REMINDER + EVENS leftover
CAP: one brief. No code. No mint. No /loop.
COST: this Cloud Agent run only
STOP-KIND: metric + cap
```

Metric met → halt.

**EVENS leftover:** name a human-run sitting on the Mac when ready (list Grok threads, then read one id). Copy `/workspace` stays Evens flag.

Yellow orphans = 8. Named, continue. Do not restore.
