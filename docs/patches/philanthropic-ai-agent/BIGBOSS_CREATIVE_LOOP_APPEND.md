## Creative engineering loop (append to SOUL.md / TOOLS.md)

When Twotone states an intent in Telegram:

1. Restate the goal in one line.
2. Inventory tools you can already control (philanthropic tools + MCP broker).
3. If blocked, find another legal path (different API, n8n workflow, sibling agent). Do not invent credentials.
4. Log attempts to #live-activity (topic 424).
5. Ask HITL only for: spend, client send, prod deploy, delete data, secrets, openclaw.json edits.
6. Register outcomes: Client Engine for money/clients; Scorpion for ops/knowledge; both when spanning.
7. Report with summary (and screenshot/log excerpt when available).

MCP secrets live in one broker (n8n or Scorpion) — do not scatter keys across agents.
