# Matrix process — level-3 software factory (not a SKU)

**Date:** 2026-08-15  
**Confirmed:** Evens — we ARE a level-3 software factory as **process**. Not a sold SKU. Not Cole unsupervised 24/7.  
**Machine:** `dark-factory` (`scripts/hive/grok-skills/dark-factory.md`)  
**Foundation:** `CONTENT/knowledge/product-factory/` (login / payments / social-share / promotion). SIP score: `FACTORY-VS-STARTUPIDEASPOD-2026-08-15.md`.  
**Spawn:** `.cursor/skills/hive-spawn-desks/SKILL.md`  
**Inventory:** `scripts/hive/agent-tool-inventory.py` → `CONTENT/AGENT_TOOL_INVENTORY.json`  
**Stack:** Cursor + Grok Bot only. Path A / Normand parked. Do not sell “the factory.”

```
DONE-CHECK: this card + hive-funnels/stack line + spawn default five + inventory status not fake-connected + OPERATOR_FOCUS line + canvas of the three-layer factory
CAP: process/matrix files. No ProofCheck product build. No Stripe. No 145 rewrite.
COST: no billed generate
STOP-KIND: cap + done-check
```

---

## Doctrine

```
FACTORY: level 3 — Evens plans + validates. Forge builds. Watchdog grades. Merge ≠ ship.
NOT: unsupervised dark plant · auto-merge to users · factory as the company SKU
COMPANY this week: Path C ProofCheck (nursing QC). Agency install later. clip-factory = clip machine.

LOOP:
  1 CODE (backend bite) — repos that run
  2 SURFACE (frontend/client) — any owned UI we built / will build / fix (websites, repos, projects, apps a human can click). Apex, /work, /pro, ProofCheck :8080, QuickMarket, CE, Path C wrap. Localhost OK when that is the ship. Repo with no UI → not a surface (API/tests/GRADE). Native mobile → ASK.
  3 CHAT (manage/engineer) — Evens in Cursor or Grok desks. Chat runs the `click-live-site` Maestro-style flow on the host browser (Cursor → cursor-ide-browser; Grok → Grok browser). Matrix = who may load a playbook. Not an 18th product.

HARD: send / pay / deploy / book / publish = Evens
NEXT-SKU: reuse primitives.json · do not rebuild login / pay / share · ProofCheck stays building
```

Level 3 is the seat `dark-factory` already names (human plans + validates). Do not jump to 4–5 unsupervised. A Watchdog GRADE on a bite is not a ship. Merge to preview ≠ prod.

---

## Three-layer loop

| # | Layer | What runs | Where | Who |
|---|-------|-----------|-------|-----|
| 1 | **CODE** | Backend bite. Repos that run. Hidden hold-outs first. | Local repos / GitHub draft | Forge builds. Watchdog grades. |
| 2 | **SURFACE** | Frontend / client the human sees. Any owned UI we built / will-build / fix — not only apex. | prod / staging / preview / localhost-if-ship (ProofCheck `:8080`, QuickMarket, CE `/pro`, Path C wrap) | Forge + HITL. Evens deploys. `click-live-site` after. |
| 3 | **CHAT** | Manage / engineer. Runs the Maestro-style flow on the host browser. Who may load a playbook. | Evens in Cursor or Grok desks | Matrix = permission, not an 18th product. Host = Cursor → `cursor-ide-browser`; Grok → Grok browser. |

Company this week is Path C ProofCheck (nursing claim-verify → report → final draft). See `WORK-AS-BUSINESSES-2026-08-14.md`. Agency install later. `clip-factory` is the clip machine, not the company.

**SURFACE / CHAT — next SKU faster:** a new product must reuse `CONTENT/knowledge/product-factory/primitives.json`. Do not rebuild login / payments / social-share. After a ProofCheck bite, update the owned offer (`PROMOTION.md`). Do not publish. Speed stays MISS until a cash SKU. Humans stay MATCH (HITL). Score table: `FACTORY-VS-STARTUPIDEASPOD-2026-08-15.md`.

---

## Wake / allow / deny / done / yellow

**WAKE (default, on the loop — not the product):** Forge · Watchdog · HITL Operator · Researcher · Communications Manager.

**On demand:** Big Boss, Day Planner, Librarian, Money Desk, Lead Hunter, Product GTM, Creative Studio, Consultant, Career Strategist, Publishing Engine — only if Evens names them.

**COLD unless Evens names them:** Wealth Manager · Personal CFO.

**Exception (all 17):** Evens says “all desks” **or** `coverage-loop --video-id`. That is a tape walk, not default factory wake. Cite `hive-spawn-desks`.

```
ALLOW: load playbooks on the named layer · write process/matrix files · Forge one bite · Watchdog GRADE in another session · draft (Gmail send-removed) · filter then model
DENY: send / pay / deploy / book / publish · sell the factory · unsupervised 24/7 host · auto-merge to users · mint icp_id · Path A / Normand · restamp 145 workflows · wake Wealth/CFO without a name · stamp skill connected because SKILL.md exists
TERRITORY: this process card + inventory + spawn + OPERATOR_FOCUS + hive-funnels/stack + `CONTENT/knowledge/product-factory/`. Not a ProofCheck rewrite. Not live Stripe.
BYPASS: none
```

`assume-it-will-touch`: permissions are tools. A row in `agents.use` means the desk **may load** that playbook. It is not a runner, not OAuth isolation, not a live webhook.

`observe-pane`:

```
ID: factory-loop
STATUS: working | yellow | done
YELLOW: ask-principal (Evens)
```

**DONE:** process card on disk · hive-funnels + stack line · spawn defaults to the five · inventory statuses are `playbook` / `wired_untested` / `connected` only if Watchdog GRADE · OPERATOR_FOCUS has factory=process and the loop · canvas of the three layers.

**YELLOW (do not proceed):** Evens must name all-desks, pay, deploy, unpark Path A, wake Wealth/CFO, or a `/loop` interval. Silence is not yes.

---

## How inventory status works

Cite `agent-tool-inventory.py` `discover_tools()`. File presence is not “connected.”

| status | Meaning |
|--------|---------|
| `playbook` | `scripts/hive/grok-skills/{slug}.md` exists. Load ≠ live. |
| `wired_untested` | Skill is on a desk `use` list (`SEED_USE`). No Watchdog `GRADE.md`. Permission ≠ ship. |
| `connected` | Watchdog `GRADE.md` on disk under `knowledge/workflows/dry-runs/{slug}-*/watchdog/`. Only then. |
| `kill` / `legacy` / `connected_n8n` | Unchanged for plugins, natives, n8n, hive tools. |

`MATRIX-SKILL-WIRE-2026-08-14.md` “wired” language is a permission pass. Do not treat SEED_USE edits as connected.

---

## Hard step

Send / pay / deploy / book / publish = Evens. Merge ≠ ship. Do not sell the factory.
