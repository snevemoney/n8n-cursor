# Money Desk — NWbh5ZoEHkA
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NWbh5ZoEHkA/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NWbh5ZoEHkA/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Arena clip (~67 words). Beats in order: host line “Fake it till you make it”; score call “it is running and you scored three”; second run; aside “something special happened”; agent used a calculator tool; Nate asked if there was a reason to add the tool; reply that LLMs are not great at math so a calculator is used when math is needed. Music bed. Visual-only scoreboard / robot bits not in captions. Timestamp locus `00:00` (json3). Caption ingest only.

## B. Atomic Knowledge
### Tool-for-weak-math
- **Claim:** LLMs are not great at math; add a calculator tool when the task needs arithmetic.
- **Reasoning:** Speaker treats the calculator as a deliberate tool add, not a flourish — the agent scored after using it.
- **Mechanism:** Agent run → tool call (calculator) → score. Human asks why the tool exists.
- **Evidence:** On-tape: “LLMs aren't great at math and if I needed to do some math, I wanted to use that.” Score “three” on a run. UNVERIFIED as a benchmark.
- **Conditions:** Task actually requires arithmetic. Tool is available to the agent.
- **Exceptions:** Does not prove the agent is generally reliable. Arena scoring is a game, not a receipt.
- **Action:** If a money-adjacent calc is required, force a checkable tool / sheet — do not trust raw model math. Do not treat arena score as ROI.
- **Confidence:** medium on the tool heuristic; low on the score as evidence
- **Source:** `NWbh5ZoEHkA` @ 00:00
- **Epistemic:** SOURCE


## C. Mental Models
Speaker treats tool choice as an answer to a known model weakness (math), not as “more tools = smarter.” Arena framing (“fake it till you make it,” scored runs) is entertainment. Uncertainty: he asks whether there was an idea behind adding the tool — the why is elicited, not assumed.

## D. Procedures
Questions: “Is there any idea behind adding this tool?” Signals: agent used calculator; score announced. Qualify: math-needed → tool. Avoid: treating a scored demo as a sold system. When to change: if the task is not arithmetic, do not add the tool for show.

## E. Examples
**Situation:** Arena run, score three, second run. **Action:** Agent uses calculator. **Reasoning:** LLMs weak at math. **Outcome:** Host notes something special; tool use is the punchline. **Lesson (implicit):** Name the weakness, then name the tool. A score without a tool-why is a vibe.

## F. Decision Rules
If the job is arithmetic → attach a calculator / sheet, do not prompt the model to “be good at math.” If it is an arena clip → steal the heuristic, do not SKU the show. Refuse: quoting “scored three” as proof we can charge.

## G. Contrarian
Rejects (implicitly) “the model can just do the math.” Field often ships agents without a calc path and then argues about intelligence.

## H. Assumptions
Assumes calculator tool is correctly wired and the score is meaningful. Survivorship: one clip, one score. Falsifier: tool call fails or score is judge-subjective. Disagreement stored: hive does not treat arena points as unit-econ.

## I. Questions
What was the actual math? Who scored “three,” and against what rubric? Is “fake it till you make it” his line or the host’s?

## J. Connections
SYSTEM SYNTHESIS: same arena family as `NO97pqqc10A` / `Q8aqkHi5qY4`. Tool-before-trust rhymes with `8IUWeF3B-hk` (evaluate with objective proof) and Money Desk `token-receipt` (do not pay for a model to fail arithmetic).

## K. Future-Use
Unassigned: arena scoring as a future QA pattern (not a product). Calculator-as-guardrail for invoice / quote math — future `unit-econ-card` line, not this week’s hunt.

## Steal / Operate-never

### Machine: Name-the-weakness then attach the tool
- **Epistemic:** SOURCE (tool why) + INFERENCE (money-adjacent math)
- **Workflow / loop:** trigger: task needs arithmetic → action: bind a calculator / sheet tool → checkable stop: tool output matches a human-readable number
- **Questions / signals:** “Does this step need math the model will flub?”
- **Qualify / frame / objections:** Frame: weakness + tool, not “smarter model.” Objection: “it’s just a demo score” — accept; do not price it.
- **Procedure:** Ask why each tool exists. If the answer is “looks cool,” drop it. If the answer is “LLMs fail this,” keep it and test.
- **Example that proves it:** Situation → Action → Reasoning → Outcome → Lesson: arena math → calculator → models fail math → scored three → name the failure mode before you add spend.
- **Why it works:** A named failure mode is cheaper than a bigger model bill.
- **Conditions / exceptions:** Works when the task is real arithmetic. Exception: arena points are not ROI.
- **Operate-never payload:** Arena / robot / copilot power-up stay operate-never. Score “three” UNVERIFIED.
- **Hive run (existing skills only):** `golden-test-loop` (check the number) · `pricing-margin-roi-guardrails` (do not analog the score) · `ask-principal` (no pay)
- **Source:** `NWbh5ZoEHkA` @ 00:00


### Operate-never (this desk will not operate)
- Treat arena score “three” / “fake it till you make it” as a machine we sell.
- SKU a calculator-tool demo as Path C.
- Move money, approve a charge, refund, or fee. Live Stripe. Auto-send / auto-pay / auto-book / auto-deploy / auto-publish.
- Quote any tape $ / student count / job-loss % / prize / 10x as FACT or as our price analog.
- Nate Skool / Plus / AIS Plus / Hostinger NATEHERK / Uppit / Glaido / sold templates as a SKU. Do not map through `usecase-to-sku`. Do not join / install / import.
- Install Claude Code / Codex / Claude / ChatGPT / Gemini / Coda / Vapi / ElevenLabs / n8n-cloud / Trigger.dev / Hermes / Base44 / Sora / NanoBanana / Poppy / Lovable as ours. Cursor + Grok only. Vendor on tape is a mention, not a Bot dispatch.
- New hunt ICP. Unpark a client. Live hunt stays `local-pro` / Normand. Clients parked. No new `icp_id`.
- Grok Bot / `sendPrompt`. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`. Overwrite `takes/money-desk.md`.

## L. Role-Specific Applications
HOLD any spend that treats an arena score as proof. Steal the math-tool heuristic for quote / invoice checks only after Evens names a Path C number we can open. Do not analog “scored three.”

**Lens only (after A–K + Steal).** This desk votes PASS/HOLD on margin. It does not move money.

- `pricing-margin-roi-guardrails`: tape $ stays **UNVERIFIED**. Our early rung stays **$500–1K/mo CAD** after a 30–60d win. Delivery ≤40% of fee. Vendor / educator $ does not move Normand Path A.
- `outcome-offer-funnel` + `checkout-proof`: count checkout + warm conversions we can open. Quarantine YouTube receipts.
- `paid-slice-funnel`: thin V1; Stripe HITL; preview ≠ domain.
- `ask-principal` + `input-required-gate`: confirm ≠ execute. Pay / refund / fee stay HITL.
- `website-offer-funnel`: Path A/B/C spine still exists; this tape does not open a client unless Evens names one.
- Proposed, not written: `unit-econ-card` (price, COGS, contribution, aha-gate — tape $ never fills the line) · `token-receipt` (session cost versus artifact; leftover quota is not a KPI).

**Business parked:** no new `icp_id`. No `business-lanes.json` row. Hunt stays `local-pro` / Normand.
