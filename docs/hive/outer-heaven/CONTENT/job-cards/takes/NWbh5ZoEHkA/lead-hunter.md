# Lead Hunter — NWbh5ZoEHkA
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NWbh5ZoEHkA/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NWbh5ZoEHkA/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** Agentic Arena: Dr Pure Eval was pure evil... (Nate short)
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Cold: host line “Fake it till you make it.” Score call: “It is running and you scored three.” Nate confirms “Okay, we got three.”
- Second run. Host: “Something special happened, folks.”
- Observation: the agent “made use of the calculator tool.”
- Aside: “Is there any idea behind that? Adding this tool.” Nate: “LLMs aren't great at math and if I needed to do some math, I wanted to use that.”
- Music sting. No CTA, no price, no client.
- **Gaps:** Arena audio + music. Visual scoreboard not in captions. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Tool-for-weakness, not tool-for-flex
- **Claim:** Give the agent a calculator because LLMs are not great at math — not because a tool list looks impressive.
- **Reasoning:** Nate answers the host’s “any idea behind adding this tool” with a capability gap, not a feature tour.
- **Mechanism:** When the task needs arithmetic, route to a calculator tool instead of trusting the model’s internal math.
- **Evidence:** SOURCE quote: “LLMs aren't great at math and if I if I needed to do some math, I wanted to use that.” Also: “it made use of the calculator tool” after a scored run.
- **Conditions:** Live trivia / scored arena where a wrong number costs the run.
- **Exceptions:** Tape does not say every agent needs a calculator; only when math is in the job.
- **Action:** Inventory the job’s known model failures; attach a tool that covers that failure; watch whether it actually fires.
- **Confidence:** high on the math-gap claim (speaker said); medium on “this won the point” (scoreboard is visual).
- **Source:** `NWbh5ZoEHkA` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Models fail at arithmetic; tools exist to cover known failure modes.
- INFERENCE: “Fake it till you make it” is host banter, not Nate’s operating doctrine.
- SOURCE: A scored second run is how you notice the tool actually fired.

## D. Procedures
- After a scored run, ask: which tool fired, and was that the intended one?
- When adding a tool, state the weakness it covers (here: math).
- Do not add a tool with no “idea behind that.”

## E. Examples
- Situation: Arena run, score three, second run. Action: Agent calls calculator. Reasoning: LLMs bad at math. Outcome: Host flags it as “something special.” Lesson: The checkable stop is “tool used,” not “we added a tool.”

## F. Decision Rules
- If the job includes arithmetic, attach a calculator (or equivalent) rather than hoping the LLM counts.
- If a tool never fires on a scored run, the YAML/description is wrong — do not assume the model “knows” the tool exists.

## G. Contrarian
- Rejects “more tools = smarter agent.” The tape’s only tool story is one tool for one known weakness.

## H. Assumptions
- Survivorship: one clip of a calculator firing. No failed-math counterexample on tape.
- Domain: live game-show scoring, not production lead math (quotes, ROI).
- Disagreement stored: hive already treats tape scores as UNVERIFIED theater.

## I. Questions
- Did the calculator change the score, or was it just visible?
- What happens when the calculator is available and the model still does mental math?

## J. Connections
- SYSTEM SYNTHESIS: pairs with `8IUWeF3B-hk` (evaluate with a dataset, not vibes) and `golden-test-loop` (watch the invoke).
- SYSTEM SYNTHESIS: Lead Hunter analog — do not invent a “math enrichment” scraper; cover the known failure (bad numbers in a quote) with a checkable calc, HITL before send.

## K. Future-Use
- Unassigned: arena / trivia scoring as a public eval format.
- Unassigned: calculator-as-guardrail on quote math before a prospect sees a number.

## Steal / Operate-never

### Machine: Cover the known model failure with one tool, then watch it fire
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** Name the weakness → attach one tool that covers it → run a scored example → checkable stop = the tool invoked (not “we installed it”).
- **Questions / signals:** What will this model get wrong without a tool? Did the tool fire on the scored run?
- **Qualify / frame / objections:** Frame: capability gap, not feature list. Objection: “LLMs can do math now” → still verify on a scored example.
- **Procedure:** Write the weakness in the tool description. Run one item that requires that skill. Confirm invoke. If no invoke, fix description before adding more tools.
- **Example that proves it:** Situation: trivia needs arithmetic. Action: add calculator. Reasoning: LLMs aren’t great at math. Outcome: tool used on second run. Lesson: one tool, one gap, watched.
- **Why it works:** From B/C: models have known holes; a tool list without a watched invoke is theater.
- **Conditions / exceptions:** Works when the failure is known and the tool is the actual remedy. Exception: visual-only score — do not treat “scored three” as FACT performance.
- **Operate-never payload:** Do not stand up an “Agentic Arena” product or auto-score leads. Do not quote the score as proof.
- **Hive run (existing skills only):** `golden-test-loop` (watch invoke). `ask-principal` before any send that uses computed numbers.
- **Source:** `NWbh5ZoEHkA` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Treat Agentic Arena scoring / “fake it till you make it” as a hunt or a client demo script.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Do not hunt “trivia bot” or “calculator agent” ICPs. The machine is: cover a known number-failure before a quote leaves the desk.
- If a list row has invented ROI math, that is a raw-50 smell — anneal, do not MUST-score.
- Hard step stays HITL: this desk drafts; Evens sends.
