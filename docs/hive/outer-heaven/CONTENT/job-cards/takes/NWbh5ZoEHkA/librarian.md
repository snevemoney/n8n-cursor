# Librarian — NWbh5ZoEHkA
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NWbh5ZoEHkA/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NWbh5ZoEHkA/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** Agentic Arena: Dr Pure Eval was pure evil...
**Channel:** Nate Herk | AI Automation
**Kind:** short (~0:25 / ~67 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Aside: "Fake it till you make it."
2. Run result: "It is running and you scored three." / "Okay, we got three."
3. Second run: "Something special happened" — the agent used a calculator tool.
4. Q: any idea behind adding that tool?
5. A: "LLMs aren't great at math and if I needed to do some math, I wanted to use that."
Gap: visual arena UI, who "Dr Pure Eval" is, scoring rubric — captions only. Timestamp UNKNOWN.

## B. Atomic Knowledge

### Tool the arithmetic
- **Claim:** LLMs are not great at math; if math is needed, attach a calculator tool.
- **Reasoning:** First run scored three; second run used the calculator. Speaker treats the tool as the fix, not a better prompt.
- **Mechanism:** Agent may call a calc tool instead of self-arithmetic.
- **Evidence:** Caption: "it made use of the calculator tool" + "LLMs aren't great at math."
- **Conditions:** Task includes arithmetic.
- **Exceptions:** Not stated whether all scores needed the tool.
- **Action:** File maker-checker: do not self-math when a checkable tool exists.
- **Confidence:** medium (short; one demo)
- **Source:** `NWbh5ZoEHkA` @ UNKNOWN
- **Epistemic:** SOURCE

### Arena score is a clip, not a FACT
- **Claim:** Speaker scored three on a run.
- **Reasoning:** Clip is a highlight, not a measured hive eval.
- **Mechanism:** Game-show scoring on tape.
- **Evidence:** "you scored three"
- **Conditions:** Agentic Arena context
- **Exceptions:** None on tape
- **Action:** Keep score UNVERIFIED as ours.
- **Confidence:** high that the words were said; low as hive metric
- **Source:** `NWbh5ZoEHkA` @ UNKNOWN
- **Epistemic:** SOURCE (words) / INFERENCE (do not graduate to FACT)

## C. Mental Models
Speaker treats tool-use as the interesting event ("something special"), not the slogan. Math is a known model weakness; the fix is a tool, not confidence. "Fake it till you make it" is an aside, not a method.

## D. Procedures
1. Run the agent.
2. Read the score.
3. If arithmetic is in the path, add a calculator tool.
4. Re-run and check whether the tool was called.
Question: "Is there any idea behind adding this tool?"
Signal: tool-call in the trace, not a vibes "it feels smarter."

## E. Examples
**Calc on second run:** Situation — first run scored three. Action — add calculator. Reasoning — LLMs aren't great at math. Outcome — second run used the tool. Lesson — tool the arithmetic; score 3 stays a clip.

## F. Decision Rules
- If the job is math → attach a calc tool.
- If the interesting event is a tool-call → file the tool-call, not the slogan.
- Refuse: treat Arena score as a hive KPI.

## G. Contrarian
Against "just prompt better at arithmetic." Speaker wants a tool. Against treating the slogan as the lesson.

## H. Assumptions
Theirs: calculator use equals better math (not shown with a correct answer on this short). Ours: 67-word caption is complete enough to file the atom, incomplete for Arena rules. Falsifier: a later tape where self-math beats the tool. Disagreement stays: clip ≠ eval.

## I. Questions
- What does score 3 mean on the Arena rubric?
- Did the calculator change the score?
- Who is Dr Pure Eval on this clip vs `NO97pqqc10A`?

## J. Connections
- SYSTEM SYNTHESIS → `golden-test-loop` (checkable tool vs vibe).
- SYSTEM SYNTHESIS → `NO97pqqc10A` (same Arena night).
- SYSTEM SYNTHESIS → 18-corpus `IVx8OSMbTss` security review: another model finds what you miss.

## K. Future-Use
Arena as a dated CHRONICLE object (game-show, charity later on `Q8aqkHi5qY4`). Unassigned: whether hive evals should require a calc tool on any numeric path.

## Steal / Operate-never

### Machine: maker-checker for math
- **Epistemic:** SOURCE
- **Workflow / loop:** numeric step in agent path → attach calculator tool → re-run → checkable stop = tool-call visible in trace (not "I think the number is right")
- **Questions / signals:** Did it call the tool? Is the arithmetic in the path?
- **Qualify / frame / objections:** "LLMs aren't great at math" is the frame; do not sell Arena.
- **Procedure:** add calc tool; re-run; file whether the tool fired
- **Example that proves it:** Situation → first run scored three → Action → add calculator → Reasoning → models miss math → Outcome → second run used the tool → Lesson → tool the arithmetic
- **Why it works:** separates generation from checkable computation
- **Conditions / exceptions:** only when math is in the job; this short does not prove correctness
- **Operate-never payload:** Agentic Arena as a product; score 3 as FACT
- **Hive run (existing skills only):** `golden-test-loop` · `tape-self-teach`
- **Source:** `NWbh5ZoEHkA` @ UNKNOWN

### Operate-never
- Arena as a hive product. Quote score 3 as FACT.
- Merge `LESSONS-FROM-TAPE.md`. Do not Load the cursor-draft.
- Overwrite `takes/librarian.md` (18-tape SSOT).
- Auto-write proposed `SKILL.md`. New `icp_id`. Unpark clients.
- Send / pay / deploy / book / publish.
- Delete a CHRONICLE entry to "clean" a game-show tape.

## L. Role-Specific Applications
File this as a tiny provenance card: path + quote + timestamp UNKNOWN. Do not flatten into "we use calculators now" without the Arena context. Outer Heaven stays the wiki — no Arena page-farm. Persist the disagreement (clip vs eval) as a labeled row, not a CUT.
