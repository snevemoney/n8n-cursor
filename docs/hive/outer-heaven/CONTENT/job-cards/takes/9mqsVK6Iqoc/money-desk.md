# Money Desk — 9mqsVK6Iqoc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9mqsVK6Iqoc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9mqsVK6Iqoc/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Short (~327 words). Beats in order: First inbox agent in n8n: start with Gmail trigger (on message received) because the builder knows the canvas is empty; then text classifier on subject+body into four categories; first execute errors — no AI connected; connect AI, rerun, email goes to customer support branch; finance/billing/high priority/promotion empty; matches the wireframe; CTA full breakdown. Visual-only UI / music not in captions. json3 start 00:00. Caption ingest only.

## B. Atomic Knowledge
### Trigger-then-classify
- **Claim:** Inbox agent = Gmail message-received → classifier → path.
- **Reasoning:** Empty canvas forces a trigger first.
- **Mechanism:** Trigger → text classifier (+ AI) → branch.
- **Evidence:** Four categories named: customer support, finance, billing, high priority, promotion (five names / “four” on tape — count is sloppy).
- **Conditions:** Gmail connected. AI attached to classifier.
- **Exceptions:** First run failed without AI — the error is the lesson.
- **Action:** Steal wireframe → trigger → classifier → prove one branch. Do not auto-reply.
- **Confidence:** high as a build order
- **Source:** `9mqsVK6Iqoc` @ 00:00
- **Epistemic:** SOURCE


## C. Mental Models
Belief: wireframe first, then nodes. Priority: start with the trigger the empty canvas requires. He treats the error (no AI) as teaching, not failure.

## D. Procedures
Procedure: add Gmail on-message-received → add text classifier → connect AI → execute → confirm the branch. Avoid: executing classifier with no model. When to change: if it lands the wrong path, fix labels (not shown).

## E. Examples
**Situation:** Empty workflow, customer-support example email. **Action:** Trigger + classifier; first execute errors; connect AI; lands support. **Reasoning:** Wireframe paths. **Outcome:** One item on support, zero on others. **Lesson:** A failed first execute is information; auto-send is not in the short.

## F. Decision Rules
If no trigger → builder blocks you (on tape). If classifier has no AI → expect error. If email is classified → that is not a reply. Refuse: auto-answer inbox.

## G. Contrarian
None vs field except “start in the middle of the graph.” He starts at the trigger.

## H. Assumptions
One example email. Category count inconsistent (four vs five names). Full course is `HN0oWxbF2bM`.

## I. Questions
What are the exact four vs five labels? Does a later node reply? Who owns the Gmail?

## J. Connections
SYSTEM SYNTHESIS: teaser for `HN0oWxbF2bM` (inbox full beginner). Classifier-then-path = `golden-test-loop`. No send = Money Desk HITL.

## K. Future-Use
Unassigned: inbox classify as an internal QoL — not a client SKU this week.

## Steal / Operate-never

### Machine: Wireframe-trigger-classify-prove-branch
- **Epistemic:** SOURCE
- **Workflow / loop:** trigger: new mail → action: classify into named paths → checkable stop: item on the expected branch; no auto-reply
- **Questions / signals:** “Which path should this email take?”
- **Qualify / frame / objections:** Frame: beginner inbox. Objection: “just auto-answer” — not on this tape; we refuse anyway.
- **Procedure:** Connect the model before execute. Do not send. Do not SKU the inbox agent.
- **Example that proves it:** Support example → error without AI → support branch after connect.
- **Why it works:** A named branch is a checkable stop; a sent reply is a hard step.
- **Conditions / exceptions:** Exception: category list is messy on tape — write our own labels.
- **Operate-never payload:** Auto-reply · n8n-cloud as ours · inbox agent as a sold SKU from this short.
- **Hive run (existing skills only):** `golden-test-loop` · `slice-build` · `ask-principal`
- **Source:** `9mqsVK6Iqoc` @ 00:00


### Operate-never (this desk will not operate)
- Auto-reply to classified mail.
- SKU “first inbox agent” from the teaser.
- Move money, approve a charge, refund, or fee. Live Stripe. Auto-send / auto-pay / auto-book / auto-deploy / auto-publish.
- Quote any tape $ / student count / job-loss % / prize / 10x as FACT or as our price analog.
- Nate Skool / Plus / AIS Plus / Hostinger NATEHERK / Uppit / Glaido / sold templates as a SKU. Do not map through `usecase-to-sku`. Do not join / install / import.
- Install Claude Code / Codex / Claude / ChatGPT / Gemini / Coda / Vapi / ElevenLabs / n8n-cloud / Trigger.dev / Hermes / Base44 / Sora / NanoBanana / Poppy / Lovable as ours. Cursor + Grok only. Vendor on tape is a mention, not a Bot dispatch.
- New hunt ICP. Unpark a client. Live hunt stays `local-pro` / Normand. Clients parked. No new `icp_id`.
- Grok Bot / `sendPrompt`. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`. Overwrite `takes/money-desk.md`.

## L. Role-Specific Applications
HOLD send. Steal classify-then-prove. No client inbox without Evens.

**Lens only (after A–K + Steal).** This desk votes PASS/HOLD on margin. It does not move money.

- `pricing-margin-roi-guardrails`: tape $ stays **UNVERIFIED**. Our early rung stays **$500–1K/mo CAD** after a 30–60d win. Delivery ≤40% of fee. Vendor / educator $ does not move Normand Path A.
- `outcome-offer-funnel` + `checkout-proof`: count checkout + warm conversions we can open. Quarantine YouTube receipts.
- `paid-slice-funnel`: thin V1; Stripe HITL; preview ≠ domain.
- `ask-principal` + `input-required-gate`: confirm ≠ execute. Pay / refund / fee stay HITL.
- `website-offer-funnel`: Path A/B/C spine still exists; this tape does not open a client unless Evens names one.
- Proposed, not written: `unit-econ-card` (price, COGS, contribution, aha-gate — tape $ never fills the line) · `token-receipt` (session cost versus artifact; leftover quota is not a KPI).

**Business parked:** no new `icp_id`. No `business-lanes.json` row. Hunt stays `local-pro` / Normand.
