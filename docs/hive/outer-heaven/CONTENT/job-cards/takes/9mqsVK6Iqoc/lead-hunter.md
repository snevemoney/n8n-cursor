# Lead Hunter — 9mqsVK6Iqoc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9mqsVK6Iqoc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9mqsVK6Iqoc/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** Build Your First Inbox Agent in 30 Minutes (short)
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Start: Gmail trigger — UI knows the canvas is empty so it offers “on message received” (26 actions exist; trigger is forced first).
- Wireframe next: text classifier on subject+body into four categories.
- First execute: error — classifier has no AI connected. Connect AI, re-execute: works.
- Result: one item in customer support; finance/billing, high priority, promotion have zero. “Sent it down the right path.”
- CTA: full breakdown play button.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### Empty-canvas trigger first; classifier fails closed without a brain
- **Claim:** An inbox agent starts at “message received,” then a text classifier — and the classifier errors until a model is attached, which is the useful failure.
- **Reasoning:** The UI and the error both teach order: trigger → classifier → model → branch.
- **Mechanism:** Gmail on-message → text classifier (subject+body) → four named branches.
- **Evidence:** SOURCE: “we get an error… because we need to connect AI to this node.” After connect: “output it in the customer support branch… finance, billing, high priority, and promotion. We have no items.”
- **Conditions:** You have a wireframe of categories before you build.
- **Exceptions:** Four categories are his example, not a universal taxonomy.
- **Action:** Draw branches first. Expect the no-model error. Confirm one item, three empty.
- **Confidence:** high on the fail-closed lesson.
- **Source:** `9mqsVK6Iqoc` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Wireframe before nodes.
- SOURCE: A classifier with no model should error, not silently pass.
- INFERENCE: Empty wrong-branches are part of the proof.

## D. Procedures
- Add trigger first. Add classifier with named categories. Attach model. Execute one real email. Confirm the right branch has the item and others are empty.

## E. Examples
- Situation: first inbox agent. Action: Gmail trigger + classifier; first run errors; attach model. Reasoning: no brain. Outcome: customer-support path. Lesson: the error is the lesson.

## F. Decision Rules
- If there is no trigger, do not add actions.
- If the classifier has no model, do not call it evaluated.
- If all four branches fire, the categories are mush.

## G. Contrarian
- Rejects “just add the AI node and go” — he shows the error on purpose.

## H. Assumptions
- Gmail + n8n on-tape. Four categories are a teaching set. No send on this clip.

## I. Questions
- What is the gold label for that email? Only his wireframe.

## J. Connections
- SYSTEM SYNTHESIS: `HN0oWxbF2bM` is the full inbox-agent course.
- SYSTEM SYNTHESIS: `8IUWeF3B-hk` evaluation sheet is how you stop trusting one green branch.

## K. Future-Use
- Unassigned: fail-closed classifier as a ready-made HITL gate on inbound mail.

## Steal / Operate-never

### Machine: Trigger → named branches → model → one-item proof
- **Epistemic:** SOURCE
- **Workflow / loop:** Trigger first → named categories → attach model → run one email → checkable stop = one branch has the item, others empty.
- **Questions / signals:** What are the four (or n) categories? Which branch should this email take?
- **Qualify / frame / objections:** Frame: path, not “inbox AI.” Objection: “it classified something” → show the empty branches.
- **Procedure:** Do not auto-reply. Do not connect a live inbox without HITL.
- **Example that proves it:** Situation: support email example. Action: error then correct branch. Reasoning: model missing, then present. Outcome: one item on support. Lesson: empty wrong-paths are the proof.
- **Why it works:** From B: he used the error and the empty branches as teaching.
- **Conditions / exceptions:** Works with a wireframe. Exception: no named categories → do not build.
- **Operate-never payload:** Do not hunt “inbox agent” as an ICP. Do not auto-send from a branch.
- **Hive run (existing skills only):** `golden-test-loop`. `send-removed`. Existing hive mail = read+draft first.
- **Source:** `9mqsVK6Iqoc` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Auto-triage a live inbox. Auto-reply from a branch.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal fail-closed + empty-branch proof. Do not hunt inbox-agent buyers. Hive mail stays read+draft.
- Hard step stays HITL: this desk drafts; Evens sends.
