# Creative Studio — 9mqsVK6Iqoc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9mqsVK6Iqoc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9mqsVK6Iqoc/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Inbox-agent short: first 30 minutes. Beats: start with Gmail trigger; n8n knows the canvas is empty so it prompts “on message received”; next, per wireframe, text classifier on subject+body into four categories; execute step errors because no AI connected; connect AI; re-run; email lands in customer support; finance/billing/high priority/promotion have zero items; “sent it down the right path just like we talked about in our wireframe”; play-button magnet. Caption ingest.

## B. Atomic Knowledge

### Wireframe before nodes
- **Claim:** The classifier categories are already on a wireframe; the canvas implements the picture.
- **Reasoning:** Empty canvas + known four buckets; the tool suggests the trigger because nothing else exists.
- **Mechanism:** Wireframe → Gmail trigger → text classifier → four branches.
- **Evidence:** “according to our wireframe… classify it in one of these four categories… just like we talked about in our wireframe”
- **Conditions:** Categories are named before execute.
- **Exceptions:** Vibe-adding nodes with no picture is the anti-pattern.
- **Action:** Draw the four buckets before the first plus-click.
- **Confidence:** high as the teaching order.
- **Source:** `9mqsVK6Iqoc` @ 00:00
- **Epistemic:** SOURCE

### Error is a missing brain, not a bad email
- **Claim:** First execute fails because AI is not connected; second execute routes correctly.
- **Reasoning:** The node cannot classify without a model.
- **Evidence:** “we get an error… we need to connect AI to this node… now it sent it down the right path”
- **Conditions:** Classifier node requires a chat model.
- **Exceptions:** A wrong bucket with AI connected is a different fail (eval tape).
- **Action:** Fail the step, attach the brain, re-run; do not redesign the wireframe yet.
- **Confidence:** SOURCE for this demo.
- **Source:** `9mqsVK6Iqoc` @ 00:38
- **Epistemic:** SOURCE

## C. Mental Models
Picture first (wireframe), then trigger, then brain, then path. Empty-canvas UX that suggests the trigger is a gift. One item in the right bucket is the proof.

## D. Procedures
1. Draw four categories.
2. Add Gmail “message received.”
3. Add text classifier on subject+body.
4. Connect AI before execute.
5. Stop when the item is in the named bucket and others are empty.

Avoid: executing a brainless classifier; inventing buckets after the error.

## E. Examples
**Situation:** Customer-support example email.  
**Action:** Trigger → classifier → error → attach AI → support branch = 1, others 0.  
**Reasoning:** Wireframe path.  
**Outcome:** “Right path.”  
**Lesson:** The walkthrough is the branch lighting up, not a screenshot of Gmail.

## F. Decision Rules
- If there is no wireframe → do not add the classifier.
- If execute errors → check the brain before changing categories.
- If all four buckets fire → the picture is wrong.

## G. Contrarian
Beginner tapes often start with the agent node. He starts with a trigger the UI already knows you need.

## H. Assumptions
Four categories (support, finance/billing, high priority, promotion) are the demo set, not a universal inbox. n8n on tape — Cursor + Grok operate.

## I. Questions
What did the wireframe look like on screen? Full-course extras in `HN0oWxbF2bM`?

## J. Connections
- SYSTEM SYNTHESIS → `HN0oWxbF2bM` (full inbox course).
- SYSTEM SYNTHESIS → `8IUWeF3B-hk` (eval the classifier).
- SYSTEM SYNTHESIS → `slice-build` (one system, picture first).

## K. Future-Use
Four-bucket wireframe as a previs artifact (boxes before nodes). Unassigned.

## Steal / Operate-never

### Machine: wireframe-then-one-right-path
- **Epistemic:** SOURCE
- **Workflow / loop:** draw buckets → trigger → classifier → attach brain → one item in the named path → stop
- **Questions / signals:** Is AI connected? Which bucket lit?
- **Qualify / frame / objections:** Error first is missing brain, not bad copy
- **Procedure:** Picture before plus-click
- **Example that proves it:** Support email → support = 1, others 0
- **Why it works:** The checkable stop is a branch, not a vibe
- **Conditions / exceptions:** Demo categories only; do not auto-send from a bucket
- **Operate-never payload:** Auto-reply from a class; n8n-cloud install; new hunt
- **Hive run:** `slice-build`; `golden-test-loop`; `cinematic-recipe` (previs)
- **Source:** `9mqsVK6Iqoc` @ 00:38

### Operate-never
- Auto-send from a classified bucket. Switch stack / n8n-cloud.
- New hunt. Merge `LESSONS-FROM-TAPE.md`.
- Game-studio / fake 3D / cheap taste / NSFW.

## L. Role-Specific Applications
Video-first: “first inbox agent in 30 minutes” — the plate is the **wireframe four boxes**, then the lit branch. Do not hero the Gmail logo. HITL on any send. Clients parked.
