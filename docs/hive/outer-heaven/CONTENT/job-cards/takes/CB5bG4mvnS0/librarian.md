# Librarian — CB5bG4mvnS0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** Codex browser agent
**Channel:** Nate Herk | AI Automation
**Kind:** video (~4330 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Codex desktop browser-use: “do anything” on web or local; sign in once, it saves. Prefer **API / plugins**; browser when there is no API.
2. Localhost form: annotate (Lovable/Claude-design style) overlapping phone field + Y-align. Headed vs **headless** (hours in background). QA prompt: try to break it. Finds required-field OK, invalid data still submits, UK country-code resets to US, keyboard-submit bypasses, **mobile** fails. Prefer 100 agent tests over 5 human ones; `/goal` 100 unique scenarios. Then “go fix.” (He planted some UI bugs for the annotate demo.)
3. No-API: Relay bank statements. NL skill: accounts → two CSVs → folder; one-shot while signed in. Bank kicks session → login screen. Codex **password manager**: import CSV (name/url/user/pass). Fills login; **2FA** would be manual — he mentions Mac-texts + computer-use could automate 2FA and says **be careful**. Watch **≥10** times; strict skill script; vision is not a contract.
4. Ladder: API first (fast/cheap/consistent) → **deterministic pixel macro** if the path never changes → vision browser-use only if the UI reasons. He has used it on Skool/X/Instagram (session persists).
5. YouTube URL → X article **draft** (API formatting/media/$); session already logged in; new chat scrolls X, sees Grok Bot / 4.6. Computer-use plugin: open Glydo, Bluetooth devices — first refuses as OS privacy, then toggles the **in-app** setting; blue hue = Codex has the machine. Phone remote into desktop. X draft: title, body, ugly thumbnail, later YouTube screenshots dragged into the article. Chess/Tetris aside.
6. Value: API covers ~**95%**; this saves **5–10 min** but stacks into a no-context-switch pipeline. Schedule browser skills (morning Instagram scroll — on-tape).
Gap: Relay UI, X draft. Timestamp UNKNOWN. Codex/Relay/X/Skool on-tape. Bank + password CSV + 2FA = operate-never.

## B. Atomic Knowledge

### API → macro → vision; watch bank-vision ten times or don’t
- **Claim:** Browser-use is the last resort after API and a pixel script. Headed QA that tries to break the UI is the good use. Saved sessions and a password-manager CSV make it look 24/7; banks kick you; 2FA is the stop. 95% still API. 5–10 min saved is the honest number.
- **Reasoning:** Vision re-decides every click. A macro does not. A human weirder than your 6 test cases will ship the bug.
- **Mechanism:** annotate → headed/headless QA → skill from NL → (optional) password CSV → schedule only after 10 watched runs.
- **Evidence:** UK-reset + mobile fail; Relay one-shot then login wall; Glydo privacy refuse then in-app toggle; X draft + screenshots.
- **Conditions:** 85 checks / 95% / 5–10 min UNVERIFIED as law.
- **Exceptions:** He still used browser for X because the API was “weird” and metered.
- **Action:** File the three-rung ladder + try-to-break QA. Do not install Codex as hive. Do not import bank passwords. Do not auto-2FA. Do not auto-publish X.
- **Confidence:** high as a when-to-browser machine
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (transcript-implied)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Relay session kick; Glydo OS-vs-app confusion
- **Speech ≠ behavior:** “literally do anything” / “automate anything” vs 2FA stop, 10-watch rule, 95% API

## C. Mental Models
Weird users exist. Headless is for hours. Blue hue = the machine is not yours for a minute. Stack minutes, don’t hunt a 10x.

## D. Procedures
1. Is there an API? Use it.
2. Is the click path identical every time? Macro.
3. Else vision, headed, try-to-break.
4. Never paste bank passwords in chat; still do not treat CSV-in-Codex as hive-safe.
5. 2FA stays human. Publish stays human.
6. Watch 10 times before a schedule.
Avoid: Codex as hive; Relay/bank skills; Instagram/X auto-post; computer-use from phone as a plan.

## E. Examples
**Form QA:** Situation — planted UI bugs + real ones. Action — break-it prompt. Outcome — invalid submit, UK reset, mobile. Lesson — agent-as-weird-user.

**Relay:** Situation — no API. Action — NL skill + later password CSV. Outcome — one-shot, then kick. Lesson — session≠forever.

## F. Decision Rules
- IF API exists → do not browser it (unless he names a format/$ exception).
- IF 2FA appears → stop.
- IF it is a bank → watch or don’t.
- Refuse: Codex as hive; auto-publish; password CSV as a hive pattern.

## G. Contrarian
Against browser-use-first. Against “the API is always better” on metered X (his exception — keep it labeled).

## H. Assumptions
Complements `YHk45NEpspE` (scrape operate-never) and `62Rfe1w9NBc`. Caption-only.

## I. Questions
Did `/goal` 100 scenarios run? What did the 85-check SaaS tape find?

## J. Connections
SYSTEM SYNTHESIS → `YHk45NEpspE`; `62Rfe1w9NBc`; `ehg4fhydTgs` (no cookies remote).

## K. Future-Use
API-macro-vision ladder + try-to-break QA + 10-watch as atoms.

## Steal / Operate-never

### Machine: try-to-break headed QA; climb API → macro → vision
- **Epistemic:** SOURCE
- **Workflow / loop:** prefer API → else macro → else headed vision that tries to break → list fails → fix → checkable stop = a fail list you re-ran, not a saved bank session
- **Questions / signals:** API? Same pixels? 2FA? Bank?
- **Qualify / frame / objections:** 95% API; 5–10 min honest.
- **Procedure:** D above.
- **Example that proves it:** mobile/UK fails; Relay kick.
- **Why it works:** Weird users are the test set.
- **Conditions / exceptions:** X API exception stays labeled; $ UNVERIFIED.
- **Operate-never payload:** Codex as hive; bank password CSV; auto-2FA; auto-publish X/IG; phone computer-use.
- **Hive run:** Steal try-to-break. Do not add Codex browser.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN

### Operate-never
- Codex as hive. Bank/password/2FA automation. Auto-publish. Merge LESSONS. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
File the three-rung ladder. Do not import a password CSV. Do not schedule Instagram.
