# Forge — CB5bG4mvnS0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk Codex desktop + in-app browser tape. Beats: “best browser use I’ve tried”; sign in once, session saves; “automate anything” (vendor line) → API / plugins first; browser use when no API → localhost form: annotate overlapping phone/company + Y-axis misalign (Lovable/Claude-design style) → headed vs headless (watch vs background hours) → “test the heck out of this / try to break it”: required fields, invalid contact data through, UK country-code reset, keyboard-submit bypass, mobile responsiveness → prior SaaS-in-one-day demo **85** focused checks UNVERIFIED → slash-goal option: don’t stop until **100** unique UI edge cases, then fix those bugs → Demo 2 no API: **Relay** banking statements — NL skill: accounts → two statements → CSV → folder; one-shot first run; bank kicks the session → password-manager CSV import (name / URL / username / password) so creds stay out of chat → **2FA from the phone stays manual**; he says be careful automating SMS via computer use → bank flows: watch **at least 10 times**, keep the skill script strict — vision deciding clicks, not a guaranteed path → ladder: **API first** (fastest, usually cheapest, most consistent) → else a **deterministic macro** (same pixels, no reasoning) → else vision browser-use → same session-save on Skool, X, Instagram → Demo 3: YouTube URL → X article **draft** (he reviews, then would publish); why not X API: formatting / media weirdness + **X charges per usage** UNVERIFIED → parallel chat: scroll signed-in X feed; captions name **Grok Bot** and **Grok 4.6** → computer-use plugin: open **Glydo**, toggle Bluetooth-devices; stop if Windows asks admin / security it can’t interpret; privacy permission blocked one path; it still flipped the in-app toggle → remote Codex from phone to drive the desktop — on tape only → close: API handles **~95%** UNVERIFIED; many “browser” jobs are a script; keep vision in the pocket; X-draft time save **5–10 min** UNVERIFIED; value = shoot a skill and return to the other task; chain skills; chess / games / **Tetris world record** on tape. Timestamp UNKNOWN. Codex / Claude / Glydo / Relay / Skool on-tape.

## B. Atomic Knowledge

### API → macro → vision last
- **Claim:** First call is always an API. No API → deterministic pixel/macro. Only then vision browser-use.
- **Reasoning:** API is fastest, usually cheapest, most consistent. Vision decides clicks from pixels — not a guaranteed path.
- **Mechanism:** Ladder he states after the Relay demo.
- **Evidence:** Relay one-shot then session expire; X draft via browser because API formatting/fees (claimed).
- **Conditions:** Any “automate this site” request.
- **Exceptions:** He says sometimes browser is cheaper than a metered API (X) — still a vendor $ claim UNVERIFIED.
- **Action:** Steal the ladder. Do not install Codex to get a headed pane.
- **Confidence:** high on the ladder; ~95% / 5–10 min / X fees UNVERIFIED.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

### Headed break-it, then a named fail list
- **Claim:** Annotate one broken UI, then try to break it. Headed when you need to see the path.
- **Reasoning:** Users are unpredictable; an agent can simulate weird humans. He planted some UI bugs to show annotate.
- **Mechanism:** Annotate → chat fix → headed QA → pass/fail list → “go fix those bugs.”
- **Evidence:** Invalid data through, UK→US reset, keyboard bypass, mobile fails.
- **Conditions:** A URL you already have (localhost / preview).
- **Exceptions:** Slash-goal 100 scenarios is optional theater; he already found bugs without it.
- **Action:** One headed pass on a shipped URL. Do not mill 100 scenarios.
- **Confidence:** high; 85 checks / 100 scenarios UNVERIFIED.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

### Money-adjacent: watch many times; 2FA stays human; draft ≠ publish
- **Claim:** Bank / session skills: sit headed ≥10 times, keep the script strict. 2FA from the phone is manual. X article stays a draft until he reviews.
- **Reasoning:** Vision is not a guaranteed path. SMS computer-use is “be careful.”
- **Mechanism:** Session save + optional password CSV; he still watches.
- **Evidence:** Relay boot-to-login; Glydo admin/privacy guardrail.
- **Conditions:** Banking, 2FA, publish, admin prompts.
- **Exceptions:** Skool/X/IG session-save is his vendor habit, not ours.
- **Action:** Steal headed watch + draft-then-HITL. No password CSV. No auto-publish.
- **Confidence:** high.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Most automation is an API or a script; vision is a pocket tool. Headless is for a path you already watched. Value is less context-switch (shoot skill, return to the other task), not a 5–10 minute FACT. Guardrail: stop on admin/security the agent cannot interpret. “Automate anything” is the hook; the ladder is the teaching.

## D. Procedures
1. Ask: is there an API? 2. Else: can a macro hit the same pixels every time? 3. Else: headed vision, watch the first many runs. 4. Annotate one real UI break, fix that. 5. Try to break it; write pass/fail; fix those. 6. Creds never in chat — and we still do not import a password CSV. 7. 2FA / admin / publish stay human. 8. Chain skills only after each has a checkable stop.

## E. Examples
**Situation:** Localhost form with overlapping fields.  
**Action:** Annotate phone/company + Y-axis → then headed try-to-break.  
**Reasoning:** Show annotate, then QA.  
**Outcome:** Named fails (invalid data, country reset, keyboard, mobile).  
**Lesson:** Named fail list + fix. Not a 100-scenario mill.

**Situation:** Relay statements, no API.  
**Action:** NL skill downloads CSVs; later session expired; password CSV; 2FA would be manual.  
**Reasoning:** Vision last.  
**Outcome:** Files landed; he says watch ≥10 times on a bank.  
**Lesson:** Steal the ladder and the watch. Do not operate bank bots.

**Situation:** YouTube → X article.  
**Action:** Browser draft; he reviews before publish.  
**Reasoning:** API formatting/fees (claimed).  
**Outcome:** Draft + weak thumbnail; human still publishes.  
**Lesson:** Draft ≠ publish.

## F. Decision Rules
- If an API exists → do not start at vision.
- If money / bank / 2FA / admin → headed, human on the hard step.
- If it is a draft → Evens publishes, not the agent.
- If 85 / 100 / ~95% / 5–10 min / Tetris WR appear → UNVERIFIED.
- If the CTA is Codex desktop / Glydo / password CSV → park.

## G. Contrarian
Field starts at computer-use because “automate anything.” He still says API ~95% and many browser jobs are a script. He shows bank automation and then says that is not how you should do a bank.

## H. Assumptions
Codex session-save and Relay/X behavior are as demoed. Falsifier: vision path drifts and the “one-shot skill” fails on run 2. We do not install Codex. Survivorship: planted UI bugs for the annotate demo.

## I. Questions
Which shipped URL still has no headed break-it list? Is Cursor browser enough for annotate-one-field, or is that a missing habit?

## J. Connections
SYSTEM SYNTHESIS: `click-live-site` + `golden-test-loop` + `slice-build`. `EuzYhzB0vbI` checkable stop (do not slash-goal 100). `ask-principal` on draft/2FA/deploy. No Codex / Claude / Glydo. Grok Bot on his X feed stays on-tape — we do not `sendPrompt`.

## K. Future-Use
Headed break-it card on the next preview URL. API-then-macro-then-vision as the intake question.

## Steal / Operate-never

### Machine: API → macro → headed vision; annotate one; break-it list; draft then human
- **Epistemic:** SOURCE
- **Workflow / loop:** API? → else macro? → else headed vision → annotate one UI → try to break it → named fails → fix those → human on 2FA / publish / deploy
- **Questions / signals:** Is there an API? Can pixels repeat? Must I watch this?
- **Qualify / frame / objections:** Headless only after a watched path. “Automate anything” is the hook.
- **Procedure:** One URL we already have. No 100-scenario mill. No password CSV. No bank skill.
- **Example that proves it:** Form QA named fails; Relay watch-10; X draft not publish; Glydo stop-on-admin.
- **Why it works:** Most jobs are an API or a script. Vision without a watch is a silent fail. A draft is not a ship.
- **Conditions / exceptions:** Metered-API-vs-browser $ is UNVERIFIED. Planted bugs in the form demo.
- **Operate-never payload:** Codex install; password CSV; 2FA SMS computer-use; auto-publish; quote 85/100/~95% as FACT.
- **Hive run:** `click-live-site` + `golden-test-loop` + `slice-build` + `ask-principal`.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN

### Operate-never
- Install Codex / Claude / ChatGPT / Gemini / Glydo / computer-use plugin.
- Password-manager CSV / bank-session bots / 2FA or SMS computer-use.
- Auto-publish X / YouTube copies. Remote-from-phone desktop drive as ours.
- Slash-goal 100 UI scenarios as a mill.
- Quote 85 / 100 / ~95% / 5–10 min / X fees / Tetris WR as FACT.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will open one existing preview, annotate one real break if it exists, write a pass/fail list, fix those. I will not install Codex to get a second mouse. Draft ≠ publish. Deploy HITL.
