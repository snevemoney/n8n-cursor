# Communications Manager — CB5bG4mvnS0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
**Title (PACKET):** Codex browser agent
**Speaker / channel:** Nate Herk | AI Automation
**Kind:** demo · 4330 words · captions in `full.txt` only · timestamps **UNKNOWN** (no VTT used).
**Gaps:** Caption ingest; ASR errors possible (Naden/Nitn = n8n). Visual UI not fully narrated. Timestamps UNKNOWN.

Beats, in order:
- Codex desktop + in-app browser; sign in once, session saved. ‘Literally do anything’ — vendor open. API or plugins first; browser-use when there is no API.
- Demo 1: localhost form. Annotate overlapping fields (phone covering company; Y misalign) → chat fixes. ‘Test the heck out of this / try to break it.’ Headless = background; headed = you watch. Finds required-field gaps, invalid contact through, UK country-code reset, keyboard-submit bypass, mobile. Captions: prior SaaS-in-one-day did 85 focused browser checks. /goal: don’t stop until 100 unique UI edge cases — then fix. UNVERIFIED.
- Demo 2 — no API: Relay banking statements. NL skill: accounts → two statements → CSV → folder. One-shot first; session later expired (bank kick). Password-manager CSV (name/URL/user/password) so creds stay out of chat. 2FA from the phone stays manual; careful automating SMS via computer-use. Watch bank flows ≥10 times; skill script strict — vision decides clicks, not a guaranteed path.
- Ladder: API first (fastest, usually cheapest, most consistent) → else deterministic macro (same pixels, no reasoning) → else vision browser-use. Same session-save on Skool, X, Instagram.
- Demo 3: today’s YouTube URL → X article draft (he reviews, then would publish). Why not X API: formatting/media + X charges per usage — UNVERIFIED. Parallel: scroll signed-in X; captions name Grok Bot and Grok 4.6. Plugin: open Glydo, toggle Bluetooth; stop if Windows admin/security it can’t interpret. Privacy permission blocked one path; still flipped in-app toggle. Remote Codex from phone — on tape only.
- Close: API handles ~95%; many ‘browser’ jobs are a script; keep vision in the pocket. X draft time-save 5–10 min realistically. Value = shoot a skill and return to the other task. Chess/games/Tetris world record — on tape. $ UNVERIFIED.

## B. Atomic Knowledge

### API first; browser-use is the pocket, not the mailer
- **Claim:** If an API exists, use it. Macros beat vision when the pixels are stable. Vision is last and must be watched on money paths.
- **Reasoning:** Gmail has an API. A cheaper browser path is not a reason to skip the send gate.
- **Mechanism:** API → macro → headed vision. Creds in a password CSV not in chat. 2FA stays on the phone.
- **Evidence:** Relay statements; X draft then review; 85/100/95% claims.
- **Conditions:** A host with no API, or a UI you must see.
- **Exceptions:** 85/100/95%/5–10 min/X fees UNVERIFIED. Saved-session Skool/X/IG mailer never. SMS computer-use never. Browser-click Send never.
- **Action:** Steal draft-then-human-publish + secrets-out-of-chat. Do not install Codex.
- **Confidence:** high
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

### Headed watch on money; draft is not publish
- **Claim:** Bank: watch ≥10 times. X: review then maybe publish. Headless is not overnight send.
- **Reasoning:** Vision is deciding clicks. Sessions expire. Admin prompts are a stop.
- **Mechanism:** Annotate one leak; try to break it; stop. Don’t /goal a send.
- **Evidence:** Overlapping fields; UK code reset; keyboard bypass.
- **Conditions:** Anything that could leave or touch a bank.
- **Exceptions:** Remote-from-phone send is never. Grok Bot on the X feed is not our Grok Bot.
- **Action:** Money-adjacent = Evens watches. Publish = Evens.
- **Confidence:** high
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- Try-to-break is the QA. **SOURCE**
- Session-save is how Skool/X/IG stay logged in — that’s the never. **SOURCE**
- Shoot the skill and return to the other task = less context-switch, not a send. **SOURCE**

## D. Procedures
- Annotate the overlapping field; don’t paste the vault. **SOURCE**
- Password CSV hygiene; 2FA human. **SOURCE**
- This desk: X-article-as-draft. No browser publish. **SYSTEM SYNTHESIS**

## E. Examples
- **Situation:** YouTube URL from a video he uploaded today. → **Action:** Codex writes an X article as a draft; he reviews before publish. → **Reasoning:** API formatting/fees. → **Outcome:** A draft. → **Lesson:** Draft then human. Implicit rule: Grok-on-the-feed is not sendPrompt.

## F. Decision Rules
- If an API exists for mail → no vision send.
- If it’s a bank → headed, ≥10 watches, 2FA human.
- If Windows asks admin it can’t read → stop.
- Refuse: 85/100/95% as FACT. Codex as ours. Saved-session mailer. Tetris as FACT.
- Optimize: one annotated leak + a draft.

## G. Contrarian
- Field hears ‘automate anything in the browser.’ He still ranks API then macro then vision. **SOURCE**

## H. Assumptions
- Vendor open + self-demo. All counts UNVERIFIED. Falsifier: a vision path that is safer than the API (he doesn’t claim that).

## I. Questions
- What money-adjacent hive path would we watch headed ten times (and still not arm)?

## J. Connections
- **SYSTEM SYNTHESIS:** `5p5cV0yVDvQ` (MCP send). `ehg4fhydTgs` (cookies). `click-live-site` · `warm-draft-hitl`.

## K. Future-Use
- API→macro→vision ladder as a standing tool chooser. 2FA-human as a never.

## Steal / Operate-never

### Machine: API first; X-article stays a draft; never browser-send; never 2FA-computer-use; never saved-session mailer
- **Epistemic:** SOURCE + SYSTEM SYNTHESIS
- **Workflow / loop:** Need a host → API if it exists → else draft in our stack → Evens publishes/sends → stop. Headed if money. No Codex.
- **Questions / signals:** Is there an API? Bank/2FA? Session save? Grok Bot line?
- **Qualify / frame / objections:** Qualify: draft vs publish. Frame: HITL. Objection: ‘no API so browser-send’ → Gmail has an API; send still never.
- **Procedure:** 1) API first. 2) Draft. 3) No CSV-in-chat. 4) No publish.
- **Example that proves it:** X article as draft; Relay + phone 2FA; 85 checks on a prior demo (UNVERIFIED).
- **Why it works:** Vision is a pocket tool. Publish is a human.
- **Conditions / exceptions:** Codex desktop tape. Exceptions: our stack is Cursor + Grok.
- **Operate-never payload:** Browser-click Send/Publish. Password-CSV to us. SMS computer-use. Quote 95%/Tetris.
- **Hive run (existing skills only):** `warm-draft-hitl` · `send-removed` · `ask-principal` · `golden-test-loop`.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN


### Operate-never (this desk will not operate)
- Codex/computer-use as ours. Browser-click Send or Publish. 2FA/SMS computer-use. Quote 85/100/95% as FACT.
- Gmail **send**. Connector write. Scheduled send. Auto-send. Ack-reply. Mass-DM. Postcard. Waitlist blast.
- Quote tape $ / student counts / job-loss % / ROI hours as FACT in a letter.
- New hunt ICP. Unpark Normand. Clients parked — no Path A this week.
- Install on-tape vendors (Vapi, Claude, Codex, ChatGPT, Gemini, Coda, Abacus, Skool, n8n-cloud). Stack stays Cursor + Grok. On-tape names stay on-tape.
- Grok Bot / `sendPrompt`. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md. New `icp_id`.

## L. Role-Specific Applications
- I draft. Evens publishes. I do not browser-send. Clients parked.
