# Researcher — CB5bG4mvnS0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Caption-only (`full.txt`, ~4330 words). Title: Codex's Browser Agent Automates Literally Anything. Visual/click **UNKNOWN**. Timestamp **UNKNOWN**. Beats: (1) Codex desktop: in-app browser. APIs/plugins first; browser when there is no API. SaaS-in-a-day leftover: **85** focused checks, click/type/break, lots of bugs — UNVERIFIED. (2) Localhost form: he *seeded* UI bugs to show **annotate** (click element → describe fix → batch send) — Lovable/Claude-design-ish. You can drive the browser yourself; Codex mouse is distinct when it takes over. (3) Prompt: “test the heck out of this / try to break it.” **Headless** = background, keep working; **headed** = you watch. Empty-submit shows required fields; then it fills and continues. Finds: invalid contact data gets through; UK country-code resets to US on edit; keyboard-submit / restart bypasses; **mobile** responsiveness fails he hadn’t tested. Then: implement the fixes. Optional `/goal` until 100 unique UI scenarios. (4) No-API: Relay bank statements. NL skill: dashboard → accounts → two statements → CSV → folder. One-shot + saved session. Re-run hits login (bank kicks you). Codex **password manager**: import CSV (name, URL, username, password, notes) — not in chat. Fills Relay; **2FA still human** (he warns against automating SMS via computer-use). Watch the skill **≥10 times**, tighten the script; vision is not a bank-grade robot. Ladder: **API first** (fast/cheap/consistent) → **deterministic pixel macro** if the path never changes → **vision browser** only if the UI reasons. (5) Skool / X / IG keep sessions longer. YouTube URL → X article draft via browser (X API formatting/media/$ — he prefers sub-included browser). Parallel chat: new session, X already signed in, scrolls feed, notices Grok Bot / Grok 4.6. Schedule those skills. (6) Computer use = vision on the **desktop** (settings, native apps). Plugin install by name. Glydo: “turn on Bluetooth devices.” First it refuses as OS privacy; then opens Glydo (blue-hue chrome so you know it’s driving) and toggles the in-app box. Phone-remote Codex → drive a PC you are not in front of. (7) X article: drafts, title, paste, weak thumbnail, then YouTube screenshots dragged into the body. Value: API covers ~**95%**; this saves **5–10 min** and kills context-switch; stack skills. Chess/Tetris world-record aside. **Do not flatten** vs `ehg4fhydTgs` Playwright-cookies · Printing Press scrape operate-never. All 85 / 95% / 5–10 min UNVERIFIED.

## B. Atomic Knowledge

### API → macro → vision is the order
- **Claim:** Browser-use is the last resort. If an API exists, use it. If the clicks never change, a pixel macro is cheaper and safer than a model that looks. Vision is for UIs that branch.
- **Reasoning:** Vision + bank login is a 10-watch job, not a one-shot flex. 2FA stays human.
- **Mechanism:** Codex headed/headless browser; optional password-manager CSV; computer-use plugin for native apps.
- **Evidence:** Relay one-shot then session death; he says don’t do banks that way; X API exists but he used browser for format/$ .
- **Conditions:** Desktop Codex as taped.
- **Exceptions:** He still used browser *despite* an X API (cost/format). That’s a disagreement with his own ladder — keep labeled.
- **Action:** Steal the ladder. Operate-never: bank CSV in Codex, 2FA-auto, IG/Skool scrape.
- **Confidence:** high as the ladder; X exception is his.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Relay kicked to login; Glydo privacy false-positive
- **Speech ≠ behavior:** “literally anything” vs 2FA wall + “watch it 10 times.”

### Break-it QA is headed first, then optional `/goal` volume
- **Claim:** Annotate seeded layout bugs; then a break-it pass finds data-integrity and mobile issues a human five-case test misses. Headed so you can see; headless for long background. Then “go fix.”
- **Reasoning:** Production users are weirder than your edge list. An agent that *tries to break* is the sim.
- **Mechanism:** In-app browser + annotate + QA prompt. 85-check claim from another tape.
- **Evidence:** Invalid data through; UK→US reset; mobile fails.
- **Conditions:** He planted some UI bugs for the annotate demo — don’t treat those as agent-found.
- **Exceptions:** `/goal` 100 scenarios is suggested, not run here.
- **Action:** Steal break-it + headed-then-fix. `golden-test-loop`. No Codex install.
- **Confidence:** high as the method; 85 UNVERIFIED.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** the bugs it found
- **Speech ≠ behavior:** seeded bugs vs “found a ton.”

### Saved session ≠ saved secret; 2FA is the wall
- **Claim:** Codex keeps cookies for Skool/X/IG. Banks boot you. Password manager CSV is “more secure than chat” and still a file of secrets. Computer-use paints a blue frame; it refused an OS Bluetooth permission then did the in-app toggle.
- **Reasoning:** Remote-phone drive of a desktop is power and a stolen-session shape.
- **Mechanism:** Import CSV; skill + schedule; computer-use plugin.
- **Evidence:** Relay re-login; X already signed in on a fresh chat; Glydo hue + toggle.
- **Conditions:** His machines. Tetris/chess claims UNVERIFIED.
- **Exceptions:** none that remove 2FA.
- **Action:** Steal session-vs-secret. Hive: no password CSV, no computer-use, publish stays HITL (X draft ≠ post).
- **Confidence:** high as the wall.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Relay session; Glydo first refuse
- **Speech ≠ behavior:** “I will review” the X draft vs title “automates literally anything.”

## C. Mental Models
Browser is a fallback, not a platform. Headed is for trust; headless is for volume. Annotate is design; break-it is QA. Skills + schedule are how 5–10 minutes stack. A password manager inside the agent is still a secret store. Blue frame = someone is driving.

## D. Procedures
1. Need a click path? Ask: API? If yes, stop.
2. If the pixels never move → deterministic macro (on-tape).
3. If the UI branches → vision browser; write the skill in NL; **watch ≥10** headed runs; tighten.
4. QA a UI: annotate known nits; then “try to break it”; include mobile; then implement.
5. Sessions die on banks; 2FA is HITL. Do not SMS-auto.
6. Computer-use only after the plugin; expect permission refusals on OS settings.
7. Hive: no Codex, no Relay CSV, no X/IG publish, no Skool scrape.

## E. Examples
- **Situation:** Localhost form. **Action:** annotate + break-it. **Outcome:** integrity + mobile bugs. **Lesson:** plant-bugs ≠ found-bugs; the QA pass still found new ones.
- **Situation:** Relay June statements. **Action:** NL skill. **Outcome:** one-shot, then login wall, then CSV password fill. **Lesson:** session ≠ bank.
- **Situation:** X article. **Action:** browser despite API. **Outcome:** draft + screenshots; thumbnail weak. **Lesson:** he broke his own API-first rule for format/$.
- **Situation:** Glydo Bluetooth. **Action:** computer-use. **Outcome:** OS refuse then in-app toggle. **Lesson:** it can mis-scope “settings.”

## F. Decision Rules
- IF an API exists → he says use it (except his X $ / format exception — labeled).
- IF path is static → macro, not vision.
- IF bank/2FA → watch or don’t automate.
- IF you need to see it → headed; if you need hours → headless.
- IF OS permission → expect a refuse.
- Refuse: Codex install; password CSV; quote 85/95%/5–10 as FACT; new ICP; publish the X draft.

## G. Contrarian
“Literally anything” dies on 2FA and a Glydo privacy check. He seeds bugs then praises the finder. He uses browser where an API exists (X). Password-manager CSV is still a plaintext import. Phone-remote computer-use is a theft story he tells as a feature.

## H. Assumptions
85 checks, 95% API, 5–10 min, Tetris record = **UNVERIFIED**.
**Desk dissent:** Steal API→macro→vision and break-it QA. Operate-never bank/social browser agents.

## I. Questions
- SaaS-in-a-day video id for the 85 checks?
- Password CSV encryption at rest?
- Did `/goal` 100 ever run?

## J. Connections
- **SYSTEM SYNTHESIS:** `ehg4fhydTgs` (cookies die remote) · `7siRW0My05o` (Playwright school) · `golden-test-loop` · scrape operate-never. Skills: `golden-test-loop` · `ask-principal` · `send-removed`.

## K. Future-Use
API-macro-vision ladder. Headed break-it QA. Annotate-then-fix. Session≠secret. 2FA wall. Blue-frame computer-use. Skill-stack for context-switch.

## Steal / Operate-never

### Machine: api-then-macro-then-vision
- **Epistemic:** SOURCE
- **Workflow / loop:** ask API → else static macro → else headed vision skill watched ×10 → optional schedule → never auto-2FA
- **Questions / signals:** Does the path branch? Will the bank kick the cookie? Am I about to put passwords in chat?
- **Qualify / frame / objections:** X-browser-despite-API is a labeled exception. 5–10 min is his feel.
- **Procedure:** D.
- **Example that proves it:** Relay login wall; form QA mobile fail; Glydo mis-scope.
- **Why it works:** Consistency lives in APIs and pixels; vision is for the leftover branchy UI.
- **Conditions / exceptions:** Hive does not install Codex. Publish HITL.
- **Operate-never payload:** Bank CSV; 2FA-auto; IG/X/Skool scrape-publish; quote 85 as FACT; new ICP.
- **Hive run (existing skills only):** `golden-test-loop` · `ask-principal` · `send-removed`
- **Source:** `CB5bG4mvnS0` @ UNKNOWN

**Operate-never**
- Codex browser on banks/social. Import password CSV. Quote tape counts as FACT. New `icp_id`. Send / pay / deploy / publish.

## L. Role-Specific Applications
Keep the ladder and the break-it QA. Do not port Codex computer-use. Label the X-API exception.
