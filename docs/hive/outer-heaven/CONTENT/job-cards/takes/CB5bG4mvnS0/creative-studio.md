# Creative Studio — CB5bG4mvnS0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: Codex **browser use** (desktop app, pane on the right). Beats: “best I’ve tried”; sign-in once, session saved; API/plugins first, browser when there is no API; SaaS-in-a-day sister: **85** focused checks (UNVERIFIED); localhost form — annotate overlapping phone/company (Lovable/Claude-design feel); headed vs **headless** (hours in background); “test the heck… try to break it”; empty-submit required fields; invalid contact still passes; UK country-code resets to US; keyboard submit; mobile fails; `/goal` 100 unique scenarios (he did not run it); then “go fix”; **Relay** bank statements, no API — vision clicks, one-shot skill, then login expired; Codex password-manager **CSV** (name/url/user/pass); 2FA he would have to do himself — “careful” about SMS + computer-use; watch **≥10 times** before you trust a bank skill; ladder: API → deterministic pixel macro → vision browser; X article from today’s YouTube — draft not publish (API formatting/media/$); session already signed in; Grok Bot / Grok 4.6 on the feed (on-tape names); computer-use plugin → Glydo Bluetooth (privacy block, then in-app toggle; blue hue = it’s driving); remote from phone; X draft thumb “doesn’t look very good”; later it screenshots the YT and **drags** stills into the article; 5–10 min saved, value = no context-switch + stacked skills; Tetris world-record aside. Visual: Codex mouse vs his, annotate pins, blue-hue desktop, X draft.

## B. Atomic Knowledge

### API, then macro, then vision
- **Claim:** Browser-use is the last resort. API is cheaper and consistent. If the path is the same pixels every time, a macro does not need to “see.” Vision is only when the next click depends on the next UI.
- **Evidence:** “your first call should always be to go for an API… if you can’t do that every single time because there is an element of vision or reasoning, then you need… browser use.”
- **Conditions:** He still used browser on X because the API “charges you” and formats badly (on-tape).
- **Exceptions:** Cheaper-than-API is a cost claim, UNVERIFIED.
- **Action:** Learn the ladder; do not install Codex.
- **Confidence:** SOURCE.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

### Watch ten times; 2FA stays human
- **Claim:** A one-shot bank skill is not a schedule. Sit and watch ~10 runs. Password CSV is “more secure than chat” on tape — still a secret file. 2FA is HITL; he flags SMS+computer-use as something to be careful about.
- **Evidence:** “especially with something like a bank account? No… at least 10 times… if this asks me for a two-factor… I’d have to do that manually.”
- **Conditions:** Relay boots sessions.
- **Exceptions:** Skool/X/IG he says stay signed in (do not copy).
- **Action:** Never import a password CSV; never auto-login a bank.
- **Confidence:** SOURCE as his caution, not a how-to.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

### Annotate the break, then try to break it
- **Claim:** Pins on the overlapping box are the design brief. “Try to break it” is the QA brief. Unpredictable users are the reason.
- **Evidence:** “would you rather have something that has been not tested at all, or… tested 100 times already… tell the agent to try to break it.”
- **Conditions:** Localhost form he seeded with bugs for the demo.
- **Exceptions:** Headless hours still need a human to read the fail list before fix-and-ship.
- **Action:** Plate the annotate pins; do not ship “85 checks” as FACT.
- **Confidence:** SOURCE.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Headed = you can shut the bad path. Headless = you cannot see it lie. Draft-not-publish on X. Blue hue is the honesty chrome. 5–10 minutes is a stacked-skill story, not a rate.

## D. Procedures
(Learn.) Prefer API → else macro → else headed vision → human watches → draft only. Annotate UI → break-test → fix list → human ships.
Avoid: Codex / password CSV / Relay / X auto-post / Skool scrape; 85 / 5–10 min as FACT; 2FA computer-use.

## E. Examples
**Situation:** Phone box covers company.  
**Action:** Two annotate pins, then send.  
**Lesson:** The still is the overlap, not “browser use.”

**Situation:** X article.  
**Action:** Format + ugly thumb + later drag YT screenshots into a **draft**.  
**Lesson:** He still reviews before publish — we never publish.

## F. Decision Rules
- If there is an API → do not start in the browser.
- If 2FA or a bank → human in the loop; no schedule.
- If the output is a social post → draft only.
- If $ / 85 / 5–10 min from this tape → UNVERIFIED.

## G. Contrarian
He sells “automate anything” and then says API covers ~95% and browser saved him minutes. The Tetris boast is flavor, not a machine.

## H. Assumptions
85 checks, 5–10 min, 95% API UNVERIFIED. On-tape Codex / Relay / X. Clients parked.

## I. Questions
Visual of the annotate pins? Did `/goal` 100 ever run? What did the X thumb look like?

## J. Connections
- SYSTEM SYNTHESIS → `R0qF17BVl9w` (`/goal`).
- SYSTEM SYNTHESIS → `ehg4fhydTgs` (do not Playwright Skool).
- SYSTEM SYNTHESIS → `click-live-site` (headed QA).

## K. Future-Use
Annotate-then-break as the QA plate. Unassigned.

## Steal / Operate-never

### Machine: pin the overlap, then try to break it
- **Epistemic:** SOURCE
- **Workflow / loop:** API? → else same-pixels macro? → else headed vision → annotate the visual bug → “try to break it” → read fails → human fix → draft only
- **Questions / signals:** Headed or headless? Session dead? 2FA up?
- **Qualify / frame / objections:** Minutes saved ≠ a bank skill
- **Procedure:** Watch repeats before any clock; never CSV passwords
- **Example that proves it:** UK→US reset; mobile fail; X draft + drag stills
- **Why it works:** Weird users beat your predicted edges; vision is last
- **Conditions / exceptions:** $ UNVERIFIED; no publish
- **Operate-never payload:** Codex; password CSV; Relay auto-login; X/IG auto-post
- **Hive run:** `click-live-site`; `golden-test-loop`; `ask-principal`
- **Source:** `CB5bG4mvnS0` @ UNKNOWN

### Operate-never
- Install Codex / computer-use. Import password CSVs. Auto-login banks. Auto-post X.
- Quote 85 / 5–10 min as FACT. Join Skool. New hunt.
- Merge `LESSONS-FROM-TAPE.md`. Game-studio / fake 3D / cheap taste / NSFW.

## L. Role-Specific Applications
Video-first: **annotate pins on the overlapping field** and the **blue-hue desktop** are the plates. X thumb is a draft we do not ship. Do not generate fake Codex chrome as the hero. HITL. Clients parked.
