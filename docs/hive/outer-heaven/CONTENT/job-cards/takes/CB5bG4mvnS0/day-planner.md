# Day Planner — CB5bG4mvnS0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/CB5bG4mvnS0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: Codex **browser use** + computer-use. Beats: sign-in-once session; **API / plugins first**, browser when no API; localhost form — **annotate** overlapping phone/company + Y-axis, then “test the heck out of this / try to break it”; **headless** vs **headed**; required-field + invalid contact + UK country-code reset + keyboard-submit + mobile; prior SaaS-in-one-day = **85** focused checks (UNVERIFIED); slash-goal **100** unique UI cases then fix; **Relay** bank statements — NL skill, one-shot, session expired; password-manager **CSV** (name/URL/user/pass) so creds stay out of chat; **2FA from the phone stays manual** (SMS computer-use = be careful); bank: watch **≥10 times**, keep the skill script strict (vision clicks, not a guaranteed path); ladder: **API → deterministic macro (same pixels, no reasoning) → vision**; same session-save on Skool / X / Instagram; YouTube URL → X article **draft** (he reviews); X API formatting/media + **X charges per usage** (UNVERIFIED); parallel chat scrolls signed-in X (names **Grok Bot** / **Grok 4.6**); computer-use plugin → **Glydo** Bluetooth toggle; stop if Windows admin/security it can’t interpret; privacy permission blocked one path, in-app toggle still flipped; remote Codex from phone (on tape); API handles **~95%**; many “browser” jobs are a script; X draft saves **5–10 min** (he says); chess / games / **Tetris world record** (on tape). Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### API first, then a pixel-macro, then vision; headed “try to break it”; money flows stay watched
- **Claim:** Browser-use is a pocket tool, not the default. QA means try-to-break + a fail list. Bank/session skills need a human watching many times; 2FA stays human.
- **Reasoning:** Vision decides clicks; banks kick sessions; a macro is cheaper/safer when the path never changes.
- **Mechanism:** Annotate → headed QA → fail list → fix. No-API: watch ≥10 → strict skill. Publish stays HITL.
- **Evidence:** “your first call should always be to go for an API.” / “watch it… at least 10 times.” / “I will go ahead and review it.”
- **Conditions:** A UI you own, or a no-API path you can watch.
- **Exceptions:** He still imported a password CSV and praised one-shot Relay — we do not.
- **Action:** Steal headed-break + API→macro→vision. Do not Codex. Do not bank-bot. Do not auto-publish X.
- **Confidence:** high as the ladder.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Relay login expired; Glydo privacy block then in-app toggle
- **Speech ≠ behavior:** “automate anything” vs 2FA/admin stops + “I will review”

## C. Mental Models
95% is API. Headed = you can steer. Context-switch is the real cost. Priority: shoot a skill and return. Uncertainty: 85 / 100 / 95% / Tetris.

## D. Procedures
1. If an API exists, use it (our stack, not his).
2. If the path is the same pixels every time, a macro — not vision.
3. Headed: try to break it; keep a fail list; then fix.
4. Money-adjacent: watch many times; 2FA never automated.
5. Social: draft only; Evens publishes.
Avoid: Codex; password CSV; bank session; Skool; quote 85/100/95% as FACT.

## E. Examples
**Form QA:** Situation → localhost form. Action → annotate, then try-to-break. Reasoning → users are weird. Outcome → required/invalid/UK/keyboard/mobile fails. Lesson → steal the fail list.

**Relay:** Situation → no API. Action → NL skill, then expired session. Reasoning → banks boot you. Outcome → CSV login (on tape). Lesson → watch ≥10; never import the CSV.

## F. Decision Rules
- IF API exists → do not open a browser agent.
- IF pixels never change → macro, not vision.
- IF 2FA / admin / bank → human.
- IF the output is an X/YouTube post → draft, not publish.

## G. Contrarian
Rejects “browser-use everything.” Field: computer-use as the product. He: pocket after API.

## H. Assumptions
Theirs: Codex session-save is safe. Ours: no Codex; no bank-bot. Falsifier: a headed QA that ships without a fail list. Survivorship: one form + one Relay + one draft.

## I. Questions
Same 85-check SaaS tape? Glydo = what product?

## J. Connections
- SYSTEM SYNTHESIS → `click-live-site` · `golden-test-loop` · `send-removed` · `jZgcWCzxh1I` (cap).

## K. Future-Use
Headed-break. API→macro→vision. Unassigned Tetris / phone-remote.

## Steal / Operate-never

### Machine: API → same-pixel macro → vision last; headed try-to-break; money stays watched
- **Epistemic:** SOURCE
- **Workflow / loop:** ask API? → else same pixels? → else headed vision with a fail list → fix → if money/2FA, human
- **Questions / signals:** API? Session expire? Admin prompt? Draft or publish?
- **Qualify / frame / objections:** “Automate anything” is the fail. Review-then-publish is the pass.
- **Procedure:** No Codex. No password CSV. No bank skill. No X publish.
- **Example that proves it:** Situation → form. Action → try to break. Reasoning → weird users. Outcome → a fail list. Lesson → steal the list.
- **Why it works:** A fail list is checkable; a vision bank click is not.
- **Conditions / exceptions:** 85/100/95%/5–10 min UNVERIFIED.
- **Operate-never payload:** Codex; Relay CSV; 2FA computer-use; auto-publish; Skool; quote benches as FACT.
- **Hive run (existing skills only):** `click-live-site` · `golden-test-loop` · `ask-principal`.
- **Source:** `CB5bG4mvnS0` @ UNKNOWN

### Operate-never
- Codex / bank-bot / password CSV / auto-publish X.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as headed-break + API-first (no Codex). Clients parked.
