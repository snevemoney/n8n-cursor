# Researcher — ZAaxx3qyT8g
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Claude Code “agent view.” Beats: (1) Four agents, one view; arrows/click; yellow = needs input (approve plan); green = done, sinks. (2) Replaces 5+ mystery terminal tabs; recap-at-end existed; now enter/exit sessions in one tab. (3) CLI not the VS Code extension — “most functionality”; challenge: use the terminal this week. (4) Left arrow = agent view; right = enter session; `/bg` parks a session; type a task in the view to spawn; `claude --bg "task"` (quotes, not brackets — research-preview bug). (5) Four vendor patterns: scale concurrent; manage long-running; `/goal` ≈ Codex Ralph loop — keep going until objective; hours/overnight; **subjective prompt is the wrong use** — want an objective metric (Karpathy auto-research). This video is not about `/goal`. (6) Preview bugs: machine slow; “build a loan” ASR/ambiguous. (7) Value of flags: **different directories** — `cd` then `--bg` so you are not stuck spawning inside Herc 2; directory not shown in the list (he wishes). (8) Kill: Ctrl-X twice. Reply from the view with space (e.g. don’t save LinkedIn posts). (9) If you don’t see the value, you’re not running multi-session = not using it as an OS → CTA 2.5h AIOS course. Timestamp UNKNOWN. Claude on-tape.

## B. Atomic Knowledge

### One pane: status + enter + spawn + reply
- **Claim:** Parallel agents are unusable without a status pane (working / needs input / done) you can enter, answer, spawn from, and kill.
- **Reasoning:** Tabs hide which session is blocked.
- **Mechanism:** Left/right arrows; `/bg`; type-to-spawn; space-to-reply; Ctrl-X×2.
- **Evidence:** Yellow plan-approve; green sink; LinkedIn-save demo refused from the pane.
- **Conditions:** Multi-session work. CLI.
- **Exceptions:** Research preview slowness/bugs. Hive maps this to Cursor Task desks, not Claude CLI.
- **Action:** Steal status-pane + input-gate + objective `/goal`. Do not install Claude.
- **Confidence:** high as a UX lesson.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

### `/goal` needs a metric, not a vibe
- **Claim:** Overnight `/goal` on “3D monster game” is the wrong shape; prompt like Karpathy — optimize a number.
- **Reasoning:** One-shot subjective goals wander.
- **Mechanism:** `/goal` + objective metric; pane shows runtime.
- **Evidence:** He says this is not the best way; then uses the bad example anyway to show the pane.
- **Conditions:** Long-running jobs.
- **Exceptions:** `ONmaDdOBGig` used `/goal` with reputation stakes — related, not the same as a numeric metric. Do not flatten.
- **Action:** If we loop, name a checkable metric.
- **Confidence:** high as his rule.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
OS = many sessions. CLI > friendly extension. Directory context is a footgun. Recap helped; a pane is better.

## D. Procedures
1. If >1 agent: one status list (we: hive desks).
2. Needs-input is a first-class state — answer or kill.
3. Spawn with an explicit cwd if the job is another project.
4. `/goal` only with an objective metric.
5. Preview software: expect slowness; quote-wrap flags.

## E. Examples
- **Situation:** Four Claude sessions. **Action:** Agent view; approve from yellow; refuse LinkedIn save from the pane. **Reasoning:** Don’t hunt tabs. **Outcome:** Green sink. **Lesson:** Input is the product. Implicit rule: if you never go yellow, you may not be running real work.

## F. Decision Rules
- If many sessions and no pane → you will lose the blocked one.
- If `/goal` is subjective → rewrite to a metric or don’t overnight it.
- Refuse: Claude CLI as hive; 2.5h AIOS course; auto-approve plans.

## G. Contrarian
The extension is the trap. Also: more `/goal` is not more OS.

## H. Assumptions
Left-arrow UX is stable. Directory guess (“pretty sure Herc 2”) shows the list is incomplete.
**Desk dissent:** Map to `hive-spawn-desks` + `ask-principal`; do not run Claude agents.

## I. Questions
- Will they show cwd in the list?
- What is the 2.5h course id?

## J. Connections
- **SYSTEM SYNTHESIS:** `xsAOpqjebOo` / `62Rfe1w9NBc` (watch agents). `ONmaDdOBGig` (`/goal`). `hive-spawn-desks` · `input-required-gate` · `ask-principal`.

## K. Future-Use
Needs-input as a first-class desk state.

## Steal / Operate-never

### Machine: status-pane-plus-input-gate
- **Epistemic:** SOURCE + SYSTEM SYNTHESIS (Cursor desks)
- **Workflow / loop:** spawn scoped jobs → one list (working/input/done) → enter the yellow → HITL answer → kill with intent → `/goal` only if metric-named
- **Questions / signals:** Which session is blocked? Which directory? Is the goal a number?
- **Qualify / frame / objections:** “I’m not using it as an OS” → he upsells a course; we already have desks.
- **Procedure:** D.
- **Example that proves it:** Yellow plan-approve; space-reply no-save; green sink.
- **Why it works:** Blocked work is visible.
- **Conditions / exceptions:** Claude preview. Hive analog is desks, not CLI.
- **Operate-never payload:** Claude agent view; overnight vibe `/goal`; course CTA; new ICP.
- **Hive run:** `hive-spawn-desks` · `input-required-gate` · `ask-principal` · `coverage-loop`
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN

**Operate-never**
- Install Claude. Auto-approve. New `icp_id`. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
Confirm hive already has the pane analog. Do not take the AIOS course CTA as a task.
