# Consultant — ZAaxx3qyT8g
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map

Claude Code “agent view” dashboard. Beats: four agents in one view; arrow-keys or click; yellow = needs input (approve plan / feedback); green = done. Replaces five VS Code terminal tabs. Recap-at-end existed; this is live switch in/out. CLI-only — he challenges extension users to use the terminal (most functionality; Codex too). Left arrow = agent view; right arrow = enter session; `/bg` parks a session; describe a task from the view to spawn; `claude --bg "task"` (quotes, not brackets — research-preview bug). `/goal` = Codex-like / Ralph Wiggum loop until objective; can run hours/overnight; **bad** on subjective “3D monster fighting game”; good when you give an objective metric (Karpathy auto-research style). Preview: machine got slow; “build a loan” cut off / too ambiguous. Ctrl-X twice kills. Space replies from the list (e.g. don’t save the LinkedIn posts). Value is low if you are not already multi-session / using it as an OS → CTA 2.5h AIOS course. No VTT. UNKNOWN. ~2123 words.

## B. Atomic Knowledge

### One pane, status colors, and a needs-input yellow
- **Claim:** Agent view is a single tab that shows working / needs-input / done so you stop hunting terminal tabs.
- **Reasoning:** Invisible parallel work was the old pain (`xsAOpqjebOo`).
- **Mechanism:** Left arrow to the list → yellow means you owe a decision → enter, answer, or space-reply from the list.
- **Evidence:** “this one turned yellow and it says it needs input.”
- **Conditions:** CLI research preview. Four sessions on tape.
- **Exceptions:** Preview is buggy/slow. Claude-only.
- **Action:** Steal the status pane + yellow gate. Do not install Claude Code. Do not `/goal` overnight.
- **Confidence:** high
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN — “manage all of your different working agents just in one terminal tab”
- **Epistemic:** SOURCE
### /goal without a metric is a subjective loop
- **Claim:** He says `/goal` will grind toward an objective for hours, and a 3D monster game is the wrong kind of prompt — you want a metric, like Karpathy auto-research.
- **Reasoning:** Until-satisfied is not a stop (same smash as `EuzYhzB0vbI`).
- **Mechanism:** If you ever loop: name a number a child could check. Else don’t.
- **Evidence:** “it's usually best if you can give it an objective metric to hit.”
- **Conditions:** Aside in a dashboard video. Codex `/goal` named as cousin.
- **Exceptions:** Overnight `/goal` is still 24/7 ≠ money.
- **Action:** Steal metric-or-don’t. Do not run `/goal` on a client.
- **Confidence:** high
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN — “this was a very subjective prompt”
- **Epistemic:** SOURCE


## C. Mental Models

He wants Claude Code to feel like an OS. He is impatient with command memorization (spawn from the view). He is honest about preview bugs. He uses the video to funnel a 2.5h course. CLI-bravado (“terminal boy” on the twin tape).

## D. Procedures

On-tape: left/right arrows, `/bg`, space-reply, Ctrl-X twice, `/goal`+metric. Ours: if we ever have parallel workers, show status + a yellow human gate; never Claude agent view; never overnight `/goal`.

## E. Examples

**Situation:** Many Claude sessions, lost tabs. **Action:** Agent view demo; `/goal` warning; course CTA. **Outcome:** He can see four workers and answer a yellow. **Lesson:** Visibility + input gate. Implicit rule: quotes not brackets on `--bg` — preview is sloppy.

## F. Decision Rules

If you cannot see which worker needs you, you are in the old tab hell. If `/goal` has no metric, fail. If the machine is a research preview, expect slowness.

## G. Contrarian

Field default: more GUI. He pushes CLI. Field default: one-shot `/goal` a game. He laughs when it says too ambiguous.

## H. Assumptions

Claude Code on-tape. Course CTA. Preview bugs. Pair `xsAOpqjebOo` / `62Rfe1w9NBc` / `EuzYhzB0vbI`.

## I. Questions

Does agent view show the working directory clearly? (He wishes it did.) What’s in the 2.5h course we will not take?

## J. Connections

**SYSTEM SYNTHESIS:** Pair `xsAOpqjebOo`. `/goal` metric maps to `EuzYhzB0vbI` + `golden-test-loop`. Hive already has desks; we do not need Claude agent view.

## K. Future-Use

Unassigned: yellow-needs-input as a UI pattern for any parallel hive; directory-visible-in-the-list as a wish.

## Steal / Operate-never

### Machine: Visible workers + yellow human gate; loops need a metric
- **Epistemic:** SOURCE
- **Workflow / loop:** Spawn parallel jobs → one list with working/input/done → human answers yellow → kill with intent → if a loop exists, name a metric or don’t start it
- **Questions / signals:** Which one needs me? Is `/goal` subjective? Can I see the directory?
- **Qualify / frame / objections:** Qualify: you are actually running more than one job. Frame: one pane. Objection: “use it as your OS” — course smash.
- **Procedure:** Status + gate. No Claude install. No overnight `/goal`.
- **Example that proves it:** Four Claude agents; yellow approve-plan; `/goal` monster game called the wrong use.
- **Why it works:** Tabs hide work. Until-satisfied hides the stop. A metric and a yellow are toddler-checkable.
- **Conditions / exceptions:** Vendor preview. Course CTA.
- **Operate-never payload:** Install Claude Code. Overnight `/goal`. Quote the course as required.
- **Hive run (existing skills only):** `EuzYhzB0vbI` checkable-stop · `agent-as-hire` · `ask-principal`
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN


### Operate-never
- Install Claude Code / agent view.
- Overnight `/goal` on a subjective prompt.
- Treat the 2.5h AIOS course as a hive step.
- Unpark a client / new `icp_id` / new `business-lanes.json` row. Learning ≠ hunt.
- Quote tape $ / student counts / job-loss % / hours×rate as FACT.
- Send / pay / deploy / book / publish. Approve draft ≠ send.
- Install on-tape vendors (Claude, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool). Stack stays Cursor + Grok.
- Grok Bot / `sendPrompt`. Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md. Overwrite `takes/consultant.md` or another desk's take.

## L. Role-Specific Applications

**Constraint first:** The stated ask is “watch/manage agents from one view.” Felt problem is not a CLI dashboard. Do not OS-ify a parked client in Claude.

**Four-blank:** If we ever parallelize, toddler stop = you can point at the yellow one.

**Skeptical-customer:** Four little agents are smash. Clients parked.
