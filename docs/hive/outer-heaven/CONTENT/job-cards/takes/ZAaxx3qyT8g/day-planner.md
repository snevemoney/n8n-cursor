# Day Planner — ZAaxx3qyT8g
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: Claude Code “agent view.” Beats: four agents in one view; arrow/click; yellow = needs input (approve plan); green = done; left arrow = view, right = enter session; `/bg` parks a session; type a task from the view to spawn; `/goal` ≈ Codex goal / Ralph Wiggum — keep going until an **objective metric** (monster-game one-shot is the bad example; Karpathy auto-research is the good shape); goals can run hours/overnight; CLI > extension; `--bg` + quotes to launch from another directory; research-preview bugs / machine got slow; Ctrl-X twice kills; spacebar reply from the view (“don’t save those LinkedIn posts”); if you don’t run multiple sessions you’re not using it as an OS → CTA 2.5h AIOS course. Timestamp UNKNOWN. Sibling of `xsAOpqjebOo` / `62Rfe1w9NBc`. Vendor: Claude Code — on-tape.

## B. Atomic Knowledge
### One board: working / needs-input / done
- **Claim:** Parallel sessions are unusable as a pile of tabs; a single view with status (yellow input, green done) is the product.
- **Reasoning:** Recap-at-end helped; switching still failed.
- **Mechanism:** Left = board, right = session, space = reply, Ctrl-X×2 = kill.
- **Evidence:** “which ones are done or which ones might be waiting on me for input.”
- **Conditions:** More than one session.
- **Exceptions:** One session → no board needed.
- **Action:** Steal the status board. Our analog is hive desks / morning plan, not Claude CLI.
- **Confidence:** high as the UX lesson.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

### `/goal` needs a metric, not a vibe — overnight is a collision
- **Claim:** `/goal` will grind for hours/overnight; subjective one-shots are the wrong use; give an objective metric.
- **Reasoning:** Same as Ralph / auto-research.
- **Mechanism:** Set goal → it experiments until the metric.
- **Evidence:** “usually best if you can give it an objective metric to hit.”
- **Conditions:** A measurable stop exists.
- **Exceptions:** He still demos a 3D game — then says that’s not best.
- **Action:** Overnight `/goal` is a CUT collision (`ONmaDdOBGig` 40% burn). Demand a fail list.
- **Confidence:** high.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
If you are not running many sessions, you are not in “OS mode” — that is his upsell. CLI is the real surface. Preview = bugs. Priority: show the board. A second directory needs an explicit launch. Uncertainty: research-preview stability.

## D. Procedures
1. Name sessions; require a status (working / input / done).
2. Input/approve = Evens.
3. If someone wants `/goal`: write the metric first; no overnight unless Evens pays.
4. Kill is allowed (Ctrl-X analog: stop the desk).
Avoid: Claude CLI as stack; overnight monster-game; 2.5h course as a morning.

## E. Examples
**Yellow needs input:** Situation → four agents. Action → yellow session asks approve-plan. Reasoning → human gate. Outcome → he can answer from the board. Lesson → input is a status, not a buried tab.

**Ambiguous `/goal`:** Situation → “build a 3D monster game.” Action → it says too ambiguous / needs quotes. Reasoning → subjective goal. Outcome → he laughs. Lesson → metric or don’t `/goal`.

## F. Decision Rules
- If a session needs input → it is on the board, not in CUT hide.
- If `/goal` has no metric → do not run.
- If it would run overnight → conflict flag.

## G. Contrarian
Rejects the friendly extension as enough. Field assumption: GUI is safer. He pushes CLI. We do not adopt CLI-Claude; we steal the board.

## H. Assumptions
Theirs: many parallel Claude sessions = OS. Ours: hive-spawn-desks is the analog; overnight is never default. Falsifier: board without kill/reply. Survivorship: one preview day.

## I. Questions
Full AIOS course id? Same `/goal` as `ONmaDdOBGig`? `62Rfe1w9NBc` long form?

## J. Connections
- SYSTEM SYNTHESIS → `xsAOpqjebOo` · `62Rfe1w9NBc` · `hive-spawn-desks` · `morning-day-plan` (conflict = two sessions, one hour) · `ONmaDdOBGig`.

## K. Future-Use
Status board + metric-goal. Unassigned CLI.

## Steal / Operate-never

### Machine: one status board; metric before `/goal`; input is HITL
- **Epistemic:** SOURCE
- **Workflow / loop:** spawn named jobs → board shows working/input/done → Evens answers input → idle/done is the stop
- **Questions / signals:** Who needs input? Is there a metric? Will this run overnight?
- **Qualify / frame / objections:** Tab pile is the fail. Overnight vibe-goal is the fail.
- **Procedure:** Board the jobs. No Claude install. No overnight grind.
- **Example that proves it:** Situation → four Claude sessions. Action → agent view, yellow approve, green done. Reasoning → one tab. Outcome → he can manage. Lesson → steal the board; not the CLI.
- **Why it works:** Status is a checkable stop; a metric keeps `/goal` from eating the night.
- **Conditions / exceptions:** One job → no board. Preview bugs on tape.
- **Operate-never payload:** Claude Code as stack; overnight `/goal`; 2.5h course as duty; quote “OS” as FACT.
- **Hive run (existing skills only):** `hive-spawn-desks` · `morning-day-plan` · `ask-principal`.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN

### Operate-never
- Install Claude Code / switch stack.
- Overnight `/goal` / auto-approve.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as status-board + metric-before-goal. Clients parked.
