# Forge — ZAaxx3qyT8g
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Claude Code **agent view**. Beats: four agents, one view, arrow/click, yellow = needs input (approve plan), green = done → vs many VS Code terminal tabs → recap-at-end existed; this is switch-in/out of one tab → **CLI, not the friendly extension** (most functionality) → left arrow = agent view; right = enter session; `/bg` parks a session; type a task from the view to spawn → `/goal` = Codex-like / Ralph loop, hours/overnight, **needs an objective metric** (Karpathy auto-research); “3D monster game” is the bad example → research preview bugs (slow machine, cut-off prompts, **wrap in quotes**) → value of flags: **different directories / projects** (Herc 2 vs skills) — he wishes cwd showed in the list → Ctrl+X twice kills; **space** replies from the view without entering → if you don’t see value, you’re not multi-session / not using it as an OS → CTA 2.5h AIOS course. Timestamp UNKNOWN. Claude/Codex on-tape. Sibling `xsAOpqjebOo` / `62Rfe1w9NBc`.

## B. Atomic Knowledge

### One pane: status, enter, reply, kill
- **Claim:** Agent view is a manager: working / needs-input / done; left/right; space-reply; Ctrl+X×2 kill; spawn from the list.
- **Reasoning:** Tabs hid which terminal was which.
- **Mechanism:** CLI left-arrow.
- **Evidence:** Yellow plan-approve; green finished; space “don’t save those.”
- **Conditions:** Multiple sessions.
- **Exceptions:** Research-preview bugs.
- **Action:** Steal the manager pane. Do not install Claude. Cursor Tasks already spawn visible desks.
- **Confidence:** high.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

### `/goal` without a metric is a monster
- **Claim:** `/goal` should optimize a metric; subjective one-shots (3D game) are the wrong use; they run overnight.
- **Reasoning:** Same as auto-research.
- **Mechanism:** Objective → experiment until hit.
- **Evidence:** He says this video isn’t about goal, then warns.
- **Conditions:** Long-running rows in the view are why the view exists.
- **Exceptions:** Preview cut off “build a loan” as too ambiguous — the product pushed back.
- **Action:** Steal metric-or-don’t-loop. No overnight money.
- **Confidence:** high.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
If you run one session, you don’t need a fleet UI. CLI > pretty extension when you live here. CWD should be visible (he wants it). OS-use is the upsell to a course.

## D. Procedures
On tape: left arrow → read statuses → right to enter / space to answer / Ctrl+X×2 to kill → spawn from the box → quote flags if cwd differs. Hive: parent `hive-spawn-desks`; don’t install Claude CLI.

## E. Examples
**Situation:** Five tabs, lost.  
**Action:** Agent view, yellow needs plan approve.  
**Reasoning:** Manager not prompter.  
**Outcome:** He clicks in, answers, returns.  
**Lesson:** Needs-input is first class.

**Situation:** `/goal` 3D monster.  
**Action:** He says that’s subjective / bad. Preview rejects a cut-off prompt.  
**Reasoning:** Metric or don’t.  
**Outcome:** Course CTA.  
**Lesson:** Ambiguity is a feature of the warning.

## F. Decision Rules
- If a session needs input → surface it, don’t hide in a tab.
- If `/goal` has no metric → don’t start.
- If cwd isn’t visible → check before you spawn.
- If the CTA is a 2.5h Claude OS course → park.

## G. Contrarian
Field stays in the friendly extension. He pushes the scary CLI because it has the pane.

## H. Assumptions
Preview bugs are temporary. Falsifier: a view that still loses cwd and kills the wrong job.

## I. Questions
Does Cursor already have an equivalent we should not reinvent? Course packet ingested?

## J. Connections
SYSTEM SYNTHESIS: `xsAOpqjebOo`, `62Rfe1w9NBc`, `ONmaDdOBGig` `/goal`. `hive-spawn-desks`. `EuzYhzB0vbI` loops. No Claude OS.

## K. Future-Use
Needs-input + cwd on any multi-agent board we already have.

## Steal / Operate-never

### Machine: one board (status/enter/reply/kill); loops need a metric
- **Epistemic:** SOURCE
- **Workflow / loop:** spawn named jobs → board shows working/input/done → human answers input → kill the wrong ones → stop
- **Questions / signals:** Which cwd? Is `/goal` objective?
- **Qualify / frame / objections:** 2.5h AIOS course is the magnet.
- **Procedure:** No Claude CLI. No overnight `/goal` site/game.
- **Example that proves it:** Space-reply “don’t save”; 3D game as anti-pattern.
- **Why it works:** A manager who can’t see input is a tab farm.
- **Conditions / exceptions:** Vendor preview.
- **Operate-never payload:** Claude Code OS; Codex `/goal` monster; quote course as ours.
- **Hive run:** `hive-spawn-desks` (parent) + `slice-build` + `ask-principal`.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN

### Operate-never
- Install Claude/Codex. `/goal` a game/site overnight.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not install agent view. Parallel work stays visible and killable. No monster `/goal`. Deploy HITL.
