# Lead Hunter — ZAaxx3qyT8g
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** Claude Code Just Got an Agent Dashboard
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Agent view: one tab for all Claude Code sessions — arrow/click, status done vs needs-input (yellow), finished goes green. Replaces many VS Code terminal tabs + end-of-session recap.
- CLI-only (he pushes CLI over the friendly extension). Left arrow = agent view; right arrow = enter session. `/bg` parks a session; describe a task from the view to spawn one. `claude agents` or `claude --bg` from a fresh terminal.
- `/goal` = Codex-like / Ralph Wiggum loop toward an objective; can run hours; better with an objective metric (Karpathy auto-research). Long goals are why the view matters.
- Research preview bugs: cutoff names, first launch slowed his machine. Directory not shown in the list — must enter to see project (Herc 2 vs skills). Can reply from the view (space). Two-directory work via `cd` + `--bg`.
- If you don’t see value, you’re not running multiple sessions / not using it as an OS. CTA: 2.5h AIOS course video.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### One pane for status (needs-input / done); goal-loops need a metric
- **Claim:** The dashboard is a status multiplexer (yellow=approve/feedback, green=done) so you stop losing terminals; `/goal` without a metric is a hours-long one-shot.
- **Reasoning:** CLI has the features. Recap helped; the pane is better.
- **Mechanism:** Spawn/bg → watch yellow/green → enter only the one that needs you → don’t `/goal` without a number.
- **Evidence:** SOURCE: “this one turned yellow and it says it needs input… approve the plan or give feedback.” “usually best if you can give it an objective metric to hit.”
- **Conditions:** You actually run >1 session.
- **Exceptions:** Claude Code CLI is on-tape, not stack. Course CTA is capture. 3D monster game is a bad subjective goal on purpose.
- **Action:** Steal yellow=HITL. Do not install Claude Code. Do not `/goal` a live business overnight.
- **Confidence:** high
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: If you’re not multi-session, you don’t need the pane — and he uses that to sell the OS course.
- INFERENCE: Missing directory in the list is a footgun (wrong project).

## D. Procedures
- If a worker is yellow, that is HITL. If a goal has no metric, don’t start it. If two projects, confirm directory before trusting the row.

## E. Examples
- Situation: four agents, one yellow. Action: click, approve/feedback, green moves down. Reasoning: tab chaos. Outcome: course CTA. Lesson: status pane + metric-on-goal; park the CLI.

## F. Decision Rules
- If you can’t see needs-input, don’t add more workers.
- If `/goal` is “build a fun game,” expect hours of mush.

## G. Contrarian
- Rejects the friendly extension as enough. Hive rejects Claude-as-OS.

## H. Assumptions
- Research preview bugs. Course 2.5h not walked here.

## I. Questions
- Does agent view show directory yet? He wishes it did.

## J. Connections
- SYSTEM SYNTHESIS: `xsAOpqjebOo` watch-agents short.
- SYSTEM SYNTHESIS: `hive-spawn-desks` visible desks.

## K. Future-Use
- Unassigned: yellow-needs-input as the visual analog of ask-principal.

## Steal / Operate-never

### Machine: Yellow = HITL; no metric, no long goal
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** See multi-worker UI → treat yellow as approve → checkable stop = no overnight `/goal` without a metric and no Claude install.
- **Questions / signals:** Who needs input? What’s the metric? Which directory?
- **Qualify / frame / objections:** Frame: status. Objection: “OS course” → capture.
- **Procedure:** Do not install Claude Code. Do not run hour-goals on live work.
- **Example that proves it:** Situation: four sessions. Action: pane + `/goal` warning. Reasoning: long loops need a view and a metric. Outcome: course CTA. Lesson: steal yellow/metric; park the OS.
- **Why it works:** From B: needs-input and metric are explicit.
- **Conditions / exceptions:** Works as a HITL visual. Exception: single session — you don’t need the pane.
- **Operate-never payload:** Claude OS. Course funnel. Overnight goals.
- **Hive run (existing skills only):** `ask-principal`. `hive-spawn-desks`. `golden-test-loop`.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Install Claude Code. Enter the 2.5h course as a hunt. Overnight `/goal` on live work.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal yellow=HITL + metric-on-goal. No Claude OS. No course hunt.
- Hard step stays HITL: this desk drafts; Evens sends.
