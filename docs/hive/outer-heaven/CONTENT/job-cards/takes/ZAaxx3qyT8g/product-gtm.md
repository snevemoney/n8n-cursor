# Product GTM — ZAaxx3qyT8g
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZAaxx3qyT8g/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
7:36 (title: “Claude Code Just Got an Agent Dashboard”). Beats: (1) four Claude Code agents in one view; arrow/click; yellow = needs input (approve plan / feedback); green = done; replaces many VS Code terminal tabs; (2) Agent View dropped today — one tab, in/out of sessions; recap-at-end existed; CLI has more functionality than the friendly extension — try the terminal this week; (3) left arrow = agent view, right = enter session; `/bg` parks current into the view; describe a task from the view to spawn; (4) four usage patterns (on-tape list): scale concurrent sessions; manage long-running; `/goal` (Codex-like / Ralph Wiggum loop) — e.g. 3D monster fighting game until objective; (clip continues patterns). Timestamp UNKNOWN. Sibling visual: `xsAOpqjebOo` / `62Rfe1w9NBc`.

## B. Atomic Knowledge
### One pane: working / needs-input / done
- **Claim:** Parallel agents are unusable as mystery tabs; a single view with input-needed (yellow) and done (green) is the unlock.
- **Reasoning:** You cannot manage what you cannot see.
- **Mechanism:** Left/right arrows; `/bg`; spawn from the view.
- **Evidence:** Yellow plan-approve; green finished.
- **Conditions:** Claude Code CLI (on-tape).
- **Exceptions:** Extension is friendlier but weaker (his challenge).
- **Action:** Steal visible states + approval. Do not install Claude. `/goal` 24/7 is operate-never.
- **Confidence:** high for the UI beat.
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
If you need five terminals, you need a pane. CLI is the real product. `/goal` until a game exists is the overnight temptation.

## D. Procedures
See all sessions → enter the yellow one → approve/feedback → return. Spawn from the pane, not a new mystery tab.
Hive analog: parent spawn + named desks, not Claude Agent View.

## E. Examples
**Situation:** Five Claude tabs, lost. **Action:** Agent view, yellow needs input, green done. **Lesson:** States are the product. Implicit: approve-the-plan is still a human gate.

## F. Decision Rules
- If it needs input → do not ignore yellow.
- If it is `/goal` overnight → hive HITL / no auto-publish.
- Refuse: Claude dashboard SKU.

## G. Contrarian
Against the friendly extension as enough (he pushes CLI).

## H. Assumptions
Theirs: CLI this week. Ours: Cursor already is a pane. Falsifier: pane with silent wrong-dones.

## I. Questions
Patterns 3–4 in the rest of the tape (long-running /goal started). Full file continues past the 3D-game example.

## J. Connections
**SYSTEM SYNTHESIS:** `xsAOpqjebOo`, `ONmaDdOBGig` `/goal`. Maps to `hive-spawn-desks` + `ask-principal`.

## K. Future-Use
Unassigned: yellow=needs-input as a Watchdog state. Keep.

## Steal / Operate-never

### Machine: one pane of named jobs with working / needs-input / done
- **Epistemic:** SOURCE
- **Workflow / loop:** spawn named session → watch state → if needs-input, human approve → if done, open the artifact
- **Questions / signals:** Who is yellow? Who is green?
- **Qualify / frame / objections:** Mystery tabs are not a fleet
- **Procedure:** Do not install Claude CLI. `/goal` games are not Path C
- **Example that proves it:** Four agents, yellow plan-approve, green finished
- **Why it works:** Visibility + a gate
- **Conditions / exceptions:** Vendor UI
- **Operate-never payload:** Claude Agent View SKU; `/goal` 24/7
- **Hive run (existing skills only):** `hive-spawn-desks` · `ask-principal`
- **Source:** `ZAaxx3qyT8g` @ UNKNOWN

### Operate-never
- Productize Claude Agent View / `/goal`
- Switch stack; new hunt; merge LESSONS; auto-write SKILL.md

## L. Role-Specific Applications
Talk track stays 17 named desks, not four mystery Claudes. Clients parked.
