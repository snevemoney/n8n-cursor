# Forge — TDHFkKSTJ30
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/TDHFkKSTJ30/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/TDHFkKSTJ30/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **n8n NL→workflow builder** (not fully rolled out). Beats: webhook form → qualify hot/warm/cold (research + budget + “custom AI”) → ClickUp CRM + Gmail + Slack; plan + **setup guide**; it invented score/100 + priority + email subject — **don’t ship a template you don’t understand**. **Vague:** “morning news email” → 7am, default topics, research tool = **empty GET**, format rules in **user** not system. **Detailed:** 7am, Tavily+Perplexity, Sonnet 3.5, HTML title/H2/bold/2–3 sentences + sources — Tavily HTTP body looked right; **append merge → two newsletters**; builder “fixed” merge, **still append**; inline cites added to user prompt, not clickable. **Complex Telegram assistant** (Gmail/Cal/ClickUp sub-agents + Think + Sonnet 3.7): one tool each, missing user messages, wrong chat trigger — **don’t one-shot autonomous multi-agent**. Invoice preset: pretty line, **no live invoice vars** into the parser. Thesis: builder = **70% start** for **linear** flows if you already know trigger/source/dest; still read every mapping. Plus agency path. Timestamp UNKNOWN. n8n on-tape.

## B. Atomic Knowledge

### Specify the stack; the builder lies about the merge; don’t one-shot a crew
- **Claim:** Vague → empty tools. Named vendors → better HTTP. It will **say** it fixed append and not. Multi-agent one-shots miss messages. A pretty invoice line can have **no live fields**.
- **Reasoning:** Same feature as `a5sJNwfZ528` with a plan-approve step. 70% is the honest number.
- **Mechanism:** Approve plan → setup guide → you still pin and read.
- **Evidence:** Empty GET; two emails; dead calendar user-message; invoice with no vars.
- **Conditions:** Pre-GA builder.
- **Exceptions:** Native nodes easier than HTTP. Community Tavily node may be invisible to it.
- **Action:** Steal specify-stack + don’t-trust-the-fix + linear-only. Do not use n8n Cloud builder as hive.
- **Confidence:** high on the failure modes.
- **Source:** `TDHFkKSTJ30` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
False sense of security. Template ≠ production. Format belongs in the system prompt (his taste). 70% time-cut is the win, not “done.”

## D. Procedures
1. Don’t open the Cloud builder. 2. If we generate: name model/tools/shape. 3. After “I fixed it,” re-read the node. 4. Don’t one-shot a Telegram crew. 5. Don’t join Plus.

## E. Examples
**Situation:** “Fix the merge.”  
**Action:** Builder claims combine.  
**Reasoning:** Still append.  
**Outcome:** Two newsletters.  
**Lesson:** Read the node.

**Situation:** Invoice pipeline preset.  
**Action:** Parser has no live vars.  
**Reasoning:** Looks complete.  
**Outcome:** Would parse air.  
**Lesson:** Pretty ≠ wired.

## F. Decision Rules
- If the prompt is one line → expect an empty tool.
- If it says it fixed it → verify.
- If the graph is a crew → don’t generate it.
- If Plus CTA → park.

## G. Contrarian
Field ships the first graph. He treats it as a 70% skeleton. Field uses it for agents; he says workflows only.

## H. Assumptions
Pre-GA UI. Falsifier: merge now sticks. We do not run n8n Cloud.

## I. Questions
Do we accept “I fixed it” from an agent without re-reading?

## J. Connections
SYSTEM SYNTHESIS: `a5sJNwfZ528` pin + linear. `jZgcWCzxh1I` don’t climb. No n8n Cloud. `slice-build`.

## K. Future-Use
Specify-stack. Verify-the-fix. Linear-only.

## Steal / Operate-never

### Machine: name the stack; treat the graph as 70%; re-read every “fix”; no crew one-shots
- **Epistemic:** SOURCE
- **Workflow / loop:** write trigger/tools/model/shape → generate → approve ≠ done → pin → if it “fixed” it, open the node
- **Questions / signals:** Empty GET? Append vs combine? Live vars?
- **Qualify / frame / objections:** Setup guide is a checklist, not production.
- **Procedure:** No n8n Cloud. No Plus. No client template from a one-shot.
- **Example that proves it:** Two newsletters after a “fix”; invoice with no vars.
- **Why it works:** The builder guesses schemas and autonomy. A line is checkable.
- **Conditions / exceptions:** Pre-GA.
- **Operate-never payload:** n8n Cloud as hive; ship the first graph.
- **Hive run:** `slice-build`. Deploy HITL.
- **Source:** `TDHFkKSTJ30` @ UNKNOWN

### Operate-never
- Use n8n Cloud builder as ours. Ship unread graphs.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not open the builder. Specify, then re-read. Deploy HITL.
