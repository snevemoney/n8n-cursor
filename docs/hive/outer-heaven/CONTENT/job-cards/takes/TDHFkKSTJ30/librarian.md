# Librarian — TDHFkKSTJ30
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/TDHFkKSTJ30/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/TDHFkKSTJ30/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** n8n's Agent Builder Somehow Made Building Agents Even Easier (text to workflow)
**Channel:** Nate Herk | AI Automation
**Kind:** video (~4694 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. ChatGPT-like builder (not rolled out to everyone). Demo: webhook form (company/budget/project) → research → hot/warm/cold (industry + budget + “custom AI”) → ClickUp CRM + Gmail + Slack. Approves a **plan**; **setup guide of six** leftover configs; score /100; HTTP Google the URL; he did not ask for half of the fields. False sense of security: **never download a template and push to prod / sell it.**
2. Vague: “morning news email.” 7am, topics default tech/AI/business, recipient blank. Behavior leaked into the **user** message (he’d put it in system). Web-research tool = empty GET, no URL/desc. HTML subject date is a nice touch. First pass = skeleton + homework.
3. Detailed: 7am, Tavily + Perplexity, Claude 3.5 Sonnet HTML, title/H2/bold/2–3 sentences, sources footer. Tavily as HTTP (community node maybe missing from the index) — body/key look right. Merge **append** → **two newsletters**. Inline-cite ask: builder *says* it fixed merge, **still appends**; only the user-prompt citation line changed. He fixes merge by hand. Links are real. Faster than building from zero — if you can see the lie.
4. Complex: Telegram + Gmail/calendar/ClickUp **sub-agents** + Think + Sonnet 3.7. Output: one tool each (send / create event / create task), not full belts. Orchestrator looks for a **missing chat trigger**. Calendar/ClickUp **no user message** (would error; “let the agent define the message”). Builder is good at **linear variable-passing**, weak at autonomous multi-agent. Preset invoice pipeline: specific extract/store/trigger — still the parse agent has **no live invoice variable**.
5. Mindset: 70% of the way / time is a win (UNVERIFIED). Know source/fields/analysis before you prompt. Plus / one-person agency CTA.
Gap: builder UI, ClickUp fields. Timestamp UNKNOWN. n8n-cloud builder. Complements `a5sJNwfZ528`.

## B. Atomic Knowledge

### Plan + leftover checklist; do not ship the graph
- **Claim:** The honest output is a plan, a setup guide of what is still red, and a graph you must read. Vague → empty tools. Detailed → better HTTP, still merge-arity lies. Multi-agent → missing triggers/messages. Invoice preset can look mapped and feed the agent nothing. Never sell/push a generated template.
- **Reasoning:** The builder searches node docs; it does not run your I/O. It will claim a merge fix it did not make.
- **Mechanism:** approve plan → read every node → execute → distrust the “we fixed it” chat → hand-fix arity.
- **Evidence:** empty GET; two newsletters; merge still append; missing Telegram trigger; invoice agent has no invoice.
- **Conditions:** Feature not universal. 70% UNVERIFIED.
- **Exceptions:** Linear, highly specified pipelines are the happy path.
- **Action:** File leftover-checklist + builder-can-lie + linear>autonomous. Do not n8n-cloud. Do not auto-Slack leads. Do not start an agency from the CTA.
- **Confidence:** high as a builder-trust machine
- **Source:** `TDHFkKSTJ30` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (transcript-implied)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** merge lie; empty tool; missing user messages
- **Speech ≠ behavior:** “even easier” vs he still hand-fixes merge and warns false security

## C. Mental Models
Template ≠ production. 70% is a start, not a ship. Behavior belongs in the system prompt. Live variables or it is theater.

## D. Procedures
1. Write trigger, sources, model, format, destination.
2. Approve the plan; walk the setup guide as a punch list.
3. Open every tool: URL, auth, description.
4. Execute; if two items, you will send twice.
5. If the chat says “fixed,” re-open the node.
6. Do not use this for a first autonomous swarm.
Avoid: n8n-cloud; sell the JSON; auto-notify leads; 70% as FACT.

## E. Examples
**Merge lie:** Situation — asked one newsletter. Action — builder claims combine. Outcome — still append. Lesson — read the node.

**Invoice preset:** Situation — looks complete. Action — open the parse agent. Outcome — no live invoice. Lesson — fixed text ≠ mapped data.

## F. Decision Rules
- IF you cannot name the fields → do not generate.
- IF you would not sell a downloaded template → do not sell this graph.
- IF it is multi-agent → expect missing messages/triggers.
- Refuse: n8n-cloud; agency CTA; auto-Slack.

## G. Contrarian
Against “NL → production agent.” Against using the builder as an architect for swarms.

## H. Assumptions
Complements `a5sJNwfZ528` (same feature, later “Build with AI” UI). Caption-only.

## I. Questions
When did it roll out to everyone? Did merge-combine ever work unaided?

## J. Connections
SYSTEM SYNTHESIS → `a5sJNwfZ528`; `4OOS96i2gfI`; `slice-build`.

## K. Future-Use
Leftover-checklist + builder-can-lie + live-variable check as atoms.

## Steal / Operate-never

### Machine: generate a skeleton; punch-list the red; never ship unread
- **Epistemic:** SOURCE
- **Workflow / loop:** specify T/S/T/D → approve plan → walk the six leftovers → execute → re-open “fixed” nodes → checkable stop = one item, live variables, and a human still on Slack/Gmail
- **Questions / signals:** Empty GET? Append vs combine? Is the invoice actually in the prompt?
- **Qualify / frame / objections:** 70% time-cut is the win, not autonomy.
- **Procedure:** D above.
- **Example that proves it:** two newsletters; invoice with no invoice.
- **Why it works:** The builder is a starter and a liar; your job is the second read.
- **Conditions / exceptions:** Feature gated; 70% UNVERIFIED.
- **Operate-never payload:** n8n-cloud; sell the graph; auto-lead Slack; agency CTA.
- **Hive run:** Same leftover-checklist on any generator. Do not add n8n builder.
- **Source:** `TDHFkKSTJ30` @ UNKNOWN

### Operate-never
- Push builder output to prod. Auto-Slack/Gmail leads. Quote 70% as FACT. Merge LESSONS. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
File leftover-checklist next to `a5sJNwfZ528`. Do not sell generated graphs.
