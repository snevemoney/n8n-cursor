# Day Planner — a5sJNwfZ528
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/a5sJNwfZ528/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/a5sJNwfZ528/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: n8n **AI workflow builder** — how to get results, not hype. Beats: blank canvas → prompt (morning food trends / Tavily / Perplexity recipe / quote → email) — skeleton works, **Tavily “include answer”** missing so trends = **null**; builder knows **core nodes** better than third-party outputs; human method = **build → run → pin → add**; sales-brief form (Chipotle fake) — spaces vs underscores, agent asks for more info; paste the bad output into the builder → it remaps; system prompt it wrote is “pretty solid”; he adds Gmail himself, builder points at missing params; Cloud-only + **monthly credits** by plan (Nov 2025); **map the process before you automate**; preset “multi-agent research” one-liner is ambiguous (trigger? sources? dest? PDF?) → errors; he wanted **linear** not orchestrator; rewrite with trigger / sources / transform / dest + Tavily advanced + include answer + Sonnet 4.5 → better; merge of three branches = three emails → he forces **one line**; three tips: **detailed prompt**, don’t expect first-shot, **linear workflows**; look at every node’s in/config/out; Plus + **$36k** member close (UNVERIFIED). Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Builder = skeleton; pin-then-add; name trigger/source/transform/dest; one line beats a merge
- **Claim:** AI can lay nodes; it guesses outputs and will miss a toggle (include answer) or a field name. A one-sentence multi-agent prompt is under-specified. Linear one-item paths debug; fan-in merges surprise you with N emails.
- **Reasoning:** Even humans don’t know a third-party node’s JSON until they run it.
- **Mechanism:** Prompt with the four data moves → execute → read every node → pin → add → if merge hurts, serialize.
- **Evidence:** “when a workflow builder or even a human tries to build anything in one fell swoop, not all the variables are going to be mapped correctly.”
- **Conditions:** You can name the process before the prompt.
- **Exceptions:** Native form fields *should* have mapped (he says) — still failed once.
- **Action:** Steal pin-then-add + four-part prompt + one-line. Do not n8n-cloud credits. Do not Plus. Do not send the brief.
- **Confidence:** high as the build rule.
- **Source:** `a5sJNwfZ528` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Tavily null; form underscores; merge→3 emails; preset orchestrator errors
- **Speech ≠ behavior:** “10x faster” title vs first-shot never works

## C. Mental Models
Learning n8n is still worth it because you must see data move. Priority: process map first. Uncertainty: credit caps; $36k.

## D. Procedures
1. Write trigger, sources, transform, destination **before** the builder prompt.
2. Prefer one linear path; say so in the prompt.
3. Execute; open every node; pin good data; add the next block.
4. On error, paste the actual output back into the helper — don’t rebuild from zero.
5. Credits are a cap (his Cloud).
Avoid: one-sentence multi-agent; n8n Cloud as hive; Plus; send Chipotle brief.

## E. Examples
**Tavily null:** Situation → morning email. Action → builder missed include-answer. Reasoning → it guessed the output. Outcome → trends empty. Lesson → steal pin-then-add.

**One line:** Situation → three Tavily branches. Action → merge → three emails. Reasoning → three items. Outcome → he serializes. Lesson → steal one-line.

## F. Decision Rules
- IF the prompt is one sentence → expect a wrong architecture.
- IF three branches must become one email → serialize or aggregate to **one item** before the agent.
- IF a variable is red/null → run and look; don’t trust the skeleton.

## G. Contrarian
Rejects both “builder replaces learning n8n” and “builder is useless.” Field: one-shot magic. He: thought-partner + you still read the nodes.

## H. Assumptions
Theirs: Cloud credits are the plan. Ours: no n8n-cloud. Falsifier: a one-shot prompt that mapped every third-party field. Survivorship: three demos.

## I. Questions
Same “map the process” course as Plus 10-hours? Chipotle = fake (he said).

## J. Connections
- SYSTEM SYNTHESIS → `slice-build` · `golden-test-loop` · `4OOS96i2gfI` (workflow before agent).

## K. Future-Use
Four-part prompt. Pin-then-add. Unassigned Cloud credits.

## Steal / Operate-never

### Machine: process map → detailed four-part prompt → pin-then-add → keep one line
- **Epistemic:** SOURCE
- **Workflow / loop:** name trigger/source/transform/dest → generate skeleton → run/read/pin → add → if merge N-items, serialize
- **Questions / signals:** One-sentence multi-agent? Null variable? N emails?
- **Qualify / frame / objections:** “10x one-shot” is the fail. Pin-then-add is the pass.
- **Procedure:** No n8n Cloud. No Plus. No send.
- **Example that proves it:** Situation → three research branches. Action → merge. Reasoning → three items. Outcome → three emails. Lesson → steal one-line.
- **Why it works:** A pinned output is checkable; a guessed JSON path is not.
- **Conditions / exceptions:** $36k / credits UNVERIFIED.
- **Operate-never payload:** n8n Cloud; Plus; quote $36k as FACT; auto-email the brief.
- **Hive run (existing skills only):** `slice-build` · `golden-test-loop`.
- **Source:** `a5sJNwfZ528` @ UNKNOWN

### Operate-never
- n8n Cloud / Plus / auto-email.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as pin-then-add + one-line. Clients parked.
