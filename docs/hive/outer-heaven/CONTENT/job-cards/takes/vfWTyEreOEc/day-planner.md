# Day Planner — vfWTyEreOEc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/vfWTyEreOEc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/vfWTyEreOEc/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: **500+ hours** Claude — rank features by **how much they change his day** (knowledge/automation, not SaaS). Beats: D = CLAUDE.md, clear/compact/resume, web, uploads, fast, permissions, themes, IDE ext; C = voice / Glydo, Cowork (he went Code), connectors, local files; B = dynamic workflows (token-heavy, new), `/deep-research`, git worktrees, `/ultra-review`, charts, recap, 365/Chrome/computer-use; A honorable = Google Workspace CLI, Dispatch/phone, desktop (routines + copy-paste), Claude Design, auto mode, ultra/hooks/effort, agent view, `/` questions without interrupt, `/context`, prompt cache; **Top 12:** (12) `/goal` — **objective done**, not 24–48h flex; globe load ~1.5h; (11) ultra plan (cloud planners, keep working); (10) `/insights` 30-day HTML; (9) auto memory / “dream”; (8) **agent teams** experimental, token dump when session leftover, debate personas; (7) `/rewind` better than “no, do it this way” (cache); (6) **sub-agents** (no peer talk; md agents); (5) `/loop` session-bound cron, ~7-day kill; (4) remote control phone; (3) **routines** = schedule an agent (desktop calendar); (2) **status line** (model, effort, context %) — why he left the extension; `/context` `/usage`; (1) **skills** = recipes, chainable, even a 4-sentence handoff. Skool slides. Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Objective done-condition; rewind beats argue; status of the window; skills are recipes
- **Claim:** Rank by day-change, not cool. `/goal` needs a pass/fail, not “hard riddles.” If it’s wrong, rewind to the checkpoint. See the window or you compact blind. A repeated prompt is a skill.
- **Reasoning:** Teams talk; subs don’t. 24h goals are demos. Leftover session ≠ a reason to burn teams.
- **Mechanism:** Write done → run → if wrong, rewind → watch context % → extract the repeat into a recipe.
- **Evidence:** “you want to have as much of an objective definition of done as you can.” / “it’s actually better for you to rewind if it gets something wrong.”
- **Conditions:** A harness that has checkpoints and a visible window (ours: Cursor).
- **Exceptions:** We do not enable Claude teams, ultra plan, or Glydo.
- **Action:** Steal objective-done + rewind + window-visible + recipe. Do not Claude. Do not 24h goal.
- **Confidence:** high as the four.
- **Source:** `vfWTyEreOEc` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** unobserved
- **Speech ≠ behavior:** “not 24h” vs globe goal ~1.5h; leftover-session teams as fun

## C. Mental Models
Features are personal. Code ≠ only software. Priority: leverage recipes. Uncertainty: 500h / 1500 messages.

## D. Procedures
1. Before a long run, write an objective done.
2. If the turn is wrong, rewind — don’t pile corrections.
3. Keep model/effort/context visible (CUT when the window is fat).
4. Third time you type it → a skill/recipe.
5. Sub vs team: peer talk required?
Avoid: Claude; experimental teams; ultra; Skool.

## E. Examples
**Globe:** Situation → 1s load. Action → `/goal` until instant. Reasoning → visible done. Outcome → instant (his). Lesson → steal the done-test.

**Bathroom loop:** Situation → `/loop` every minute. Action → session cron. Outcome → two reminders. Lesson → loops die with the session (his).

## F. Decision Rules
- IF done is subjective → rewrite it.
- IF wrong → rewind, don’t argue in-thread.
- IF you can’t see context % → you’re flying blind (his).
- IF leftover tokens → not a reason to spawn a debate board.

## G. Contrarian
Rejects “every feature, every day” and 48h goal theater. Field: more agents. He: skills #1. We keep Cursor recipes, not his S-tier.

## H. Assumptions
Theirs: Claude is the OS. Ours: Cursor + Grok. Falsifier: a 24h goal we treat as a win. Survivorship: his day.

## I. Questions
Same teams tape as `vDVSGVpB2vc`? Insights report — local only?

## J. Connections
- SYSTEM SYNTHESIS → `vDVSGVpB2vc` · `jZgcWCzxh1I` · `morning-day-plan` (status/CUT).

## K. Future-Use
Objective-done. Rewind. Unassigned auto-memory.

## Steal / Operate-never

### Machine: objective done → rewind if wrong → watch the window → third-repeat becomes a recipe
- **Epistemic:** SOURCE
- **Workflow / loop:** write pass/fail → run → rewind or keep → glance context → extract the repeat
- **Questions / signals:** Subjective done? Argue-in-thread? Blind window? 24h flex?
- **Qualify / frame / objections:** Leftover-session swarm is the fail. Recipe + rewind is the pass.
- **Procedure:** No Claude. No teams flag. No ultra. No Skool.
- **Example that proves it:** Situation → bad turn. Action → rewind. Reasoning → cache. Outcome → cleaner than “do it this way.” Lesson → steal rewind.
- **Why it works:** A done-test and a % are checkable; a tier list is not.
- **Conditions / exceptions:** 500h UNVERIFIED.
- **Operate-never payload:** Claude; teams; ultra; Glydo; Skool.
- **Hive run (existing skills only):** `golden-test-loop` · `morning-day-plan`.
- **Source:** `vfWTyEreOEc` @ UNKNOWN

### Operate-never
- Claude / teams / ultra / Skool.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as objective-done + rewind. Clients parked.
