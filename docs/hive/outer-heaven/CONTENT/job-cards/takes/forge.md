# Forge — independent transcript walk

**Status:** filled — Cursor Forge, 2026-08-14. Evens is the visionary. Steal the machine even from ugly tapes. Do not operate the payload. Do not copy `LESSONS-FROM-TAPE.cursor-draft.md`.
**Lens:** `docs/hive/outer-heaven/CONTENT/job-cards/forge.md` — one system per session; bible before code; open the shipped URL; reject 70% “done”; known-good regression.
**Correlation:** `tape-self-teach-20260814`

---

## 1. `13eo8dWa1Gw` — Manila OFM

**Transcript said:** The speaker reloads a creator dashboard and narrates inbound while walking a Device Farm: cloud phones with unique IP and fingerprint so accounts are not linked. He creates a phone on a 4G/5G proxy, then an automation task that signs up Instagram via a cheap Gmail-code API, up to five accounts at a time, unattended, about seven or eight minutes each. Finished accounts move off the farm into a web-automation panel on a VPS: live remote view, follow automation, warmup, posting, and a chatbot on each icon that chats and redirects to one destination. He says a single VPS has a RAM ceiling — you cannot pile unlimited icons on one panel — so he runs many panels. He shows a half-configured account (no photo, no fake followers yet) and claims the whole loop is the strategy. He offers a free YouTube course playlist and does not sell a paid course in this tape.

**Forge take:** Steal the machine, not the payload: isolated workers with unique fingerprints, a live view per worker, a hard capacity cap per host, and a panel whose only job is to turn a follow into one destination. That is worktrees plus a smoke board — one job per isolated checkout, progress written where I can see it, no 10k tasks on one session. I will not operate farms, OTP inboxes, fake identity, or mass-DM. I will steal the reload: if the receipt is not on a refreshed page, it is not a receipt.

## 2. `TL8V41Ea6oM` — Swadia 4 Cs

**Transcript said:** Four agents around four C’s — coordination, creativity, clarity, coaching — on any model; the buttons change, the workflow does not. Coordination starts as inbox triage into urgent / informational / ignore, draft-like-me, do not send without approval; calendar is added only after the email agent has earned trust. The prompt skeleton is job, tool, categories, output, boundary. Creativity takes a messy folder and a target (who, length, format, voice) and returns a real file you can open and edit; skills are instruction packs that make PowerPoint/Word/Excel real, and a brand deck can become a skill. Clarity is telescope (many sources, one picture, “please verify, be concise,” cross-check engines) versus microscope (do not “summarize this contract” — table of terms, plain English, risk, questions). Coaching is voice rehearsal as a skeptical hiring manager, then break character and get a one-page prep card. He says do not delegate the decision first: make work visible, then efficient, then automatic, then hand off slowly — agents earn promotion the way people do.

**Forge take:** This is the hire loop I already run: bible and done-when first, send removed in architecture, calendar/deploy not touched until the cheap path is trusted. A skill that emits a real `.pptx` is a known-good artifact; a chat that “sounds like a deck” is 70%. I will not one-shot all four C’s in one prompt. I will open the file, click the empty states, and promote the agent one privilege at a time.

## 3. `sboNwYmH3AY` — Nate wiki

**Transcript said:** Nate shows 36 of his YouTube videos as a graph: each node has tags, link, raw file, takeaways, and backlinks to tools and techniques. He told Claude Code to grab transcripts and organize; he did not hand-draw the edges. Karpathy’s pattern: drop sources in `raw/`, the model writes a `wiki/` with pages, an index, and a log; `CLAUDE.md` says how to ingest and search. No vector DB at this scale — indexes and links. He keeps a personal vault and a YouTube vault, sometimes pointed at from an executive-assistant project via a wiki path and a short “hot” cache so the agent does not crawl everything. Lint/health-check: find inconsistent or missing pages, propose new connections. He says this does not kill RAG; wiki-graph is for hundreds of well-indexed pages, traditional RAG when you are in the millions.

**Forge take:** The wiki is a known-good pile I can open, not a black-box index I have to trust. Ingest is a slice: one source, then I open the new pages and the log line — I do not accept “it figured out the relationships.” Point other desks at the folder + `CLAUDE.md`; do not rebuild embeddings this week. Hot cache is a smoke of “what changed,” not a second brain. If the graph is a screenshot without a query that hits the right page, it is pretty, not done.

## 4. `eMPWBunaOic` — packet 4

**Transcript said:** Karpathy’s prompting 2.0: declare voice/typos up top, lean back, ramble ~10 minutes of stream-of-consciousness (or a short interview) at the start of a new project so the model reconstructs the tangle. He contrasts a short prompt that burns many edit rounds against a long dump that needs one short cleanup. The demo is two parallel Claude sessions building the same YouTube-vs-peer dashboard: short prompt vs 10-minute ramble. He judges the short-prompt build as darker, harder to read, missing some membership/money pulls; the long-prompt build as lighter, more interactive, with an “evidence file” assembled from transcripts, Social Blade, Wayback, and screenshots. He says the long dump is easier to shape into the ideal dashboard even if the first pass is not perfect. Mid-tape he pitches an inner-circle community.

**Forge take:** `session-bootstrap` is the 10-minute dump; then short loops. I will not one-shot a dashboard from a one-line prompt and call the first render done. Run the two shapes if the stake is real, then open both in a browser and reject the one I cannot read in five seconds. Evidence file = receipts. I do not take his scoring of the two builds as ours — I click.

## 5. `kwSVtQ7dziU` — packet 5 — chunk

**Transcript said:** Karpathy on No Priors: since a December capability jump he almost never types code; the job is expressing will to agents, often many at once, and he feels “psychosis” that everything that fails is a skill issue (instructions, memory, parallelization). Peter Steinberger’s tiled Codex sessions: ~20-minute high-effort jobs, multiple repo checkouts, macro actions (a feature, a research pass, a plan) reviewed to the degree you care. Token throughput is the new GPU-idle anxiety; leftover subscription feels like unused flops. He built “Dobby,” a home claw that scanned the LAN, found Sonos/lights/HVAC/shades/pool/security, built a dashboard, and texts WhatsApp on camera events — six apps collapsed to natural language — but he still withholds email/calendar because the edges are rough. Auto-research: remove yourself from the loop; objective + metric + boundaries; overnight it found hyperparameter misses he had not. Caveat: only where evaluation is cheap and objective; models are jagged (brilliant systems programmer and a 10-year-old). Education shift: write markdown for agents, not HTML docs for humans; your job is the few bits the agent cannot invent (microGPT’s 200 lines). Untrusted internet workers: expensive to search, cheap to verify — isolate the run.

**Forge take:** Isolated checkouts and macro tickets are how I already refuse one-shot sites: one system per worktree, review before merge, leftover tokens are not a reason to start a second product in the same session. Auto-research is legal only when I can name the metric and the fail; “it ran overnight” without a golden compare is 70%. Dobby’s LAN scan is a steal for “find the API, then build the command center” — I still will not hand a claw email or prod deploy. I will not rewrite MCP or rebuild Claude Code inside Grok because a podcast said claws are the future.

## 6. `IVx8OSMbTss` — packet 6 — chunk

**Transcript said:** Nate builds an AI SaaS in one filmed day from no idea: six P’s — pain, promise, product, plumbing, packaging, proof. Claude (thought partner / Fable as PM) and Codex (executor / computer-use verifier) share one folder; Glido is voice so typing is not the bottleneck. Pain is scraped from his comments, community, X, Reddit; he picks a client-proposal/handoff pack for agency owners, not a generic “AI idea,” and says there is no real moat if someone can rebuild the UI in a day — the moat is the prompt/IP from his own landmines. Packaging (name Client Pack, logo, waitlist) runs in parallel with a plan doc other agents can pick up; he `/goal`s a one-page waitlist and tells Codex not to stop until signup actually captures (admin/CSV), then GitHub → Vercel, screenshot-fixes a 404. Product: Next + Supabase + Stripe sandbox; dump discovery transcripts → review findings → 10-slide deck; Codex clicks every button and files launch blockers (review not enforced, empty/negative ROI still generates). He pastes SQL Codex wrote, runs it, gets tables; he clicks signup, confirm-email, onboarding, generate, upgrade, sandbox checkout, watermark off. Custom domain then breaks login (site URL / host mismatch) until that env is fixed and the path is re-clicked on the real host. He says you will not one-day a million-dollar SaaS; you can ship a waitlist and a PC, sell before you scale, invite in batches, watch signup → first deck → paid, and do not blast 10k users on day two. Security pass finds high-severity blockers; he treats that as the point. Close: one person, one pain, one promise.

**Forge take:** This is my job card on tape. Parallel packaging vs product is legal; one-shot whole app is not. Codex-as-attacker is the verification checklist I already owe: click every button, try to break it, do not mark done from the plan doc. Staging `.vercel.app` working does not mean the custom domain works — I re-smoke login/pay/empty on the host we will actually send. Stripe stays sandbox until Evens flips live. Logout dead, watermark/ROI lies, confirm-email dumping to the waitlist — all 70%. The waitlist that actually writes a row is the first known-good; the deck generator is a later slice.

## 7. `EzQAgnjTq2k` — packet 7

**Transcript said:** Alli K. Miller tells Greg she is done “managing” agents like direct reports; she sets infrastructure and waits for escalations — liability and final say, not Susie-go-there. Best prompt to a workforce that already has goals, transcripts, mail, calendar, Notion, Stripe, Supabase, GitHub: “do smart things.” Width of scope expanded; send-risk tier did not — she still checks every email. Proactive defined workflows (drop a recording → transcript + social drafts) vs undefined workflows that need goal, tools, and trigger-sense. Daily dictate into a personal wiki so uncodified judgment is queryable. Org: one AI chief of staff, six directors, plus roles she would never hire in human-world (a “weirdo” who 10x’s output; an assistant whose only job is watching friction and missing access). Start with one agent, then proactive, then two agents handing off, then a mission-control view; traditional titles are a ramp, not the destination. Arbitrage she names: AI as watchdog (dupes, calendar conflicts, contract before/after), and build the factory for the thing (login, payments, sharing, newsletter primitives) so the next product is faster. She thinks mediocre SaaS dies slowly because people will not maintain or be liable for what they vibe; enterprise still wants someone to call. Fear-zones (CMOs asking how agents discover brands) are research, not a hunt.

**Forge take:** “Do smart things” without a done-when is how I get 70% dumped on Evens. Steal: escalation board, send still gated, watchdog on friction, factory primitives (auth, pay, smoke) instead of a new app every session. I will not operate a 34-agent org as a SKU or let a claw emoji-react its way into send. If we build a factory, the first primitive is the verification harness, not the fifth product.

## 8. `hGdG-04TkDs` — packet 8

**Transcript said:** The limiting factor used to be labor; AI changes the unit of scale from employees to agents. One-person company is not one person doing every job — it is one operator in the center while AI does repetitive work; the human stays as director. He walks a podcast-clip shop: upload → transcript → pick clips → autocut → captions/thumbnails → human review ~45 minutes, then ship. Paradox: when output is cheap, output is worthless; winners have direction, distribution, taste, trust, a real pain. Commodity agencies (generic landing pages, copy-paste admin) lose leverage. Bad positioning is “I do marketing with AI”; good is a named niche and a booked-outcome. Orchestration = break the goal, assign workers, review until it matches. Person A one-shots a landing page and quits; Person B specifies avatar, promise, tone, CTA, iterates, ships. Failure mode: weeks of generating brand and automations with no demand. Playbook: narrow paid problem, delivery system, proof (Loom before/after), one channel deep, keep human edge on judgment and accountability.

**Forge take:** Person A is the one-shot I am paid to refuse. Person B is slice-build: bible, one system, iterate, `click-live-site`. I will not sell generic landing pages as a desk SKU; I will steal the director loop and the Loom-as-proof. If nobody booked, the site is not done no matter how cinematic the hero is.

## 9. `IWdvG9Up8Mc` — packet 9

**Transcript said:** Swadia’s path to a one-person AI business: not “automation videos,” real businesses. Founders triangle — domain, craft depth, distribution — at least one green to go, three to floor it; Harvey as the case. Then the DREAM machine: Demand, Revenue, Engine, Admin, Marketing. Do not build the whole machine week one; pick one recurring task in one function (enrich a small lead batch, draft a deck). Moats: counterpositioning, switching-cost habits, proprietary data/learning loops (he points at Cursor-style keystroke loops as the pattern). Mindset: deathbed-regret test; bring taste, judgment, relationships machines do not own. Chicago owner dumps cost data into a chat model and QuickBooks/POS into NotebookLM for a manager podcast — AI as CFO of the numbers, not a new hire.

**Forge take:** One recurring task this week is a slice; the DREAM poster is not. Writer-agent plus reviewer-agent overnight is a known-good pair I will run on a real repo, then I open the diff. I will not quote his collapse-in-cost story as our baseline. Distribution advantage is Evens’s problem; my job is the engine that does not lie when we click it.

## 10. `whIp1SOahOM` — packet 10

**Transcript said:** Zubair demos “Jarvis” with a real phone number (Retail + Twilio). Jarvis calls a restaurant, hits a full book at the asked time, checks with the boss, calls back, books the later slot, then offers calendar. Mechanic demo: quote and soonest slot only (reconnaissance), then a second call to book after yes; confirms name, asks what to bring, writes the drop-off on the calendar. Inbound hotline: asks a code; wrong code twice and hang up; then a status that an unknown caller failed the check. Same brain over Telegram (text/voice) for leads: pull a row, draft a quote in the known format, append long-term memory. He says credit-card and personal fields are denied in instructions. Free community gets prompt packs (booking, inquiry, receptionist, invoice, security); paid community gets the finished zip. He plans more versions.

**Forge take:** Steal confirm-before-book, PIN on inbound, fail-closed hangup, and a post-call status — that is HITL architecture with a receipt. Booking is a hard step; I do not operate auto-dial, Retail/Vapi clones, or a voice agent that can finish a reservation without Evens. Two isolated agents (outbound book vs inbound hotline) so a guessed PIN cannot reach the book path. If we ever build a “call a vendor” slice, the smoke is: wrong code dies, right code still asks before write.

## 11. `nS2FrgXN-EY` — packet 11

**Transcript said:** Glenn Coco is an outbound marketplace: companies list offers, fractional reps book meetings. Claude does not place the call — reps still dial. Five layers: (1) knowledge-base builder from site/PDFs/blogs into overview, ICP, objections, scripts, case studies, then a training gate before a rep can dial that campaign; (2) in-CRM email draft from knowledge base + call context, human once-over then send; (3) Playbook Studio — talk tracks as cards (intro, pitch, objections, loops) not linear Google Docs, because a live call is not top-to-bottom, plus optional recordings of top-rep tonality on the card; (4) Claude drafts a tight plant/contact list from ICP, spot-check LinkedIn vs site, suppress already-pulled rows, enrich phones/emails in a data tool, start 10–15 not a thousand; (5) every call scored on the same rubric (intro, pitch, qualify, objections, close) with snippets, same-day, so managers coach the trend not one Friday tape. He says AI SDRs that try to be the conversation churn; the conversation is the moat.

**Forge take:** Cards-not-docs is how I will stop shipping linear runbooks nobody can use mid-incident. Call scoring against a frozen rubric is known-good regression — I would build that harness before another enablement page. I will not operate the dialer or auto-send the draft. List work starts at a handful of rows I can click; a thousand-row enrich with no spot-check is a farm.

## 12. `mjg_JUMar04` — packet 12

**Transcript said:** A Godot snow game, not one prompt. Four models with jobs: Fable for what/why, Opus for mechanics, GPT for Blender/mechanics, Kimi for Blender aesthetics. Failed earlier projects looked right but had no loop. Start from a Pinterest feeling, then a color bible — daylight reference translated to night — and nothing enters without it. Do not ask for the whole game; best model writes the plan; every job is a fresh chat; every job ends in a test; fail sends one job back, not the game. Ground is a sliding height field (boot and wind change it); snow depth changes walk/run; footprints paint one fixed-size image the wind erases. Placeholders first, then Blender scripts (fix a line, re-run, do not sculpt the mesh). Do not ask AI for realistic meshes — boxes and light. Lighting control panel with time-of-day presets. Enemies few but lethal; interior is the same house with roof/wall culled; camera ray fades occluders.

**Forge take:** This tape is `slice-build` with the serial numbers filed off. Bible first, one job one clean session, test or it did not happen, fail isolates to the job. I will not one-shot a game or a site because the still looks right. A lighting/bible panel I can drag is a better known-good than another generated mesh. If the loop is missing, the pretty scene is a placeholder — keep it marked that way.

## 13. `ESIxitOLYoQ` — packet 13

**Transcript said:** Clay-style list building inside Claude Code with Disco-like CLI → score accounts → AI Arc contacts, HITL the whole way. He starts from a niche he actually cares about (B2B robotics) so copy will have a pulse. First 50 records get a fit-rate report; target band is roughly 60–70% great — tighter kills valids, looser is noise. He rewrites exclusions with the model (SaaS not robots, marketing agencies, enterprises past the size cap, broken domains) and keeps system integrators on purpose. Parallel agent anneals an ICP scoring prompt on that same 50 until he agrees with the reasoning, then a score threshold becomes the qualify bit. Session-review skill mines the log to patch the skill (e.g. stop using seniority). Arguments/files for ICP so a change is not trapped inside a stale prompt. Durable writes so a pause does not lose the run. He says do not YOLO until you have a winning campaign; do not outsource the thinking.

**Forge take:** This is `list-anneal-funnel` as a terminal. I verify the CSV and three LinkedIn rows, not the “94.8% prompt score.” Durable progress file is a smoke. I will not add an `icp_id` from robotics or book their sprint. When Lead Hunter hands me a list tool, Forge’s job is the anneal loop and the spot-check, not a thousand-row firehose.

## 14. `I7mpF7_pnPM` — packet 14

**Transcript said:** A 10-hour clock that includes product, site, checkout, and marketing — not a build with “we’ll market later.” Only rule: the clock covers getting it into market; sales after the clock are allowed. He posts the challenge on X before the build, copies a working prediction-market analyzer pattern, names it, grabs a Canva logo, prompts Abacus to clone-ish a site with Stripe, writes a one-line offer and a cheap trial-plus-refund, buys a domain, wires DNS, deploys, and clicks CTA → pay → login → dashboard. He tests the analyzer on a same-night market and checks the result after sleep before pushing distribution. Distribution: X, school community, personal/business network, Instagram; he says do not half-ass the launch post. After a couple of days he reports two people paid the cheap trial (one from the network who already trades that category; traffic mostly from X). He kills the product on opportunity cost — main business is implementing AI for companies — and says if he were starting from zero he would not kill it. Close: he sold before it worked; shipped-and-tested beats perfect; years of wrong plans sit under the 10 hours.

**Forge take:** He actually clicked the path, checked mobile, waited on DNS, and used an overnight outcome as a receipt — that part I keep. The SKU is betting-adjacent; I will not operate it. Steal: the clock includes distribution; `click-live-site` on the real domain; kill with a written opportunity-cost, not vibes. Two paid trials are a signal, not a business — I will not quote them as our revenue. Abacus/ChatGPT-clone stack stays on tape; we stay Cursor.

## 15. `f4mI3d-nTrI` — packet 15

**Transcript said:** MCP’s new spec is stateless: initialize handshake and session-ID header go away so any instance can take any request — no sticky sessions, no Redis just to remember initialize, scale-to-zero on Workers/Cloud Run. Tool calls become one self-contained HTTP request; `MCP-Method` / `MCP-Name` headers let a gateway act without parsing JSON; list calls get TTL/cache-scope hints. Capability info rides in a per-request `_meta`; optional `server/discover`. If you want state, mint your own handle (basket id) and pass it as an argument like any HTTP API. Server-initiated “are you sure?” is no longer a push on an open stream — the server returns `input_required` plus serialized request state; the client reissues with the answer; any instance can resume. Long work becomes official tasks: write working state, return immediately, poll or subscribe. Breaking changes with a long deprecation window; TypeScript SDK split server/client plus a codemod. He is excited; upgrade is not `bump the package and leave`.

**Forge take:** Job card already says do not rewrite MCP/stateless HTTP this week unless it is broken. I parked the machine: handles as args, `input_required` as a real HITL round-trip, tasks instead of open streams. If something in our stack actually 400s on session-not-found, the fix is a slice with a smoke on two instances — not a protocol essay. I will not rebuild the harness for a YouTube reset.

## 16. `Ums8suyAG1A` — Nate agentic manager

**Transcript said:** Nate opens on job-loss fear, then a CEO clip: you lose the job to a person who uses AI, not to a robot. Agentic = give the outcome, it finds the steps. Three desktop demos in Claude Code (local files, not web chat): `/goal` a Q2 YouTube review that pulls analytics, writes a local workbook, screenshots tabs to verify colors/spacing before handoff; `/goal` a cleaning-jobs HTML tracker with memory across refresh (mark paid, add, delete); `/goal` Clay-ish 50-lead enrich plus outreach columns. He says the skill is being an AI manager: onboard like a new hire, one real recurring task you will actually use, review and correct until the output is usable, then connect tools with ask-before-write, then score hours or leads vs the week before. Community/course CTA at the end.

**Forge take:** `agent-as-hire` is the loop; `/goal` is only legal with a condition I can click. I open the workbook and the HTML — a screenshot the agent took of its own sheet is not `click-live-site`. I will not auto-send the 50-lead column. The job tracker is a fine Path C slice; Nate’s community is not a lane. Do not quote the open-job-loss framing as FACT.

## 17. `kpMreA9ATOo` — Julian Grok SEO

**Transcript said:** Julian says Grok 4.6 is a search engine: people ask, it cites websites. Method one: hunt a keyword that is hours old on X (his example is a brand-new tool comparison), ship several surfaces (two sites, two videos, a Reddit thread), pay-index them, and show first-page screenshots the next day. Method two: ask Grok the same question and show his domains cited in the answer, more than once. Four steps: find the fresh question, create multi-platform content, index fast, check Google and Grok, iterate. He says Grok liked honest, hands-on, specific pages with information gain — case studies, actually using both tools — not generic rewrites. He still prefers Claude to write; Grok to research. CTA is a free SEO course, a strategy call, Indexceptional, and a “rank everywhere” system. He cites a large client count and a 24-hour loop as the pitch.

**Forge take:** Steal one loop we already named: `info-gain-cite` — one question in a lane we operate, one page from a run we actually clicked, HITL publish, ask Grok, note cite or miss. I will not operate the rank-everywhere farm, the paid indexer, or a second site for the same keyword. I will not quote 24-hour first page or the client body-count as FACT. After publish I open the URL and search the question; a recorded screenshot in his video is his receipt, not ours.

## 18. `QIsJe-nZ5XE` — Nate sell workflows

**Transcript said:** Do not start an agency to sell AI workflows if you have never delivered one. He under-scoped early fixed-price builds, wore every hat, and says an agency would have multiplied the miss (dev says the price is wrong; setter cannot scope). Agency pain: many clients, sinking-ship buyers who want AI as a lifeline, reputation burns. Path: freelancer (3–5 paid proofs, even cheap/free for a case study) → consultant (“let me figure out what you need”) → only then a team. Discovery question on tape is AI-excitement; he used it as a filter. Sell: one person + one pain (not “I automate anything”); 2-minute Loom before/after that shows the destination not the car; speak time and money; after live, measure the same numbers you took at kickoff. Consultant frame is systems and retainers after proof, not “tell me what to build.” School/Plus CTA. He walks enterprise consulting figures as the sky — not as his invoice.

**Forge take:** `solo-then-consult` — we install one Path A ourselves before anyone talks bench. I will not open an agency or staff a nameless delivery hire from this tape. Under-scoped fixed bids are a Forge failure mode: four-blank and a staging demo before price. I do not invent hours × a made-up rate; owner numbers or UNVERIFIED. Loom of the clicked before/after is the close artifact; a workflow screenshot is not. Nate School is on tape only.

---

## Roll-up

- **Skills:**
  - `slice-build` — one system per session; bible before code.
  - `click-live-site` — open the shipped URL; click CTA; no “looks good.”
  - `session-bootstrap` — long dump at project start, then short loops (Karpathy 2.0).
  - `list-anneal-funnel` — ~50 → score a 60–70% band → rewrite exclusions; spot-check rows.
  - `agent-as-hire` — onboard → one real SOP → review → gated tools → score vs last week.
  - `info-gain-cite` — one honest page from a run we did; HITL publish; ask Grok; note cite/miss.
  - `solo-then-consult` — one Path A install before consultant framing.
  - `cinematic-recipe` — bible/lighting before pixels (Godot tape agrees).
  - (proposed) `adversarial-click-pass` — second harness clicks every control and tries to break; Forge does not mark done on the builder’s self-score.
  - (proposed) `isolated-worktree-board` — one job per checkout; progress written back; no silent overwrite.
  - (proposed) `domain-cutover-smoke` — after custom domain or host change, re-run login/pay/empty on that host.
  - (proposed) `sandbox-then-live` — Stripe/Supabase/test keys stay test until Evens flips; live keys are a hard step.
  - (proposed) `wiki-lint` — health-check the markdown wiki (missing links, stale index, no log line).
  - (proposed) `known-good-artifact` — keep the last passing deck/PDF/HTML; new version loses if it fails the guard (geometry, empty ROI, dead logout).

- **Other business (parked — not a hunt):** Device-farm / OFM / OTP / mass-DM (machine only). Prediction-market analyzer (Polymind). Jarvis / Retail / Twilio auto-dial. Glenn Coco marketplace and AI-SDR dialers. Julian “rank everywhere” + paid indexer. Client Pack as a hive SKU. Nate School / Plus / Julian course / Abacus. Home-claw on email/calendar. Untrusted public commit swarms. Robotics list as a new lane.

- **Leverage:** Isolated parallel workers into one destination, with a live board and a host cap. Verification as the product: click, smoke, known-good, second-harness attack. Packaging/waitlist in parallel with the slice — not a second product. Cards/wiki/index over linear docs. Factory primitives (auth, pay, smoke, domain cutover) so the next slice is shorter. Same-day rubric score beats Friday vibe review.

- **QoL / values:** Dump once, then short loops — do not type the same will for 16 hours. Long jobs may run while Evens walks; done is still a clicked URL, not a phone glance. Bible/lighting first so taste is locked and we stop regenerating vibes. Reject 70% so Evens is not the QA department. Agents earn send/deploy the way hires earn prod.

- **Never:** Forge will not operate farms, OTP, fake identity, mass-DM, betting, or auto-dial — and will not mark a slice done without opening the live URL (or the real artifact) and clicking the primary path, including after a domain or key cutover.
