# HITL Operator — lokbsA5VXOk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lokbsA5VXOk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lokbsA5VXOk/LEARNED.md`
**ICP:** parked unless Evens named one.

Evens is the visionary. Operate ≠ learn. Role did not filter what was learned. Stack stays Cursor + Grok. Clients parked. No send / pay / deploy / book / publish. Tape $ UNVERIFIED.

## A. Source Map

**Title (PACKET):** OpenAI Just Leveled Up n8n AI Agents (here's how it works)
**Speaker / channel:** Nate Herk | AI Automation
**Kind / words:** long cut of Responses API short · 2655 words
**Gaps:** No VTT cited in this take. Timestamps UNKNOWN. Visual-only UI clicks inferred only as INFERENCE.

Beats in order:

- Two agents, same questions (golf flag-stick rule + Bears record): one has Perplexity + Supabase tools; one has no tools/no system prompt — both answer because the OpenAI chat model has Responses API web+file search on. 'You are a helpful assistant' only.
- Needs n8n ≥1.118, OpenAI chat model v1.3 — not via OpenRouter yet. Built-ins: web, file, code interpreter; MCP possible but not shown. API key from platform.openai.com + billing, not ChatGPT.
- Web: context low/med/high; city/country/region; allowed domains. Off: World Series → cutoff June 2024. On: Dodgers 2025 vs Jays + cites + visual. Allowlist upai.com + GPT-4.1 fails (filter unsupported); GPT-5-mini correctly refuses (no Series on that domain).
- File search: vector store ID as array; filter required or error; max results. Golf PDF. OpenAI 10¢/GB/day even idle vs Gemini upload-only — he leans Gemini for metadata/cites; has not A/B tested retrieval. Default answer has no section cite unless prompted.
- Extra options: saved prompt ID, service tier, safety identifier, conversation ID (memory in OpenAI not n8n), prompt cache key, metadata, top logprobs (he doesn't understand). Plus CTA.

## B. Atomic Knowledge

### Hidden built-in tools + allowlist proof
- **Claim:** Allowlisted web search is a real constraint — he proved it by failing on his own dead site. File search without a cite is incomplete.
- **Reasoning:** Built-in tools hide in the model; a 'no tools' agent can still browse/send-risk if the brain has them.
- **Mechanism:** Toggle Responses → allowlist/store → ask → require cites.
- **Evidence:** On-tape: upai.com refuse; idle 10¢/GB/day; no default cite.
- **Conditions:** On-tape demo / short captions.
- **Exceptions:** Tape $ and vendor names stay on-tape.
- **Action:** Steal allowlist + cite-or-fail. Operate-never OpenAI memory/conversation-ID as hive brain, idle billed stores, code interpreter, Plus.
- **Confidence:** medium — caption ingest, timestamp UNKNOWN
- **Source:** `lokbsA5VXOk` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models

- A tool-less canvas can still have tools in the model — inspect the brain.
- Cheaper idle (Gemini) matters if stores sit.
- He will say when he has not tested.

## D. Procedures

- Inspect whether Responses/web/file/code are on even if the agent shows no tools.
- Set allowlist. Require a cite. Don't leave billed stores idle.
- Keys from the API console, not chat — still a secrets/pay card.
- Do not enable code interpreter. Do not use OpenAI conversation memory as ours.

## E. Examples

- **On-tape run** — Situation: World Series on allowlisted upai.com → Action: Model says it cannot find it on that domain → Reasoning: Prove the filter → Outcome: Correct refuse → Lesson: Constraint works; sports $ not ours

## F. Decision Rules

- If the agent has no tools listed → still check the chat-model toggles.
- If no cite → fail the answer for outbound.
- If a store sits unused → money leak.
- 10¢/GB/day UNVERIFIED as our price.

## G. Contrarian

- Field assumes no-tools means safe. He shows the brain still searches.

## H. Assumptions

- Bears 8-3 / Dodgers 2025 / 10¢ / 1.118 UNVERIFIED or on-tape.
- OpenAI/Gemini/Plus on-tape.

## I. Questions

- What does the required filter JSON actually constrain?
- MCP via extra coding — send risk?

## J. Connections

- Short: `QrJhdTbK3TU`. Gemini: `KVFfApQZhE4`.
- SYSTEM SYNTHESIS → `info-gain-cite` · `ask-principal` (keys/pay).

## K. Future-Use

- Hidden-tool inspection as a default HITL check. Keep.

## Steal / Operate-never

Informed by A–K. Auto-send / auto-book stay operate-never. The machine is still stolen.

### Machine: Inspect the brain toggles; allowlist + cite-or-fail
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** open the model node → list built-ins → allowlist or store ID → ask → require cite → Evens if outbound
- **Questions / signals:** Is web/file/code on? Which domains? Idle store billing?
- **Qualify / frame / objections:** No-tools on the canvas is not safe. Conversation-ID memory is not our wiki.
- **Procedure:** No OpenAI install. No code interpreter. No Plus. Keys are a card.
- **Example that proves it:** **On-tape run** — Situation: Tool-less agent answers Bears + rule 17 → Action: Responses API on the model → Reasoning: Looks like magic → Outcome: Same answers + cites from the web → Lesson: Hidden tools are the blast radius
- **Why it works:** He designed the A/B so you see the brain, not the canvas.
- **Conditions / exceptions:** Hard steps stay HITL.
- **Operate-never payload:** OpenAI as hive. Idle billed stores. Code interpreter. Conversation memory in a vendor. Quote 10¢ as FACT. Plus.
- **Hive run (existing skills only):** `info-gain-cite` · `ask-principal` · `send-removed`
- **Source:** `lokbsA5VXOk` @ UNKNOWN

### Operate-never (this desk will not operate)

- Auto-send / auto-book / auto-voice-book / auto-publish / auto-pay / auto-deploy.
- Quote tape $ / student counts / job-loss % / token burns as FACT.
- Install on-tape vendors (Claude Code, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool) as our stack. Cursor + Grok only.
- New `icp_id`. Unpark Normand. Outreach / hunt because a tape was interesting.
- Always-allow MCP / classifier / guardrail-pass as Evens.
- Merge `LESSONS-FROM-TAPE.md`. Send / pay / deploy / book / publish.

## L. Role-Specific Applications

Hidden built-ins are a send/browse risk. ACTION = inspect toggles; REJECT vendor memory and billed idle stores. Clients parked.
