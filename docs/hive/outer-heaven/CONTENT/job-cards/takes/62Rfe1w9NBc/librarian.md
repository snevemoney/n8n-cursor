# Librarian — 62Rfe1w9NBc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/62Rfe1w9NBc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/62Rfe1w9NBc/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** I Can Actually Watch My AI Agents Work Now
**Channel:** Nate Herk | AI Automation
**Kind:** video (~2945 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Demo in Claude Code terminal: ask main to spin two researchers (Google / OpenAI) + a visual diagram → three agents in parallel; second terminal: YouTube comment analysis last couple videos → notification when a sub-agent needs **approval**; first pair finish and go **idle**; diagram (edges/gaps, some text errors); comment analysis (API cost pain, first client/zero proof, requested videos). Point of demo = **see** terminals/subs, not the artifacts. He kills the sessions.
2. VS Code + **Pixel Agents** extension: coding agents as animated pixel-art office characters. Claude Code = terminal AI that writes/edits/runs; you usually only see think/do text while files/plans/tests happen off-screen. Pixel Agents reads Claude’s **default activity log** and animates it (similar to his OpenClaw glance). Two-monitor glance.
3. Setup: VS Code; **Windows only** at filming (days old); Mac/other not yet. Extensions → “pixel agents” / “pixel art office.” One agent per character; live tracking; custom floors/furniture; speech bubbles; sounds; sub-agent viz; six spawnable characters. Roadmap: Claude Code agent teams (today = parallel only).
4. Gotcha he learned hard: **folder cannot have a space or a period** or the activity log will not bind (`Herkace 2.0` / `Herk 2.0` → `herk-2`). Open folder → terminal (Ctrl+`) → Pixel Agents tab → drag to sidebar → +agent (spawns Claude terminal + idle character) → more pluses. Layout/furniture toys. Sessions folder / debug view / sound toggle (hooks could do sounds anyway).
5. Why it is not useless: (1) entertainment + two-monitor load-more-work glance; (2) visual lure for people scared of terminals — n8n blew up on visual understanding. He wants the missing visual of **what is being built** (n8n-style flow while Claude builds), not just headcount. Taxonomy: **parallel** = separate terminals; **sub-agents** = parent delegates, own context, maybe other models, can need approval; **teams** = share a task list, talk, know when the other is done — Pixel does not show teams yet. Four of “the same girl” = one main + four subs. Boris: ~5 terminal agents + ~10 on the web at once.
6. Security pass before install: publisher Pablo Deuca, verified Marketplace, real co-founder, GitHub ~1300 stars / 100+ forks. He looked for outbound/exfil/shell injection/suspicious deps/secrets/FS/remote scripts — “didn’t see anything”; stays on machine; no commands. Still: only install verified/known-safe extensions.
7. Wanted product: not “an agent is working” but **what they’re building, what they decided, whether he would agree — stop before wrong, keep them from idle**. Plus 3,000 CTA.
Gap: marketplace page, office UI. Timestamp UNKNOWN. Windows-only. Claude/Pixel/OpenClaw/n8n/Plus on-tape.

## B. Atomic Knowledge

### Glance ≠ inspect; idle/approval are the useful signals
- **Claim:** Pixel Agents is an activity-log costume over Claude Code. Useful as idle/count/approval glance and as a non-technical lure. The real product is decision/build visibility + stop-before-wrong + no-idle management. Folder names with space/`.` break the log bind.
- **Reasoning:** Terminal hides file/plan/test work; n8n’s visual is why it spread; he still cannot see the graph they are building.
- **Mechanism:** Activity log → animation; +agent per terminal; subs vs parallel vs teams.
- **Evidence:** Demo idle/approval; `herk-2` rename; his security skim.
- **Conditions:** Windows-only at tape time. Verified publisher is his bar, not a proof.
- **Exceptions:** Entertainment is an allowed reason. Teams not visualized.
- **Action:** Steal glance-signals + folder-name gotcha + “what/decide/agree” product spec. Do not install Pixel Agents as hive. Plus/3k UNVERIFIED.
- **Confidence:** high as a want; security skim is his, not an audit FACT
- **Source:** `62Rfe1w9NBc` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (transcript-implied)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** `Herk 2.0` failed to bind until rename
- **Speech ≠ behavior:** “watch them work” vs he admits you only see that they are working

## C. Mental Models
Managers need a glance and a stop. Visual is how non-terminal people enter. Parallel ≠ sub ≠ team. Security before cute.

## D. Procedures
1. If you try it: Windows, no space/`.` in folder, verified publisher, local-only check.
2. Use idle/approval as the glance; do not trust the costume as the build.
3. Name the three agent kinds when you talk.
4. Wanted loop: see decision → stop if you’d disagree → refill idle.
Avoid: Mac install (then); Plus; hive = Pixel/Claude.

## E. Examples
**herk-2:** Situation — activity not showing. Action — rename off `2.0`. Outcome — log binds. Lesson — binder path hygiene.

**Approval ping:** Situation — comment-analysis sub. Action — needs approval. Outcome — he can see the wait. Lesson — HITL is a visible state.

## F. Decision Rules
- IF the visual only shows headcount → it is not inspect.
- IF folder has space or `.` → expect bind fail.
- IF extension is unverified → he would not install.
- Refuse: Pixel as hive; 1300 stars as security FACT; Plus.

## G. Contrarian
Against “this is useless fluff” (he keeps entertainment + lure). Against “I can see what they’re building” (he cannot, and says so).

## H. Assumptions
Security skim ≠ audit. Complements `xsAOpqjebOo` (visible idle/approval) and `ZAaxx3qyT8g`. Caption-only.

## I. Questions
Mac support later? Did the security skim miss anything? What is OpenClaw on this channel?

## J. Connections
SYSTEM SYNTHESIS → `xsAOpqjebOo`; `ZAaxx3qyT8g`; hive HITL.

## K. Future-Use
Idle/approval glance + folder-bind gotcha + inspect-product spec as atoms.

## Steal / Operate-never

### Machine: glance for idle/approval; inspect is a different product
- **Epistemic:** SOURCE
- **Workflow / loop:** spawn parallel/subs → glance idle/approval → refill or approve/stop → checkable stop = no silent idle and no unapproved side-effect
- **Questions / signals:** Parallel, sub, or team? Would I agree with the next action?
- **Qualify / frame / objections:** n8n-visual lure; entertainment allowed.
- **Procedure:** D above.
- **Example that proves it:** Approval ping; `herk-2` rename.
- **Why it works:** Managers fail when work is invisible; costume is a start.
- **Conditions / exceptions:** Windows-only; costume ≠ inspect.
- **Operate-never payload:** Install Pixel Agents; Plus; Claude as hive; treat stars as an audit.
- **Hive run:** Visible idle/approval already in `xsAOpqjebOo`. Cursor + Grok.
- **Source:** `62Rfe1w9NBc` @ UNKNOWN

### Operate-never
- Install the extension as hive. Plus. Merge LESSONS. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
File title vs “I only see that they are working.” Keep the folder-bind gotcha. No Pixel wiki.
