# Day Planner — 62Rfe1w9NBc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/62Rfe1w9NBc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/62Rfe1w9NBc/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: **Pixel Agents** (VS Code) — pixel-office over Claude Code terminals. Beats: demo — main delegates Google vs OpenAI researchers + diagram; second terminal comment-analysis (needs **approval**); kill sessions; Claude Code = terminal that reads/plans/runs/tests; Pixel reads the **activity log** (same vibe as his OpenClaw watch); **Windows only** (days old); folder **no space/period** (`Herk 2.0` → `herk-2`, learned the hard way); drag panel, +agent, furniture toys, sessions/debug, sound; entertainment + glance-count + lure non-terminal people (n8n analogy); he wants **n8n-style flow of what it’s building**, not just who’s walking; parallel vs sub (own context) vs **teams** (shared task list, Pixel can’t show yet); Boris “5 in terminal + ~10 on web” (UNVERIFIED); security: publisher Pablo Deuca, verified, ~1300 stars, he checked outbound/exfil/injection/deps/secrets/fs/remote — **saw none, local only** (his claim); still: he wants **decisions + agree/disagree before it acts**, not “they’re working”; keep them from sitting idle; Plus. Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Glance-count is not a manager; folder names can silently break the log
- **Claim:** A cute office tells you agents are busy; the product he actually wants is **what they’re deciding and whether he’d stop it**; a space/period in the folder name made the log not attach.
- **Reasoning:** Multitask glance is entertainment; management is intervene-before-wrong.
- **Mechanism:** Activity log → sprites; broken path → silent miss.
- **Evidence:** “Pixel agent just tells me that an agent… are working. What I actually want to know is what they’re building, what decisions they’re making.” / “your folder cannot have a space or a period.”
- **Conditions:** Windows + Claude Code + this extension (tape).
- **Exceptions:** A real approval gate (comment agent “needs approval”) is closer to the product — still Claude.
- **Action:** Steal “busy ≠ decided” + path-gotcha. Do not install Pixel/Claude/OpenClaw.
- **Confidence:** high as his want; security clean = **his** review UNVERIFIED.
- **Source:** `62Rfe1w9NBc` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Herk 2.0 rename
- **Speech ≠ behavior:** security “we didn’t see anything” vs “only install verified”

## C. Mental Models
Visual is how n8n won civilians. Humans become managers. Idle agents should get more work (his). Priority: see-to-stop. Uncertainty: Windows-only, teams roadmap.

## D. Procedures
1. Busy-sprites ≠ an approve/deny on the hard step.
2. If a watcher is path-based, forbid space/period in the folder (his miss).
3. Before any extension: publisher + outbound/exfil check — still don’t install this one.
Avoid: Claude Code; Pixel Agents; OpenClaw; Plus; 5+10 fleets.

## E. Examples
**Herk 2.0:** Situation → office dead. Action → rename to `herk-2`. Reasoning → log path. Outcome → sprites attach. Lesson → steal the gotcha.

**Comment agent approval:** Situation → second terminal. Action → it stops for approval. Reasoning → tool gate. Outcome → he can see it. Lesson → that’s the real UI; we still don’t run Claude.

## F. Decision Rules
- IF the dashboard only shows “working” → not enough to manage.
- IF folder has space/period and a log-watcher → expect silent fail.
- IF Boris-count is the reason to run 15 agents → CUT.

## G. Contrarian
He ships a toy and then says the toy isn’t the product. Field: more parallel terminals. He: I want the decision.

## H. Assumptions
Theirs: extension is local-only. Ours: we did not re-audit; no install. Falsifier: a watcher that shows the pending hard step. Survivorship: one Windows machine.

## I. Questions
OpenClaw tape id? Did Pixel later get Mac?

## J. Connections
- SYSTEM SYNTHESIS → `c0kaKxM2pHg` (checkpoint) · `ask-principal` · `-Lo_SlSgtnA` (HITL dial).

## K. Future-Use
Busy≠decided. Path-gotcha. Unassigned pixel-office.

## Steal / Operate-never

### Machine: working-count ≠ decision; path must be log-safe; no sprite install
- **Epistemic:** SOURCE
- **Workflow / loop:** if a watcher only shows busy → fail → require a stop on the hard step → if path-based, no space/period
- **Questions / signals:** Can I see the decision? Would I stop it? Is the folder name clean?
- **Qualify / frame / objections:** “I can glance at five girls” is the fail. Approval ping is closer.
- **Procedure:** No Claude. No Pixel. No OpenClaw. No Plus.
- **Example that proves it:** Situation → Herk 2.0. Action → rename. Reasoning → log path. Outcome → sprites. Lesson → steal the gotcha, not the office.
- **Why it works:** A silent log miss looks like “idle”; a decision view is a stop.
- **Conditions / exceptions:** Windows-only on tape. Security review UNVERIFIED.
- **Operate-never payload:** Claude / Pixel / OpenClaw; Plus; 15-agent fleets.
- **Hive run (existing skills only):** `ask-principal` · `c0kaKxM2pHg`-checkpoint.
- **Source:** `62Rfe1w9NBc` @ UNKNOWN

### Operate-never
- Install VS Code extension / Claude / OpenClaw.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as busy≠decided (no Pixel). Clients parked.
