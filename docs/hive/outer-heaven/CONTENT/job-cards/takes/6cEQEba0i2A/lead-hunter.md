# Lead Hunter — 6cEQEba0i2A
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/6cEQEba0i2A/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/6cEQEba0i2A/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** Give Me 10 Mins and I'll Save You Millions of Claude Tokens
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Hook: 91M cached tokens one day, 300M in a week (UNVERIFIED). Cached input = 10% cost. Sub TTL 1 hour; idle >1h uncaches. API/sub-agents TTL 5 min (can pay for 1h). Thorik/Anthropic: they fix low hit-rate (helps them too: faster, cheaper serve, limits feel bigger).
- Prefix match: system+project cached; each turn only new reply+message. Change system prompt or wait an hour on turn 16 = recache everything. Extra-usage/API territory = 5 min — dangerous with many sessions. Sub-agents always 5 min. claude.ai cache unknown.
- Don’t pause too long. After an hour: session-handoff (files, decisions, pickup) → /clear → paste (his /compact replacement). Big docs: use a Project, not a chat paste. Model switch recaches; `/opus plan` (Opus plan / Sonnet exec) resets cache each toggle — may still save long-run but know the reset. CLAUDE.md edit mid-session OK (applies on restart). School: token dashboard + handoff skill.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### Hour TTL + no mid-run model switch; handoff instead of sitting idle
- **Claim:** Cache dies after an hour idle or a model/system-prompt change; the operator move is handoff→clear→paste, and don’t use Opus-plan toggles if you care about this turn’s cache.
- **Reasoning:** 91M/300M are his dashboard flex. 10% cached input is the why.
- **Mechanism:** Keep prefix stable → work inside the hour → handoff if you must leave → don’t switch model mid-thread.
- **Evidence:** SOURCE: “if you leave a session sitting for an hour or longer, then you're going to pay more.” “each plan toggle is a model switch and starts a fresh cache.” 91M/300M UNVERIFIED. School capture.
- **Conditions:** You are in a long thread on a vendor that caches prefixes (we do not run Claude).
- **Exceptions:** Token dashboard CTA. API 5-min is a footgun if someone did enable Claude — we don’t.
- **Action:** Steal handoff-on-idle + don’t-switch-mid-run as general session hygiene. Do not install Claude. Do not enter School.
- **Confidence:** high on the TTL/switch rules
- **Source:** `6cEQEba0i2A` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: 80/20 of caching is enough; you don’t need the whole Thorik article.
- SOURCE: Sub-agents are 5-minute TTL even on a sub.

## D. Procedures
- If you will pause > TTL, write a handoff and start fresh. Don’t change the system prompt late. Don’t toggle plan-model to “save” this turn.

## E. Examples
- Situation: long HTML-build session. Action: handoff, /clear, paste. Reasoning: hour TTL. Outcome: continue without recache-from-zero. Lesson: steal handoff; park the dashboard CTA.

## F. Decision Rules
- If you switched models, you paid the recache.
- If a sub-agent sat 6 minutes, assume cold.
- If 91M is the hook, UNVERIFIED.

## G. Contrarian
- Rejects /compact as his default — handoff replaced it. Rejects needing full cache internals.

## H. Assumptions
- All million-token claims UNVERIFIED. Claude-only mechanics — analog only for hive sessions.

## I. Questions
- How does claude.ai cache? He doesn’t know.

## J. Connections
- SYSTEM SYNTHESIS: `kB9iMD0EjT8` session-handoff four fields.
- SYSTEM SYNTHESIS: Researcher cheap-read / expensive-decide.

## K. Future-Use
- Unassigned: TTL-aware handoff as a desk habit when a thread goes cold.

## Steal / Operate-never

### Machine: Handoff before TTL death; don’t switch the brain mid-thread
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** Long thread → if pause or model-switch needed, write handoff → checkable stop = no Claude install and token $ UNVERIFIED.
- **Questions / signals:** Have we sat an hour? Did we toggle the model? Is the system prompt about to change?
- **Qualify / frame / objections:** Frame: hygiene. Objection: “millions of tokens saved” → UNVERIFIED flex + School.
- **Procedure:** Do not install Claude. Do not enter School. Do not buy the dashboard.
- **Example that proves it:** Situation: 91M cache-read day. Action: teach TTL + handoff. Reasoning: prefix match. Outcome: School pack. Lesson: steal handoff/TTL; park Claude.
- **Why it works:** From B: hour vs 5 min and plan-toggle reset are explicit.
- **Conditions / exceptions:** Works as session hygiene. Exception: hive is not on Claude — still steal the handoff shape.
- **Operate-never payload:** Quote 91M/300M as FACT. Hunt token-dashboard. Install Claude.
- **Hive run (existing skills only):** Stack Cursor+Grok. Do not auto-write the handoff as a new SKILL.md.
- **Source:** `6cEQEba0i2A` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Install Claude. Enter School. Quote 91M as FACT. Hunt token-saving clients.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal handoff-before-idle. No Claude. No School. Counts UNVERIFIED.
- Hard step stays HITL: this desk drafts; Evens sends.
