# Day Planner — Ek1NBfnnTH0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/Ek1NBfnnTH0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/Ek1NBfnnTH0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: five tricks so an **AI OS** doesn’t hallucinate as it grows. Beats: **OS audit** skill — read-only, 10 findings + 10 fixes, **yes/no**, writes `audits/`; four failure modes: **poison / bloat / confusion / clash**; two context types: **expertise** (always-on rulebook) vs **situational** (just-in-time; principal vs teacher seating-chart); Hyper Agent sponsor council + **$1,000** credits (on tape); audit example: routing red, index **55 folders vs 79 on disk**, knowledge frozen **June 29**; fan-out explore agents on big trees; (1) root `CLAUDE.md` as **router** (paths, wiki, hot cache, index), nested mds for project prompts; (2) **AI audits itself** (weekly/monthly or the skill); Claude suggested split YouTube vs meeting wikis; **can you find it in the file explorer without search?**; (3) **crons ingest** predictable feeds (Monday Q&A, Tuesday leadership) so you don’t forget; (4) **segment** growing nodes; clients: **internal** notes/price/scope in OS, **deliverables in a separate repo**; (5) **backtrack** a miss (where did you look?) then patch routing — better than “don’t do that again.” Team sync = **people/habit**, not a tool. Skool skill. Caption-only. Timestamp UNKNOWN. Clients parked.

## B. Atomic Knowledge
### Router not a novel; audit read-only then approve; expertise vs just-in-time; backtrack the miss
- **Claim:** Wrong answers come from poison/bloat/confusion/clash, not “the model is dumb.” Always-on context is the rulebook; tickets/meetings load on demand. An index that disagrees with disk is a red. Fix after a yes. A miss gets a postmortem, then a route patch.
- **Reasoning:** A customer ticket in the always-on window is bloat. Two refund policies are a clash. If *you* can’t walk the folders, the agent can’t either.
- **Mechanism:** Router md → audit (no write) → approve fixes → cron the predictable → segment when a node grows → backtrack misses.
- **Evidence:** “Read only, never fix, or rename or delete.” / “if you can find the path yourself, then it’s probably set up pretty intuitively.”
- **Conditions:** One repo you can walk.
- **Exceptions:** Hyper Agent marketplace / $1k credits = magnet. Client folders stay parked.
- **Action:** Steal four-modes + router + read-only audit + explorer-test + backtrack. Do not Claude. Do not Hyper Agent. Do not Skool.
- **Confidence:** high as the OS hygiene.
- **Source:** `Ek1NBfnnTH0` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** third audit run that day; stale June
- **Speech ≠ behavior:** “won’t actually do anything yet” vs later “Opus, go ahead and do that”

## C. Mental Models
OS quality = routing truth, not more files. Priority: fewer wrong answers. Uncertainty: Hyper Agent roast.

## D. Procedures
1. Name always-on vs just-in-time before adding a file.
2. Root file is a router (if X, go here).
3. Audit read-only → you approve writes.
4. If you add the same feed every week, cron the ingest (our stack).
5. On a miss: have it replay the search, then patch the route.
6. Explorer test: find last week’s artifact without search.
Avoid: Claude; Hyper Agent; Skool; dump client deliverables into the OS; unpark.

## E. Examples
**55 vs 79:** Situation → index lie. Action → audit red. Reasoning → claims ≠ disk. Outcome → fix list. Lesson → steal index-truth.

**Wiki split:** Situation → one master wiki. Action → agent suggested two. Reasoning → bloat. Outcome → cheaper/faster (his). Lesson → steal segment-when-it-grows.

## F. Decision Rules
- IF a fact can be wrong → verify or HITL (poison).
- IF two sources disagree → pick a winner date (clash).
- IF the audit wants to delete → still yes/no.
- IF it’s a client deliverable → other repo (his); we still park clients.

## G. Contrarian
Rejects “one true folder taxonomy.” Field: copy Nate’s tree. He: whatever you can walk. Team sync isn’t a SaaS.

## H. Assumptions
Theirs: one mega Herk folder. Ours: this repo already has rails. Falsifier: audit that auto-deletes. Survivorship: his July 22 audit.

## I. Questions
Same Karpathy wiki as other OS tapes? Fireflies cron overlap `KGXFkUlBHxw`?

## J. Connections
- SYSTEM SYNTHESIS → `hQvwMj7IJe4` · `brB-hSiV2iU` · `ZRb7D6R64hM` (md as constitution).

## K. Future-Use
Four failure modes. Explorer-test. Unassigned team-habit.

## Steal / Operate-never

### Machine: router md → read-only audit → approve → cron predictable → segment → backtrack misses
- **Epistemic:** SOURCE
- **Workflow / loop:** classify expertise vs JIT → audit claims vs disk → yes/no fixes → cron weekly feeds → on miss, replay then patch
- **Questions / signals:** Index≠disk? Ticket in always-on? Can’t find it yourself?
- **Qualify / frame / objections:** Auto-fix-the-OS is the fail. Read-only then yes is the pass.
- **Procedure:** No Claude. No Hyper Agent. No Skool. No client dump.
- **Example that proves it:** Situation → “I don’t have that.” Action → backtrack. Reasoning → prove the miss. Outcome → route patch. Lesson → steal the replay.
- **Why it works:** Disk-vs-index and an explorer walk are checkable; a $1k credit is a magnet.
- **Conditions / exceptions:** Clients parked.
- **Operate-never payload:** Claude; Hyper Agent / $1k; Skool skill as hive; auto-delete; unpark.
- **Hive run (existing skills only):** `hQvwMj7IJe4` · `golden-test-loop`.
- **Source:** `Ek1NBfnnTH0` @ UNKNOWN

### Operate-never
- Claude / Hyper Agent / Skool / auto-delete / unpark.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as router + read-only audit. Clients parked.
