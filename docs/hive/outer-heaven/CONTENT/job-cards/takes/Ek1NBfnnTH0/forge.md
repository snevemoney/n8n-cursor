# Forge — Ek1NBfnnTH0
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/Ek1NBfnnTH0/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/Ek1NBfnnTH0/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **five OS-accuracy tricks** (Herk 2 / Claude). Why: unorganized OS hallucinates into skills and builds. Four **context failures:** **poison** (false fact — verify / HITL); **bloat** (too much / needle); **confusion** (missing/irrelevant → fill-in); **clash** (Mar refund-always vs Jun never). Two types: **expertise** (always-on rulebook / principal) vs **situational** (JIT teacher / yesterday’s ticket). Four C’s: context, connections, capabilities, cadence. **OS audit skill** (Skool): read-only, write `audits/`, 10 findings + fixes, **await approval**; fan-out sub-agents if 100+ folders. His run (Jul 22): knowledge through **Jun 29**; routing/index/freshness **red** (index 55 folders / disk 79). Would answer Q3 OTAs from June. Fix list: catch-up ingest + weekly Fireflies/YouTube crons. **Five hacks:** (1) root `claude.md` is a **router** (paths, wiki, hot cache, index, GP fallback, keys, skills, other-worlds) — nested projects have their own; one GitHub root. (2) **AI audits itself** (Claude suggested split YT vs meeting wikis). Human test: can *you* walk to a file without search? (3) **Crons** for data that is *not* JIT (Monday Q&A, Tuesday leadership). (4) **Segment** growing nodes; **clients:** internal dates/scope/price in OS `clients/A`; **deliverables in a separate repo** (collab/deploy) with a pointer back. (5) **Backtrack** the miss (where did you look?) then patch routing — not “don’t err.” Team sync = **people/habit**, not Drive vs Notion vs Git. Sponsor: **Hyper Agent** (Airtable) five-persona council + $1,000 credits CTA. Hermes/Codex/Claude interchangeable because it’s files. Timestamp UNKNOWN. Claude / Hermes / Codex / Modal / Hyper Agent / Skool on-tape. Caption-only: folder tree unobserved beyond his words.

## B. Atomic Knowledge

### Router + read-only audit + JIT vs always-on; clients’ build repos stay outside the brain
- **Claim:** Wrong answers come from poison/bloat/confusion/clash, not a “smarter” model. Root file routes; expertise stays small; situational is fetched. Audit reports, you approve. Client *work* is a different git than client *memory*.
- **Reasoning:** Stale index (55 vs 79) confidently lies. One mega-wiki bloated until *the agent* asked to split. “Don’t mess up” doesn’t fix a bad path.
- **Mechanism:** Router md → segmented wikis → cron the cadence feeds → audit vs disk → backtrack then patch.
- **Evidence:** Jun 29 knowledge in a Jul 22 ask; Claude-suggested wiki split; pointer-not-own for client repos.
- **Conditions:** His Herk 2 / Claude. Hyper Agent is a paid plug.
- **Exceptions:** No universal tree — wrong is *wrong answers you ignore*.
- **Action:** Steal router-not-prompt + read-only audit + expertise/JIT + client-repo split + backtrack. Do not install Hyper Agent / Hermes / Claude. Do not auto-apply audit fixes. Clients parked.
- **Confidence:** high on the failure modes; his folder taste is local.
- **Source:** `Ek1NBfnnTH0` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Poison / bloat / confusion / clash. Principal vs teacher. Router ≠ system novel. Internal memory ≠ shipping repo. Habit > tool.

## D. Procedures
1. Don’t install Hyper Agent / Hermes / Claude / Modal. 2. Don’t let an audit rename/delete unattended. 3. Don’t dump client deliverables into the hive brain. 4. Don’t quote $1,000 credits. 5. Don’t send Skool.

## E. Examples
**Situation:** “How are Q3 OTAs?”  
**Action:** Audit shows June-stale + misroute.  
**Reasoning:** Clash + freshness red.  
**Outcome:** Confident wrong.  
**Lesson:** Index vs disk is the test.

**Situation:** Agent says “I don’t have that.”  
**Action:** Backtrack the search path, then fix the router.  
**Reasoning:** The miss is a map bug.  
**Outcome:** Next find is cheap.  
**Lesson:** Prove the miss, then patch.

## F. Decision Rules
- If a fact is always needed → expertise; if it’s yesterday’s ticket → JIT.
- If index ≠ disk → don’t trust answers until catch-up.
- If it’s a client shipping artifact → other repo + pointer.
- If the audit wants to write → you approve.

## G. Contrarian
Field stuffs one mega-wiki and one mega-prompt. He routes, segments, and treats team-sync as habit.

## H. Assumptions
Herk 2 as taped. Falsifier: a smaller hive doesn’t need his tree. Clients parked.

## I. Questions
None. Don’t add Hyper Agent.

## J. Connections
SYSTEM SYNTHESIS: `ZRb7D6R64hM` claude.md <200 + update-on-err. `3XIGcM7VICc` context > prompt. `xJ5oz63mIec` cron vs cloud. No vendor install.

## K. Future-Use
Router + audit-await + JIT. Client repos stay out. Don’t buy the council.

## Steal / Operate-never

### Machine: route, don’t novel; audit read-only; keep expertise small; park client builds in another git
- **Epistemic:** SOURCE
- **Workflow / loop:** router → segment → cron cadence → audit vs disk → approve fixes → backtrack misses
- **Questions / signals:** Poison or clash? Can you walk there by hand? Index = disk?
- **Qualify / frame / objections:** No right tree. Team sync is habit.
- **Procedure:** No Hyper Agent/Hermes/Claude. No unattended writes. No client dump.
- **Example that proves it:** 55 vs 79; wiki split; pointer to client A.
- **Why it works:** Wrong answers are map failures.
- **Conditions / exceptions:** His OS. Tape $ UNVERIFIED.
- **Operate-never payload:** Install Hyper Agent; auto-fix; merge client repos; Skool send.
- **Hive run:** none. Deploy HITL.
- **Source:** `Ek1NBfnnTH0` @ UNKNOWN

### Operate-never
- Install Hyper Agent / Hermes / Claude / Modal. Auto-apply audit. New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will keep router + read-only audit + client-repo split. I will not add Hyper Agent. Deploy HITL.
