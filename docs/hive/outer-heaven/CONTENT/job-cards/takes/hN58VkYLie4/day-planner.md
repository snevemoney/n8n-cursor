# Day Planner — hN58VkYLie4
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/hN58VkYLie4/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/hN58VkYLie4/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Short: Claude Code + Trigger.dev. Beats: NL in Claude Code (“monitor YouTube… daily summary”) → it asks for X/Y/Z → working automation as a **code project/file** → must leave the laptop and run on Trigger.dev in the cloud “all the time”; ClickUp demo: new task “Nvidia” → research polar picks it up → company researcher → search web twice + one more + read URL (~45s) → task marked complete with a brief; follow-up “@UPAI how is their stock doing?” → follow-up responder resolves “they” from context. CTA to full. Timestamp UNKNOWN. Vendors: Claude Code, Trigger.dev, ClickUp — on-tape.

## B. Atomic Knowledge
### Local NL becomes a file; always-on is a different host
- **Claim:** Claude Code turns a vague ask into a code project on the laptop; Trigger.dev is how it runs all the time.
- **Reasoning:** Local is not a cron.
- **Mechanism:** NL → questions → file → deploy to Trigger.dev.
- **Evidence:** “get that out of our local cloud code environment… into trigger.dev on the cloud so that these can actually run all the time.”
- **Conditions:** You want always-on.
- **Exceptions:** A one-shot headed run can stay local.
- **Action:** Learn the split. Do not deploy Trigger.dev from this desk. Always-on send is never.
- **Confidence:** high as his split.
- **Source:** `hN58VkYLie4` @ UNKNOWN
- **Epistemic:** SOURCE

### Task in → research → mark complete is a loop
- **Claim:** A ClickUp task named “Nvidia” is enough to start research; completion writes the brief back; a follow-up can resolve pronouns from task context.
- **Reasoning:** The task is the ticket.
- **Mechanism:** New task → researcher → tools → complete + brief → @mention follow-up.
- **Evidence:** “marked as complete… full research brief about Nvidia.”
- **Conditions:** A human created the task.
- **Exceptions:** Auto-complete without a brief is a fail.
- **Action:** Ticket-in, brief-back. Deploy/send stay HITL.
- **Confidence:** high as the demo.
- **Source:** `hN58VkYLie4` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Vague English is allowed if the tool asks for the missing pieces. Always-on is a hosting decision, not a prompt. He watches the researcher live (headed). Priority: show ClickUp as the board. Uncertainty: 45s / three searches UNVERIFIED as “enough.”

## D. Procedures
1. Dump the ask; let the tool list missing X/Y/Z (`session-bootstrap`).
2. Keep the artifact as a file/project.
3. Headed one-shot is allowed.
4. Always-on / cloud deploy = Evens.
5. Complete only when a brief exists.
Avoid: Claude Code / Trigger.dev install; 24/7 YouTube-monitor that emails.

## E. Examples
**Nvidia task → brief:** Situation → he adds “Nvidia” in ClickUp. Action → researcher searches 3×, reads URL, marks complete with a brief. Reasoning → ticket is enough. Outcome → brief on the task. Lesson → ticket-in, brief-back; do not deploy the polar.

**“How is their stock”:** Situation → pronoun on the same task. Action → follow-up responder reads context. Reasoning → “they” = Nvidia. Outcome → a reply from “Upet AI.” Lesson → context lives on the ticket.

## F. Decision Rules
- If it must run while the laptop is closed → deploy is HITL, and send is still never.
- If complete has no brief → fail.
- If the vendor is Claude Code / Trigger.dev → on-tape.

## G. Contrarian
Rejects “the laptop agent is always on.” Field assumption: local Claude is a server. He says you must host it.

## H. Assumptions
Theirs: Trigger.dev is the host; Claude Code is the builder. Ours: stack is Cursor + Grok; 24/7 ≠ auto-money. Falsifier: researcher marks complete on an empty brief. Survivorship: one Nvidia.

## I. Questions
Full long tape? What X/Y/Z did it ask for on the YouTube example? Who is Upet AI?

## J. Connections
- SYSTEM SYNTHESIS → `session-bootstrap` (vague → ask for X/Y/Z) · `coverage-loop` (not 24/7 money) · `ehg4fhydTgs` (24/7 Claude agents).

## K. Future-Use
Ticket-in / brief-back as a research loop on boards we already have. Unassigned host.

## Steal / Operate-never

### Machine: NL → missing X/Y/Z → file; ticket-in / brief-back; deploy HITL
- **Epistemic:** SOURCE
- **Workflow / loop:** vague ask → list missing inputs → write the file → headed run → brief on the ticket → Evens decides host
- **Questions / signals:** What X/Y/Z is missing? Is there a brief? Does this need always-on?
- **Qualify / frame / objections:** Local ≠ cron. Always-on send is never.
- **Procedure:** One headed research. No Claude Code / Trigger.dev install. No complete-without-brief.
- **Example that proves it:** Situation → Nvidia task. Action → 3 searches + URL + complete with brief. Reasoning → ticket is the job. Outcome → brief lands. Lesson → steal the ticket loop; never the 24/7 host.
- **Why it works:** A ticket is a checkable stop; a cloud cron is how send sneaks in.
- **Conditions / exceptions:** One-shot can stay local. YouTube daily email = operate-never.
- **Operate-never payload:** Claude Code / Trigger.dev as stack; 24/7 auto-email; quote 45s as FACT.
- **Hive run (existing skills only):** `session-bootstrap` · `coverage-loop` · `ask-principal`.
- **Source:** `hN58VkYLie4` @ UNKNOWN

### Operate-never
- Install Claude Code / Trigger.dev / switch stack.
- 24/7 send / YouTube-monitor email.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as ticket-in / brief-back (no deploy). Clients parked.
