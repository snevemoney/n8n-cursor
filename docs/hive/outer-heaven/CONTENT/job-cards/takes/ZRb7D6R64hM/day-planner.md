# Day Planner — ZRb7D6R64hM
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZRb7D6R64hM/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/ZRb7D6R64hM/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: **five levels of Claude** (~400h). Beats: L1 search-bar — screenshot cheat; L2 **project** + memory/search + 50 connectors + file create + persistent artifacts (public link) + inline charts + Office add-ins (free vs Pro split); L3 Cowork paid — folder grant, skills marketplace (**careful where you download**), `/schedule` (laptop on) vs cloud routines, Dispatch, Claude Design “Figma killer,” plugins, computer-use; stall = fear or Cowork ceiling; **$5k+** systems (UNVERIFIED); folder rails: about-me / templates never-edit / outputs; L4 Code — Boris **5 parallel** workspaces; `CLAUDE.md` **<~200 lines**, **update on every mistake**; plan mode / Opus-plan+Sonnet-exec; subs (no peer talk); worktrees 3–4; **CLI > MCP** (60–70% fewer tok), tool-search at 10% window; compact **before 50%**; `/context`; auto mode (classifier); `/focus`; **verification loop** (Chrome) 2–3× quality (Boris); `/command` if typed twice; rewind / BTW / branch / insights / output styles; stall = babysitting; **$5–15k** (UNVERIFIED); L5 architect — cloud routines (schedule/API/GitHub), hooks as rails, channels (Discord/Telegram/iMessage), headless `-p`, Agent SDK, remote control, Auto Dream, task budgets (API), experimental teams, 5k skills/800 MCP (UNVERIFIED); **trust stall** — parking-lot: low-stakes **to-you-only**, watch weeks; deterministic move-data vs agentic. Skool + Plus. Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Project → rails → mistake-into-md → CLI-before-MCP → verify → parking-lot trust
- **Claim:** Each level is a ceiling (chat can’t touch disk; Cowork isn’t git; Code babysits; L5 is trust). The durable moves: a project, a short constitution you update on error, CLI not a tool dump, a way the agent checks itself, and unattended only after weeks of to-you-only.
- **Reasoning:** A fat CLAUDE.md is a token tax. MCP loads tools you aren’t using. Auto mode is a permission classifier (blast radius).
- **Mechanism:** Project + about-me/templates/outputs → md <200, patch on fail → CLI first → compact <50% → verify in a browser → if unattended, to-you, watch weeks.
- **Evidence:** “Every single time Claude makes a mistake, you say… update your Claude.md.” / “when a CLI tool exists… use the CLI instead of the MCP.” / “You don’t start on the highway, you start in an empty parking lot.”
- **Conditions:** A repo you own. We map md → our AGENTS/rules, not Claude.
- **Exceptions:** Public artifact links, computer-use, Telegram channels = send/leak. Auto mode ≠ Always allow.
- **Action:** Steal rails + patch-on-mistake + CLI-first + parking-lot. Do not Claude. Do not $5k hunt. Do not Plus.
- **Confidence:** high as the ladder (overlaps `vfWTyEreOEc` / `xJ5oz63mIec` / `9IzGe0BBj_c`).
- **Source:** `ZRb7D6R64hM` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** unobserved
- **Speech ≠ behavior:** “Figma killer” vs handoff still needed; auto mode vs our never-Always-allow

## C. Mental Models
Levels are trust + surface, not IQ. Priority: Boris verify. Uncertainty: 400h / $5–15k / 5k skills.

## D. Procedures
1. One project with real files (not a search bar).
2. Rails: read-me-first, never-edit templates, write only to outputs.
3. On each fail, patch the constitution (keep it short).
4. CLI, then API, then skill, MCP last.
5. Compact before half-window; rewind a bad turn.
6. Unattended: to-you-only, watch weeks.
Avoid: Claude; marketplace skills from anywhere; auto-send; Plus; quote $ as FACT.

## E. Examples
**Downloads sort:** Situation → chaos folder. Action → Cowork grant. Reasoning → L3. Outcome → sorted (his). Lesson → steal grant-least-folder.

**Parking lot:** Situation → L5 fear. Action → standup to yourself. Reasoning → drive. Outcome → trust reps. Lesson → steal to-you-only.

## F. Decision Rules
- IF CLAUDE.md (or our rules) >~200 → split and @-include.
- IF a CLI exists → no MCP.
- IF window >50% → compact now.
- IF the routine leaves the building → not a parking lot.

## G. Contrarian
Rejects chat-as-OS and MCP-first. Field: more connectors. He: CLI. We keep never-Always-allow (disagreement with his auto mode).

## H. Assumptions
Theirs: Anthropic is the ladder. Ours: Cursor+Grok. Falsifier: a public artifact we treat as shipped. Survivorship: Boris stories.

## I. Questions
Same CLI>MCP as `9IzGe0BBj_c`? Same parking-lot as `xJ5oz63mIec`?

## J. Connections
- SYSTEM SYNTHESIS → `vfWTyEreOEc` · `xJ5oz63mIec` · `9IzGe0BBj_c` · `c0kaKxM2pHg`.

## K. Future-Use
Patch-on-mistake. Parking-lot. Unassigned Auto Dream.

## Steal / Operate-never

### Machine: project rails → short constitution patched on fail → CLI-first → verify → to-you-only for weeks
- **Epistemic:** SOURCE
- **Workflow / loop:** project + folders → fail→patch md → CLI not MCP → compact <50% → verify → if unattended, to-you
- **Questions / signals:** Search-bar chat? Fat md? MCP dump? Highway routine?
- **Qualify / frame / objections:** Telegram-always-on is the fail. Parking-lot standup is the pass.
- **Procedure:** No Claude. No Plus. No $5k hunt. No Always-allow.
- **Example that proves it:** Situation → mistake. Action → update md. Reasoning → Anthropic does this. Outcome → fewer repeats (his). Lesson → steal the patch.
- **Why it works:** A <200 file and a to-you routine are checkable; $15k is not FACT.
- **Conditions / exceptions:** $ / 400h / 5k skills UNVERIFIED.
- **Operate-never payload:** Claude ladder as hive OS; marketplace-any-skill; computer-use send; Plus; quote $ as FACT.
- **Hive run (existing skills only):** `c0kaKxM2pHg` · `golden-test-loop` · `9IzGe0BBj_c`.
- **Source:** `ZRb7D6R64hM` @ UNKNOWN

### Operate-never
- Claude / Plus / $ as FACT / Always-allow / public artifact as ship.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as rails + parking-lot. Clients parked.
