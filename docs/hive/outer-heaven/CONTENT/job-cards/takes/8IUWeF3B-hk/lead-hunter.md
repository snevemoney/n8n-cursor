# Lead Hunter — 8IUWeF3B-hk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/8IUWeF3B-hk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/8IUWeF3B-hk/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** STOP Guessing! Evaluating Your Agents is Easy
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Thesis: if you want to build and sell workflows, learn to evaluate. Definition: “validating your hypothesis with objective proof.”
- Folk method: unhappy with output → “if I do X it gets better” → change → subjectively judge better/worse.
- n8n eval flow: dataset of six examples with expected category + expected priority → run through the model → write AI answers back to the sheet → check evaluations tab.
- Run test: averages tokens, prompts total, duration, plus average correctness on priority and category. Six items processed; can see which were right/wrong.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### Hypothesis + labeled set, not vibes
- **Claim:** Evaluation is validating a hypothesis with objective proof: a labeled set (here six rows of expected category + priority) run through the model, scores written back.
- **Reasoning:** The folk loop (change X, feel better) is what he is replacing.
- **Mechanism:** Sheet of expected labels → run → write outputs → correctness average + per-row right/wrong.
- **Evidence:** SOURCE: “validating your hypothesis with objective proof.” “data set of six examples with our expected category and our expected priority.” “which ones… were right and which ones weren't.”
- **Conditions:** You have expected labels before you change the prompt.
- **Exceptions:** Six is his demo size, not a standard. Token/time averages are extra, not the stop.
- **Action:** Write expected labels. Run. Read the wrong rows before changing X again.
- **Confidence:** high.
- **Source:** `8IUWeF3B-hk` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Selling workflows without eval is guessing.
- SOURCE: Subjective better/worse is the thing to retire.
- INFERENCE: The sheet is the productized golden-test.

## D. Procedures
- State hypothesis (if I do X, metric Y moves).
- Freeze a labeled set.
- Run. Write outputs back. Inspect wrong rows. Only then change X.

## E. Examples
- Situation: tagger for category+priority. Action: six labeled rows through the model. Reasoning: objective proof. Outcome: average scores + per-row right/wrong. Lesson: the wrong rows are the next hypothesis.

## F. Decision Rules
- If there are no expected labels, you are not evaluating.
- If you change X before reading wrong rows, you are back to vibes.

## G. Contrarian
- Rejects “run it again and see if you like it” as evaluation.

## H. Assumptions
- Six-row set is tiny. Correctness on his labels ≠ production truth. n8n Evaluations tab is on-tape UI.

## I. Questions
- Who labeled the six? What is the fail threshold before he would not sell?

## J. Connections
- SYSTEM SYNTHESIS: `golden-test-loop` is the hive name.
- SYSTEM SYNTHESIS: `NWbh5ZoEHkA` calculator-watch is the tiny version of “did the thing fire.”

## K. Future-Use
- Unassigned: labeled-set size as a qualify question for anyone selling an agent.

## Steal / Operate-never

### Machine: Labeled set → run → wrong rows → new hypothesis
- **Epistemic:** SOURCE
- **Workflow / loop:** Write hypothesis → freeze labeled rows → run → checkable stop = wrong rows visible (not “it felt better”).
- **Questions / signals:** What is the hypothesis? What is the expected label? Which rows failed?
- **Qualify / frame / objections:** Frame: proof, not vibe. Objection: “I already ran it” → show the sheet.
- **Procedure:** Do not sell on a green average hiding wrong rows.
- **Example that proves it:** Situation: six category/priority examples. Action: run test, inspect right/wrong. Reasoning: sellable workflows need proof. Outcome: averages + per-row. Lesson: wrong rows drive the next change.
- **Why it works:** From B: he defined eval as objective proof of a hypothesis.
- **Conditions / exceptions:** Works with labels. Exception: no labels → stop.
- **Operate-never payload:** Do not hunt “eval consultants.” Do not treat six rows as FACT quality.
- **Hive run (existing skills only):** `golden-test-loop`. `ask-principal` before a sell motion.
- **Source:** `8IUWeF3B-hk` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Sell a workflow on a vibe pass. Treat the six-row average as FACT.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- This is the qualify gate for any inbound “we have an agent”: where is the labeled set? No set → raw 50.
- Hard step stays HITL: this desk drafts; Evens sends.
