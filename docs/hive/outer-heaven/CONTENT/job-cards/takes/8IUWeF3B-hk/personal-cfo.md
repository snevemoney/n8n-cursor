# Personal CFO — 8IUWeF3B-hk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/8IUWeF3B-hk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/8IUWeF3B-hk/LEARNED.md`
**ICP:** parked unless Evens named one.
**Walk:** VIDEO-FIRST from `full.txt` (332 words, short 1:31). json3 present. Tape $ UNVERIFIED. No spend. Clients parked. Stack Cursor + Grok.

## A. Source Map
1. If you build and sell workflows, you must evaluate them. **SOURCE**
2. Evaluation = validating a hypothesis with objective proof (not vibes). **SOURCE**
3. Old loop: change X → rerun → subjectively judge better/worse. **SOURCE**
4. Demo: dataset of six examples with expected category + expected priority → run through the model → write answers back to a Google Sheet → evaluations tab. **SOURCE**
5. Metrics collected: average tokens, prompts total, duration, average priority/category correctness. **SOURCE**
6. Result: six processed; some right, some wrong — visible. **SOURCE**
Gap: no $. Token averages are his run, not our bill.

## B. Atomic Knowledge

### Hypothesis + labeled set beats vibe QA
- **Claim:** Evaluation is “validating your hypothesis with objective proof,” not a feeling after one rerun.
- **Reasoning:** Subjective better/worse is how you ship junk.
- **Mechanism:** Expected labels in a sheet → run → scores.
- **Evidence:** Definition + six-row demo.
- **Conditions:** Classification / tagging workflows.
- **Exceptions:** Six rows is a toy set.
- **Action:** Steal labeled-set-before-sell (and before we trust a spend).
- **Confidence:** high
- **Source:** `8IUWeF3B-hk` @ UNKNOWN
- **Epistemic:** SOURCE

### Tokens and time are first-class eval outputs
- **Claim:** The eval run reports average tokens, prompt count, and duration plus correctness.
- **Reasoning:** Cost and latency sit next to quality.
- **Mechanism:** n8n evaluations tab (on-tape vendor).
- **Evidence:** “average of how many tokens were used, um, prompts total, and then also how long it took.”
- **Conditions:** Their n8n eval feature.
- **Exceptions:** Averages on six rows ≠ monthly bill.
- **Action:** Pair quality with a token meter before any paid batch.
- **Confidence:** high he showed it
- **Source:** `8IUWeF3B-hk` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Speaker believes you cannot sell what you cannot score. **SOURCE** He treats tokens as a visible average, not a vibe. **SOURCE** Uncertainty: he does not say a pass threshold.

## D. Procedures
- Write a hypothesis (“if I change X, output improves”). **SOURCE**
- Put expected labels in a sheet (category, priority). **SOURCE**
- Run the set; read correctness + tokens + time. **SOURCE**
- Signals: some rows wrong — that is the point.
- Avoid: shipping on one happy path.
- When to change: if scores fail — tape shows visibility, not the fix loop.

## E. Examples
**Situation:** Classifier should tag category + priority. **Action:** Six labeled examples through the model; write back; read the eval tab. **Reasoning:** Objective proof. **Outcome:** All six processed; mix of right/wrong; token/time averages shown. **Lesson:** A failing row is cheaper than a bad production run. Implicit rule: tokens belong on the same screen as accuracy.

## F. Decision Rules
- If you would sell or rely on a workflow → you need a labeled set. **SOURCE**
- If tokens are not metered → do not batch. **SYSTEM SYNTHESIS**
- Refuse: buying n8n-cloud because eval exists.

## G. Contrarian
Field ships on “looks good.” He says that is not evaluation. **SOURCE** Keep it.

## H. Assumptions
- Theirs: six rows generalize; sheet labels are gold.
- Domain-specific: classification, not generative video.
- Falsifier: eval green, production red (distribution shift).
- Disagreement: none.

## I. Questions
- What pass score does he require before a client sees it? Not said.
- What did the token average cost in $? Not said.

## J. Connections
- `coverage-loop` · `golden-test-loop` · `NWbh5ZoEHkA` (tool vs vibe math). **SYSTEM SYNTHESIS**
- Long eval / data-table tapes: `lcNN3X9gXls` · `QCjMBOEhpLE`.

## K. Future-Use
Token+correctness on one screen as the pattern for any paid batch. Unassigned.

## Steal / Operate-never

### Machine: labeled-set + token meter
- **Epistemic:** SOURCE
- **Workflow / loop:** hypothesis → labeled rows → run → read correctness AND tokens/time → checkable stop = do not scale a failing or unmetered run
- **Questions / signals:** What is the expected label? What did tokens do?
- **Qualify / frame / objections:** “I ran it once and liked it” → not eval.
- **Procedure:** Six (or more) rows before any batch that can spend. Cap tokens.
- **Example that proves it:** Six examples → mix of right/wrong + token averages → Lesson: see the misses before a bill.
- **Why it works:** Subjective QA hides both quality and spend.
- **Conditions / exceptions:** Toy n=6. Do not install their eval vendor. Cursor + Grok.
- **Operate-never payload:** n8n-cloud because “evaluations tab”; unmetered 24/7 runs.
- **Hive run:** `coverage-loop` · `golden-test-loop` · `batch-credit-cap` (proposed, do not auto-write) · `ask-principal`.
- **Source:** `8IUWeF3B-hk` @ UNKNOWN

### Operate-never
- Buy a seat for eval. Quote token averages as our bill.
- Auto-send / unmetered batch. New hunt. Merge LESSONS. Auto-write SKILL.md.

## L. Role-Specific Applications
This is a subscription-audit tape: tokens and time are the bill. Do not run a 12h loop without a cap. Correctness without a meter still leaks months. Still no dated runway number — I will not invent one from six demo rows.
