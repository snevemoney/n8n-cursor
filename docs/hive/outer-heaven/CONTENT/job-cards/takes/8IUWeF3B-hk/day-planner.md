# Day Planner — 8IUWeF3B-hk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/8IUWeF3B-hk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/8IUWeF3B-hk/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Short: evaluate workflows if you want to sell them. Beats: evaluation = validating a hypothesis with objective proof; old habit = change X, rerun, subjectively judge better/worse; n8n eval flow: dataset of six examples with expected category + priority → through the model → write answers back to Google Sheet → evaluations tab → run test → tokens/prompts/time averages + correctness scores; he opens the run: six processed, which were right/wrong. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Eval = hypothesis + objective proof
- **Claim:** Evaluation is validating a hypothesis with objective proof, not a vibe after one rerun.
- **Reasoning:** Subjective “better/worse” is what people already do and it is not enough to sell.
- **Mechanism:** Hypothesis (if I do X, output improves) → change → measure against expected labels.
- **Evidence:** “validating your hypothesis with objective proof.”
- **Conditions:** Expected answers exist.
- **Exceptions:** No labeled set → you are still in vibe mode.
- **Action:** A weekday eval block needs a labeled set and a score, not “tweak it.”
- **Confidence:** high as doctrine.
- **Source:** `8IUWeF3B-hk` @ UNKNOWN
- **Epistemic:** SOURCE

### Six labeled rows is enough to run a test
- **Claim:** He pulls six examples with expected category and priority, writes model answers back to the sheet, and gets average correctness plus token/time.
- **Reasoning:** Small set, visible right/wrong.
- **Mechanism:** Sheet → model → sheet → evaluations tab.
- **Evidence:** “data set of six examples… which ones were right and which ones weren’t.”
- **Conditions:** Expected category + priority columns exist.
- **Exceptions:** Six is his demo, not a law.
- **Action:** Steal “labeled set + score tab.” Do not treat six as FACT coverage.
- **Confidence:** high he ran it; six UNVERIFIED as sufficient.
- **Source:** `8IUWeF3B-hk` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
If you sell it, you must be able to show a score. Priority: expected labels before the tweak. Contrarian vs “run it again and see.” Uncertainty: token averages are extra telemetry, not the pass.

## D. Procedures
1. Write the hypothesis (if X then better).
2. Put expected category + priority on rows.
3. Run the set through the model.
4. Write answers back.
5. Read right/wrong + averages.
6. Stop. Do not “keep tweaking” through CUT.
Avoid: subjective-only QA; quoting token averages as FACT quality.

## E. Examples
**Six-row category/priority test:** Situation → tagging model. Action → sheet of six expected pairs → run test. Reasoning → objective proof. Outcome → all six processed; some right, some wrong. Lesson → the tab is the stop, not another prompt tweak.

## F. Decision Rules
- If there are no expected labels → do not call it an eval block.
- If the score is not read → the block is not done.
- If “don’t stop until 100” appears on a sibling tape → conflict flag (see `CB5bG4mvnS0`).

## G. Contrarian
Rejects subjective reruns as evaluation. Field assumption: you can feel when it is better. He wants a sheet.

## H. Assumptions
Theirs: Google Sheet + n8n evaluations tab is the harness. Ours: `golden-test-loop` is the hive analog. Falsifier: labels themselves are wrong. Survivorship: one demo set.

## I. Questions
What were the six? What score did he accept? Full eval setup on a long tape?

## J. Connections
- SYSTEM SYNTHESIS → `golden-test-loop` · `CB5bG4mvnS0` (until-100 is not done) · `coverage-loop` (score then wire one).

## K. Future-Use
Labeled-six as a minimum dry-run shape. Unassigned: whether morning plan ever includes an eval block.

## Steal / Operate-never

### Machine: hypothesis → labeled set → score tab → stop
- **Epistemic:** SOURCE
- **Workflow / loop:** write “if X then better” → load expected rows → run → read right/wrong → stop
- **Questions / signals:** What is the hypothesis? Where are expected labels? What is the score?
- **Qualify / frame / objections:** “I ran it again and it felt better” is the fail. Score tab is the pass.
- **Procedure:** One eval block. No send. No until-100 grind.
- **Example that proves it:** Situation → tagging agent. Action → six expected category/priority rows. Reasoning → objective proof. Outcome → right/wrong visible. Lesson → steal the tab.
- **Why it works:** A buyer (or Evens) can see misses without watching you vibe.
- **Conditions / exceptions:** No labels → not this machine. Six is a demo size.
- **Operate-never payload:** Quote six/token averages as FACT; grind CUT on tweaks; n8n-cloud.
- **Hive run (existing skills only):** `golden-test-loop` · `coverage-loop`.
- **Source:** `8IUWeF3B-hk` @ UNKNOWN

### Operate-never
- Subjective-only “it’s better” as a ship rule.
- Until-100 eval grind through CUT.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet by wiring **ONE** labeled dry-run (not a 100-case grind). Clients parked.
