# Librarian — 3TdD8Qv5Tk8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/3TdD8Qv5Tk8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/3TdD8Qv5Tk8/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** Master 97% of Codex in 1 Hour (full course)
**Channel:** Nate Herk | AI Automation
**Kind:** video (~1:00:59 / ~14901 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Hook: Codex is a “super app.” He is **not** ditching Claude Code — both, different jobs. Promise: first-open → finished automations/sites. UI looks like ChatGPT (projects, chats, model toggle 5.5/5.4, speed, intelligence low/medium/high/extra high) and like Claude Code desktop. Title says **97%**; on-tape project folder is “Codex **95%**” — keep the wobble.
2. What Codex is: ChatGPT-in-projects plus local files, Excel, mouse/keyboard, browser, skills, sites/apps/games, sleep automations. Web ChatGPT has connectors (hands); Codex can do everything chat can; chat cannot do Codex. Different harness + models: Claude = Opus/Sonnet/Haiku, exploratory/brainstorm/creative/plan; Codex = ChatGPT models, **pragmatic**, follows a **long** plan, executes, finds issues Claude “for some reason” can’t. Not “I love Codex more.” Pet sits on-screen and narrates work (multitask). Heart Two / Herc 2 OS is **folders**; any harness (Codex / Claude / Cursor / Open Claw) can sit on the same directory if instructions say what lives where.
3. Build today: YouTube comment intelligence — pull comments → analyze → Excel workbook + viz → web dashboard he can check on a walk → ship. Needs a ChatGPT plan; free can try (limited); he recommends **$20/mo**; hit limits → Pro. ChatGPT sub also plugs into Open Claw / Hermes (cheaper than per-token). Today = **app**; VS Code extension / terminal = more functionality. Random chats can build games; deliverables have no home — work in a **project** with a local directory (Desktop / Codex YouTube / YouTube analytics demo).
4. Permissions: default vs **full access** (orange). School PDF guide (classroom → all YouTube resources). First job: don’t give the path — “look on Desktop in YouTube OS, read 5–10 raw transcripts.” It searches, then stalls on raw vs processed. Warning: search-without-path **wastes tokens**; copy the exact path. Extra high is overkill for that job; he uses **medium** for plan/brainstorm, **high** for huge builds, **extra high** almost only for unsolved bugs. Transcript knowledge lives in ChatGPT memory + **this chat** — new chat loses it → write `AGENTS.md` (Claude’s `CLAUDE.md` under Codex name): who / goal / Vercel dashboard / YouTube connect / skills.
5. **Plan mode first** (won’t execute). Glydo STT (left Whisper; now on Glydo team). Mindset: if you don’t know it’s possible, ask Codex/Claude — don’t default to YouTube or a consulting call. Plugins/MCP/skills/connectors: Hugging Face, Vercel, GitHub, Game Studio, Remotion, Figma, Hyperframes, Canva, Drive, Slack, SharePoint, Teams. **No YouTube plugin** → he asks how. Plan questions: API key vs OAuth vs both (he takes rec = API key); recent videos. It **found an API key in another project**; he still wants a **fresh** key — separate Codex vs Claude Cloud keys. Creates `.env.local`. Google Cloud: project Codex demo → YouTube Data API v3 → restrict key to that API. Channel ID + uploads playlist already in the other project (speech≠behavior: he was going to look them up).
6. Default permissions pause for approve; settings → auto review / full access. Horror stories (delete DB / mass email); he has **never** had that; usually context rot / vague / bad plan. Beginner: stay default; later full access to stop babysitting. Key works; PowerShell `Invoke-WebRequest` **TLS fail**; Node + Python clean — persist that as **golden knowledge** so it never retries PowerShell. He asks it to write a memory file; it stuffs into `AGENTS.md`. Caution: don’t bloat `AGENTS.md` (token tax). Context bar: Opus ~1M vs Codex ~**4× less** (UNVERIFIED); both auto-compact; he points to other context-mgmt videos.
7. Plan: ~100–200 recent comments → patterns → Excel for a creator. He accepts recs; 200 across recent videos. Pet badge when he leaves the tab; color dots when a session needs attention; parallel chats in one project. Slash menu: auto review, code review, feedback, MCP, memories, model, personality, pet, plan, reasoning, browser, GitHub, Higgsfield, PDF, skill-creator. `@` tags plugins/files. `/personality` → friendly vs **pragmatic** (he prefers pragmatic). Side chat = don’t interrupt the main run (`/loop`-like). Deliverable: 200 comments / **204** rows (first rows “nonsense”); generated May 5 11:00 a.m.; Claude Code / Higgsfield / Codex API mentions; vague prompt = generic insights — plan harder or feed tracked questions.
8. Skill = recipe (pancakes again). Plugins ≠ skills (connections/MCP vs markdown instructions). Best skill path: brainstorm → like the output → “turn that into a skill.” Lands in **`.codex` global** (all projects) vs local; say which. Call via slash or NL. Herc 2 `.claude/skills` morning-coffee example (YAML + steps). Pets: Codex / Dewey / CD / Beasod / Stacky / Fireball; `/pet` hides it; still visible on other dashboards.
9. New chat + plan: `@` the Excel → localhost dashboard, charts, **GPT Image 2** concepts + logo. Extra high defaulted again → he snaps back to **medium**. Extra high (1) over-engineers simple tasks (2) burns session. Settings → rate limits: **every 5 hours** 97% left; **weekly** 99% (UNVERIFIED). Codex sessions last **longer** than Claude Code; 5.5 “really efficient” (points to Opus-vs-5.5 tape). Visual direction: Creator Ops / Playful Studio / Executive Clean — he takes rec. Data source = existing Excel. First screen = analytics overview. Built-in verification: browser pass, then visual pass finds **3** issues, fix, rebuild, second pass. “Creator Ops Signal Desk.” Comment rows have links — **click does not open** (he still wants a link, maybe in-dashboard YouTube reply — operate-never). Localhost works for him; **your** paste of that URL is empty.
10. Ship: GitHub (free) + Vercel (generous free). Git = local; GitHub = cloud. Don’t commit secrets; `.env` / `.env.local` (the **dot** is the exclude signal — not a file named `secrets`). GitHub CLI already set; would prompt auth. Vercel ↔ GitHub: push → auto deploy. Private repo “YouTube Analytics Demo.” Import → deploy → `youtube-analytics-demo.vercel.app`. Custom domain = DNS (he doesn’t). Localhost iterate; **push only if you like it** — production stays clean. ~30–40 min of files in the folder.
11. Automations tab = scheduled chats (status report, release prep). Teach-a-kid-to-ride-a-bike: hold the bars, then walk behind, then training wheels off — don’t expect first-shot perfect. Sunday 5 p.m.: run comment-insights skill → merge Excel rows → refresh dashboard → commit/push → Vercel. `Run now` injects prompt + automation ID + memory + last run into a **new** chat (Claude local routine / Cowork analog). **Laptop/app off = cron dies.** True 24/7 needs Codex-on-the-cloud; he points at Claude **cloud routines**. Keep with `xJ5oz63mIec` / `gb5TlGw6Uks` 15/day dissent.
12. Browser-use QA: open in-app, click, try to break. Also: no-API downloads, cookie/login (School named). Alice-logo mouse. In-app browser didn’t open YouTube tab (same bug he saw). Codex browser-use “much more smooth” than his other stacks / Playwright-in-Claude (UNVERIFIED). Bake “don’t come back until you’ve stress-tested with browser-use” into skills. Six findings: YouTube links, empty explorer, literal search, tab state visual-only, awkward UI.
13. Ugly run: Excel **open** → cannot overwrite (~20 min burn). Automation defaulted to **GPT 5.2** — he retriggers **5.5 high**. Never uses **Fast** (eats usage; not time-sensitive). Watch early; stop if a 20-second human fix exists (close the file). Second stall; he asks “what do you need from me?” → confirmations → **~7 min**. 200 → **208** comments, **deduped** (not 200+200). Two GitHub commits; Vercel deploy 1 min later. Dark code: you don’t need every line; you need what/why; ask “streamline / more robust?” It says still **too agent-orchestrated / too heavy** for a weekly refresh — add a repo script, stop full Next build. Speed doesn’t matter if weekly (6 vs 7 p.m.) unless the job is latency-sensitive.
14. Close: idea → project → connect → skill → automation → dashboard → browser-use → weekly refresh. The whole thing is **a folder**. OpenClaw / Claude / anything can sit on it. In-app terminal: call Claude **inside** the Codex repo — Claude brainstorms a file; Codex `@` executes. Use-case not crown (same as `RLjaUES9P8A`). Next: AI OS video (Claude-based; files travel; `CLAUDE.md` → `AGENTS.md` ~30 seconds). School / Glydo / Vercel / Codex / Open Claw / Hermes stay on tape.
Gap: the Excel + dashboard UI. Timestamp UNKNOWN. Speech≠behavior: “don’t search, give the path” after he demoed search; “I never had a problem” + horror-story caution; comment links shown but dead; 97% title vs 95% folder; 200 vs 204 vs 208.

## B. Atomic Knowledge

### Use-case not crown — same folder, two harnesses
- **Claim:** Claude explores/plans; Codex executes long plans and debugs. Not ditching Claude. Any harness (incl. Cursor) works if the directory has instructions. Terminal: Claude brainstorm file → Codex `@` execute.
- **Evidence:** “I’m not saying that I’m ditching Claude Code” / “which tool is best for this specific use case, not which tool is best”
- **Action:** File use-case split; hive stays Cursor + Grok (`35WuZxbAY68` / `RLjaUES9P8A`)
- **Confidence:** high as his doctrine
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Project directory is the home; chats are homeless
- **Claim:** Random chats can build; deliverables scatter. Work in a named project folder. `AGENTS.md` is the onboarding doc every new chat reads. Don’t dump every failure into it (token tax).
- **Evidence:** “any deliverable that we create doesn’t really have a home” / “every time you open up a new chat, it’s first of all going to read the agents.md”
- **Action:** File project-home + agents.md + don’t-bloat
- **Confidence:** high
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Plan first; persist the failure; path beats search
- **Claim:** Plan mode before execute. Exact path > “find it on my Desktop” (tokens). Failures are golden knowledge — write them down so PowerShell TLS never retries. Extra high over-engineers and burns session; medium default; high for big; extra high for stuck bugs. Fast eats usage.
- **Evidence:** “I always start with plan mode” / “not the most effective use of… tokens” / “treat that as golden knowledge”
- **Action:** File plan→path→persist-fail; rate-limit % UNVERIFIED
- **Confidence:** high
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Skill after a liked output; global vs local
- **Claim:** Don’t invent the recipe first. Do the work, like the Excel, then “turn that into a skill.” `.codex` global vs project-local — say which. Slash or natural language. Pancake recipe updates after each batch.
- **Evidence:** “after I get an output I like, I say, okay, turn that into a skill” / global `.codex` vs local
- **Action:** Map to existing hive skills; do not ingest his YouTube-insights skill
- **Confidence:** high
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Localhost ≠ production; push is the hard step
- **Claim:** Localhost is his machine. GitHub private repo + Vercel auto-deploy. Iterate locally; push only if you like it. `.env*` stays uncommitted. Custom domain = DNS (HITL).
- **Evidence:** “if I gave you this localhost… you would get nothing” / “just because we make changes doesn’t mean that it’s actually going to go live”
- **Action:** File test-then-push; deploy stays `ask-principal`
- **Confidence:** high
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Laptop cron is not 24/7
- **Claim:** Codex automations = scheduled chats injected into a new project chat. Close the app / sleep the PC → it dies. Cloud routines (Claude named) for always-on. Bike-training: watch, then trust.
- **Evidence:** “if you close out of the Codex app or if you turn off your PC… this will stop running”
- **Action:** File local-cron-dies; keep 15/day / Hermes-cron dissent
- **Confidence:** high
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Watch the first runs; default model can be wrong
- **Claim:** Open Excel locked the write (~20 min). Automation defaulted to GPT **5.2**; he wanted 5.5 high. Stop when a 20-second human fix exists. Ask “what do you need from me?” Dark code: understand what/why; weekly refresh was “too agent-orchestrated.”
- **Evidence:** “it couldn’t overwrite a file that I had open” / “set up by default to run with GPT 5.2” / “too agent orchestrated and too heavy”
- **Action:** File watch-first + check-the-model + script-not-agent-for-refresh
- **Confidence:** high
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

### Separate keys; default permissions until the flow is known
- **Claim:** Found a key in another project; still minted a Codex-only YouTube key. Restrict to YouTube Data API v3. Full access is orange; beginners stay default. He claims zero horror-story incidents (UNVERIFIED).
- **Evidence:** “separate API key for Codex and a separate one for Claude code” / “leave it on default”
- **Action:** File named keys + default-until-known; never-had-a-problem UNVERIFIED
- **Confidence:** high as procedure
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Super-app vs web chat. Use-case not crown. Folder = OS; harness is a hat. Plan before build. Failure = gold. Skill after liked output. Localhost vs URL. Bike training for automations. Dark code. Pet as progress badge. Pragmatic personality. Ask the tool, not a consultant.

## D. Procedures
New project folder → `AGENTS.md` → plan mode → exact paths → named `.env.local` key → test connection → persist the fail → liked deliverable → skill (say global vs local) → `@` file into next chat → localhost + visual verify → GitHub (no secrets) → Vercel import → automation with **correct model** → `Run now` while watching → close locking files → ask streamline. Avoid: extra-high on chores; Fast; full access on day one; token-in-chat; laptop-as-24/7; bloat agents.md. Signals: raw-vs-processed stall; PowerShell TLS; dead comment links; Excel-open lock; 5.2 default.

## E. Examples
**PowerShell TLS:** Situation — test YouTube key. Action — PowerShell fails; Node/Python work; he forces a project memory. Reasoning — don’t retry the dead path. Outcome — written into `AGENTS.md` (with bloat warning). Lesson — failure is an atom.

**Excel lock:** Situation — Sunday automation / run-now. Action — 20 min stuck; file open; wrong model 5.2. Reasoning — stop, close file, set 5.5 high, ask what it needs. Outcome — 208 deduped, Vercel refresh. Lesson — watch first runs; check the model.

**Dead comment links:** Situation — polished dashboard. Action — rows look linked; click does nothing. Reasoning — verification loop missed the human want. Outcome — he names in-dashboard reply as possible. Lesson — visual pass ≠ product pass; auto-reply stays never.

## F. Decision Rules
- If explore/plan → he uses Claude; if long execute/debug → Codex (hive: Cursor + Grok).
- If new chat → `AGENTS.md` must already exist.
- If you don’t know the path → still give the path once you have it.
- If you liked the output → skill it; say global vs local.
- If localhost looks good → do **not** treat as shipped until Evens pushes.
- If automation → check model/reasoning; laptop off = dead; weekly refresh may want a script not an agent loop.
- If beginner → default permissions.
- Refuse: Codex/Vercel/Glydo/School/Open Claw/Hermes as hive; 97%/95%/$20/97%-remaining as FACT; full-access-always; auto YouTube reply.

## G. Contrarian
Against ditching Claude for Codex. Against “which tool is best.” Against extra-high-as-default. Against Fast. Against first-shot-perfect automations. Against laptop-24/7. Against dark-code-and-ship. Against consulting-call-first. Against a file named `secrets` instead of `.env`.

## H. Assumptions
$20 / Pro / 5h 97% / week 99% / 4× context / 97% title / 95% folder / 200/204/208 / “never had a problem” / “browser-use much more smooth” / 30-second CLAUDE→AGENTS — UNVERIFIED. School + Glydo + Vercel are the cart. Deploy/push is HITL. Comment-reply API is named as doable — operate-never. Do not flatten with `RLjaUES9P8A` (Code vs Codex) or `gb5TlGw6Uks` (Hermes crons on a VPS).

## I. Questions
Did the Sunday 5 p.m. job stay on 5.5 high after the tape? Did he add the repo script it recommended? Live URL still up? Receipts for Codex lasting longer than Claude?

## J. Connections
SYSTEM SYNTHESIS → `RLjaUES9P8A` (use-case not crown; portable md) · `gb5TlGw6Uks` (pillars; VPS cron vs laptop cron) · `xJ5oz63mIec` (15 remote runs / 24/7 title) · `3GAxd90fEE4` (cron ≠ watching; leaked key) · `35WuZxbAY68` (Cursor graduated **for Nate**) · `8QQ_INxAhRs` (keys not prompts) · `HN0oWxbF2bM` (auto-reply never) · `tDGiWn0flK8` (plan then persist profile).

## K. Future-Use
Project-home, plan-first, persist-fail, skill-after-liked, localhost≠prod, laptop-cron-dies, watch-first-model-check, script-not-agent-for-refresh as atoms. Do not install Codex as hive.

## Steal / Operate-never

### Machine: folder OS → plan → liked output → skill → watch the cron → push only if you like it
- **Epistemic:** SOURCE (vendors on-tape)
- **Workflow / loop:** named folder + `AGENTS.md` → plan mode → exact path + named key → persist the fail → liked deliverable → skill (global vs local) → localhost verify → HITL push → scheduled chat with **checked model** → watch first runs → ask streamline → checkable stop = commit message + why
- **Questions / signals:** New chat lost context? Extra high on a chore? File open? Wrong model on the cron? Localhost pasted as if live?
- **Qualify / frame / objections:** “97% in 1 hour” is the hook; 95% folder and 5.2 default are the wobble
- **Procedure:** separate keys; default permissions; `.env*`; bike-train automations
- **Example that proves it:** PowerShell TLS; Excel lock; 200→208 dedupe; dead links
- **Why it works:** harnesses share a directory; failures become files; production only moves on push
- **Conditions / exceptions:** app ≠ CLI; laptop ≠ 24/7; weekly refresh may want a script
- **Operate-never payload:** Codex/Vercel as hive; auto-deploy without Evens; full-access-always; YouTube auto-reply; quote 97%/$20 as FACT
- **Hive run:** `agent-job-card` · `ask-principal` · `coverage-loop`
- **Source:** `3TdD8Qv5Tk8` @ UNKNOWN

### Operate-never
- Install Codex / Vercel / Glydo / School / Open Claw / Hermes as hive. Auto-reply YouTube from the dashboard. Full access as day-one policy.
- Quote 97% / 95% / $20 / 97% remaining / 4× context / “never had a problem” as FACT.
- Treat localhost as shipped. Laptop cron as 24/7. Merge `LESSONS-FROM-TAPE.md`. New `icp_id`. Overwrite `takes/librarian.md`.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
File use-case-not-crown and laptop-cron-dies as labeled rows — do not flatten 97% vs 95% or Claude-vs-Codex costume. Job cards name the folder and the hard step (push). No Codex classroom wiki.
