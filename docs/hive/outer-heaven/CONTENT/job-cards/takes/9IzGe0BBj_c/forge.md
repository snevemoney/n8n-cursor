# Forge — 9IzGe0BBj_c
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9IzGe0BBj_c/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/9IzGe0BBj_c/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Instance-level MCP teaser. Beats: old n8n MCP = you build MCP server triggers for **assigned** tools/workflows, then Claude/Cursor call those → new **instance** MCP = client can search the **whole instance**, read schemas, **execute any workflow** → metaphor: ChatGPT is the agent; every workflow is a tool it can see/call → play-button to longs `mPflFTQUCGk` / `5p5cV0yVDvQ`. Timestamp UNKNOWN.

## B. Atomic Knowledge

### Assigned MCP vs whole-instance execute
- **Claim:** Native MCP servers were allow-listed tools. Instance MCP lets the client search and run anything in the instance.
- **Reasoning:** He likes “just throw the instance at Lovable or Claude.”
- **Mechanism:** Search workflows → understand schema → execute.
- **Evidence:** Contrast paragraph.
- **Conditions:** MCP access enabled on the instance.
- **Exceptions:** He calls it “just an AI agent” as a metaphor, not a spec.
- **Action:** Steal the **risk**: assigned tools ≠ whole-instance execute. Allow-list is the upgrade we keep.
- **Confidence:** high on the contrast.
- **Source:** `9IzGe0BBj_c` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
He wants the chat app to be the manager of every workflow. That is power and blast radius. “It’s just an agent” hides the execute-any.

## D. Procedures
On tape: enable instance MCP → connect Claude/ChatGPT/Lovable → they search and fire workflows. Hive procedure: do not enable execute-any. If MCP exists, allow-list visible tools.

## E. Examples
**Situation:** You have “tons of workflows” you wish Claude could use.  
**Action:** He says instance MCP lets it use all of them.  
**Reasoning:** Agent + tool list = instance.  
**Outcome:** Teaser only; send-email demo is on `mPflFTQUCGk`.  
**Lesson:** Whole-instance is the magnet and the incident.

## F. Decision Rules
- If the client can execute any workflow → treat as prod-admin, not a convenience toggle.
- If you only need one tool → assigned MCP (old shape) is enough.
- If the pitch is Lovable+your-instance → operate-never.

## G. Contrarian
Field wants one chat to run the company. He celebrates that. We steal the older allow-list.

## H. Assumptions
Instance MCP can actually execute any workflow as claimed. Falsifier: hidden auth still blocks writes. Survivorship: no incident on tape.

## I. Questions
Is there a deny list? Does execute require a second confirm? Long-tape send-email always-allow — how bad?

## J. Connections
SYSTEM SYNTHESIS: `mPflFTQUCGk` (Claude sends mail + ClickUp complete). `5p5cV0yVDvQ`. Forge job: gated tools. `ask-principal` on any execute.

## K. Future-Use
Allow-list vs execute-any as a Watchdog/Forge interview question.

## Steal / Operate-never

### Machine: allow-list tools; refuse execute-any
- **Epistemic:** SOURCE (contrast) + SYSTEM SYNTHESIS (we keep the old side)
- **Workflow / loop:** name the workflows a client may call → expose only those → search/execute inside that set → stop
- **Questions / signals:** Can it see billing? Can it send? Can it delete?
- **Qualify / frame / objections:** “Just an agent” is the danger sentence.
- **Procedure:** Do not toggle instance-wide MCP on a live box.
- **Example that proves it:** He states execute-any as the upgrade.
- **Why the steal works:** Assigned tools have a blast radius you can name.
- **Conditions / exceptions:** Demo instance ≠ prod.
- **Operate-never payload:** Instance MCP on hive n8n; Claude/Lovable execute-any; always-allow send.
- **Hive run:** `ask-principal` + existing error-heal. No new MCP install.
- **Source:** `9IzGe0BBj_c` @ UNKNOWN

### Operate-never
- Enable instance-wide execute. Always-allow send.
- Install Claude/Lovable/n8n-cloud. New hunt.
- Send / pay / deploy / book / publish. Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not point a chat at the whole estate. Tools stay gated. A PR that “just adds MCP” is a blast-radius change. Deploy HITL.
