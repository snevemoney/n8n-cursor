# Forge — 27Y44JYXZJ8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/27Y44JYXZJ8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/27Y44JYXZJ8/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **Anthropic managed agents** (3h play, “disappointed”). Beats: **4 days ago** — no Claude **subscription inside third-party harnesses** (OpenClaw); people pay more unless local → **yesterday** “model too dangerous,” crushes Opus 4.6 UNVERIFIED → **today** managed agents, “production **10×** faster” UNVERIFIED → host the agent in their sandbox; define tasks/tools/guardrails → Notion example: drag a task, Claude picks it up → Console: templates or chat-to-build; **no Code sub** — API + **~$5** to start UNVERIFIED → competitor-intel agent: name/desc/model/prompt/MCP/tools/skills; he swaps Opus 4.6 → environment = cloud box + networking; demo **unrestricted** → session live; ClickUp via **OAuth/MCP vault** (team-shareable) → chat has **no business context**; “guided edit” actually changed the prompt (tab versions didn’t) → test run: bash/read/grep/search/fetch = Code SDK with a Chat UI; **Ask Claude** about the setup → charge: **$0.08/hr** while a **session is live** + API tokens; idle env **$0** UNVERIFIED → 3-min competitor report **didn’t** post to ClickUp (prompt gap) → field-monitor → ClickUp in **~2 min** setup → ClickUp research queue: move to-do → comment + sources + complete — but agents **only wake on API call**; **no cron**; ClickUp won’t fire them; he’d glue n8n → HTTP (over-engineered) → he’d rather **Trigger.dev** (cron + cheaper) than this → teased (apply): **outcomes** (self-eval until criteria), **multi-agent** callable swarm, **persistent memory** (today: stateless + system prompt; he Frankenstein-logs) → CLI from Claude Code: build managed agents using project context; **don’t put keys in the system prompt** (UI defaults to MCP vault) → he made a Code project to compare stacks → OpenClaw he still uses: **heartbeats** + **Telegram**; Code desktop schedule ≠ 5-min heartbeat → who: beginners maybe; builders maybe not → Skool PDF. Timestamp UNKNOWN. Claude / OpenClaw / Trigger.dev / n8n / ClickUp on-tape.

## B. Atomic Knowledge

### A hosted agent without a wake-up is a form, not a worker
- **Claim:** Pretty console + OAuth vault ≠ production. If it can’t cron or take a ClickUp webhook, you still glue. He already has Trigger.dev / desktop schedules / OpenClaw heartbeats.
- **Reasoning:** Research-queue demo needed *him* to tell the agent to poll. No native schedule.
- **Mechanism:** API-trigger only; $0.08/hr while the session lives; vault for OAuth.
- **Evidence:** ClickUp miss on first agent; 2-min field-monitor that *did* post.
- **Conditions:** Day-one product as taped.
- **Exceptions:** Teased outcomes/memory/swarm. Notion in-app may be a different shape.
- **Action:** Steal “wake-up is the product” and “keys in a vault, not the prompt.” Do not install managed agents / OpenClaw / Trigger.dev / Claude.
- **Confidence:** high on the gap; 10× / $0.08 / $5 UNVERIFIED.
- **Source:** `27Y44JYXZJ8` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Chat-user vs Code-user: same feature, different value. Guided-edit vs silent no-op versions. Tool connected ≠ tool used. Dangerous-model PR ≠ they shipped cron. Best tool = best for *this* job.

## D. Procedures
1. Don’t open Claude Console managed agents. 2. Don’t install OpenClaw / Trigger.dev. 3. Don’t put keys in a system prompt. 4. Don’t unrestricted-network a cloud agent. 5. Don’t quote 10× as FACT.

## E. Examples
**Situation:** Competitor intel + ClickUp connected.  
**Action:** Run.  
**Reasoning:** Prompt never said “post it.”  
**Outcome:** Report, no ClickUp.  
**Lesson:** Connection ≠ behavior.

**Situation:** Research queue.  
**Action:** Wants auto-pickup.  
**Reasoning:** No cron, no ClickUp trigger.  
**Outcome:** He’d glue n8n or use Trigger.dev.  
**Lesson:** Wake-up is the missing product.

## F. Decision Rules
- If there’s no wake-up → it’s a chat with a costume.
- If keys might land in the prompt → vault or refuse.
- If OpenClaw / Telegram-heartbeat / Trigger.dev CTA → park.
- If 10× / $0.08 / too-dangerous-model appear → UNVERIFIED.

## G. Contrarian
Field hears “10× to prod.” He spent 3h and wanted cron. Field dumps Code because the sub can’t ride OpenClaw; he still prefers Code+Trigger.dev over this wrapper.

## H. Assumptions
Day-one console as demoed. Falsifier: they add cron tomorrow. We do not run Claude or OpenClaw. Unrestricted network is operate-never.

## I. Questions
Do we already confuse “tool connected” with “tool will fire”?

## J. Connections
SYSTEM SYNTHESIS: `ehg4fhydTgs` routines *do* schedule (1h) — complementary gap. `YHk45NEpspE` OpenClaw on-tape. `jZgcWCzxh1I` confirm-before-width. No Claude / OpenClaw / Trigger.dev. Cursor + Grok stays.

## K. Future-Use
Wake-up-is-the-product. Vault-not-prompt. Don’t buy the wrapper.

## Steal / Operate-never

### Machine: a worker needs a wake-up; a vault beats keys in the prompt; don’t rent their sandbox
- **Epistemic:** SOURCE
- **Workflow / loop:** define the job → who wakes it? → where do secrets live? → if no wake-up, it’s a form
- **Questions / signals:** Cron? Webhook? Or only “start session”? Did the connected tool actually fire?
- **Qualify / frame / objections:** Beginner UX is real. Builders already have a host. Teased features aren’t here.
- **Procedure:** No managed agents. No OpenClaw. No Trigger.dev. No unrestricted cloud net.
- **Example that proves it:** ClickUp connected, not used; no cron on the queue.
- **Why it works:** Production is wake-up + secrets + a checkable done, not a template.
- **Conditions / exceptions:** Tape $ UNVERIFIED. Notion in-app unobserved.
- **Operate-never payload:** Claude managed agents; OpenClaw heartbeats; quote 10× as FACT.
- **Hive run:** none. Deploy HITL.
- **Source:** `27Y44JYXZJ8` @ UNKNOWN

### Operate-never
- Install Claude managed agents / OpenClaw / Trigger.dev.
- Unrestricted network. Keys in the prompt.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not open managed agents. Wake-up or it isn’t a worker. Deploy HITL.
