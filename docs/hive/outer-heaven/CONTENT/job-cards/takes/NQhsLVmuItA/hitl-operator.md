# HITL Operator — NQhsLVmuItA
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NQhsLVmuItA/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NQhsLVmuItA/LEARNED.md`
**ICP:** parked unless Evens named one.

Evens is the visionary. Operate ≠ learn. Role did not filter what was learned. Stack stays Cursor + Grok. Clients parked. No send / pay / deploy / book / publish. Tape $ UNVERIFIED.

## A. Source Map

**Title (PACKET):** n8n's NEW Guardrail Node is a Gamechanger for AI Agents
**Speaker / channel:** Nate Herk | AI Automation
**Kind / words:** short · 339 words
**Gaps:** No VTT cited in this take. Timestamps UNKNOWN. Visual-only UI clicks inferred only as INFERENCE.

Beats in order:

- Two guardrail actions: check text for violations (uses AI) vs sanitize text (no AI — encrypt/desensitize before the LLM).
- Keyword block demo: three items; one pass (omelette order); two fail (update the system setting; enter your password).
- Blocked keywords: password, system.
- He says if it passes you can send your email or update CRM; if it fails, Slack yourself, throw an error, stop the workflow.
- CTA to full cut.

## B. Atomic Knowledge

### Guardrail fail is a stop, not a suggestion
- **Claim:** Sanitize-before-model is the cheap guardrail; keyword fail must stop the world action.
- **Reasoning:** He names send-email as the pass path — that is the operate-never payload, not the steal.
- **Mechanism:** Text in → check or sanitize → pass/fail branches.
- **Evidence:** On-tape three fixtures; omelette passes; password/system fail.
- **Conditions:** On-tape demo / short captions.
- **Exceptions:** Tape $ and vendor names stay on-tape.
- **Action:** Steal fail-stops-the-workflow. Do not steal pass→send.
- **Confidence:** medium — caption ingest, timestamp UNKNOWN
- **Source:** `NQhsLVmuItA` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models

- Non-AI sanitize is preferred when you can desensitize without a model.
- You fully control pass vs fail behavior — control ≠ auto-send.

## D. Procedures

- Sanitize PII before the LLM when possible.
- Keyword/block list for password/system-class strings.
- Fail → Slack Evens or error. Pass → still a card if the next node is Send.

## E. Examples

- **On-tape run** — Situation: Three test strings → Action: Keyword check password/system → Reasoning: Show pass vs fail → Outcome: Omelette pass; two fails → Lesson: Fail branch is the machine; pass→email is the trap

## F. Decision Rules

- If text contains password/system-class tokens → fail/stop.
- If sanitize can strip PII without AI → do that first.
- If pass path includes Send → strip Send anyway.

## G. Contrarian

- Field treats guardrails as permission to auto-send. He shows them as branch control — this desk still holds Send.

## H. Assumptions

- Keyword lists are brittle (false fail on 'system'). He does not discuss that.
- Long cut: `oWdJMJp2HgM`.

## I. Questions

- Does sanitize reversible-encrypt or irreversible-redact?
- Who maintains the keyword list?

## J. Connections

- SYSTEM SYNTHESIS → `send-removed` · `input-required-gate`.
- SYSTEM SYNTHESIS → classifier-is-not-Evens (AI violation check).

## K. Future-Use

- Sanitize-before-model for any inbox/RAG. Unassigned.

## Steal / Operate-never

Informed by A–K. Auto-send / auto-book stay operate-never. The machine is still stolen.

### Machine: Sanitize / fail-stop (pass is not send)
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** text in → sanitize if PII → keyword/AI check → fail: Slack + error → pass: draft only → Evens if outbound
- **Questions / signals:** Did we sanitize? Which keywords? Is Send on the pass branch?
- **Qualify / frame / objections:** A guardrail is not Evens. A pass is not APPROVE.
- **Procedure:** Keep fail = stop. Rewrite his pass example from 'send email' to 'queue draft.'
- **Example that proves it:** **On-tape run** — Situation: password / system fixtures → Action: Check text → Reasoning: Block secrets → Outcome: Two fail, one pass → Lesson: Fail stop stolen; pass-send rejected
- **Why it works:** He proves fail can halt. He also names send-on-pass — that payload stays never.
- **Conditions / exceptions:** Hard steps stay HITL.
- **Operate-never payload:** Auto-send because the guardrail passed. Treat AI violation-check as Evens.
- **Hive run (existing skills only):** `send-removed` · `input-required-gate` · `ask-principal`
- **Source:** `NQhsLVmuItA` @ UNKNOWN

### Operate-never (this desk will not operate)

- Auto-send / auto-book / auto-voice-book / auto-publish / auto-pay / auto-deploy.
- Quote tape $ / student counts / job-loss % / token burns as FACT.
- Install on-tape vendors (Claude Code, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool) as our stack. Cursor + Grok only.
- New `icp_id`. Unpark Normand. Outreach / hunt because a tape was interesting.
- Always-allow MCP / classifier / guardrail-pass as Evens.
- Merge `LESSONS-FROM-TAPE.md`. Send / pay / deploy / book / publish.

## L. Role-Specific Applications

Guardrail nodes are allowed as stops. They are not a second approver. Pass + Send on the same canvas is REJECT. Clients parked.
