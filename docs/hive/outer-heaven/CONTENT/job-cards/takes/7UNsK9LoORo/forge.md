# Forge — 7UNsK9LoORo
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/7UNsK9LoORo/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/7UNsK9LoORo/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **n8n “Photoshop” agent** (Nano Banana / Telegram / Drive). Beats: text or image in; 5 tools: combine, edit, rename, search raw, search AI-out; Telegram pocket → photo → Drive (default **today’s date**) → “what name?” → rename Nate / granola → “combine Nate+granola, man holding granola hiking” (memory skips search) → Hormozi + JBL on a boat (search raw for IDs) → edit granola → Eiffel ad; text miss (“ingredients you can see and pronounce”) blamed on **low-res raw** + thin prompt → switch: photo vs text, normalize `json.message.text` → **tiny** system prompt (list tools; one rule: ask name then rename); GPT-5.1 + Sonnet 3.5 fallback; memory = Telegram chat ID → tools: update-by-ID; two search folders → **workflow-as-tool**: combine takes prompt + id1 + id2 + title → array → split → Drive download → **imgBB** public URL (API needs URL not binary) → aggregate → **FAL** Nano Banana; strip newlines/`"` so JSON lives → 10s then poll (he’d shorten) → GET result → Drive → reply link → edit = same, one ID → FAL **~$0.04**/image, **25/$1** UNVERIFIED; OpenRouter free window maybe gone; playground prompts **before** n8n → harden: dedicated **prompt agent** (main is busy); **logger** (Ultimate Media Agent sheet); image→**V3** video as another workflow-tool; Skool JSON + setup sticky; Plus. Timestamp UNKNOWN. n8n / FAL / imgBB / Telegram / Nano Banana on-tape. Caption-only: images unobserved beyond his words.

## B. Atomic Knowledge

### Workflow-as-tool + normalize the inbox; don’t let the busy agent write the image prompt
- **Claim:** Main agent routes. A child workflow does the API/poll/upload. Same child can hang on many parents. A specialist should write the gen prompt. Thin parent prompt + add rules when it fails.
- **Reasoning:** Combine vs edit is one ID. JSON breaks on quotes. Main “has other jobs.”
- **Mechanism:** Switch → same field; `$fromAI` IDs; imgBB; FAL poll; Drive out.
- **Evidence:** Memory skip vs Hormozi search; Eiffel text miss.
- **Conditions:** n8n + FAL as taped.
- **Exceptions:** imgBB = public URL of the asset (privacy). $0.04 UNVERIFIED.
- **Action:** Steal modular child + inbox normalize + specialist prompt. Do not stand up Telegram/FAL/imgBB. Don’t sell granola ads.
- **Confidence:** high on the shape; prices UNVERIFIED.
- **Source:** `7UNsK9LoORo` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Memory skips search until it can’t. Public-URL shim is a leak. Poll-until-done (`ehg4fhydTgs` / `7siRW0My05o`). Customize after download, don’t gold-plate the template.

## D. Procedures
1. Don’t import the Skool JSON. 2. Don’t put client photos on imgBB. 3. Don’t add FAL/Telegram. 4. If we ever compose: child workflow + poll + don’t JSON-break. 5. Don’t quote $0.04 as FACT.

## E. Examples
**Situation:** Nate + granola.  
**Action:** Memory has IDs; combine.  
**Reasoning:** Just uploaded.  
**Outcome:** Hiking shot (claimed).  
**Lesson:** Memory is a cache, not a store.

**Situation:** Eiffel granola.  
**Action:** Edit one ID.  
**Reasoning:** Low-res + thin prompt.  
**Outcome:** Text miss.  
**Lesson:** Specialist prompt + better raw.

## F. Decision Rules
- If two parents need the same API → child workflow.
- If the asset must stay private → no imgBB.
- If $0.04 / Plus / V3 CTA → park.
- If Hormozi/JBL is the proof → taste, not a SKU.

## G. Contrarian
Field stuffs one mega-agent with FAL JSON. He splits. Field writes a novel system prompt first; he starts tiny and adds on fail.

## H. Assumptions
FAL/imgBB as demoed. Falsifier: poll never returns. We do not run n8n-cloud. Public image host is operate-never for client work.

## I. Questions
None for hive ads. Modular-child is the steal.

## J. Connections
SYSTEM SYNTHESIS: `lcNN3X9gXls` logs. `7siRW0My05o` poll + linear. No n8n-cloud / FAL / Telegram-as-ours. Clients parked — no UGC mill.

## K. Future-Use
Child-workflow. Normalize inbox. Specialist for the hard prompt. No public-URL leak.

## Steal / Operate-never

### Machine: thin router + child that polls + specialist prompt; never public-host the raw
- **Epistemic:** SOURCE
- **Workflow / loop:** inbox → normalize → route → child (fetch → shim if you must → API → poll → store) → reply
- **Questions / signals:** Is the parent writing the gen prompt? Will JSON break? Is the URL public?
- **Qualify / frame / objections:** Demo ads. imgBB is a leak. V3 is another vendor.
- **Procedure:** No FAL/Telegram/imgBB. No Skool template as ours.
- **Example that proves it:** Combine vs edit; Eiffel text miss; quote-strip.
- **Why it works:** One child, many parents. The busy agent is a bad copywriter.
- **Conditions / exceptions:** Tape $ UNVERIFIED.
- **Operate-never payload:** n8n-cloud Photoshop; public raw; quote $0.04 as FACT; UGC mill.
- **Hive run:** none. Deploy HITL.
- **Source:** `7UNsK9LoORo` @ UNKNOWN

### Operate-never
- Import the Telegram/FAL stack. Host client photos on imgBB.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not add a Photoshop agent. No public image shim. Deploy HITL.
