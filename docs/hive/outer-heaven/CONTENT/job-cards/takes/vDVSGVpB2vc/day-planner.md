# Day Planner — vDVSGVpB2vc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/vDVSGVpB2vc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/vDVSGVpB2vc/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: Claude **agent teams** (experimental). Beats: “Neuroflow” FE/BE/QA on Sonnet — QA finds **3 critical**, send-back, second pass “one-shot” landing page (he still wants iterate); **sub** = solo, return to main; **team** = lead + **shared task list** + **peer talk**; disabled by default — one env var in `settings.local.json`; first dump official docs into `/docs`; prompt pattern: **goal** (they wake with **no history**) + N agents + model + role/produce/talk-to + **named recipients** + final deliverables (running app, pass/fail, decisions doc); dos: **own files**, defined output, name who to message, **3–5 not 10+**, full context; research-team demo (researcher/strategist/critic) + main **shuts them down**; VS Code extension hides thinking; **tmux** = see panes + message one teammate; three rules: territory, DM, **parallel** (if it’s only a handoff chain, you don’t need a team). Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Teams are a QA loop with territories; they wake empty; 10+ is a tax
- **Claim:** The unlock is peer talk + a shared list so QA can bounce work; each agent owns files or they overwrite; they only know the spawn prompt — so the **goal** must ride along; 10+ is ~10× $.
- **Reasoning:** A serial handoff is a sub-agent chain, not a team.
- **Mechanism:** Enable → docs in-repo → goal+roles+recipients → run → shutdown.
- **Evidence:** “When the agents wake up, they have no context.” / “don’t go for massive agent swarms of 10+.”
- **Conditions:** Experimental flag on (tape).
- **Exceptions:** He still one-shots a page and calls it powerful — we don’t enable Claude teams.
- **Action:** Steal empty-wake + own-files + 3–5 + QA-bounce. Do not set the env var. Do not 10+.
- **Confidence:** high as the rules.
- **Source:** `vDVSGVpB2vc` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** first QA fail → second pass
- **Speech ≠ behavior:** “one-shot” vs “we’d want to iterate”

## C. Mental Models
More expensive, slower, higher quality **if** used right. Main is a PM. tmux is the real dashboard. Priority: named recipients. Uncertainty: Windows tmux workaround.

## D. Procedures
1. If it’s only A→B→C with no talk, don’t make a team.
2. Write the goal first (they wake empty).
3. 3–5, own files, name who messages whom, name the artifacts.
4. Shut them down when done (his main did).
Avoid: Claude env flag; 10+; swarm for a landing page.

## E. Examples
**QA bounce:** Situation → fictional startup page. Action → FE/BE → QA 3 critical → send back → pass. Reasoning → peer talk. Outcome → a page. Lesson → steal the bounce; don’t ship the page.

**Empty wake:** Situation → research team. Action → spawn prompt includes “message the critic.” Reasoning → no history. Outcome → inventory + gaps doc. Lesson → steal the spawn prompt.

## F. Decision Rules
- IF agents share a file → expect overwrite.
- IF N>5 → tax (his).
- IF no peer dependency → use a sub, not a team.
- IF the dashboard is “main only” → you can’t steer a teammate (tmux is his fix; we still don’t).

## G. Contrarian
Rejects “just spin more agents.” Field: bigger crew. He: 3–5 + territory.

## H. Assumptions
Theirs: experimental is safe to flip. Ours: no Claude. Falsifier: a 2-agent handoff that needed a team. Survivorship: one landing page.

## I. Questions
Same ladder as `jZgcWCzxh1I`? When-not-to-use later in the tape (partial read of rest)?

## J. Connections
- SYSTEM SYNTHESIS → `jZgcWCzxh1I` · `Tj3018n5MVg` (sub vs team $) · `c0kaKxM2pHg`.

## K. Future-Use
Empty-wake goal. Territory. Unassigned tmux panes.

## Steal / Operate-never

### Machine: goal-in-the-spawn → 3–5 with file territories → QA can bounce → shutdown
- **Epistemic:** SOURCE
- **Workflow / loop:** write goal + artifacts → 3–5 named roles + who messages whom → own files → if QA fails, bounce → shut down
- **Questions / signals:** Peer talk needed? Shared file? N>5? Empty wake?
- **Qualify / frame / objections:** “10+ Neuroflow” is the fail. Three-critical bounce is the pass.
- **Procedure:** No env flag. No Claude team. No 10+.
- **Example that proves it:** Situation → landing page. Action → QA fails then passes. Reasoning → talk. Outcome → a page. Lesson → steal the bounce.
- **Why it works:** A named recipient and a file owner are checkable; a swarm is a cap.
- **Conditions / exceptions:** Experimental on tape.
- **Operate-never payload:** Claude teams; 10+; publish the page.
- **Hive run (existing skills only):** `c0kaKxM2pHg` · `golden-test-loop`.
- **Source:** `vDVSGVpB2vc` @ UNKNOWN

### Operate-never
- Enable Claude agent teams.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as empty-wake-plus-territory (no teams). Clients parked.
