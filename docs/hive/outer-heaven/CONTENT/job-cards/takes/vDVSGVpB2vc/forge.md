# Forge — vDVSGVpB2vc
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/vDVSGVpB2vc/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/vDVSGVpB2vc/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **Claude Code agent teams**. Beats: “create Neuroflow, 3 Sonnet teammates” (FE/BE/QA) → `team create` → shared task list, they **talk**; FE+BE → QA finds **3 critical**; main sends work back; second QA pass; one-shot fictional AI landing (copy/color/motion; “not perfect”) → **sub-agents** = independent, report to main; **teams** = lead + shared list + peer messages + reject loops → experimental; enable via **settings.json env** (docs JSON → Claude writes `.claude/settings.local.json`) → tip: paste official docs URL → local **master reference** md → prompt pattern: goal (they wake with **no history**) + “team of N on model X” + role/produce/talk-to + **named recipients** + final deliverables (running app, pass/fail, decisions doc) → dos: **own files**, define output, name who to message, **3–5** not 10+, full context → don’ts: shared files overwrite, vague, assume routing, swarm cost → live “research team” (researcher/strategist/critic): spawn prompts shown; main nags “did you message both?”; shutdown: “save your work” → VS Code extension hides thinking; **tmux** split panes, talk to one teammate → three rules: territory, DM, **parallel** (pure 1→2→3 may be sub-agents) → inherit **permissions** (bypass = all bypass); share files/MCP/skills → **plan-approval** (main or a reviewer teammate) → pitfalls: pre-approve tools; file owners; idle = missing work/dependency; too many tokens → fewer agents; persist to temp files; you approve until you get it → use if multi-area + parallel + react + high quality; skip if sequential / one window / same files / simple → 3 sessions ≈ 3× cost; shut down clean, don’t force-kill. Timestamp UNKNOWN. Claude on-tape. Caption-only: tmux panes unobserved.

## B. Atomic Knowledge

### Talking specialists with file owners — only when the work is parallel
- **Claim:** Teams earn the tax when peers must reject each other (QA loop). Sequential 1-2-3 is cheaper as sub-agents. Each agent owns files. They wake empty — the spawn prompt is the brief.
- **Reasoning:** Three critical bugs came back. Shared files overwrite. 10+ is 10×.
- **Mechanism:** Enable flag → named roles + who-messages-whom + deliverables → plan approve → parallel → clean shutdown.
- **Evidence:** Neuroflow QA bounce; research-team inventory nag.
- **Conditions:** Claude experimental teams.
- **Exceptions:** He says they’re slower/costlier. Bypass inherits — a gun.
- **Action:** Steal the ladder (talk+reject vs sequential) and file-owners. Do not enable Claude teams / tmux / bypass.
- **Confidence:** high on the shape; “most powerful feature” is pitch.
- **Source:** `vDVSGVpB2vc` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Orchestrator ≠ teammate. Idle teammate = bad brief. Shutdown is a save, not a kill. Docs-in-repo beats hoping it remembers the URL. Hive already has councils — don’t rename them Claude.

## D. Procedures
1. Don’t enable Claude teams. 2. If we ever spawn: 2–5, file owners, named handoffs, checkable deliverable. 3. Don’t inherit bypass. 4. Sequential → don’t fan out. 5. Clean stop, don’t kill mid-write.

## E. Examples
**Situation:** FE/BE/QA landing.  
**Action:** QA fails 3; bounce; pass.  
**Reasoning:** Peers talk.  
**Outcome:** One-shot site (claimed).  
**Lesson:** Reject loop is the product.

**Situation:** Sequential pipeline.  
**Action:** He’d use sub-agents.  
**Reasoning:** No need to talk.  
**Outcome:** Lower tax.  
**Lesson:** Don’t buy the team for a line.

## F. Decision Rules
- If they must reject each other → team-shaped (hive council), not a blob.
- If 1-2-3 only → one desk.
- If 10+ / bypass / Claude enable → park.
- If “one-shot polished startup” is the close → ignore.

## G. Contrarian
Field treats teams as default. He says most jobs are overkill. Field lets everyone edit everything; he assigns territory.

## H. Assumptions
Experimental flag as taped. Falsifier: they overwrite anyway. We do not run Claude. Hive spawn desks already exist.

## I. Questions
Do hive councils already enforce file owners?

## J. Connections
SYSTEM SYNTHESIS: `jZgcWCzxh1I` width vs depth. Hive: skills + spawn + council. No Claude / tmux-as-required. `golden-test-loop` = QA teammate without the vendor.

## K. Future-Use
File-owners. Named handoffs. Don’t climb for a line.

## Steal / Operate-never

### Machine: parallel + reject-loop + file owners; sequential stays one desk
- **Epistemic:** SOURCE
- **Workflow / loop:** is it multi-area and must they talk? → 2–5 named roles → own files → checkable done → clean shutdown
- **Questions / signals:** Overwrite risk? Idle teammate? 3× cost for a line?
- **Qualify / frame / objections:** Experimental. Bypass inherits. Demo is a toy landing.
- **Procedure:** No Claude teams. No bypass. No 10-swarm.
- **Example that proves it:** QA bounce; research-team nag to message both.
- **Why it works:** Empty-context workers only do what the spawn says. Territory stops clobber.
- **Conditions / exceptions:** Claude-specific wiring.
- **Operate-never payload:** Enable teams; quote one-shot site as FACT.
- **Hive run:** existing council, not a new vendor. Deploy HITL.
- **Source:** `vDVSGVpB2vc` @ UNKNOWN

### Operate-never
- Enable Claude agent teams / bypass-all.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not turn on Claude teams. File owners if we spawn. Deploy HITL.
