# Librarian — pxzo2lXhWJE
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/pxzo2lXhWJE/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/pxzo2lXhWJE/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** Watch Me Build a Multi-Agent Newsletter System in n8n
**Channel:** Nate Herk | AI Automation
**Kind:** video (~6837 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Weekly (or daily) newsletter: research → plan topics → deep-research each → write sections → aggregate → editor HTML → **Gmail**. Three specialized agents, not one mega. Wireframe first (Excalidraw): green = AI. Stack: n8n, Tavily ×2, Gmail, OpenRouter (GPT-5 family "new and fun"). Draft to you; you tweak and shoot — or "send to the list" (operate-never).
2. Schedule weeks / Sunday midnight — **leave inactive** while building. Tavily community node (≥1.101 + verified community). Query locked: "AI adoption for small businesses." Options: topic=news, past week, max 3. Rename + **pin**. Planner agent (GPT-5 mini): dynamic format of the results array (Skool function) > hard-drag three articles. Specialist: title + 3–5 word topics; one blob is useless → **structured output parser** (JSON schema). Pin. **Split Out** topics → Tavily each (general, past month, **include raw content** this time).
3. Sponsor cut: **Hostinger** self-host n8n, KVM2, code **Nate Herk 10%** — operate-never as hive host. Pin research. Section writer (same mini model): one standalone section, heading, **cite sources**, no intro/outro. Pin. **Aggregate** outputs 3→1. Editor on **GPT-5** (not mini) for HTML voice. User msg: `join` with newlines; must **name** the planner node or the title ref breaks (he fixes live). Long editor prompt + structured `{subject, content}` for Gmail. Run **~4 min** (he expected 30–90s). Gmail **create draft** (then he **sends** to test inbox). HTML type or you see raw tags. Result: "Gap to Growth…" dated **Aug 17, 2025**; per-sentence "source"; clickable list. Tune labels in the prompt (Forbes vs "source").
4. Unpin, save, **activate** = Sunday midnight forever. Next: log + feedback sheet, error handling, HITL revision, maybe Perplexity+Tavily+SER. Skool JSON/PDF. Plus: two courses (counts wobble).
Gap: full prompts, Hostinger panel. Timestamp UNKNOWN. n8n/Tavily/Hostinger/Gmail on-tape. SMB newsletter ICP parked.

## B. Atomic Knowledge

### Wireframe; specialize; pin; draft not list-send
- **Claim:** One agent "just write a newsletter" is too much. Planner / writer / editor, each with a format contract. Pin so you don't re-spend. Name nodes or refs die. Raw content only when you will write. Schedule stays off until you trust a draft. A log+feedback sheet is how prompts improve. Hostinger is a sponsor, not a hive OS.
- **Reasoning:** Complements `vFepZE_wrfg` (Code WAT newsletter). This is the n8n-node twin. Send-to-list is the never.
- **Mechanism:** wireframe → inactive schedule → pin each stage → structured fields → aggregate → editor subject/body → Gmail **draft** → human send.
- **Evidence:** blob vs parsed topics; unnamed planner break; 4-min editor; Aug 17 date.
- **Conditions:** Tavily 1,000 free, 4 min, Plus counts UNVERIFIED.
- **Exceptions:** Hive does not host n8n on Hostinger or mail a list.
- **Action:** File specialize+parser, pin, name-the-node, draft-not-send, log-for-prompt-tune. Do not activate Sunday blast.
- **Confidence:** high as an n8n-newsletter anatomy
- **Source:** `pxzo2lXhWJE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (transcript-implied)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** title ref until rename; 4-min editor surprise
- **Speech ≠ behavior:** "create a draft" vs he sends the test; "cut all the time" vs live 4-min editor + pins

## C. Mental Models
Wireframe before nodes. One job per agent. Pin is a fixture. Draft vs send. Sponsor host. Skool room.

## D. Procedures
1. Draw the green-AI boxes first.
2. Keep the schedule off.
3. Pin after every paid/slow step.
4. Parser when the next node needs fields.
5. Name nodes before you `@` them.
6. Gmail draft; you send.
7. Log outputs to retune prompts.
Avoid: Hostinger as hive; Sunday activate; list send; n8n-cloud.

## E. Examples
**Blob topics:** Situation — planner returns one string. Action — JSON schema parser. Outcome — split-out works. Lesson — format is a contract.

**Unnamed planner:** Situation — editor title red. Action — rename. Outcome — ref greens. Lesson — names are APIs.

## F. Decision Rules
- IF the next step needs fields → parser now.
- IF the schedule is on and you have not read a draft → off.
- IF Gmail is "send" not "draft" → HITL.
- Refuse: Hostinger/n8n as hive; SMB hunt.

## G. Contrarian
Against one mega newsletter agent. Against raw-off when you will write (first Tavily was summary-only on purpose).

## H. Assumptions
Caption-only. Complements `vFepZE_wrfg`. Keep Plus-count wobble.

## I. Questions
Did Sunday midnight ever fire to a list? What did the feedback sheet change?

## J. Connections
SYSTEM SYNTHESIS → `vFepZE_wrfg`; `ask-principal`; n8nbuilder pin habit.

## K. Future-Use
Specialize+parser + pin + draft-not-send + name-the-node as atoms.

## Steal / Operate-never

### Machine: three specialists; pin; draft; log the miss
- **Epistemic:** SOURCE
- **Workflow / loop:** wireframe → inactive cron → pin → parse → write → edit → draft → human send → sheet feedback
- **Questions / signals:** Schedule on? Draft or send? Is the node named?
- **Qualify / frame / objections:** Newsletter mill is not Path A. The steal is the split and the draft.
- **Procedure:** D above.
- **Example that proves it:** parser; rename; 4-min editor.
- **Why it works:** One job and a fixture beat a Sunday surprise.
- **Conditions / exceptions:** Times UNVERIFIED. Hive does not mail SMB news.
- **Operate-never payload:** Activate-and-forget. Hostinger as hive. List send. Nate Herk coupon as a buy.
- **Hive run:** File draft-not-send. Do not stand the cron.
- **Source:** `pxzo2lXhWJE` @ UNKNOWN

### Operate-never
- Hostinger / n8n-cloud as hive. Sunday list send. Quote 1,000 Tavily as capacity. Merge LESSONS. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
Upgrade old take: add name-the-node + 4-min surprise. Hard steps HITL.
