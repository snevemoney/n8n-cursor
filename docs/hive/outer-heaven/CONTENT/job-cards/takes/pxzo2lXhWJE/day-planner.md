# Day Planner — pxzo2lXhWJE
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/pxzo2lXhWJE/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/pxzo2lXhWJE/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: live-build **newsletter** — research → plan topics → deeper research → write sections → **editor** HTML → Gmail. Whiteboard first (Excalidraw/Miro); green = AI. Stack: n8n, Tavily ×2, Gmail, Open Router (GPT-5 on tape). Schedule: **Sunday midnight** once activated. Name the node or references break (“planning agent”). Structured output: `subject` + `content`; Gmail type **HTML** or you get raw `-p-` tags. Pin after a **4-minute** editor run. He says **create a draft**, then executes a send to a test inbox and later “push this into production.” Next: log issues, feedback sheet, error handling, HITL revision; optional extra research APIs. Close: Skool JSON + Plus wireframe course. Caption-only. Timestamp UNKNOWN. Clients parked.

## B. Atomic Knowledge
### Whiteboard → three AI slices → human draft; activate is a send
- **Claim:** Linear three-agent is enough. The stop is a Gmail **draft** Evens tweaks. Sunday-midnight + Active is a cron send if you skip the box. Naming nodes is how references stay green.
- **Reasoning:** Speech≠behavior: “draft” vs test send + “make this active.” Pin long steps. Sources-after-sentence is a verify hook, not a publish.
- **Mechanism:** Wireframe → build one slice → pin → draft folder → never activate.
- **Evidence:** “send it off to you for human approval.” / “I would push this into production by making this active.”
- **Conditions:** Draft-only. No list.
- **Exceptions:** Tavily / Open Router / Skool / Sunday cron = magnet.
- **Action:** Steal whiteboard-then-linear + name-the-node + draft-not-active. Do not activate. Do not Tavily.
- **Confidence:** high as the draft/active split.
- **Source:** `pxzo2lXhWJE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** unnamed planning agent broke the title ref; 4-min wait → pin
- **Speech ≠ behavior:** “create a draft” vs “notify us that it was in fact sent” + activate weekly

## C. Mental Models
Editor is a formatter, not a publisher. Priority: never Active. Uncertainty: 4 minutes / GPT-5 quality.

## D. Procedures
1. Whiteboard weekly trigger + three green boxes + human box.
2. Name every agent before you reference it.
3. Structured subject/body; HTML type.
4. Pin expensive steps.
5. Stop in drafts. Do not Active.
Avoid: Tavily; Open Router; Sunday cron; Skool; unpark.

## E. Examples
**Unnamed node:** Situation → title ref red. Action → rename “planning agent.” Reasoning → n8n can’t resolve. Outcome → green. Lesson → steal the name.

**4-minute pin:** Situation → editor slow. Action → pin. Reasoning → don’t re-pay the wait. Outcome → Gmail config. Lesson → steal pin-then-tweak.

## F. Decision Rules
- IF the next click is Active → stop.
- IF Gmail type is text → HTML or the tags leak.
- IF a ref is red → rename before debugging the prompt.
- IF he says draft and then send → believe the send.

## G. Contrarian
Rejects “automate the newsletter.” Field: Sunday send. He: human approval — then he almost activates it.

## H. Assumptions
Theirs: a list exists. Ours: no list send. Falsifier: Active + midnight. Survivorship: one August 17 2025 draft.

## I. Questions
Same pin habit as `a5sJNwfZ528`? Same human-review as `vFepZE_wrfg`?

## J. Connections
- SYSTEM SYNTHESIS → `vFepZE_wrfg` (W+T not A) · `send-removed` · `wiki-ingest`.

## K. Future-Use
Name-the-node. Draft-not-active. Unassigned: Sunday cron.

## Steal / Operate-never

### Machine: whiteboard → name nodes → three slices → pin → Gmail draft → never Active
- **Epistemic:** SOURCE
- **Workflow / loop:** diagram → build → pin long steps → land in drafts → Evens tweaks
- **Questions / signals:** Is it Active? Is the type HTML? Is the planning node named?
- **Qualify / frame / objections:** Midnight activate is the fail. Draft folder is the pass.
- **Procedure:** No Tavily. No Open Router. No list send. No Skool.
- **Example that proves it:** Situation → “create a draft.” Action → execute send to test Gmail, then Active. Reasoning → words ≠ node. Outcome → operate-never activate. Lesson → steal the folder, not the cron.
- **Why it works:** A draft folder is checkable; “cut down all the time” is not.
- **Conditions / exceptions:** Clients parked.
- **Operate-never payload:** Tavily; Open Router; Sunday-midnight Active; Skool; unpark.
- **Hive run (existing skills only):** `send-removed` · `coverage-loop`.
- **Source:** `pxzo2lXhWJE` @ UNKNOWN

### Operate-never
- Tavily / Open Router / Sunday cron / skip approval / Skool / unpark.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as draft-not-active. Clients parked.
