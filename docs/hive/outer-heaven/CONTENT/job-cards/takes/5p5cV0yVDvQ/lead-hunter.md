# Lead Hunter — 5p5cV0yVDvQ
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/5p5cV0yVDvQ/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/5p5cV0yVDvQ/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** Unlock the Full Power of Your n8n Agents (instance MCP)
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Long of `9IzGe0BBj_c`+`mPflFTQUCGk`: instance MCP = search/get/execute any workflow (need ≥1.21.2). Claude OAuth; tools execute/get/search. Chat-to-send Michael@dundermifflin; ClickUp complete (get id then update; when-to-use-each-tool in prompt). Manual “available in MCP” toggle + active/published — he calls that good so keys/data aren’t all exposed. Description field required so the client knows fields. Triggers that work: webhook/schedule/chat/form. ChatGPT native connectors can’t send mail — developer mode orange; ChatGPT OAuth flaky → old MCP-server-trigger + no-auth workaround (worse). Lovable: oneshot form for “AI opportunity map” + ROI-to-inbox — webhook body inferred. Careful which workflows you expose.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### Manual enable + description are the only brakes; chat-to-send is the never
- **Claim:** Instance MCP can execute anything that’s toggled on; the useful brake is per-workflow enable + a description; the demo still sends mail and completes tasks from chat.
- **Reasoning:** He knows all-on would leak keys. ChatGPT workaround drops auth.
- **Mechanism:** Inventory send/pay/delete → do not enable those → if anything is on, description required → never always-allow send.
- **Evidence:** SOURCE: “really good thing that you have to manually enable these because otherwise… sensitive API keys.” “use nodn to send that email.” Lovable ROI-to-inbox. No-auth workaround.
- **Conditions:** An instance can send.
- **Exceptions:** Lovable/Claude/ChatGPT on-tape. Opportunity-map + ROI email is a magnet shape, not a hunt. No-auth is a Watchdog kill.
- **Action:** Steal manual-enable + description-as-schema. Do not enable instance MCP. Do not send. Do not no-auth.
- **Confidence:** high
- **Source:** `5p5cV0yVDvQ` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Get-then-update because you need the task id.
- SOURCE: Native ChatGPT connectors find mail, don’t send — he used n8n to bypass that.

## D. Procedures
- If a workflow can send, it stays off MCP. Write descriptions only for safe read workflows. Never no-auth. Never Lovable-oneshot a send.

## E. Examples
- Situation: Michael email + PTO + Lovable form. Action: search, execute, oneshot page. Reasoning: convenience. Outcome: mail sent, task done, ROI inbox promised. Lesson: steal the toggle warning; operate-never the rest.

## F. Decision Rules
- If available-in-MCP is on a send workflow, turn it off.
- If auth is “none,” refuse.
- If the landing promises ROI-to-inbox, that’s a send.

## G. Contrarian
- Tape celebrates execute-any. Hive keeps the manual-enable sentence and refuses the enablement.

## H. Assumptions
- Version pin 1.21.2. Dunder Mifflin demo. Disagreement stored.

## I. Questions
- Did Lovable actually send the roadmap? Excerpt implies a form submit.

## J. Connections
- SYSTEM SYNTHESIS: `9IzGe0BBj_c` / `mPflFTQUCGk`.
- SYSTEM SYNTHESIS: `bX_stJ_Eiog` form→diagnosis email.

## K. Future-Use
- Unassigned: description-as-tool-schema as a general MCP hygiene — still don’t enable.

## Steal / Operate-never

### Machine: No instance MCP; if anything were exposed, manual enable + no send
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** See execute-any demo → refuse toggle → checkable stop = no OAuth to Claude/Lovable and no send workflow visible.
- **Questions / signals:** Which workflows are available in MCP? Can they send? Is auth none?
- **Qualify / frame / objections:** Frame: trust boundary. Objection: “oneshot landing” → ROI-to-inbox is a send.
- **Procedure:** Do not enable. Do not no-auth workaround. Do not hunt opportunity-map.
- **Example that proves it:** Situation: Claude send + Lovable form. Action: execute + oneshot. Reasoning: convenience. Outcome: side effects. Lesson: steal the brake sentence; never the toggle.
- **Why it works:** From B: he said all-on would leak keys, then sent mail anyway.
- **Conditions / exceptions:** Works as a never-rule. Exception: none this walk.
- **Operate-never payload:** Enable MCP. No-auth. Chat-to-send. Lovable hunt.
- **Hive run (existing skills only):** `send-removed`. Watchdog. `ask-principal`.
- **Source:** `5p5cV0yVDvQ` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Enable instance MCP. No-auth connector. Chat-to-send. Hunt Lovable/opportunity-map. Quote ROI as FACT.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal manual-enable warning. Do not enable. No hunt.
- Hard step stays HITL: this desk drafts; Evens sends.
