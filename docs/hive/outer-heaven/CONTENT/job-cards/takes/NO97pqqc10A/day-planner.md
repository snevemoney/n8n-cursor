# Day Planner — NO97pqqc10A
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NO97pqqc10A/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/NO97pqqc10A/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Short Agentic Arena: trivia bot + robot smackdown. Beats: Nate uses a copilot power-up so ChatGPT generates a system prompt; “we using five… pretty smart… sometimes it has some issues”; applause for the robot that will get hit if players are not perfect; they may have designed one very hard question. Visual-only: robot, scoreboard. Timestamp UNKNOWN. Gap: the actual trivia, the five (model? lives?), who “chat chief t” is beyond the ASR.

## B. Atomic Knowledge
### Copilot writes the system prompt
- **Claim:** A copilot power-up can generate the agent’s system prompt live.
- **Reasoning:** Faster than hand-writing the prompt in-arena.
- **Mechanism:** Power-up → ChatGPT → system prompt → agent.
- **Evidence:** “using his co-pilot power up to get chat chief t to generate a system prompt.”
- **Conditions:** Live arena / time pressure.
- **Exceptions:** Prompt quality “sometimes has some issues.”
- **Action:** Treat generated prompts as drafts; do not ship them as the morning SOP.
- **Confidence:** high that he did it; low that it is reliable.
- **Source:** `NO97pqqc10A` @ UNKNOWN
- **Epistemic:** SOURCE

### Hard question as designed fail
- **Claim:** They may have designed one question that is very hard to get right.
- **Reasoning:** Entertainment + a visible miss.
- **Mechanism:** Imperfect score → robot gets hit.
- **Evidence:** “we may have even designed one question that is going to be very hard.”
- **Conditions:** Game show, not a client QA.
- **Exceptions:** A real eval set should not hide a gotcha as the only stop.
- **Action:** If you design a hard case, name it on the card as a known fail, not a surprise.
- **Confidence:** medium (they say “may”).
- **Source:** `NO97pqqc10A` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Live spectacle over weekday physics. A generated prompt is “pretty smart” but he already flags issues. The robot is a consequence display: imperfect score has a body. Priority: play, then comment. Uncertainty: whether the hard question is fair eval or theater.

## D. Procedures
1. If a prompt must appear under time pressure, generate a draft, then skim for issues.
2. Name the designed-hard case before the run.
3. Do not treat “pretty smart” as a pass.
Avoid: scheduling a robot / trivia block; installing the on-tape copilot.

## E. Examples
**Generated system prompt:** Situation → arena needs an agent prompt now. Action → copilot → ChatGPT writes it. Reasoning → speed. Outcome → “pretty smart” + “sometimes issues.” Lesson → generated prompt is a draft; issues are expected.

**Designed hard question:** Situation → players must be perfect or the robot is hit. Action → include one very hard item. Reasoning → spectacle + stress test. Outcome → unnamed. Lesson → a gotcha is not a morning Top 3.

## F. Decision Rules
- If the prompt was generated → review before it becomes the SOP.
- If a case is designed to fail → label it; do not call the suite “green.”
- If the payload is a game show → CUT.

## G. Contrarian
Rejects “hand-write every system prompt in the moment.” Also rejects “perfect score or we do not run.” Field assumption: evals are fair. He plants a hard item on purpose.

## H. Assumptions
Theirs: copilot + ChatGPT is available and fast enough. Ours: ASR “chat chief t” = ChatGPT (INFERENCE). Survivorship: we do not see the bad prompts. Falsifier: generated prompt silently wrong in a money path. Disagreement: robot-hit as feedback is operate-never.

## I. Questions
What is “five”? Which question was the gotcha? Same event as `NWbh5ZoEHkA` / `Q8aqkHi5qY4`? Does a designed-hard item belong in `golden-test-loop`?

## J. Connections
- SYSTEM SYNTHESIS → `NWbh5ZoEHkA` (arena score) · `Q8aqkHi5qY4` (winner / donate).
- SYSTEM SYNTHESIS → `session-bootstrap` (dump the prompt once) · `golden-test-loop` (named hard case).

## K. Future-Use
“Designed hard question” as a labeled fail in a dry-run suite. Unassigned: whether hive ever wants a live prompt-gen block. Keep the tape even though the desk will not operate a game show.

## Steal / Operate-never

### Machine: draft-prompt then named-hard-case
- **Epistemic:** SOURCE
- **Workflow / loop:** need a prompt under time → generate draft → skim issues → run → include one named hard case → stop on the named miss
- **Questions / signals:** Did we review the generated prompt? Which item is designed to fail?
- **Qualify / frame / objections:** “Pretty smart” is not a pass. Robot-hit is theater.
- **Procedure:** Draft in the tool. Name the hard case on the card. Do not schedule the arena.
- **Example that proves it:** Situation → live prompt needed. Action → copilot writes it. Reasoning → speed. Outcome → smart + issues. Lesson → draft, then name the gotcha.
- **Why it works:** Generation is fast; a labeled hard case makes the miss visible.
- **Conditions / exceptions:** No time pressure → write the prompt once in `session-bootstrap`. Money path → no unreviewed generated prompt.
- **Operate-never payload:** ChatGPT / copilot as stack; robot / trivia morning; auto-send from a generated prompt.
- **Hive run (existing skills only):** `session-bootstrap` + `golden-test-loop`.
- **Source:** `NO97pqqc10A` @ UNKNOWN

### Operate-never
- Install ChatGPT / copilot / switch stack.
- Arena / robot / trivia as a weekday block.
- Treat “pretty smart” as FACT quality.
- Send / pay / deploy / book / publish.
- New hunt / unpark Normand.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet / wire **ONE** named-hard-case into a dry-run list. Clients parked — I do not put Agentic Arena on the board.
