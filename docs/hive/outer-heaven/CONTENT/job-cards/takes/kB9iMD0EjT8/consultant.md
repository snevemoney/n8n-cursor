# Consultant — kB9iMD0EjT8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map

Port a Claude Code project to Codex in five minutes. Beats: he has been unstuck by handing a problem to Codex without a new repo. Claude looks for `CLAUDE.md` + `.claude`; Codex looks for `agents.md` + `.codex` + `.agents`. Shared knowledge (docs, refs, scripts) stays. Tool-specific names differ. His Herc 2 `CLAUDE.md` = executive assistant + wiki path; `agents.md` says the same. Skills = same markdown+YAML, different folders (`.claude` vs `.agents`). Sub-agents: Claude markdown vs Codex TOML; Codex sub-agents do **not** auto-invoke — you must call them. Global vs project layers exist on both. Fastest convert: natural language to Codex — read `CLAUDE.md`, write `agents.md`, place skills/agents, research both docs. Maintenance: when `CLAUDE.md` changes, update `agents.md` too. Session-handoff skill: summary, active files, decisions, next steps — paste into the other tool. Dual-terminal on one project; they co-built an HTML cheat sheet (Claude styled, Codex restored lost value) — **careful, they can overwrite the same file**. Tool-agnostic: if Claude is down all day, can you still work? Both have VS Code extensions; he is “terminal boy.” Skool for the HTML + handoff skill. Comparison video coming. No VTT. UNKNOWN. ~2202 words.

## B. Atomic Knowledge

### Shared knowledge vs tool-shaped config
- **Claim:** Docs/refs/scripts are the portable layer; each harness wants its own instruction filename and config folder.
- **Reasoning:** If the brains live only in `CLAUDE.md`, Codex cannot see them.
- **Mechanism:** Keep a shared wiki; generate the thin adapter (`agents.md`, folders) in natural language; re-sync when the source instruction changes.
- **Evidence:** “three layers… shared knowledge… skills… tool-specific config.”
- **Conditions:** Herc 2 OS. He already keeps both files.
- **Exceptions:** Two subscriptions. Skool CTA. Claude/Codex on-tape.
- **Action:** Steal the three layers. Our portable layer is the hive vault, not a Codex adapter. Do not install either harness.
- **Confidence:** high
- **Source:** `kB9iMD0EjT8` @ UNKNOWN — “they all are going to work out of basically the same shared knowledge”
- **Epistemic:** SOURCE
### Handoff note, then the other tool — and don’t dual-write one file
- **Claim:** Session-handoff (what we talked about, files, decisions, next) lets him paste into Codex when Claude is stuck; two agents on one file can overwrite.
- **Reasoning:** Unstuck ≠ merge blindly.
- **Mechanism:** Write a handoff → switch tool → do not let both edit the same path at once.
- **Evidence:** “if you're working on the same file with two agents, they might overwrite each other's stuff”
- **Conditions:** HTML cheat-sheet demo. Sometimes “10 seconds.”
- **Exceptions:** Codex-unstuck is anecdotal. Sub-agents won’t auto-fire on Codex.
- **Action:** Steal handoff + single-writer. Do not pay two vendor subs. Do not join Skool.
- **Confidence:** high
- **Source:** `kB9iMD0EjT8` @ UNKNOWN — “session handoff… paste it into Codex and keep going”
- **Epistemic:** SOURCE


## C. Mental Models

He wants people tool-agnostic after getting stuck. He treats the two harnesses as close enough that mastery transfers, with a few landmines (TOML, no auto sub-agents). He is selling a free Skool pack. He likes terminal over extensions. This is the how-to twin of `-nG-9vlSkho`’s portability sermon.

## D. Procedures

On-tape: NL convert; keep three layers; handoff; single-writer. Ours: hive files are the shared layer; Cursor+Grok only; a handoff note is still useful between desks; do not stand up `.codex`.

## E. Examples

**Situation:** Stuck in Claude, Codex in the other pane. **Action:** Dual config + handoff skill. **Outcome:** He says Codex sometimes fixes it in seconds. **Lesson:** Portable knowledge, thin adapters, one writer. Implicit rule: Codex sub-agents are manual.

## F. Decision Rules

If instructions live in one vendor file, you are locked. If two agents share a path, lock the file. If someone says “mastered Claude = mastered Codex,” still read the landmines.

## G. Contrarian

Field default: duplicate the repo. He converts in place. Field default: one ecosystem. He says what if it goes down all day.

## H. Assumptions

Claude/Codex/Skool on-tape. Two-sub spend. “5 mins” is the title, not a measured migrate. Pair `-nG-9vlSkho`.

## I. Questions

What are the five beginner trip-ups on the HTML he didn’t read aloud? How often does the handoff actually unstick?

## J. Connections

**SYSTEM SYNTHESIS:** Pair `-nG-9vlSkho`. Maps to `wiki-ingest` / `context-docs` (shared layer) + `session-bootstrap` + hive desks as the “other pane” without Codex.

## K. Future-Use

Unassigned: three-layer diagram as a vault rule; session-handoff fields (talked / files / decisions / next) as a desk-to-desk note.

## Steal / Operate-never

### Machine: Shared vault + thin adapter + handoff; one writer per file
- **Epistemic:** SOURCE
- **Workflow / loop:** Keep knowledge in portable docs → if a second tool appears, generate a thin adapter from the source file → handoff note (talked/files/decisions/next) → never two writers on one path → do not install his two harnesses
- **Questions / signals:** Where does the source of truth live? Who is writing this file? Are we locked to one filename?
- **Qualify / frame / objections:** Qualify: stuck vs curious. Frame: tool-agnostic. Objection: “just pay both subs” — not our stack.
- **Procedure:** Shared layer. Single writer. No Claude/Codex/Skool.
- **Example that proves it:** Herc 2 has CLAUDE.md and agents.md; handoff pasted to Codex; HTML built by both with overwrite risk.
- **Why it works:** Knowledge should survive a harness outage. Adapters are cheap. Dual-write is not.
- **Conditions / exceptions:** Vendor pair. Skool. Anecdotal unstuck.
- **Operate-never payload:** Install Codex/Claude. Join Skool. Pay two AI subs. Dual-write a client file.
- **Hive run (existing skills only):** `wiki-ingest` · `context-docs` · `session-bootstrap` · `ask-principal`
- **Source:** `kB9iMD0EjT8` @ UNKNOWN


### Operate-never
- Install Codex or Claude Code.
- Join Skool for the cheat sheet.
- Let two agents edit one file.
- Pay a second vendor “to be agnostic.”
- Unpark a client / new `icp_id` / new `business-lanes.json` row. Learning ≠ hunt.
- Quote tape $ / student counts / job-loss % / hours×rate as FACT.
- Send / pay / deploy / book / publish. Approve draft ≠ send.
- Install on-tape vendors (Claude, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool). Stack stays Cursor + Grok.
- Grok Bot / `sendPrompt`. Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md. Overwrite `takes/consultant.md` or another desk's take.

## L. Role-Specific Applications

**Constraint first:** The stated ask is “use Codex in my Claude project.” Felt problem is not a second harness. Do not dual-stack a parked client.

**Four-blank:** None.

**Skeptical-customer:** Side-by-side terminals are smash. Clients parked. Hive vault is our shared layer.
