# Forge — kB9iMD0EjT8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Stuck in Claude Code → hand to Codex in the **same project** (no duplicate). Claude wants `CLAUDE.md` + `.claude`; Codex wants `agents.md` + `.codex` + `.agents`. Shared layer = docs/refs/scripts; tool-specific = instruction filename + config folder. Herc 2: same EA/wiki text in both md files; `.claude` has memory/agents/rules/skills/settings; `.codex` has agents + config. Skills = same markdown+YAML, different folders (`.claude` vs `.agents`). Agent defs: Claude markdown vs Codex **TOML**; same job (ClickUp searcher). Three layers: shared knowledge / skills / tool-specific config. Convert prompt: “I built this in Claude; make `agents.md` from `CLAUDE.md`, `.codex` config, skills→`.agents`, agents→`.codex`, research both docs.” Maintenance: change one instruction file, update the twin. Codex subagents **don’t auto-invoke**. Dual-terminal HTML: Claude styled, Codex restored lost value — **overwrite risk** on the same file. **Session-handoff** skill: summary, active files, decisions, next steps → paste into the other CLI. Tool-agnostic: if Claude is down, can you still work? He prefers terminal over both extensions. Skool HTML/cheat-sheet CTA. Timestamp UNKNOWN. Claude/Codex/Hermes/Skool on-tape.

## B. Atomic Knowledge

### Three layers: shared / skills / tool-config
- **Claim:** Knowledge files are portable; skills are portable markdown in different folders; config/agent-format is the only glue.
- **Reasoning:** That’s how you don’t duplicate the OS.
- **Mechanism:** Twin instruction files + folder map.
- **Evidence:** Herc 2 walk + convert prompt.
- **Conditions:** You already live in one CLI.
- **Exceptions:** Codex subagents must be called explicitly.
- **Action:** Steal the three-layer map. Do not add Codex. Our shared layer is already the repo; we don’t twin into `.codex`.
- **Confidence:** high.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

### Handoff object, not two mice on one file
- **Claim:** Session-handoff (talked / files / decisions / next) lets the other tool continue; two agents on one file can clobber.
- **Reasoning:** Unstuck without a new project.
- **Mechanism:** Summarize → paste.
- **Evidence:** Close + HTML overwrite warning.
- **Conditions:** Both CLIs open on one repo.
- **Exceptions:** 10-second fix is a flex, UNVERIFIED.
- **Action:** Steal the handoff card. One writer per file. No second subscription.
- **Confidence:** high.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Tool-agnostic = portable knowledge, not two paid CLIs. If Claude vanishes (`-nG-9vlSkho`) the shared folder should still work. Terminal over extension.

## D. Procedures
On tape: write convert prompt → twin files → handoff when stuck → don’t dual-edit one path. Hive: no convert-to-Codex. Handoff stays inside Cursor.

## E. Examples
**Situation:** Stuck in Claude.  
**Action:** Same repo, Codex, handoff paste.  
**Reasoning:** Shared knowledge already there.  
**Outcome:** Sometimes unstuck in “10 seconds” (claimed).  
**Lesson:** Handoff card. Not a second seat.

## F. Decision Rules
- If knowledge isn’t in a normal file → not portable.
- If two agents share a file → serialize or they overwrite.
- If Codex subagent is expected to self-start → it won’t (his).
- If Skool has the HTML map → don’t join.

## G. Contrarian
Field rebuilds the project for the new CLI. He twins the instruction file.

## H. Assumptions
“Mastered Claude ⇒ mastered Codex” is sales. Falsifier: TOML agents that don’t match markdown behavior.

## I. Questions
Full compare tape (`RLjaUES9P8A`)? What’s in session-handoff exactly?

## J. Connections
SYSTEM SYNTHESIS: `-nG-9vlSkho` portability. `RLjaUES9P8A`. Stack Cursor+Grok — we already refused both CLIs. `session-bootstrap`.

## K. Future-Use
Handoff card (files/decisions/next) between Cursor sessions.

## Steal / Operate-never

### Machine: shared files + handoff card; no twin CLI
- **Epistemic:** SOURCE + SYSTEM SYNTHESIS
- **Workflow / loop:** keep knowledge in normal files → when switching context, write talked/files/decisions/next → one writer per path
- **Questions / signals:** Would this survive Claude vanishing? Are two mice on one file?
- **Qualify / frame / objections:** Tool-agnostic is not “pay for both.”
- **Procedure:** No `.codex` / `agents.md` twinning. No Skool.
- **Example that proves it:** Convert prompt + session-handoff; HTML overwrite warning.
- **Why it works:** Shared knowledge is the OS. Config folders are costumes.
- **Conditions / exceptions:** He already pays two subs.
- **Operate-never payload:** Codex install; two subscriptions; Skool cheat-sheet.
- **Hive run:** `session-bootstrap` + Cursor only.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN

### Operate-never
- Install Codex/Claude/Hermes. Join Skool.
- Quote 10-second unstick as FACT. New hunt.
- Send / pay / deploy / book / publish. Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not twin the repo into Codex. Knowledge stays in files we already have. One writer per file. Deploy HITL.
