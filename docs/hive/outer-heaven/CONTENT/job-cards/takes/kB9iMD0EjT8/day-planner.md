# Day Planner — kB9iMD0EjT8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: use the same project in Claude Code and Codex. Beats: he got unstuck by handing a Claude problem to Codex without a new project; Claude wants `CLAUDE.md` + `.claude`; Codex wants `agents.md` + `.codex` + `.agents`; shared knowledge (docs/refs/scripts) is the same; skills = same markdown+YAML, different folders; agents = md vs TOML; global vs project layers; NL convert prompt (research both docs, copy CLAUDE.md → agents.md, move skills/agents); maintain both when CLAUDE.md changes; Codex sub-agents do **not** auto-invoke; two agents on one file can overwrite; **session-handoff** skill (summary, active files, decisions, next steps) paste into the other tool; tool-agnostic / what if Claude is down; both extensions exist — he is “terminal boy”; Skool HTML cheat sheet. Timestamp UNKNOWN. Pair with `-nG-9vlSkho` / `RLjaUES9P8A`. Vendors on-tape.

## B. Atomic Knowledge
### Three layers: shared knowledge / skills / tool-specific config
- **Claim:** Any agent can read the shared folder; skills are portable markdown in different paths; config/agent format is tool-specific.
- **Reasoning:** Don’t duplicate the wiki; do duplicate the thin adapter.
- **Mechanism:** Shared tree + `.claude` vs `.codex`/`.agents`.
- **Evidence:** “shared knowledge… workflows or skills… tool-specific config.”
- **Conditions:** You already have a project OS.
- **Exceptions:** A tool that cannot read markdown skills — not on tape.
- **Action:** Steal the three layers. We already have repo docs; we do not add Codex adapters.
- **Confidence:** high as the map.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

### Handoff note, then the other brain — watch overwrite
- **Claim:** When stuck, write a session-handoff (talked / files / decisions / next) and paste; two writers on one file can clobber.
- **Reasoning:** Codex sometimes unsticks Claude in seconds (UNVERIFIED).
- **Mechanism:** Handoff skill → paste → continue.
- **Evidence:** “summarizes what have we talked about, what are the active files… next steps.”
- **Conditions:** A second tool exists.
- **Exceptions:** Same-file parallel is the danger he names.
- **Action:** Steal the handoff four-liner. Do not buy a second subscription. Do not dual-write one file.
- **Confidence:** high as the procedure; “10 seconds” UNVERIFIED.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Tool-agnostic or you freeze when one vendor dies. Shared knowledge is the moat. He wants you to pay for two subs “if you can afford it.” Priority: show the folders. Terminal over GUI. Uncertainty: comparison video still coming (`RLjaUES9P8A`).

## D. Procedures
1. Keep a shared knowledge tree any reader can open.
2. If stuck: write handoff (talked / files / decisions / next) — stay in Cursor/Grok.
3. One writer per file.
4. Do not maintain CLAUDE.md + agents.md for vendors we will not install.
Avoid: Claude/Codex/Hermes; Skool; dual-sub as a plan.

## E. Examples
**HTML built by both:** Situation → cheat-sheet page. Action → both research; Claude styled; Codex restored lost value. Reasoning → two strengths. Outcome → one file. Lesson → he also says they can overwrite — one writer.

**Unstuck handoff:** Situation → stuck in Claude. Action → session-handoff → Codex. Reasoning → different strengths. Outcome → sometimes 10s fix. Lesson → steal the note; not the second vendor.

## F. Decision Rules
- If two agents would edit one file → serialize.
- If CLAUDE.md changes and you kept an adapter → update both (only if Evens added an adapter).
- If the pitch is “pay for two” → CUT.

## G. Contrarian
Rejects lock-in to one harness. Field assumption: pick Claude or Codex. He wants both. We already picked Cursor + Grok — portability without the second bill.

## H. Assumptions
Theirs: skill markdown is identical across tools. Ours: we will not install either; Skool is never. Falsifier: handoff that drops decisions. Survivorship: a few unstuck stories.

## I. Questions
`RLjaUES9P8A` compare? Session-handoff text? Hermes/OpenClaw mention — operate-never.

## J. Connections
- SYSTEM SYNTHESIS → `-nG-9vlSkho` (hour-portable) · `RLjaUES9P8A` · `session-bootstrap` · `agent-job-card`.

## K. Future-Use
Four-line handoff. Three-layer repo. Unassigned adapters.

## Steal / Operate-never

### Machine: shared knowledge + handoff four-liner; one writer per file
- **Epistemic:** SOURCE
- **Workflow / loop:** keep a shared tree → if stuck, write talked/files/decisions/next → continue in the current stack → never two writers
- **Questions / signals:** Is this shared knowledge or tool config? Who owns the file?
- **Qualify / frame / objections:** “Buy Codex too” is the fail. Handoff note is the pass.
- **Procedure:** One handoff doc if stuck. No Claude/Codex. No Skool.
- **Example that proves it:** Situation → stuck in Claude. Action → handoff → Codex. Reasoning → tool-agnostic. Outcome → unstuck story. Lesson → steal the note; not the sub.
- **Why it works:** Shared docs survive a vendor; a handoff is a checkable state.
- **Conditions / exceptions:** We do not run two paid harnesses. Tape “10s” UNVERIFIED.
- **Operate-never payload:** Claude/Codex/Hermes as stack; two-sub morning; Skool; overwrite wars.
- **Hive run (existing skills only):** `session-bootstrap` · `agent-job-card`.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN

### Operate-never
- Install Claude / Codex / Hermes / switch stack.
- Dual-write one file / join Skool.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as handoff-four-liner (no second vendor). Clients parked.
