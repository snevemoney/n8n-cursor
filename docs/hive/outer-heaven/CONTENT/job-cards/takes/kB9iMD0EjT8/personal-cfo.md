# Personal CFO — kB9iMD0EjT8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/LEARNED.md`
**ICP:** parked unless Evens named one.
**Walk:** VIDEO-FIRST from `full.txt` (2202 words, video 9:05). json3 present. Tape $ UNVERIFIED. No spend. Clients parked. Stack Cursor + Grok.

## A. Source Map
Caption-only. Visual/click UNKNOWN.
1. Hook: stuck in Claude Code; handed the same project to Codex; it solved; no duplicate project. Split-screen claim. **SOURCE**
2. Problem: Claude wants CLAUDE.md + `.claude`; Codex wants AGENTS.md + `.codex` + `.agents`. Shared knowledge (docs, refs, scripts) is the same; names differ. **SOURCE**
3. Demo: Herc 2 OS — CLAUDE.md = exec assistant / wiki; AGENTS.md = same ideas. `.claude` = memory, agents, rules, skills, settings; `.codex` = agents + config. HTML cheat sheet will be in free Skool. **SOURCE**
4. Global vs project: `~/.claude` vs project `.claude`; Codex same split. Skills = markdown+YAML both sides; agents = Claude markdown vs Codex TOML (ClickUp searcher example). **SOURCE**
5. Three layers: shared knowledge (any agent) → skills/workflows (same files, different folders) → tool-specific config (`settings.local.json` vs `config`). **SOURCE**
6. Fast convert: natural language to Codex — invent AGENTS.md from CLAUDE.md, `.codex`, move skills to `.agents`, agents to `.codex`, research both docs. Maintenance: when CLAUDE.md changes, also patch AGENTS.md. Codex sub-agents do not auto-invoke. **SOURCE**
7. “If you mastered Claude Code you basically mastered Codex.” Comparison video coming. HTML built by both: Claude styled, Codex restored lost value — overwrite risk if two agents edit one file. **SOURCE**
8. Session-handoff skill (Skool): summarize talk / active files / decisions / next steps; paste into the other harness. Sometimes “fixes in 10 seconds.” **SOURCE**
9. Close: try both; might mean two subscriptions “if you can afford it”; tool-agnostic so if Claude is down you still work; he is “terminal boy” now (both have VS Code extensions). Like. **SOURCE**

## B. Atomic Knowledge

### Three layers: shared knowledge / skills / tool-specific config
- **Claim:** Any coding agent can read the same knowledge; only folder names and a few configs change.
- **Reasoning:** So you do not duplicate the project to try the other harness.
- **Mechanism:** Keep docs/refs/scripts canonical; mirror instruction files; isolate settings.
- **Evidence:** CLAUDE.md↔AGENTS.md; `.claude` skills vs `.agents`; markdown vs TOML agents.
- **Conditions:** A repo that already has an OS.
- **Exceptions:** Hive stack is Cursor + Grok — do not stand up Claude+Codex to practice this.
- **Action:** Steal the three-layer split for our repo. Do not buy a second harness.
- **Confidence:** high
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

### Session handoff beats re-explaining; two agents can overwrite
- **Claim:** A written handoff (talk, files, decisions, next) lets the other harness continue; two writers on one file can clobber.
- **Reasoning:** Context is the expensive part; the second model is a fresh window.
- **Mechanism:** Summarize → copy → paste into the other tool; do not dual-edit blindly.
- **Evidence:** He built the HTML with both; Codex restored value Claude lost; he warns overwrite.
- **Conditions:** When stuck, not as a default dual-sub.
- **Exceptions:** “10 seconds” and “if you can afford two subs” are magnets. UNVERIFIED.
- **Action:** Steal handoff-on-stuck. Do not pay for two seats. Do not auto-invoke the other vendor.
- **Confidence:** high as procedure
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Tool-agnostic > loyal. The OS is the files, not the CLI name. Two subscriptions are a luxury he names, not a requirement he proves. Uncertainty: “fixes in 10 seconds” UNVERIFIED.

## D. Procedures
1. Keep shared knowledge outside vendor folders.
2. If you must mirror: instruction file + skills folder + config folder per tool.
3. When the canonical instruction changes, patch the mirror.
4. Stuck → write a handoff → one other tool, not both editing the same file.
5. Avoid: buying Codex “to be agnostic”; Skool HTML; dual-write.

## E. Examples
**Situation:** Stuck in Claude Code on an existing OS. **Action:** Same repo, Codex side, session handoff. **Reasoning:** Shared knowledge already there. **Outcome:** He says it unstuck him; HTML co-built. **Lesson:** Portability is a folder discipline, not a second bill. Implicit: maintenance of two instruction files is the real cost.

## F. Decision Rules
- If stuck in one harness → handoff, do not duplicate the repo. **SOURCE**
- If two agents touch one file → serialize or you overwrite. **SOURCE**
- If CLAUDE.md changes → patch AGENTS.md. **SOURCE**
- Refuse: two paid seats; Skool join; Codex as hive.

## G. Contrarian
The field picks a religion. He wants both — and immediately sells a free community file. The useful reject is “duplicate the project.” **SOURCE**

## H. Assumptions
Assumes both CLIs already paid. Survivorship: one unstick story. Hive: we already picked Cursor + Grok; portability means our files, not his two vendors.

## I. Questions
- What did Codex actually fix?
- How often does he pay for both vs film both?

## J. Connections
- **SYSTEM SYNTHESIS:** `-nG-9vlSkho` (portable or die). `6cEQEba0i2A` (handoff vs compact). `session-bootstrap`.

## K. Future-Use
Three-layer repo as unassigned hygiene. No second seat.

## Steal / Operate-never

### Machine: shared-knowledge-then-handoff (advise-only)
- **Epistemic:** SOURCE
- **Workflow / loop:** one repo → canonical docs → thin vendor mirrors if ever needed → stuck → written handoff (files/decisions/next) → one other tool → checkable stop = no second subscription, no dual-edit
- **Questions / signals:** Is this knowledge or config? Will a second writer clobber? Can we afford two seats? (His question — our answer is do not buy.)
- **Qualify / frame / objections:** “Master Claude = master Codex” → on-tape; not a reason to install Codex. “If Claude is down” → we already have Cursor + Grok.
- **Procedure:** D.
- **Example that proves it:** Herc 2 OS; ClickUp searcher md vs TOML; HTML styled by Claude, value restored by Codex; overwrite warning.
- **Why it works:** The expensive object is context, not the binary name. Conditions: already-paid dual use. Exceptions: hive will not pay dual.
- **Operate-never payload:** Buy Codex + Claude; join Skool; quote 10-second fix as FACT; new hunt.
- **Hive run:** `session-bootstrap` · `subscription-audit` · `ask-principal`
- **Source:** `kB9iMD0EjT8` @ UNKNOWN

### Operate-never
- Move money, approve a charge, buy a seat, or cancel a lock-in.
- Quote tape $ / hours / student counts as FACT or household income.
- Install on-tape vendors. Cursor + Grok only.
- Auto-send / auto-pay / auto-book / auto-deploy / auto-publish / auto-dial.
- New hunt / unpark client / new `icp_id`. Merge `LESSONS-FROM-TAPE.md`. Auto-write `SKILL.md`.


## L. Role-Specific Applications
Steal folder discipline and written handoff. Do not buy a second coding subscription “if you can afford it.” Skool HTML is a magnet. Still no dated runway.

Employment covers baseline (OPERATOR_MEMORY). No dated runway-months number — I will not fake one. Career Strategist owns quit math; this desk owns the months.
