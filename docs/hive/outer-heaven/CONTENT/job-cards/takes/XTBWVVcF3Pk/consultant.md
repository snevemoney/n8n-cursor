# Consultant — XTBWVVcF3Pk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/XTBWVVcF3Pk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/XTBWVVcF3Pk/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map

“How I make Opus think like Fable.” Beats: model is not the moat — Karpathy on Sonnet 3.7 beats a beginner on Fable 5. Dynamic workflows: all-Fable vs Fable+Opus/Sonnet workers ≈ same result, Fable-everywhere costs exponentially more. Keep the **process**, not the model. Treat frontier as teacher/officer, not every-task workhorse: extract how it thinks, hand to cheaper models. Leaked Fable system prompt: memory ≠ current knowledge (verify); implied file ≠ file exists; answer ambiguous queries first then one clarifying question; effort bands (1 / 3–5 / 5–10). Effort level is a second axis next to model pick. Cost/quality chart on release blog (UNVERIFIED). No VTT. UNKNOWN. ~2472 words. Pair `R0qF17BVl9w`.

## B. Atomic Knowledge

### Keep the process; route the model; effort is a second knob
- **Claim:** A beginner with the expensive model loses to a person with loops. All-frontier workers often do not beat a teacher+cheaper-workers shape on his tests.
- **Reasoning:** Intelligence you cannot afford is not a process you can keep.
- **Mechanism:** Let the expensive model plan/review. Let cheaper models execute. Set effort to the task, not default-max.
- **Evidence:** “you can't keep the model's intelligence, but you can keep its process.”
- **Conditions:** A few thousand $ usage UNVERIFIED. His dynamic-workflow A/Bs.
- **Exceptions:** Same-looking outputs can still be wrong. Leaked prompt is not ours to ship.
- **Action:** Steal teacher/workers + effort knob. Do not install Fable. Do not max-effort everything.
- **Confidence:** high
- **Source:** `XTBWVVcF3Pk` @ UNKNOWN — “the model isn't really the moat”
- **Epistemic:** SOURCE
### Verify memory and existence; one question max after a first answer
- **Claim:** Partial training-recall is not current knowledge. A prompt that implies a file does not create the file.
- **Reasoning:** These are harness rules he lifted from a leak — useful as hygiene, not as a stolen system prompt.
- **Mechanism:** Check the file exists. Check the fact. Answer, then at most one clarify.
- **Evidence:** On-tape from the leaked prompt walkthrough.
- **Conditions:** Fable 5 docs / leak. Dated.
- **Exceptions:** Do not paste leaked prompts into hive agents.
- **Action:** Steal the hygiene. Do not ship the leak.
- **Confidence:** medium
- **Source:** `XTBWVVcF3Pk` @ UNKNOWN — “Partial recognition from training does not mean current knowledge”
- **Epistemic:** SOURCE


## C. Mental Models

He is teaching cost-aware orchestration after burning credits. He wants people to stop defaulting to the most expensive model. He still worships Fable as the teacher.

## D. Procedures

1. Write the process (plan → do → check). 2. Pick model by task. 3. Pick effort by task. 4. Verify files/facts. Avoid: all-Fable dynamic workflows. Avoid: leaked prompt as ours.

## E. Examples

**Situation:** Same dynamic workflow, Fable workers vs Opus/Sonnet workers. **Action:** Compare outputs and cost. **Outcome:** Similar results, huge cost delta. **Lesson:** Process > model. Implicit rule: teacher ≠ every employee.

## F. Decision Rules

If every worker is the most expensive model, you do not have a process. If effort is always default, turn the knob. If a file was only implied, check.

## G. Contrarian

Field default: buy the best model. He says Karpathy-on-worse-model still wins. Field default: max effort. He shows a cost/quality chart.

## H. Assumptions

$ thousands / chart scores UNVERIFIED. Claude family on-tape. Pair `R0qF17BVl9w` / `6cEQEba0i2A` / `4OOS96i2gfI` (cheapest layer).

## I. Questions

What was actually different in the cheaper-worker outputs?

## J. Connections

**SYSTEM SYNTHESIS:** Maps to `slice-build` + cheapest-pyramid (`4OOS96i2gfI`) + `golden-test-loop`. Model-routing is not a hive SKU.

## K. Future-Use

Unassigned: effort-as-second-knob; teacher-not-employee.

## Steal / Operate-never

### Machine: Process is the moat; expensive model teaches; cheaper models do; effort matches the task
- **Epistemic:** SOURCE
- **Workflow / loop:** Write plan/do/check → assign expensive model to plan/review only → cheaper models execute → set effort band → verify files/facts exist
- **Questions / signals:** Is the frontier model doing grunt work? Is effort maxed by habit? Did we check the file exists?
- **Qualify / frame / objections:** Qualify: they are burning tokens. Frame: keep the process. Objection: “I need Fable for everything” — his A/B said no.
- **Procedure:** Route. Effort knob. No Fable install. No leaked-prompt paste.
- **Example that proves it:** All-Fable dynamic workflow ≈ Fable+cheaper workers, much more cost; Karpathy-on-worse-model thought experiment.
- **Why it works:** You cannot keep a model. You can keep a loop. Default-max is how limits die.
- **Conditions / exceptions:** A/Bs are his. Leak hygiene is dated.
- **Operate-never payload:** Install Fable. Quote thousands as FACT. Paste leaked system prompt. Max-effort every task.
- **Hive run (existing skills only):** `slice-build` · `golden-test-loop` · `ask-principal`
- **Source:** `XTBWVVcF3Pk` @ UNKNOWN


### Operate-never
- Install Fable / paste the leaked system prompt.
- Quote a few thousand $ as FACT.
- Max-effort / all-frontier workers by default.
- Unpark a client / new `icp_id` / new `business-lanes.json` row. Learning ≠ hunt.
- Quote tape $ / student counts / job-loss % / hours×rate as FACT.
- Send / pay / deploy / book / publish. Approve draft ≠ send.
- Install on-tape vendors (Claude, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool). Stack stays Cursor + Grok.
- Grok Bot / `sendPrompt`. Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md. Overwrite `takes/consultant.md` or another desk's take.

## L. Role-Specific Applications

**Constraint first:** The stated ask is “make Opus think like Fable.” Felt problem is still the leak. Do not buy a model ladder for a parked Path A.

**Four-blank after constraint:** Toddler stop = plan/do/check named. Token $ stay out.

**Skeptical-customer:** Fable-everywhere is smash. Same-result cheaper workers is the honest demo. Clients parked.
