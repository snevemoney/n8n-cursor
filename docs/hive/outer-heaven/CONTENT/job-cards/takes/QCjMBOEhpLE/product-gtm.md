# Product GTM — QCjMBOEhpLE
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/QCjMBOEhpLE/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/QCjMBOEhpLE/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk. Title: “n8n's NEW Native Data Tables Just Made Building Agents So Much Easier.” Beats: (1) sales agent + native table: “Bluetooth speaker revenue?” → query + calculator = **$546** (UNVERIFIED); no internet/API — lives in an n8n **Data Tables** tab; (2) pre-GA recording; update n8n to see it; create table: created/updated defaults; column types string/number/boolean/date; manual rows or import; (3) Data Tables node: delete/get/insert/update/upsert — Sheets-shaped, **no credential** because it is local to the instance; IDs keep incrementing after delete; type mismatch blocks insert; (4) Gmail trigger → get-rows filter on from-email → notes into the reply agent (Nate Herklman / upitigmail.com demo); workflow + two example sets via his free drop. Timestamp UNKNOWN. **$546 UNVERIFIED.**

## B. Atomic Knowledge
### In-instance memory is a lock-in, not a moat we buy
- **Claim:** Native tables make “the agent knows our rows” a 10-minute demo. The data now lives *inside* n8n. That is convenience and lock-in.
- **Reasoning:** No credential looks like simplicity. It is also “your CRM is the vendor.”
- **Mechanism:** Lookup before generate (email → notes → draft). Same physics as a local DB — do not install n8n for it.
- **Evidence:** $546 walk-through; filter-on-from-email before the draft.
- **Conditions:** On-tape n8n. Hive does not add n8n-cloud.
- **Exceptions:** Pre-GA — UI may have moved.
- **Action:** Steal “lookup before draft.” Do not productize n8n tables.
- **Confidence:** high as demo.
- **Source:** `QCjMBOEhpLE` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Local table vs API. No-credential = lock-in. Lookup-then-generate. Types must match.

## D. Procedures
If a demo has no auth: ask where the rows live and how you leave. Draft only after a keyed lookup.

## E. Examples
**Situation:** Incoming mail. **Action:** Filter table on from-email, pass notes, then draft. **Lesson:** The agent is not magic; the row is.

## F. Decision Rules
- If the pitch is “no API” → name the lock-in.
- Refuse: n8n Data Tables SKU; $546 as FACT; free workflow magnet.

## G. Contrarian
Against “native = better product.” Native is their gravity.

## H. Assumptions
Theirs: n8n instance. Ours: Cursor + Grok. Falsifier: tables export cleanly and we still will not install n8n.

## I. Questions
Sibling `lcNN3X9gXls` table hacks. Rest of use-cases after the email demo.

## J. Connections
**SYSTEM SYNTHESIS:** Draft-queue = `ECfusvK5tEU`. Client’s card / where data lives = `Lg5TYWPSg6M`. Maps to `ask-principal`.

## K. Future-Use
Unassigned: “lookup before draft.” Keep.

## Steal / Operate-never

### Machine: keyed lookup before the draft — do not buy the table
- **Epistemic:** SOURCE
- **Workflow / loop:** event → match a row → pass notes → draft → HITL send
- **Questions / signals:** Where do rows live? Can we leave?
- **Qualify / frame / objections:** “No API” is the lock-in.
- **Procedure:** No n8n install.
- **Example that proves it:** From-email → notes → reply draft; $546 query.
- **Why it works:** Context is a row, not a model.
- **Conditions / exceptions:** $546 UNVERIFIED. Pre-GA.
- **Operate-never payload:** n8n tables; free workflow
- **Hive run (existing skills only):** `ask-principal`
- **Source:** `QCjMBOEhpLE` @ UNKNOWN

### Operate-never
- Productize n8n Data Tables
- Quote $546 as FACT
- New hunt; merge LESSONS; auto-write SKILL.md
- Auto-send the drafted email

## L. Role-Specific Applications
Do not anneal n8n tables into the sentence. Clients parked.
