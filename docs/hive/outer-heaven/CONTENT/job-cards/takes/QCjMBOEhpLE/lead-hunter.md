# Lead Hunter — QCjMBOEhpLE
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/QCjMBOEhpLE/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/QCjMBOEhpLE/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** n8n's NEW Native Data Tables Just Made Building Agents So Much Easier
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Sales agent + native data tables (no internet/API). Bluetooth speaker revenue $546 via product-name query + calculator (UNVERIFIED demo). Tab after update; created/updated cols; types; row ids persist after “reset” (starts at 21). Contacts sheet → table (no creds) → Gmail trigger lookup by email → notes into agent for a reply (linear, not tool-autonomy). Sales Qs: count on date (date as string because sheet hyphens), product qty, avg per day for ID — prompt lists valid names/date format/IDs. Speed: 400 rows local vs sheet. Free workflow+sheets CTA. Calculator echo of `NWbh5ZoEHkA`.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### In-instance table + calculator; lookup before reply; date format in the prompt
- **Claim:** A local table plus a calculator answers sales questions without an API; a Gmail flow looks up the contact row before drafting — and date filters only work if the prompt states the string format.
- **Reasoning:** Native = no credential, persists across executions.
- **Mechanism:** Land data in the table → constrain tools (name/date/id) → calc for math → lookup before draft → HITL send.
- **Evidence:** SOURCE: “our agent didn't have to go over the internet.” “the hyphens basically make it a string.” “feed the notes… into the AI agent that's going to make the email.” $546 UNVERIFIED. Reply is a send risk.
- **Conditions:** You have a small table and gold questions.
- **Exceptions:** Free template CTA. Auto-reply never. Delete-row tool exists in the node menu — loaded.
- **Action:** Steal lookup-before-draft + format-in-prompt + calc-for-math. Do not auto-send. Do not hunt data-table setups.
- **Confidence:** high
- **Source:** `QCjMBOEhpLE` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Linear lookup vs later tool-autonomy — he chose linear for email.
- SOURCE: Table remembers ids after a visual reset.

## D. Procedures
- State valid keys and date format in the prompt. Look up the row before drafting. Calculator for sums. Human sends.

## E. Examples
- Situation: Bluetooth revenue + inbound email. Action: query+calc; email-equals-trigger lookup. Reasoning: local table. Outcome: $546 claimed; personalized draft. Lesson: steal lookup+format; park send.

## F. Decision Rules
- If dates are hyphen-strings, don’t type a date object.
- If you draft without lookup, you missed the notes.
- If the next node sends, HITL.

## G. Contrarian
- Rejects Sheets-over-the-wire as required for this size.

## H. Assumptions
- $546 / 400-row speed UNVERIFIED. n8n-on-tape.

## I. Questions
- How fast was 400 rows vs sheet? Excerpt cuts the number.

## J. Connections
- SYSTEM SYNTHESIS: `ZwQ8rJhVCr4` filter/SQL vs vector.
- SYSTEM SYNTHESIS: `NWbh5ZoEHkA` calculator. `send-removed`.

## K. Future-Use
- Unassigned: valid-enum-in-prompt as a default on table agents.

## Steal / Operate-never

### Machine: Lookup row → draft; calc math; HITL send
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** Table in → constrained query + calc → lookup notes → checkable stop = draft only, no send.
- **Questions / signals:** What’s the date format? Did we look up the email? Who sends?
- **Qualify / frame / objections:** Frame: local table. Objection: “easier agents so we auto-reply” → never.
- **Procedure:** Do not send the help-with-my-business mail. Do not enter the free template as a hunt.
- **Example that proves it:** Situation: $546 + Gmail notes. Action: calc + lookup. Reasoning: no API. Outcome: draft path. Lesson: steal lookup+calc; park send.
- **Why it works:** From B: notes feed the agent before the email.
- **Conditions / exceptions:** Works on small local data. Exception: no gold question → don’t trust the sum.
- **Operate-never payload:** Auto-reply. Quote $546 as FACT. Hunt table-agent buyers.
- **Hive run (existing skills only):** `send-removed`. `golden-test-loop`.
- **Source:** `QCjMBOEhpLE` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Auto-send from the lookup. Quote $546 as FACT. Hunt data-table clients. Delete-rows from an agent without HITL.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal lookup-before-draft + format-in-prompt. No send. No hunt.
- Hard step stays HITL: this desk drafts; Evens sends.
