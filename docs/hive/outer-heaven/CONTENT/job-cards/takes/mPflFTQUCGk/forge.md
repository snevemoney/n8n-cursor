# Forge — mPflFTQUCGk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/mPflFTQUCGk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/mPflFTQUCGk/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Instance MCP how-to teaser (siblings `9IzGe0BBj_c` / `5p5cV0yVDvQ`). Beats: n8n settings → **MCP access** toggle on → Claude: add connectors → n8n native connector → connect / grant → first time: paste **server URL** → demo 1: instead of copy-paste an email, “use n8n to send to michael@dundermifflin.com” → searches workflows → get details → **always allow** → knows three body fields + webhook → sent → he opens the inbox → demo 2: busy day, ClickUp “email Michael about PTO” urgent/today → “use n8n to move that task to complete” → finds ClickUp task manager → always allow → ClickUp shows complete. Play-button. Timestamp UNKNOWN. Always-allow send = operate-never.

## B. Atomic Knowledge

### Search → details → execute
- **Claim:** Claude searches the instance, fetches workflow details (fields, webhook), then fires it — you don’t hand-build the POST.
- **Reasoning:** That’s the instance-MCP pitch.
- **Mechanism:** Toggle → connector → natural language → search → details → run.
- **Evidence:** Email + ClickUp demos.
- **Conditions:** Workflows already exist and are visible.
- **Exceptions:** First-time URL paste.
- **Action:** Steal the three-step inspect. Do not always-allow. Do not send.
- **Confidence:** high.
- **Source:** `mPflFTQUCGk` @ UNKNOWN
- **Epistemic:** SOURCE

### Always-allow is how send escapes
- **Claim:** He clicks always-allow on get-details and on the send/complete tools.
- **Reasoning:** Convenience in a demo.
- **Mechanism:** Permission prompt → always.
- **Evidence:** Both demos.
- **Conditions:** His Claude desktop.
- **Exceptions:** He does not show a deny.
- **Action:** This is the incident. Gated tools, per-run approve (`xsAOpqjebOo`).
- **Confidence:** high.
- **Source:** `mPflFTQUCGk` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Chat becomes the shell for every workflow. Context-switch (Claude → ClickUp) is the QoL. Always-allow is the cost.

## D. Procedures
On tape: enable MCP → connect Claude → speak the verb → always-allow → check the side system. Hive: do not enable. If a tool exists: search/details on a dry run, Evens executes send/complete.

## E. Examples
**Situation:** Email already written.  
**Action:** “Use n8n to send to Michael.” Always-allow. Inbox confirms.  
**Reasoning:** Don’t copy-paste.  
**Outcome:** Real send.  
**Lesson:** The QoL is the never.

**Situation:** ClickUp PTO task.  
**Action:** “Mark complete” via MCP.  
**Reasoning:** Stay in Claude.  
**Outcome:** Task complete.  
**Lesson:** Status writes need HITL too.

## F. Decision Rules
- If the verb is send → never always-allow.
- If the client can see every workflow → `9IzGe0BBj_c` blast radius.
- If the pitch is “I’m already in this chat” → not a reason to grant write.

## G. Contrarian
Field wants one chat to run the company. He demos it. We keep the older allow-list.

## H. Assumptions
Native Claude connector exists as shown. Falsifier: a search that fires the wrong workflow.

## I. Questions
Can MCP access be read-only? Long-tape deny list?

## J. Connections
SYSTEM SYNTHESIS: `9IzGe0BBj_c`, `5p5cV0yVDvQ`. `xsAOpqjebOo` approval. `send-removed`. error-heal is notify, not execute-any.

## K. Future-Use
Search→details→**stop** as a dry-run shape (no execute).

## Steal / Operate-never

### Machine: search and read details; execute is HITL
- **Epistemic:** SOURCE + SYSTEM SYNTHESIS
- **Workflow / loop:** find workflow → read schema → draft the call → human runs send/complete
- **Questions / signals:** Did anyone click always-allow? Can it send mail?
- **Qualify / frame / objections:** “Full power of your agents” is execute-any.
- **Procedure:** MCP access stays off on hive n8n.
- **Example that proves it:** Always-allow send to Dunder Mifflin; task auto-complete.
- **Why the steal works:** Inspect-without-execute is how you learn the schema.
- **Conditions / exceptions:** Demo convenience ≠ prod.
- **Operate-never payload:** Instance MCP on; always-allow; Claude connector.
- **Hive run:** `ask-principal` + gated tools only.
- **Source:** `mPflFTQUCGk` @ UNKNOWN

### Operate-never
- Enable instance MCP. Always-allow send/complete.
- Install Claude. New hunt.
- Send / pay / deploy / book / publish. Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not point Claude at n8n. A schema I can read is enough. Send/complete stay Evens. Deploy HITL.
