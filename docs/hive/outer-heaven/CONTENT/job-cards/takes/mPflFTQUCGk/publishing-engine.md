# Publishing Engine — mPflFTQUCGk
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/mPflFTQUCGk/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/mPflFTQUCGk/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** Unlock the Full Power of Your AI Agents with n8n Instance MCP
**Channel:** Nate Herk | AI Automation

## A. Source Map
1. Settings → MCP access → enable. In Claude: add connectors; n8n is a native connector; connect; first time paste server URL.
2. Pitch: instead of copy-paste an email, say 'send that off now' if an n8n send workflow exists.
3. Demo: use n8n to send that email to michael@dundermifflin.com. Claude searches workflows, asks for details, he always-allows; it finds three body fields + webhook; always-allow; confirmation sent; inbox shows it.
4. Busy day: ClickUp to-do 'email Michael about PTO' urgent today. Instead of context-switch, ask Claude to use n8n to move that task to complete. Finds ClickUp task manager; done; ClickUp shows complete.
5. CTA: full breakdown. Always-allow send = operate-never.
Timestamp UNKNOWN (no VTT unless noted). Tape $ / student counts / job-loss % = UNVERIFIED.

## B. Atomic Knowledge

### Search, then always-allow, then send
- **Claim:** Instance MCP finds a send workflow and a ClickUp workflow; he always-allows both.
- **Reasoning:** The teach is search-the-instance. The payload is unattended send.
- **Mechanism:** Connect → 'use n8n to…' → search → get details → allow → side effect.
- **Evidence:** You can go ahead and just say we'll send that off now.
- **Conditions:** A send workflow exists in the instance.
- **Exceptions:** Assigned-tool MCP (`9IzGe0BBj_c` old world) would not have found a random sender.
- **Action:** Steal search-the-catalog. Never always-allow send.
- **Confidence:** high
- **Source:** `mPflFTQUCGk` @ UNKNOWN
- **Epistemic:** SOURCE

### Stay in the chat; do not context-switch
- **Claim:** The ClickUp close is: he is already in Claude, so he will not open ClickUp to mark PTO done.
- **Reasoning:** Context-switch is the pain. The tool is whatever is already wired.
- **Mechanism:** Speak the task name → MCP finds the manager → status complete.
- **Evidence:** I could context switch all the way back into ClickUp or I could just use my ClickUp task manager AI agent through MCP.
- **Conditions:** The task name is unique enough.
- **Exceptions:** A vague 'finish my todos' is a different (dangerous) ask.
- **Action:** Package stay-in-seat for read/status. Sends still HITL.
- **Confidence:** high
- **Source:** `mPflFTQUCGk` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
- Native connector + URL is the setup.
- Always-allow is the tell.
- Dunder Mifflin is a dummy — still a send.

## D. Procedures
- Enable is a secret/URL — Evens. Do not always-allow send.
- If we demo MCP, use a read-only workflow.

## E. Examples
- Situation: Written email + a ClickUp PTO task. Action: Claude+instance MCP sends mail and closes the task. Reasoning: Stay in chat. Outcome: Mail in inbox; task complete. Lesson: Catalog search is the steal; always-allow send is the never.

## F. Decision Rules
- If always-allow is on a sender → never.
- If we publish this as 'just send it' → never.
- Claude is not the hive writer.

## G. Contrarian
- Field wants YOLO send from chat. He shows it. We steal the catalog and refuse the YOLO.

## H. Assumptions
- Theirs: always-allow is convenience. Ours: it is the bug.
- Dummy address still leaves the box.

## I. Questions
- What are the three body fields?
- Can it find a delete workflow the same way?
- Is n8n 'native' in Claude still true?

## J. Connections
- **SYSTEM SYNTHESIS:** `9IzGe0BBj_c` definition. `nQtogLs_dlg` also mails Michael Scott.
- **SYSTEM SYNTHESIS:** `input-required-gate`.

## K. Future-Use
- Unassigned: read-only MCP catalog.
- Unassigned: always-allow as a Watchdog never.

## Steal / Operate-never

### Machine: catalog-search-never-always-allow-send
- **Epistemic:** SOURCE + SYSTEM SYNTHESIS
- **Workflow / loop:** connect HITL → search workflows by name → show details → human allow once per send → checkable stop = no always-allow on send/pay/publish
- **Questions / signals:** Did it search? Is always-allow on? Did mail leave?
- **Qualify / frame / objections:** 'Send that off now' is the never-line.
- **Procedure:** I will not package this send. I may package a read-only catalog.
- **Example that proves it:** Claude finds a send workflow and a ClickUp completer after always-allow.
- **Why it works:** Search is useful. Unattended send is the payload we refuse.
- **Conditions / exceptions:** Dummy inbox still counts. Claude on tape.
- **Operate-never payload:** Always-allow send; instance execute-any; Claude as hive.
- **Hive run (existing skills only):** `ask-principal` · `input-required-gate`
- **Source:** `mPflFTQUCGk` @ UNKNOWN

**Operate-never**
- Publish / schedule live / paid boost without Evens.
- Republish Nate or any source creator.
- Quote tape $ / hours×rate / student counts as FACT or as our price.
- Send / pay / deploy / book.
- New icp_id / unpark a client / Grok Bot sendPrompt.
- Install on-tape vendors (n8n-cloud, Skool, Vapi, Claude, ChatGPT, Gemini, Coda, Abacus).
- Always-allow a send workflow.
- Send mail from Claude/n8n MCP.

## L. Role-Specific Applications
- I will not cut 'just say send that off now.'
- Stay-in-seat is fine for a status change Evens asked for — still HITL.
- I do not send. I do not always-allow.
