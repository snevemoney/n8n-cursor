# Researcher — tDGiWn0flK8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/tDGiWn0flK8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/tDGiWn0flK8/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Caption-only (`full.txt`, ~1035 lines). Title: From Zero to Your First Agentic AI Workflow (Claude Code). Visual/click **UNKNOWN**. Timestamp **UNKNOWN**. Beats: (1) Traditional n8n/Make = you map every node; agentic = outcome + questions + self-fix. Deterministic (boring=beautiful) vs non-deterministic; job is make AI **as deterministic as possible**. Paper-map vs phone blue-line. n8n stays for repetitive. Messy: research / content / support / lead-gen. (2) **WAT**: Workflows = md SOP (competitor analysis: 5 sources → analyze us → PDF); Agent = Claude coordinator; Tools = one-job Python (scrape / analyze / PDF) auto-built and auto-fixed. Feedback updates the workflow file. (3) Setup: VS Code + Claude Code extension; paid Pro/Max (Opus 4.5 on tape); blank folder `first aentic workflow`; drop `claude.md` (WAT, operate: try existing tools, on error read/fix/reset, e.g. rate-limit → batch endpoint); folders temp / tools / workflows. `/init` or NL. Bypass permissions = settings “dangerously skip”; preferred: **plan → questions → then bypass**. (4) Live: plan-mode competitor PDF + logo + guidelines. Questions: autodiscover vs list; collect all business fields; analyze all aspects; branding from other project — he says **no, I’ll provide**. Monthly; PDF only; mid budget. Stack: Sonnet, Firecrawl, Perplexity, reportlab, matplotlib. Five tools. First run ~$1.50; cache later; +comp ~50¢. Keep planning: drop `AIS.png` + brand guidelines PNG; extract colors. Auto-accept. (5) It asks **him** to install deps — he pastes and it runs them anyway. `.env` from example: Anthropic + Firecrawl (codeenate 10% / 1k credits). Context 23% left; he clears >60% (rot). Run without naming the workflow — it finds it. Saves `business_profile.json`; Unicode/emoji Windows fail → fixes the tool. Competitors: Apollo, Outreach, Clay, Instantly, Lemlist. Recs: white-label, $99–149 starter, agency positioning. First PDF: colors yes, **white logo invisible**, $1.43. Vague “fix charts/logos” (not plan mode). Regen from cache; logo + pricing chart visible. Iterate in NL. Skool CTA. **Do not flatten** vs `vFepZE_wrfg` WAT · `3GAxd90fEE4` 10yo · `iTY8Q449YNQ` verify. $1.50 / $1.43 / $99–149 UNVERIFIED.

## B. Atomic Knowledge

### WAT: SOP md + coordinator + one-job tools
- **Claim:** Workflow = sequence in markdown (updates from feedback). Agent sequences tools. Tool = one action, modular, Claude writes/fixes Python. Don’t start chat-only (locker metaphor).
- **Reasoning:** Structure keeps a non-deterministic brain on a deterministic business process.
- **Mechanism:** `claude.md` layers + folders tools/workflows/temp.
- **Evidence:** Five tools + competitor-analysis.md from one plan.
- **Conditions:** Claude/VS Code on-tape. Hive: `workflow-compiler` / `slice-build` — no Claude install.
- **Exceptions:** n8n still for boring predictable.
- **Action:** Steal SOP-as-workflow + one-job tools. No Firecrawl as hive default.
- **Confidence:** high as the frame.
- **Source:** `tDGiWn0flK8` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Unicode Windows; white-on-white logo
- **Speech ≠ behavior:** “no code” vs Python tools and `.env` keys.

### Plan → questions → assets → then bypass
- **Claim:** Plan mode first. Answer discovery/brand/budget. Drop real brand files before accept. Bypass after the plan, not as a lifestyle. Clear when context >~60%.
- **Reasoning:** Dangerously-skip on a vague task = unsupervised execute.
- **Mechanism:** Keep-planning to inject logo/guidelines; then auto-accept.
- **Evidence:** It invented brand config until he dropped PNGs.
- **Conditions:** Paid Claude. Hive: `ask-principal` before keys/spend.
- **Exceptions:** He later uses vague-fix (not plan) to demo self-heal.
- **Action:** Steal plan-then-bypass + drop assets before accept.
- **Confidence:** high as the sequence.
- **Source:** `tDGiWn0flK8` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** it asked him to pip when it could run pip
- **Speech ≠ behavior:** “ask me any questions” then he wonders why it didn’t ask business info until run time.

### Self-heal + cache; first PDF will miss
- **Claim:** Error → read → patch tool → update workflow. Cache research so regen is cheap. First branded PDF missed a white logo; second pass after “investigate” showed logo + chart. Business facts go in JSON so you don’t re-tell.
- **Reasoning:** Holes appear on run 1–2; that’s the loop.
- **Mechanism:** `business_profile.json` + per-competitor files + analysis history.
- **Evidence:** Emoji encoding fix; $1.43 then cached regen; recs $99–149 (his demo pricing, not hive).
- **Conditions:** Keep vs `vFepZE_wrfg` W+T≠A. Tape $ UNVERIFIED.
- **Exceptions:** Vague-fix skipped plan “for the demo.”
- **Action:** Steal cache + profile JSON + visual check. Keys HITL.
- **Confidence:** high as the scar.
- **Source:** `tDGiWn0flK8` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** white logo; Unicode
- **Speech ≠ behavior:** “so simple” vs Anthropic+Firecrawl keys, pip, plan agent.

## C. Mental Models
Boring automation stays. Agentic = outcome + questions. WAT is locker shelves. Plan then bypass. Brand files beat invented config. First output is a hole-finder. Cache the research.

## D. Procedures
1. Blank folder + `claude.md` (WAT + self-improve + folders).
2. Plan the outcome (branded PDF, frequency, budget). Answer questions. Drop real assets.
3. Accept only after brand path is real. Then bypass.
4. Keys in `.env` HITL. Don’t paste keys in chat.
5. Run; if it doesn’t ask profile, force-save facts to JSON.
6. On error: let it patch the tool. On visual miss: say what’s wrong; regen from cache.
7. Clear ~60%. Hive: no Claude; no Firecrawl default; no new ICP.

## E. Examples
- **Situation:** Autodiscover + all fields. **Action:** plan. **Outcome:** five tools. **Lesson:** questions are the spec.
- **Situation:** Invented brand file. **Action:** keep planning + drop PNGs. **Outcome:** colors extracted. **Lesson:** assets before accept.
- **Situation:** White logo. **Action:** vague fix. **Outcome:** visible + chart. **Lesson:** run reveals holes.
- **Situation:** Unicode Windows. **Action:** patch tool. **Outcome:** list Apollo…Lemlist. **Lesson:** self-heal is the product.

## F. Decision Rules
- IF repetitive/predictable → n8n, not agentic.
- IF messy judgment → WAT.
- IF no plan yet → don’t bypass.
- IF brand not on disk → don’t accept.
- IF context >60% → clear.
- Refuse: Firecrawl as hive default; Claude install; spend keys unattended; new ICP.

## G. Contrarian
Zero-to-first still needs a paid Claude + two APIs. “10% off Firecrawl” is affiliate. $99–149 rec is the model talking about **his** demo company, not a hive price. Anti-gravity named on-tape only.

## H. Assumptions
$1.50 / $1.43 / 50¢ / $99–149 / $17 Pro (other tape) / 10% Firecrawl = **UNVERIFIED**.
**Desk dissent:** vs `vFepZE_wrfg` WAT completeness · `3GAxd90fEE4` steak metaphor · `iTY8Q449YNQ` Playwright verify (this tape is visual-PDF only). Hive Cursor+Grok.

## I. Questions
- Same WAT `claude.md` as `vFepZE_wrfg` / `3GAxd90fEE4`?
- Did Perplexity key ever get used or only Firecrawl+Anthropic?
- Logo fix: background invert or file swap? Caption unclear.

## J. Connections
- **SYSTEM SYNTHESIS:** `vFepZE_wrfg` · `3GAxd90fEE4` · `iTY8Q449YNQ`. Skills: `workflow-compiler` · `slice-build` · `golden-test-loop` · `ask-principal` · `context-docs`.

## K. Future-Use
WAT locker. Plan-then-bypass. Assets-before-accept. Profile JSON. Cache regen. White-logo scar. n8n-still-for-boring.

## Steal / Operate-never

### Machine: wat-plan-assets-run-heal
- **Epistemic:** SOURCE
- **Workflow / loop:** claude.md structure → plan+questions → drop brand → accept → keys HITL → run → patch tools → visual check → cache regen
- **Questions / signals:** Deterministic enough for n8n? Brand on disk? Context %? First PDF actually show the logo?
- **Qualify / frame / objections:** W+T≠A on `vFepZE_wrfg` — keep. “No code” is speech.
- **Procedure:** D.
- **Example that proves it:** invented brand → PNG drop; Unicode heal; white logo then chart.
- **Why it works:** Outcome + shelves + a first-run that is allowed to be ugly.
- **Conditions / exceptions:** Claude on-tape. Hive compiler/slice, not Claude.
- **Operate-never payload:** Firecrawl default; unattended bypass; new ICP; stack switch.
- **Hive run (existing skills only):** `workflow-compiler` · `slice-build` · `golden-test-loop` · `ask-principal` · `context-docs`
- **Source:** `tDGiWn0flK8` @ UNKNOWN

**Operate-never**
- Dangerously-skip with no plan. Firecrawl as hive scraper. New `icp_id`. Switch to Claude Code.

## L. Role-Specific Applications
Map WAT + plan-then-bypass onto hive compiler. Keep W+T≠A labeled. File white-logo and Unicode scars. Keys stay HITL.
