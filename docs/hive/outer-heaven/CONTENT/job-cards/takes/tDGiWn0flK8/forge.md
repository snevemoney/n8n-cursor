# Forge — tDGiWn0flK8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/tDGiWn0flK8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/tDGiWn0flK8/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **first agentic workflow from zero**. Traditional n8n/Make: you draw every node, you fix every error. Agentic: you state the outcome; the system reasons, asks, adapts, self-heals. Deterministic = boring-beautiful; job is to make non-deterministic *as* deterministic as the business allows. WAT: Workflows (markdown SOPs that update from feedback), Agent (coordinator), Tools (one-job Python the agent writes/fixes). Carnival map vs phone-blue-line. Live: VS Code + Claude Code extension (paid Pro/Max), empty folder, drop `claude.md` (WAT + self-improve + folders `tmp`/`tools`/`workflows`), `/init` or NL. Plan mode → questions (discover/hybrid, what to save, what to analyze, branding source, monthly, PDF-only, mid budget) → plan agent → Firecrawl + Perplexity + ReportLab + matplotlib. He rejects auto brand-config, drops logo + guidelines, then auto-accept. Agent asks him to install deps (it could have). `.env` from example: Anthropic + Firecrawl (codeenate 10% UNVERIFIED). Context 23% → `/clear` (rot). Run without naming the workflow — it finds it. Saves `business_profile.json`. Unicode/emoji Windows fail → fixes the tool. White-on-white logo/charts → “investigate and fix”; cache reuse; logo + pricing chart appear. First-run cost ~$1.43–1.50 UNVERIFIED. Caption-only. Claude / Firecrawl / Perplexity / Skool on-tape.

## B. Atomic Knowledge

### Outcome, not steps; WAT keeps the locker from becoming a pile
- **Claim:** Traditional automation isn’t dying — it’s for predictable work. Agentic unlocks messy (research, content, support, lead-gen). Without WAT, Claude “does fine” then gets messy.
- **Reasoning:** School locker: you can get A’s from a pile, but you’ll lose paper. Workflow = job description; agent = PM; tool = one action.
- **Mechanism:** Agent reads workflow + tool list, sequences, heals, writes the healing back into the tool/workflow so the error doesn’t recur (rate-limit → find batch endpoint → verify → update SOP).
- **Evidence:** Competitor analysis: collect info → discover → research → analyze → branded PDF.
- **Conditions:** His `claude.md` pack.
- **Exceptions:** Bypass-permissions without a plan is the danger he names.
- **Action:** Steal WAT + plan-then-bypass. Do not install Claude/Firecrawl. Do not scrape competitors for a hunt.
- **Confidence:** high on the frame.
- **Source:** `tDGiWn0flK8` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** none yet
- **Speech ≠ behavior:** none

### Plan mode, persist the business, heal in public
- **Claim:** Speak the outcome in plan mode; answer questions; keep planning when brand assets are wrong. Persist profile so you don’t re-explain. Self-heal (Unicode) and visual-heal (white PNG) by NL. Cache research so the second PDF is cheap.
- **Reasoning:** First run is questions + holes. Battle-tested = run, find hole, fix tool, keep data.
- **Mechanism:** `business_profile.json`, `competitor_list`, per-competitor files, `analysis_history`. `.env` not chat. Clear when context > ~60% remaining-to-compact (his habit).
- **Evidence:** Apollo/Outreach/Clay/Instantly/Lemlist list; recs: white-label, $99–149 starter, agency positioning UNVERIFIED. Logo visible on regen.
- **Conditions:** He gave a thin “Get Leads with AI” brief on purpose.
- **Exceptions:** Agent asked him to run pip; he made it run it. Didn’t ask for business info until mid-run.
- **Action:** Steal persist-profile + heal-the-tool + cache. Do not store keys in chat. Do not quote $1.43 as FACT.
- **Confidence:** high on the loop.
- **Source:** `tDGiWn0flK8` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** demonstrated
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Unicode; invisible logo/charts
- **Speech ≠ behavior:** “you don’t have to touch the ugly code” vs he still pastes keys and saves `.env`

## C. Mental Models
Paper map vs blue line — same carnival, different recovery. Boring deterministic is a feature. Agentic shines when judgment is required *and* you still pin it with SOPs + one-job tools. Curiosity (“what is glob?”) is how he learned. First run is supposed to be holey.

## D. Procedures
1. Do not install Claude Code, Firecrawl, Perplexity, VS Code-as-required, Skool `claude.md`.
2. Do not use codeenate. Do not quote run $ as FACT.
3. Do not scrape a live competitor set for outbound. Clients parked.
4. Empty folder → onboarding md (WAT + folders + heal loop) → plan the outcome → answer discovery/info/analysis/brand/cadence/budget → drop real brand files → accept.
5. Keys in `.env`, never chat. Save the file.
6. `/clear` when rot / ~60% toward compact (his rule).
7. Run by stating the job; let it find the workflow.
8. “Save this about the business so I don’t repeat it.”
9. On error: read, fix script, re-run, update workflow.
10. On invisible logo: say investigate-and-fix; reuse cache.
11. Bypass only after a plan you trust.

## E. Examples
**Situation:** White PNG logo on light PDF.  
**Action:** “Can’t see charts or logos, same color as background — investigate and fix.”  
**Reasoning:** Vague NL, no plan mode (demo).  
**Outcome:** Regen from cache; logo + pricing chart visible.  
**Lesson:** Visual hole is a tool bug; don’t re-research.

**Situation:** Unicode error on Windows.  
**Action:** Agent reads, strips/fixes emoji, updates the tool.  
**Reasoning:** Self-improve loop in `claude.md`.  
**Outcome:** Discover continues; competitor list written.  
**Lesson:** Heal the tool, not the one run.

**Situation:** He never named the workflow.  
**Action:** Agent searches `workflows/`, reads it, runs.  
**Reasoning:** Structure + names are enough.  
**Outcome:** Questions for missing profile, then files.  
**Lesson:** Folder contract > magic prompt.

**Situation:** Plan invented brand config.  
**Action:** Keep planning; drop real logo + guidelines.  
**Reasoning:** Don’t accept a hallucinated brand.  
**Outcome:** Plan updated; assets extracted.  
**Lesson:** Plan mode is for the one thing that’s wrong.

## F. Decision Rules
- IF the task is repetitive and known → traditional automation is fine.
- IF it needs judgment every step → agentic + WAT.
- IF no plan yet → plan mode, not bypass.
- IF brand/config is invented → keep planning, drop files.
- IF context rotting → clear.
- IF error → fix the tool and the SOP.
- IF data already fetched → cache, don’t re-pay.
- IF keys → `.env` only.
- IF send/scrape-for-hunt → HITL / refuse.

## G. Contrarian
Field says draw every n8n node or you’re not a real builder. He says state the outcome. Field says bypass is reckless; he uses it *after* plan. Field wants a perfect first PDF; he wants a hole-finding first run.

## H. Assumptions
Paid Claude + Firecrawl/Perplexity as taped. Cost figures UNVERIFIED. “Get Leads with AI” is a demo ICP — do not unpark. Falsifier: scrape blocks / Firecrawl down. Hive stack Cursor + Grok.

## I. Questions
What’s our WAT folder contract in Cursor without Claude? What’s a hive-safe research tool instead of Firecrawl? When is n8n still the right deterministic spine?

## J. Connections
SYSTEM SYNTHESIS: `AO5aW01DKHo` agentic vs n8n; self-heal in the *build*. `xJ5oz63mIec` WAT. `UGIZnh6HNLc` secrets not in chat. `Ek1NBfnnTH0` context failures. `iTY8Q449YNQ` verify + rot. Cursor + Grok.

## K. Future-Use
WAT. Plan-then-run. Persist profile. Heal tools. Cache. Invisible-asset check. No Firecrawl default.

## Steal / Operate-never

### Machine: WAT folder + plan-the-outcome + persist + heal-the-tool + cache the research
- **Epistemic:** SOURCE
- **Workflow / loop:** empty project → onboarding md → plan + questions → real assets → build tools/SOP → `.env` → clear if fat → run job → save profile → on error fix tool → on visual miss fix generator from cache
- **Questions / signals:** Deterministic enough for n8n? What’s the deliverable? What must persist? What’s the budget?
- **Qualify / frame / objections:** Paper-map still works. Bypass without plan is the named danger.
- **Procedure:** No Claude/Firecrawl/Perplexity install. No coupon. No competitor scrape for outbound. Keys not in chat.
- **Example that proves it:** Unicode heal; white-logo regen; workflow found by search; $1.43 first run UNVERIFIED.
- **Why it works:** Outcome + structure; memory in files; errors become SOP updates.
- **Conditions / exceptions:** First run is holey on purpose. Tape $ UNVERIFIED.
- **Operate-never payload:** Claude/Firecrawl/Perplexity; codeenate; quote $1.43; hunt from the competitor list; Skool.
- **Hive run:** none. Deploy HITL.
- **Source:** `tDGiWn0flK8` @ UNKNOWN

### Operate-never
- Do not install Claude Code, Firecrawl, Perplexity, or his Skool `claude.md`.
- Do not quote $1.43 / $1.50 / 10% off as FACT.
- Do not scrape or outbound the competitor list.
- Do not put API keys in chat.
- Clients parked. Deploy HITL.

## L. Role-Specific Applications
Forge steals **WAT + plan-then-bypass + persist-in-JSON + heal-the-tool + cache**. We do not stand up Claude Code or Firecrawl. Cursor + Grok can keep the folder contract. No new ICP from “Get Leads with AI.”
