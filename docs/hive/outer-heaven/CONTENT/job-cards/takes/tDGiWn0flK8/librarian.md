# Librarian — tDGiWn0flK8
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/tDGiWn0flK8/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/tDGiWn0flK8/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** From Zero to Your First Agentic AI Workflow in 26 Minutes
**Channel:** Nate Herk | AI Automation
**Kind:** video (~7458 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Beginner WAT in VS Code + Claude Code. Traditional Make/n8n = you draw every node and you fix every break. Agentic = say the outcome; it reasons, asks, adapts, self-fixes. **Deterministic** (boring is beautiful) vs **non-deterministic**; the job is to make ND **as D as possible**. n8n stays for repetitive predictable work. Carnival map vs phone-blue-line. Title **26 minutes** — body is a full first-workflow class.
2. **WAT:** Workflows = markdown SOPs (headers matter); they **update from feedback**. Agent = coordinator (Claude). Tools = one-job Python; Code writes/fixes them; reusable across workflows. Locker metaphor: folders beat a pile. CLAUDE.md onboarding: look in existing tools first; on error read → fix script → update workflow so it never repeats (rate-limit → batch endpoint example). Folders: temp / tools / workflows.
3. Install: VS Code → Claude Code extension → **paid Pro/Max** (free has no Code). Open a blank folder. Drop CLAUDE.md (Skool). `/init` or NL "initialize and ask." Modes: plan / ask / auto-edit / **dangerously bypass permissions** (settings). His flow: **plan first, then bypass**. Init creates the three folders; he `/clear`.
4. Live: competitor-analysis → **branded PDF**. Plan mode, spoken brief (logo + guidelines + find competitors + opportunities). Questions: autodisco vs list (autodisco); save all business fields; analyze all aspects; use existing YouTube-project brand? **No — he will drop files.** Frequency monthly; PDF only; mid API budget. Plan: Sonnet + Firecrawl + Perplexity + reportlab + matplotlib; tools collect / discover / research / analyze / generate PDF; edge cases (block, too few, rate limit, bad brand); first run ~**$1.50**, later cache, +comp ~50¢ UNVERIFIED. He **keeps planning** to drop `AIS.png` + brand-guidelines image; Code extracts colors. Auto-accept. README + `.env.example`. He pastes "install deps" and Code runs it (he notes it could have just run). Keys: Anthropic console + Firecrawl (code **nate** 10% / 1,000 credits). Save `.env`. Context **23% remaining until autocompact**; he clears when that meter is "over 60%" (keep both wordings).
5. Run: he does **not** name the workflow — Code finds `competitor analysis`. Thin brief: Get Leads with AI, scrape + lists + outreach. Then questions: agencies; credit usage; all-in-one; **save this in the project**; $200–500/mo. Writes `business_profile.json`. Unicode/emoji **Windows** encoding fail → it patches the tool. Competitor list (Apollo, Outreach, Clay, Instantly, Lemlist…). Recs: white-label, $99–149 starter, agency positioning. PDF: his colors, **white logo invisible**, run **$1.43**. Vague "fix charts and logos" (no plan mode). Regen from cache; logo + pricing chart appear. Tweaks from here are NL. Skool **quarter million**. Firecrawl affiliate.
Gap: full CLAUDE.md, tool scripts. Timestamp UNKNOWN. Claude / Firecrawl / VS Code on-tape. Get-Leads ICP parked. Do not flatten with `vFepZE_wrfg` (WAT newsletter) or hive Cursor stack.

## B. Atomic Knowledge

### WAT folders; plan then bypass; persist the business; fix the tool not the one-off
- **Claim:** First agentic workflow is a folder contract, not a node canvas. Workflows are living SOPs; tools are one job; the agent sequences. Plan → questions → accept → keys → run. Name-optional if CLAUDE.md taught the locker. Persist profile JSON so the next run does not re-interview. Self-heal the **tool** when Windows encoding breaks. White-on-white logo is a brand-asset bug, not a WAT bug. $1.50 / 26 min / 250k are tape numbers. Bypass is named dangerous; he still uses it after a plan.
- **Reasoning:** Same WAT as `vFepZE_wrfg` / later 10-year-old tape. Hive does not install Claude or Firecrawl. Steal the locker + persist-profile + fix-the-tool.
- **Mechanism:** CLAUDE.md → init folders → plan + assets → `.env` → clear rotting context → run (agent finds the SOP) → save profile → patch tools → HITL read the PDF.
- **Evidence:** autodisco questions; Windows unicode patch; missing white logo then fix; $1.43; he didn't invoke the workflow by name.
- **Conditions:** $1.50 / 26 min / quarter-million UNVERIFIED. Pro/Max required on-tape.
- **Exceptions:** Do not enable always-bypass. Do not buy Firecrawl for a hive OS.
- **Action:** File WAT-locker, plan-then-execute, persist-profile-json, fix-the-tool, white-logo-check. Do not `/bypass` as default.
- **Confidence:** high as a first-WAT anatomy
- **Source:** `tDGiWn0flK8` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (transcript-implied)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** white logo; Windows unicode; it asked install instead of running
- **Speech ≠ behavior:** "26 minutes / so simple" vs keys + two plan rounds + a regen; "bypass isn't too bad" vs the setting is named dangerous

## C. Mental Models
Boring is beautiful. Make ND as D as you can. Locker not pile. Plan then go. Context rots. The SOP should be findable without naming it. Skool room.

## D. Procedures
1. Paid Code + empty folder + CLAUDE.md.
2. Init the three directories.
3. Plan mode; answer discovery / brand / budget.
4. Drop real brand files; keep planning until the plan sees them.
5. Keys in `.env`; save; clear if the window is fat.
6. Run; tell it to persist facts; read the PDF; patch tools not one PDFs.
Avoid: always-bypass; Firecrawl as hive; 26-min as FACT; Claude as hive OS.

## E. Examples
**Unnamed run:** Situation — "run a competitor analysis." Action — it searches workflows/. Outcome — it uses the SOP. Lesson — the locker works if CLAUDE.md taught it.

**White logo:** Situation — white PNG on white. Action — vague fix. Outcome — second PDF shows mark + chart. Lesson — brand assets are a test, not a hope.

## F. Decision Rules
- IF the task is boring-repeatable → traditional n8n (his).
- IF you have not accepted a plan → do not bypass.
- IF a tool throws → patch the tool and the SOP.
- IF the logo is light → check it on the page, don't trust "branded."
- Refuse: Claude/Firecrawl as hive; always-allow.

## G. Contrarian
Against "n8n is dead." Against throwing every paper in the locker. Against one-shot bypass with no plan.

## H. Assumptions
Caption-only. Complements `vFepZE_wrfg` / `3GAxd90fEE4`. Keep 26-min vs live length. Keep community-count wobble.

## I. Questions
Did monthly cron ever exist? Did Firecrawl 1,000 credits cover a second full scrape?

## J. Connections
SYSTEM SYNTHESIS → WAT tapes; hive Cursor+Grok; `ask-principal` on keys.

## K. Future-Use
WAT-locker + persist-profile + fix-the-tool + plan-then-execute as atoms.

## Steal / Operate-never

### Machine: locker, plan, persist, patch the tool
- **Epistemic:** SOURCE
- **Workflow / loop:** CLAUDE.md → plan → assets → keys → clear → run → save profile → patch tool → HITL PDF
- **Questions / signals:** Did it find the SOP? Is the logo visible? Did the error rewrite the tool?
- **Qualify / frame / objections:** Get-Leads is a demo. 26 minutes is a title.
- **Procedure:** D above.
- **Example that proves it:** unnamed run; white logo; Windows unicode.
- **Why it works:** Folders and a living SOP beat a one-off chat.
- **Conditions / exceptions:** $ UNVERIFIED. Hive does not run Claude WAT.
- **Operate-never payload:** Always-bypass. Firecrawl coupon as a buy. Claude as hive. 26-min FACT.
- **Hive run:** File persist-profile + fix-the-tool on Cursor/Grok WAT.
- **Source:** `tDGiWn0flK8` @ UNKNOWN

### Operate-never
- Claude Code / Firecrawl as hive. Always-allow send. Quote $1.50 / 26 min as FACT. Merge LESSONS. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
Upgrade old take: add unnamed-SOP-find + white-logo + Windows patch. Do not migrate the hive. Hard steps HITL.
