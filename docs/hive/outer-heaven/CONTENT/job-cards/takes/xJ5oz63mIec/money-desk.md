# Money Desk — xJ5oz63mIec
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xJ5oz63mIec/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xJ5oz63mIec/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
~5337 words. Nate: three ways to run agents while you sleep — plus WAT (workflow / agent / tools) and a 2-axis map (machine vs cloud × script vs agentic). Caption-only; timestamp UNKNOWN. Beats in order: **1 `/loop`** — cron create/list/delete, session-scoped (five tabs = five loop sets). NL or `/loop`. Desktop: next-in-Xs, **ends in 3 days**, jitter so not everyone hits :00. Kill = cron delete or close tab; **desktop `/clear` kills the cron; terminal `/clear` does not** (trash-every-minute survived clear). Extra terminal trick: a second cron that injects `/clear` every 5 min to fight context rot — CC thought it wouldn’t work; it did; both crons still listed. Terminal loops **7 days**; desktop showed 3 (maybe stale). Jitter: fire **up to 30 min late** — don’t need 9:17 exact. Early YouTube: loop every 10 min reply-to-comments from transcript, optional kill at 24h. Pros: zero setup, full WAT + skills, self-shutdown, slash in-loop. Cons: session open, machine on, 7d, interval+jitter, desktop vs terminal. Skills sit in W+T (LinkedIn-only = W; +infographic API = W+T). **2 desktop scheduled + cloud routines** — local dies if app closed; cloud = Anthropic infra, **Max 15 remote runs/day, Pro 5, Team/Ent 25**, else extra usage. Same idea: inject a prompt into a CC session (School wins-engagement, later moved to Hermes). Cloud = repo clone + different env. Local catch-up: PC off 5 days → app open replays missed (pause if you don’t want that). Remote also GitHub-event or API webhook. Cons: cloud **1h min interval**; cap; local needs machine; **careless prompt → unwanted action**. Local: machine on, app on, new session (not the old tab). Cloud: nothing on. **3 Modal / Trigger** — Python vs TS; schedule or webhook; dashboards. Modal = cron-in-the-cloud; Trigger = durable WF that also crons. Deploy W+T (the skill), **not the A** unless Agent SDK. AI there = API $ (not the CC sub) — or no AI if the job is deterministic (his prefer). Bonus Agent SDK = CC harness (tools/loop) vs raw chat brain; **stateless unless you send session_id**; auto-compact, no `/clear` (new session). **Cannot use CC sub — needs API key** (more $). Mid-wait: May 13 monthly credit can apply to Agent SDK but **dedicated budget ≠ weekly**; Theo breakdown. Managed agents: he tried, doesn’t love; stay on own infra if you already know CC. Hooks: event-deterministic (pre/post tool, session start/end, notify-on-message). Close: School guide. Auto-reply / 15-runs / CC/Modal/Trigger as ours = operate-never.

## B. Atomic Knowledge
### Pick-the-axis-not-the-brand
- **Claim:** Machine vs cloud × script vs agent. Loop = full WAT, laptop+session. Local routine = full WAT, app+machine, new session, catch-up risk. Cloud routine = full WAT, 15/5/25 cap, 1h min. Modal/Trigger = W+T (or A via SDK+API $), laptop off.
- **Reasoning:** Desktop `/clear` kills loops; terminal doesn’t. Jitter ≤30 min. Local catch-up after 5 days off.
- **Mechanism:** Steal the map. Do not stand up CC/Modal/Trigger. Comment-reply loop is publish HITL.
- **Evidence:** On-tape trash cron; 3d vs 7d; Max 15; May 13 credit note.
- **Conditions:** A deploy choice.
- **Exceptions:** Claude / Modal / Trigger / auto-reply operate-never. $ caps UNVERIFIED.
- **Action:** Steal the map. HOLD the vendors.
- **Confidence:** high as a model
- **Source:** xJ5oz63mIec @ UNKNOWN
- **Epistemic:** SOURCE
### Careless-prompt-is-an-unwanted-action
- **Claim:** Unattended inject (routine/loop/SDK) will do what the prompt allows. Watch it. Scope it. Pause catch-up if you don’t want five days of replay.
- **Reasoning:** YouTube 10-min reply loop is the example — that’s a public send.
- **Mechanism:** HITL anything that comments, emails, pays. Tape $ UNVERIFIED.
- **Evidence:** On-tape wins-engagement moved to Hermes; comment loop.
- **Conditions:** An unattended run.
- **Exceptions:** Auto-reply / auto-send operate-never.
- **Action:** Steal watch-then-unattend. HITL the send.
- **Confidence:** high
- **Source:** xJ5oz63mIec @ UNKNOWN
- **Epistemic:** SOURCE
### SDK-is-hands-plus-a-bill
- **Claim:** Raw API = brain. Agent SDK = CC loop (tools, skills, MCP) but **API key**, not the sub. Stateless unless session_id. May 13 credit ≠ weekly bucket.
- **Reasoning:** Deterministic no-AI scripts are why he still uses Modal/Trigger.
- **Mechanism:** Don’t analog API $ to the sub. Don’t treat managed agents as the path (he bounced).
- **Evidence:** On-tape Theo link; managed-agents skip.
- **Conditions:** A paid loop.
- **Exceptions:** Anthropic API / CC as ours operate-never.
- **Action:** Steal brain-vs-hands. HOLD the SDK.
- **Confidence:** medium-high
- **Source:** xJ5oz63mIec @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
Belief: deploy method follows the axis, not the tweet. Priority: map, HITL sends, watch unattended prompts. Experience: terminal vs desktop loop; 15/day; managed-agents bounce. Contrarian: no one best deploy. Uncertainty: 3d vs 7d; May 13 budget; 5/15/25.

## D. Procedures
His order: loop → local/cloud routines → Modal/Trigger (+ SDK bonus) → skip managed, mention hooks. Our order: do not deploy on CC. Steal the map + HITL. Caption-only: clicks UNKNOWN.

## E. Examples
**Situation:** Trash every minute. **Action:** desktop vs terminal `/clear`. **Reasoning:** context rot. **Outcome:** desktop dies; terminal lives; `/clear` cron works. **Lesson:** Terminal if you need the loop to survive compact.

**Situation:** PC off 5 days. **Action:** open desktop. **Reasoning:** local catch-up. **Outcome:** missed tasks replay. **Lesson:** Pause if you don’t want replay.

**Situation:** Comment loop. **Action:** every 10 min reply. **Reasoning:** new upload. **Outcome:** public sends. **Lesson:** That’s HITL, not a cron toy.

## F. Decision Rules
IF send/comment/pay → HITL. IF 15/5/25 / 3d / 7d / May 13 → UNVERIFIED. Refuse: CC/Modal/Trigger as ours; auto-reply; analog API $.

## G. Contrarian
Rejects one-best-deploy. Rejects managed-agents (for him). Rejects treating SDK as the CC sub.

## H. Assumptions
Caps are plan-slide. 3d vs 7d he doesn’t re-check. Survivorship: already on Max. Falsifier: jitter misses a real 10-min SLA. Speech≠behavior: ‘while you sleep’ + machine-must-stay-on for method 1.

## I. Questions
What’s live remote-run cap? Did desktop loops move to 7d? Any checkout we can open from a sleep-deploy?

## J. Connections
SYSTEM SYNTHESIS: axis-map = `vfWTyEreOEc` rank-by-day. Unattended-prompt = `UGIZnh6HNLc` QA. Auto-reply = `playbook-before-send`. CC/Modal/Trigger operate-never. Hive already has loops/routines — don’t import Anthropic’s.

## K. Future-Use
Unassigned: jitter-up-to-30-min as an SLA killer. Local catch-up as a pause-or-flood fork.

## Steal / Operate-never

### Machine: Map-the-axis-HITL-the-send-watch-the-inject
- **Epistemic:** SOURCE
- **Workflow / loop:** trigger: ‘run while I sleep’ → action: pick machine-vs-cloud and script-vs-agent; scope the prompt; human still hits comment/email/pay → checkable stop: a pause switch, not a 10-min public reply
- **Questions / signals:** Laptop or cloud? Script or agent? Who sends? Catch-up or pause?
- **Qualify / frame / objections:** Frame: no one best deploy. Objection: ‘15 runs/day’ — plan cap, UNVERIFIED.
- **Procedure:** Do not stand up CC/Modal/Trigger. HITL comment/send/pay. Tape $ UNVERIFIED.
- **Example that proves it:** Terminal loop survives `/clear`; local catch-up; YouTube reply loop. UNVERIFIED caps.
- **Why it works:** The axis is the choice. The inject will do what you allowed. A comment is a send.
- **Conditions / exceptions:** Works as a map. Exception: CC / Modal / Trigger / auto-reply / 15-as-FACT operate-never.
- **Operate-never payload:** Claude Code · Modal · Trigger.dev · auto-reply comments · analog 15 runs · Agent SDK as the sub
- **Hive run (existing skills only):** `playbook-before-send` · `ask-principal` · `loop` (ours, not Anthropic) · `pricing-margin-roi-guardrails`
- **Source:** xJ5oz63mIec @ UNKNOWN


### Operate-never (this desk will not operate)
- Quote Max 15 / Pro 5 / 3d / 7d / May 13 credit as FACT or as our analog.
- Claude Code / Modal / Trigger as ours. Auto-reply YouTube. Treat Agent SDK as the CC subscription.

- Move money, approve a charge, refund, or fee. Live Stripe. Auto-send / auto-pay / auto-book / auto-deploy / auto-publish.
- Quote any tape $ / student count / job-loss % / prize / 10x as FACT or as our price analog.
- Nate Skool / Plus / AIS Plus / Hostinger NATEHERK / Uppit / Glaido / sold templates as a SKU. Do not map through `usecase-to-sku`. Do not join / install / import.
- Install Claude Code / Codex / Claude / ChatGPT / Gemini / Coda / Vapi / ElevenLabs / n8n-cloud / Trigger.dev / Hermes / Base44 / Sora / NanoBanana / Poppy / Lovable as ours. Cursor + Grok only. Vendor on tape is a mention, not a Bot dispatch.
- New hunt ICP. Unpark a client. Live hunt stays `local-pro` / Normand. Clients parked. No new `icp_id`.
- Grok Bot / `sendPrompt`. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`. Overwrite `takes/money-desk.md`.

## L. Role-Specific Applications
HOLD Claude Code, Modal, and Trigger.dev. Steal the deploy-axis map and HITL-the-inject. Comment / send / pay stay HITL. Early rung $500–1K/mo CAD.

**Lens only (after A–K + Steal).** This desk votes PASS/HOLD on margin. It does not move money.

- `pricing-margin-roi-guardrails`: tape $ stays **UNVERIFIED**. Our early rung stays **$500–1K/mo CAD** after a 30–60d win. Delivery ≤40% of fee. Vendor / educator $ does not move Normand Path A.
- `outcome-offer-funnel` + `checkout-proof`: count checkout + warm conversions we can open. Quarantine YouTube receipts.
- `paid-slice-funnel`: thin V1; Stripe HITL; preview ≠ domain.
- `ask-principal` + `input-required-gate`: confirm ≠ execute. Pay / refund / fee stay HITL.
- `website-offer-funnel`: Path A/B/C spine still exists; this tape does not open a client unless Evens names one.
- Proposed, not written: `unit-econ-card` (price, COGS, contribution, aha-gate — tape $ never fills the line) · `token-receipt` (session cost versus artifact; leftover quota is not a KPI).

**Business parked:** no new `icp_id`. No `business-lanes.json` row. Hunt stays `local-pro` / Normand.
