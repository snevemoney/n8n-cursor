# Day Planner — xJ5oz63mIec
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xJ5oz63mIec/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xJ5oz63mIec/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: three ways to **deploy** Claude automations (run-where × deterministic↔agentic). Beats: **WAT** = workflow / agent / tools (skills = W+T); (1) **`/loop` cron** — session-scoped, desktop **3-day** vs terminal **7-day**, jitter up to **30 min**, desktop `/clear` kills cron, terminal `/clear` keeps it; pair a `/clear` loop vs context rot; YouTube **auto-reply comments** every 10 min / kill at 24h (on tape); machine+session must stay up; (2) **desktop scheduled** (local, catch-up if PC was off — pause if you don’t want that) vs **cloud routines** (Anthropic, machine off; Max **15/day**, Pro **5**, team **25** UNVERIFIED; **1h min** interval; GitHub/API webhook); Skool wins skill paused → Hermes; careless prompt = unwanted actions; (3) **Modal / Trigger.dev** — script on a timer, WAT without the A unless Agent SDK; pay-per-token not the sub; prefer for **no-AI deterministic**; Agent SDK = stateless unless session id; API key $; May 13 credits nuance (Theo); **managed agents** he doesn’t love; **hooks** = deterministic on events (notify on message, pre/post tool, session start/end). Skool guide. Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Pick deploy by “must the laptop stay on?” and “does it need a brain?”; loops die with the session
- **Claim:** No one best host. Loop = full agent, laptop on. Cloud routine = full agent, cap + 1h. Modal/Trigger = script, always-on, often no model. Auto-reply comments is a send.
- **Reasoning:** Jitter and catch-up surprise you. Careless autonomous prompts act.
- **Mechanism:** If interval <1h and you can watch the session → loop. If laptop off + agent → cloud (his). If no AI → a script cron (ours: not his hosts).
- **Evidence:** “there’s not one best way to deploy an automation.” / “careless prompt scoping can cause unwanted actions.”
- **Conditions:** Claude on tape. We already have Grok routines (`PQBYZQqan2g`).
- **Exceptions:** We do not Claude-loop comment replies, Modal, or Trigger.dev.
- **Action:** Steal laptop-on vs off + WAT + watch-before-autonomous. Do not auto-reply. Do not Skool.
- **Confidence:** high as the slider.
- **Source:** `xJ5oz63mIec` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Claude thought `/clear` cron wouldn’t work (it did, his)
- **Speech ≠ behavior:** “simple deploy” vs comment-reply bot

## C. Mental Models
WAT over “just cron it.” Priority: sleep-while-it-runs. Uncertainty: 15/5/25 caps.

## D. Procedures
1. Ask: laptop on? session open? needs a model?
2. If yes/yes/yes and short interval → a watched loop (not a send).
3. If catch-up would be bad → pause local schedules before a long off.
4. Deterministic no-AI → a script, not an agent (our stack).
5. Watch autonomous prompts a long time before unattended.
Avoid: Claude comment-loop; Modal/Trigger; Skool; unmanaged hooks that send.

## E. Examples
**Comment loop:** Situation → new upload. Action → every 10 min reply. Reasoning → full WAT. Outcome → a send farm. Lesson → operate-never.

**Catch-up:** Situation → PC off 5 days. Action → local tasks fire in a burst. Reasoning → desktop remembers. Outcome → surprise runs. Lesson → steal pause-before-off.

## F. Decision Rules
- IF the job is a send/reply → never a loop.
- IF you need exact :17/:27 → jitter will miss (his).
- IF no model needed → don’t pay agent tokens.

## G. Contrarian
Rejects one-host religion. Field: always Trigger. He: slider. We reject his hosts.

## H. Assumptions
Theirs: Claude sub is the cheap agent. Ours: Grok Bot. Falsifier: unattended comment replies. Survivorship: trash-remind loops.

## I. Questions
Same Trigger tape as `UGIZnh6HNLc`? Hooks masterclass later?

## J. Connections
- SYSTEM SYNTHESIS → `UGIZnh6HNLc` · `i4Q8wHZNPBU` · `PQBYZQqan2g` · `send-removed`.

## K. Future-Use
Laptop-on slider. Pause-before-off. Unassigned Agent SDK $.

## Steal / Operate-never

### Machine: laptop-on? needs-a-brain? → pick loop vs cloud vs script; watch before unattended; never reply-loop
- **Epistemic:** SOURCE
- **Workflow / loop:** name WAT → pick host class → if autonomous, watch many runs → pause local before long off
- **Questions / signals:** Session-die? Catch-up burst? Comment reply?
- **Qualify / frame / objections:** YouTube reply cron is the fail. Watched trash-remind is the pass (and we still don’t Claude).
- **Procedure:** No Claude deploy. No Modal/Trigger. No auto-reply. No Skool.
- **Example that proves it:** Situation → 5-day off. Action → catch-up. Reasoning → local queue. Outcome → burst. Lesson → steal pause.
- **Why it works:** On/off and brain/no-brain are checkable; “deploy while you sleep” is a send risk.
- **Conditions / exceptions:** Caps UNVERIFIED.
- **Operate-never payload:** Claude loops that send; Modal/Trigger; Skool; unmanaged cloud routine.
- **Hive run (existing skills only):** `i4Q8wHZNPBU` · `send-removed` · `PQBYZQqan2g`.
- **Source:** `xJ5oz63mIec` @ UNKNOWN

### Operate-never
- Claude deploy / comment-reply / Modal / Trigger / Skool.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as laptop-on slider (no reply-loop). Clients parked.
