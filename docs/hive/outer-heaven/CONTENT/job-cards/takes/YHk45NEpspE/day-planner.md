# Day Planner — YHk45NEpspE
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/YHk45NEpspE/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/YHk45NEpspE/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: **Printing Press** (CLI factory + ~50 CLI library). Beats: Skool has **no API** — Claude + CLI pulls 9 wins / 10 posts; **~132k tokens back, ~2k into context** (UNVERIFIED), ~10 min to build; CLI vs API vs MCP: APIs = fat JSON (for code); MCP = discovery + **always-on tool-list bloat** (`/context`); CLI = local, short text, SQLite mirror, lazy discovery; claimed **MCP 35× tokens, reliability 100%→72%** as tasks harden (UNVERIFIED); Steinberger/OpenClaw inspired (Gog > GWS, his claim); starter pack (ESPN/Flight/Movie/Recipe) + factory; need **Go**; he built Skool (reverse-engineered), Tally, YouTube; Contact Goat = LinkedIn + email verify (he hasn’t tried); HN factory demo, time-estimates wrong; auth in **`.env` not the script**; quotas still apply; share via private repo + teammate key; tier: **CLI > API > MCP**; publish back to the library. Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Fat I/O stays outside the window; CLI > MCP for token-tax; keys never in the script
- **Claim:** Agents pay per token; a CLI can fetch 132k and hand the model 2k; MCP taxes you even when idle; keys live in `.env`; a CLI does **not** skip API quotas.
- **Reasoning:** APIs were for code; MCP for tool menus; CLI for short agent I/O.
- **Mechanism:** Factory → Go binary → skill wraps the command → SQLite cache.
- **Evidence:** “none of that hit my context window.” / “you don’t want to be putting any of your API keys… inside of the CLI scripts.”
- **Conditions:** A site/tool you already use; Go + a harness (his: Claude).
- **Exceptions:** Reverse-engineering Skool / Contact Goat hunt = operate-never.
- **Action:** Steal out-of-window I/O + key-split + CLI>MCP tax. Do not install Printing Press / Go-via-Claude / OpenClaw.
- **Confidence:** high as the tax story; 35× / 72% / 132k UNVERIFIED.
- **Source:** `YHk45NEpspE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** “30–60 min” estimate (he says always wrong)
- **Speech ≠ behavior:** “10 minutes” vs HN 30–60 pitch

## C. Mental Models
Official CLIs can be worse than a fan CLI. No-API sites are the prize. Priority: session-limit hygiene. Uncertainty: library quality, Contact Goat.

## D. Procedures
1. Before adding an MCP, ask: is there a CLI / can we keep the fat payload out of context?
2. Keys in `.env`, never in the binary you might push.
3. Quotas still count.
4. Time estimates from the model are a joke (his).
Avoid: Printing Press; Skool reverse-engineer; Contact Goat; OpenClaw; quote 35× as FACT.

## E. Examples
**Skool wins:** Situation → no API. Action → 10-min CLI, 9 wins, 3 strongest + links. Reasoning → route through CLI. Outcome → 132k out / 2k in (claimed). Lesson → steal the split; don’t scrape Skool.

**Password-in-repo warning:** Situation → share Tally CLI. Action → private repo, teammate swaps `.env`. Reasoning → same as API hygiene. Outcome → team can clone. Lesson → steal the split.

## F. Decision Rules
- IF MCP is loaded and unused → assume a token tax (his `/context`).
- IF keys would ship in the CLI → never.
- IF the CLI is for LinkedIn email-find → hunt / never.
- IF the only option is MCP → last resort (his tier), still not our install.

## G. Contrarian
Rejects “MCP everywhere.” Field: more servers. He: CLI is tier one. We steal the tax, refuse the factory.

## H. Assumptions
Theirs: 35× / 72% bench. Ours: UNVERIFIED; no Claude/Go factory. Falsifier: an MCP cheaper than a CLI on the same task. Survivorship: day-old tool.

## I. Questions
Same Steinberger as `EuzYhzB0vbI` loops? OpenClaw tape?

## J. Connections
- SYSTEM SYNTHESIS → `lokbsA5VXOk` (bloat) · `ehg4fhydTgs` (no .env in git) · `send-removed`.

## K. Future-Use
Out-of-window I/O. CLI>MCP tax. Unassigned Printing Press catalog.

## Steal / Operate-never

### Machine: keep fat I/O out of context; keys in .env; CLI tax < MCP tax — do not install the factory
- **Epistemic:** SOURCE
- **Workflow / loop:** need a tool → prefer a short local command → never ship keys → if someone pitches MCP-first, price the idle tool-list
- **Questions / signals:** What’s in `/context`? Would the binary contain a key? Is this a hunt CLI?
- **Qualify / frame / objections:** “MCP is the answer” is the fail. 132k-out/2k-in is the steal (UNVERIFIED).
- **Procedure:** No Printing Press. No Skool scrape. No Contact Goat. No OpenClaw.
- **Example that proves it:** Situation → Skool posts. Action → CLI summary only. Reasoning → don’t pollute the window. Outcome → claimed 2k in. Lesson → steal the split.
- **Why it works:** Context is the day; a dump is how you hit the cap.
- **Conditions / exceptions:** 35×/72%/132k UNVERIFIED.
- **Operate-never payload:** Printing Press; reverse-engineer Skool; LinkedIn email CLI; OpenClaw; quote 35× as FACT.
- **Hive run (existing skills only):** `coverage-loop` · `ask-principal`.
- **Source:** `YHk45NEpspE` @ UNKNOWN

### Operate-never
- Install Printing Press / Go-via-Claude / OpenClaw.
- Hunt via Contact Goat.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as fat-I/O-out-of-window (no factory). Clients parked.
