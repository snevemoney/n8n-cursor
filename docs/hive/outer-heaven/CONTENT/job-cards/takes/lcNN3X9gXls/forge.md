# Forge — lcNN3X9gXls
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lcNN3X9gXls/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lcNN3X9gXls/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk **n8n Data Tables “secret hacks”** (follow-on to `QCjMBOEhpLE`). Beats: three hacks. **(1) Models + prompts as a table:** research agent row `workflow=research agent` → chat model / user prompt / system prompt; client edits the table not the graph; share the workflow **without** the IP (variable, not the text); OpenRouter model string swapped 3.7 Sonnet + Tavily vs Perplexity — Sonnet also called Perplexity (autonomy) → newsletter: planning vs editor rows; user prompt into Tavily topic; same model to two agents → bonus: **daily sync** n8n↔Sheets (clear+rewrite either direction); tables are a weak UI (no dropdowns; model string must be exact; Sheets has a dropdown). **(2) Action logs:** production needs fail/why/good; Ultimate Media Agent used a Sheet (ts, workflow, in, out, actions, tokens); same columns in a table; agent option **return intermediate steps**; Telegram “lunch with Michael Scott 3pm” → contact agent → calendar; log shows think → contact → think → calendar + per-tool tokens; code node cleans counts; **error logger** table (date/time/message/node/**workflow**); error workflow + Slack/Telegram; context vars: now/today/execution id/workflow id+name. **(3) Evals:** dataset in/expected/actual/score in a table not a Sheet; RAG agent + correctness; Tesla op-inc **−42%** example UNVERIFIED; scores 5 then 4; **change one variable per run**; optimize speed/cost/quality; avg tokens/time. Plus **200+** / agency courses. Timestamp UNKNOWN. n8n / OpenRouter / Sheets / Plus on-tape.

## B. Atomic Knowledge

### Prompts are IP; logs + one-variable evals are how it gets better
- **Claim:** Don’t bury the system prompt in a graph you share. Log intermediate steps. Eval with expected vs actual. Change **one** knob per run.
- **Reasoning:** Patterns (good or edge) only show if you write them down. Two knobs at once lie about cause.
- **Mechanism:** Table row per agent → filter → variables; return intermediate steps → log; eval table → score.
- **Evidence:** Model/tool swap without opening the graph; Michael Scott lunch log; Tesla 5/4 scores.
- **Conditions:** n8n tables as taped.
- **Exceptions:** Tables lack dropdowns — Sheets as a safer frontend, sync if you must.
- **Action:** Steal IP-out-of-the-share, intermediate-step log, one-variable eval. Do not migrate hive prompts into n8n-cloud tables.
- **Confidence:** high on the hygiene; 200+ / −42% UNVERIFIED.
- **Source:** `lcNN3X9gXls` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Frontend for non-breakers. Shared template ≠ shared prompt. Autonomy may call extra tools after a model swap. Errors belong in one place with workflow name. Evals without “what changed in run 33” are theater.

## D. Procedures
1. Don’t add n8n tables. 2. If we share a workflow: keep the prompt out of the export. 3. Log actions + errors with workflow id. 4. Eval: expected vs actual; one variable. 5. Don’t quote Tesla −42% as FACT.

## E. Examples
**Situation:** Client shouldn’t open the graph.  
**Action:** Edit model/prompt in a table.  
**Reasoning:** Filter the row.  
**Outcome:** Tavily + Sonnet without touching nodes.  
**Lesson:** Control plane ≠ canvas.

**Situation:** Lunch with Michael Scott.  
**Action:** Intermediate steps on.  
**Reasoning:** See think → contact → calendar.  
**Outcome:** Event + log.  
**Lesson:** Hidden toggle is the receipt.

**Situation:** RAG eval.  
**Action:** Table dataset; score 5 then 4.  
**Reasoning:** One variable next time.  
**Outcome:** Track quality.  
**Lesson:** Isolate the knob.

## F. Decision Rules
- If you share the graph → don’t share the prompt text.
- If prod → log actions and errors.
- If eval → one variable.
- If 200+ / −42% appear → UNVERIFIED.
- If Plus / n8n-cloud as hive DB → park.

## G. Contrarian
Field keeps prompts in the node and logs nowhere. He puts both in a table. Field changes model+prompt+tools at once; he says isolate.

## H. Assumptions
Pre/post-GA tables as demoed. Falsifier: sync wipe races. We do not run n8n-cloud. Michael Scott is a toy.

## I. Questions
Do we already one-variable our `golden-test-loop` runs?

## J. Connections
SYSTEM SYNTHESIS: `QCjMBOEhpLE` filter-before-model. `EuzYhzB0vbI` checkable done. `golden-test-loop` = eval. No n8n-cloud. Prompt IP stays in repo, not a vendor table.

## K. Future-Use
One-variable eval card. Intermediate-step log on a real agent. Don’t export the system prompt.

## Steal / Operate-never

### Machine: prompt as IP → log intermediates → eval one knob
- **Epistemic:** SOURCE
- **Workflow / loop:** change one of {model, prompt, tools} → run → write in/out/actions/score → next knob
- **Questions / signals:** Can a client edit behavior without the graph? Do we know what run 33 changed?
- **Qualify / frame / objections:** Tables are a weak UI. Autonomy may add tools.
- **Procedure:** No n8n-cloud tables. No Plus course. No Sheets as hive brain.
- **Example that proves it:** Table-swapped Sonnet/Tavily; lunch log; 5 then 4.
- **Why it works:** You can’t improve what you don’t write down. Two knobs hide the cause. A shared graph shouldn’t leak the prompt.
- **Conditions / exceptions:** n8n-specific wiring. Tape % UNVERIFIED.
- **Operate-never payload:** n8n-cloud as prompt store; quote −42% as FACT; agency course.
- **Hive run:** `golden-test-loop`. Deploy HITL.
- **Source:** `lcNN3X9gXls` @ UNKNOWN

### Operate-never
- Move hive prompts into n8n-cloud.
- Quote eval $ / Tesla % as FACT.
- New hunt. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not add Data Tables. I will change one knob when I eval. Deploy HITL.
