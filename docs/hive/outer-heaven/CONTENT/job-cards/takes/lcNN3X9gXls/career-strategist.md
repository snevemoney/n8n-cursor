# Career Strategist — lcNN3X9gXls
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lcNN3X9gXls/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lcNN3X9gXls/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Caption ingest (~3791 words). Three n8n data-table hacks. Beats: (1) store model/prompt per workflow; client edits the table not the graph; share template without the prompt IP (2) dentists research example — swap Perplexity→Tavily + Sonnet 3.7 via the row (3) calendar + contact email (Michael Scott) (4) error logger table (5) evals: one variable per run; tokens/time/correctness. Client mention parked.

## B. Atomic Knowledge

### Config as data; eval one knob; template ≠ the prompt IP
- **Claim:** Prompts/models in a table let you change the brain without opening the graph. Evals only mean something if one variable moves. A shared JSON can hide the prompt behind a variable — that is an IP move, not a hunt.
- **Reasoning:** Same isolate-variable as any honest test (`X80ljdCPM_U`).
- **Mechanism:** Filter row → inject prompt/model.
- **Evidence:** “every single time you do another run, only change one variable.”
- **Conditions:** n8n data tables exist.
- **Exceptions:** Stronger model called extra tools (Perplexity *and* Tavily) — autonomy surprises.
- **Action:** No n8n tables. No client-facing prompt UI. Do steal one-knob evals.
- **Confidence:** high.
- **Source:** `lcNN3X9gXls`
- **Epistemic:** SOURCE

## C. Mental Models
Config vs graph. Prompt as IP. One-knob. Error table. Dentists are a demo industry.

## D. Procedures
If you ever eval: change one thing. Do not give a stranger the graph *and* the live send tools.

Questions: What changed this run? Is the prompt in the JSON?

## E. Examples
**Situation:** Swap model + tool in the table.  
**Action:** Rerun.  
**Reasoning:** Front-end for non-breakers.  
**Outcome:** Tavily + Sonnet; also still hit Perplexity.  
**Lesson:** Table works; autonomy adds tools.

## F. Decision Rules
- If two knobs moved, the eval is junk.
- If “client edits the table,” parked.

## G. Contrarian
Title: hidden hacks. Body: a lookup table and an eval hygiene lecture.

## H. Assumptions
**Theirs:** Client-safe table. **Ours:** n8n on-tape. IP-hide is a tactic, not a product.

## I. Questions
- What were hacks 2–3 besides models/prompts and evals? (error log / calendar — captions thin)

## J. Connections
- SYSTEM SYNTHESIS → `X80ljdCPM_U` (eval) / `TDHFkKSTJ30`.
- Stack Cursor + Grok.

## K. Future-Use
Unassigned: one-knob eval. Not an n8n data-table SOP.

## Steal / Operate-never

### Machine: one variable per eval; config out of the graph
- **Epistemic:** SOURCE
- **Workflow / loop:** change one knob → measure → do not send
- **Questions / signals:** Prompt leaked in the JSON? Two knobs?
- **Qualify / frame / objections:** Client-table is parked.
- **Procedure:** No n8n data tables for a client.
- **Example that proves it:** Tavily swap (E).
- **Why it works:** You cannot learn two changes (B).
- **Operate-never payload:** Client-editable prod table.
- **Hive run:** none
- **Source:** `lcNN3X9gXls`

### Operate-never
- Give a client a live prompt table on a sending workflow.
- Auto-write `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
Employment covers baseline. Steal one-knob evals. Do not steal n8n tables or a client UI. Clients parked.
