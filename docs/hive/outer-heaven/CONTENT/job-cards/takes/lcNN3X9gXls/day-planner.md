# Day Planner — lcNN3X9gXls
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lcNN3X9gXls/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/lcNN3X9gXls/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: Data Tables **hacks** (after `QCjMBOEhpLE`). Beats: (1) **models+prompts table** keyed by workflow — client edits table not canvas; share template without the IP (prompt lives in the table); Open Router model as a variable; dentist research → swap Tavily + Claude 3.7 (agent then used **both** tools); newsletter: topic/query + planning/editor prompts from rows; **daily sync** n8n↔Sheets (Sheets wins **dropdowns**; tables have four types, easy typo); (2) **action logs** — `return intermediate steps` on; replace Sheets with a table (ts, workflow, in/out, actions, tokens); Telegram → **create calendar “lunch with Michael Scott” + invite** (book/send); think → contact → calendar; error-logger table + error workflow (id/name/now); (3) **evals** in a table (input / expected / actual / score); isolate **one variable** per run; Tesla −42% score 5 then 4; optimize speed/cost/quality; Plus. Caption-only. Timestamp UNKNOWN.

## B. Atomic Knowledge
### Prompts-as-data; log the intermediates; eval one knob; calendar create is a book
- **Claim:** Behavior should live in a row you can change without opening the graph; logs need **intermediate steps** or you only see the smile; evals only teach if one thing changed; a “hack” that invites Michael is a hard step.
- **Reasoning:** Patterns in logs become guardrails; a typo’d model name is a silent miss (dropdowns).
- **Mechanism:** Filter row → inject model/prompt → (optional) log steps → eval vs expected.
- **Evidence:** “only change one variable.” / “return intermediate steps… game changer.”
- **Conditions:** n8n Data Tables + an agent node.
- **Exceptions:** Hiding the prompt is IP theater if the table is in the same instance.
- **Action:** Steal prompts-as-data + intermediate log + one-knob eval. Do not book lunch. Do not Plus.
- **Confidence:** high as the three hacks; scores UNVERIFIED.
- **Source:** `lcNN3X9gXls` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** demonstrated (speech)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** 3.7 called Perplexity *and* Tavily
- **Speech ≠ behavior:** “client won’t break the workflow” vs they can still break the prompt row

## C. Mental Models
Frontend for non-builders. IP in the prompt. Patterns are beautiful. Priority: tweak from logs. Uncertainty: Plus 200.

## D. Procedures
1. Key prompts/models by workflow name; filter one row.
2. If sharing a graph, don’t paste the secret prompt into the node (his) — we still don’t ship n8n templates as a SKU.
3. Turn on intermediate steps; log actions+tokens.
4. Error workflow writes workflow name + node + message.
5. Eval: one knob per run.
Avoid: Telegram→calendar invite; n8n-cloud; Plus.

## E. Examples
**Prompt swap:** Situation → dentists + Perplexity. Action → row → Tavily + 3.7. Reasoning → table is the frontend. Outcome → new tools/model without opening the agent. Lesson → steal the row.

**Michael Scott lunch:** Situation → “create event + invite.” Action → contact then calendar. Reasoning → log the steps. Outcome → event on the calendar. Lesson → steal the log; never the book.

## F. Decision Rules
- IF the prompt is only inside the node → you can’t A/B it from a table.
- IF logs lack intermediate steps → you can’t see the tool path.
- IF two knobs moved → the eval is junk.
- IF the agent can create a calendar event → HITL / never.

## G. Contrarian
He still uses Sheets as the nicer frontend. Field: “native tables replace Sheets.” He: sync both.

## H. Assumptions
Theirs: table = safe client UI. Ours: book/send still hard. Falsifier: a typed model name that never typos. Survivorship: one media agent.

## I. Questions
Eval tape id? Ultimate Media Agent tape?

## J. Connections
- SYSTEM SYNTHESIS → `QCjMBOEhpLE` · `golden-test-loop` · `send-removed` · `-Lo_SlSgtnA`.

## K. Future-Use
Prompts-as-data. One-knob eval. Unassigned error-logger.

## Steal / Operate-never

### Machine: row holds model+prompt → log intermediates → eval one knob; never auto-book
- **Epistemic:** SOURCE
- **Workflow / loop:** filter the workflow row → run → write steps+tokens → if eval, change one thing → compare score
- **Questions / signals:** Can a non-builder edit the row? Did we log tools? Two knobs? Can it invite?
- **Qualify / frame / objections:** “Smarter agent” that books lunch is the fail. One-knob score is the pass.
- **Procedure:** No Telegram calendar. No Plus. No n8n-cloud.
- **Example that proves it:** Situation → Tesla eval. Action → expected vs actual → 5 then 4. Reasoning → isolate. Outcome → a number. Lesson → steal the isolation.
- **Why it works:** A row and a score are checkable; a silent calendar invite is not a hack, it’s a book.
- **Conditions / exceptions:** Scores UNVERIFIED.
- **Operate-never payload:** Auto-calendar; Telegram send; Plus; n8n-cloud.
- **Hive run (existing skills only):** `golden-test-loop` · `send-removed`.
- **Source:** `lcNN3X9gXls` @ UNKNOWN

### Operate-never
- Book / invite / Telegram.
- Send / pay / deploy / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as prompts-as-data + one-knob eval. Clients parked.
