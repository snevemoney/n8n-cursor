# Forge — jdbOVepEtUE
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jdbOVepEtUE/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jdbOVepEtUE/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk, ~6h **“Claude Code for Non-Coders.”** Caption-only. Visual/click **UNKNOWN** (desktop UI, VS Code, GWS OAuth, slides, Obsidian graph, token dashboard). He says skip around; this walk is in order. Promise: non-coder → AI-native; one person ≈ old team UNVERIFIED. Content/education/certs/events/consulting. **Chat vs Cowork vs Code:** Cowork shipped in ~a week by a few engineers using Code UNVERIFIED; he skips Cowork; Code is the power product; skills transfer. Code can touch local files **and** Gmail/Slack/CRM (operate-never). **Harness** = model + car + you; you are the important layer; models/harnesses swap. Book *Becoming AI Native* / 12 mindset shifts (on-tape). **Six career skills:** (1) become the AI person in the current job — IBM 85% UNVERIFIED; one tool + one weekly workflow; document before/after; **permission before workplace automation**; if Claude banned, map the policy. (2) Taste/judgment — bullet→email→bullet joke; em-dashes; your name is on it; feed corrections back. (3) Context engineer (Karpathy) not prompt engineer; intern onboarding; don’t open blank chats; custom GPT / Claude project. (4) Iteration speed + **know when to stop**; bike/training wheels; ugly POC; **one North Star number before the build**. (5) Jarvis = unattended trigger; vending machine vs slot machine; default simplest; many “Jarvis” asks don’t need an agent. (6) Job-stacking / unemployment insurance — **do not quit**; check contract/non-compete; don’t burn the day job; one passion, multiple branches; building in public. Operate-never as a hunt. Mindset: AI-native = what your hand reaches for; don’t quit in the dip; you are a manager of agents (onboard, define good/bad, review, feed back). Install: Pro → Max $100/$200 UNVERIFIED; desktop; VS Code also fine; files survive switching UI. Demo: find AIS Live logo in Downloads via vision + PowerShell. Prompting: role / context / negative / **make it prove its work** (form email 100×). Tokens: in cheaper than out; Haiku/Sonnet/Opus/Fable prices UNVERIFIED; Excel sheet ~25k out / ~$0.63 Opus UNVERIFIED; 428k/1M in a session UNVERIFIED; **5-hour rolling + weekly**; $200 plan ≈ $8k inference UNVERIFIED. `CLAUDE.md` = system prompt + routing map; update it when output is bad. `.env` never pushed. Permissions: auto / manual / accept edits / plan / **bypass = dangerously skip**. He uses bypass in demos — hive never. Internal **150k discount blast** because the agent had the send tool, not because of mode. Scoped keys (11 Labs sound-effects key + credit cap). Settings.json allow/never; he puts never-delete then bypasses. Auto mode is “fine for now.” Fast mode he never touches. Effort: he stays on high; GPT 5.5 effort ↑ cost not score UNVERIFIED; Fable high overthinks. Privacy: closed models leave the house; don’t chuck legal/PII; consumer training opt-in/forced-choice Oct 8 UNVERIFIED; they still have the data. Connect tools: tabs/bookmarks = the list; Fireflies API → `.env`; **desktop connectors are easy and non-portable** — files+keys survive Codex/Hermes/OpenClaw/VS Code. GWS CLI OAuth (internal audience, enable APIs, retry); unread-email score; Slides need ChromeDev vision because programmatic ≠ see. Skills = recipes / markdown; YAML front matter = progressive disclosure; slash + skill-creator; two paths: plan-then-write vs **do-then-extract**; inbox skill starts **brief-only, no labels**; packaging skill routes to playbooks. Context rot = truck-driver hours; reset ~**250k–300k**; session-handoff → `/clear` → paste; blank session already 30–40k (system/MCP/skills); autocompact too late. Memory: `/memory` lives in terminal more than desktop; automemory on; memories are markdown. AI slop: reviewer not writer; “outsource thinking, never understanding”; phrase kill-list. **Five build mindsets:** constraint is the only place work compounds; don’t automate a broken process; AI-native ≠ AI-for-everything; can’t automate what you can’t map; one North Star before the build. Pipe/clog; “5× customers, what breaks first?” Ads $200k→$1M is visible; constraint work is not. Sub-agents: built-in researcher on cheaper model + clean context; custom = markdown like a skill; YAML name/description/model/color/disallowed tools; misfire = tune description; skill can fire agents; specialist > mega-assistant; awesome-claude-code-subagents = read-only verify first (prompt injection). Plan-roaster demo: unclosed YAML quotes = mechanical miss; roast skill collided; 22.8k on the sub did not pollute main. Websites: localhost ≠ shareable (meme). Five hacks: (0) `CLAUDE.md` as rules; (1) frontend-design skill always; brand_assets folder; (2) screenshot/Puppeteer loop; (3) clone inspiration (full-page shot + copied styles); still on bypass; name temp screenshots; (4) component inspiration; (5) GitHub + Vercel auto-deploy — **don’t push until told**; test localhost first. Claude Design = other course. Trust: deterministic vs slot; pyramid chatbot → workflow → AI workflow → agent; simplest for the job. Prompt layer ≠ tool layer; no send tool if draft-only. Inbox agent (huge prompt) failed; **objective routing** (contacts / @client.com / billing / VIP) had almost no failures. Forgot ClickUp weekly-stats **routine**: prompt said DM; it had every channel; weeks 1–5 OK then public; bike/nap. Evals = golden set before prod. Safe-failure language after Modal deploy. Wiki: 36 YT transcripts / AI 2027 article (~25 pages → ~23 wiki pages, ~10 min); Karpathy LLM wiki; Obsidian is a viewer; markdown+index+links; hot.md ~500 chars for EA, not for YT dump; lint; wiki beats RAG at hundreds of pages, RAG at millions. Five second-brain levels: exact word → topic pull → semantic → relationship chains → autonomous; **lowest that fits**; no pain = don’t upgrade; hoop/ball = reverse-engineer recall; `CLAUDE.md` as router; no proven standard folder tree. Routines: local (laptop open) vs cloud (Anthropic web); ~15/day UNVERIFIED; oneshot prompt; GitHub clone has no `.env` → cloud-environment vars; trusted vs full network (ClickUp needed full). Prompt cache: 1h TTL on sub; API/sub-agents 5 min default; cache read ~10× cheaper; don’t pause >1h; `/clear` or handoff when switching tasks; **model switch / opus-plan breaks cache**; edit `CLAUDE.md` mid-session is safe until restart. Token dashboard = local GitHub repo. Close: 4Cs AIOS (context / connections / capabilities / cadence) is a **different 2h Skool course** — this tape is not the OS finish. Plus community. On-tape: Claude Code, Cowork, Codex, Hermes, OpenClaw, Skool, ClickUp, Slack, Gmail, Fireflies, Gamma, Nano Banana, Modal, Vercel, Obsidian, 11 Labs. Cursor + Grok only.

## B. Atomic Knowledge

### You are the harness layer; files survive the product
- **Claim:** Model + harness + you. You steer. Chat / Cowork / Code are close enough that skills transfer; Code is the power product because it can touch local files and connected tools. Cowork shipped fast *using* Code UNVERIFIED. Desktop connectors are easy and die when you switch harness or even desktop→VS Code. `.env` + folders + markdown skills/agents are tool-agnostic. `CLAUDE.md` orients a fresh session (role + routing map) and gets rewritten when output is bad.
- **Reasoning:** Tools and models swap weekly. The durable asset is the intern-onboarding packet, not the UI.
- **Mechanism:** One folder. Secrets in `.env` (gitignored). Skills/agents as markdown. Ask the model to write the never-do list into settings. Do not rely on in-app connectors.
- **Evidence:** Hello → AIS Live / CIA dates from `CLAUDE.md`. GWS CLI after OAuth retry. Connector warning.
- **Conditions:** You will switch products. You have a local folder you own.
- **Exceptions:** Auto mode is “fine” for beginners; he still bypasses in demos.
- **Action:** Steal folder-as-OS + rewrite-the-router. Do not install Claude Code / Cowork / Codex / Hermes / OpenClaw.
- **Confidence:** high as a thesis.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** GWS OAuth not first-try; Slides couldn’t see until ChromeDev
- **Speech ≠ behavior:** “auto is fine” vs he demos on bypass

### Permission is a tool, not a sentence
- **Claim:** Prompt “never send / never delete” is a suggestion. The 150k discount blast happened because the agent *had* the send tool, not because of bypass. Intern test: would you give a new hire this card? Scoped keys (name + credit cap + endpoint). Settings never-delete then he bypasses. ClickUp weekly-stats routine: “DM me” for five weeks, then public channels — he forgot it existed. Bike: helmet/training-wheels/hold; do not nap. Inbox: huge-prompt agent failed; objective routing (in contacts? domain? VIP?) almost never failed. Draft tool in; send tool out.
- **Reasoning:** If it can read/touch/use it, assume worst case. Prompt cloudiness on the 101st run.
- **Mechanism:** Remove the tool. Scope the key. Watch the first N runs. Brief-only before labels. Human sends.
- **Evidence:** 150k / 100k list blast (same family as `RzLV8sfFdMM` / `8QQ_INxAhRs`). Forgot routine. Routing vs agent inbox.
- **Conditions:** Anything unattended (routine, schedule, Modal, cloud clone).
- **Exceptions:** Human-triggered skill with you watching is lower risk (bottom of his pyramid).
- **Action:** Steal tool-layer + watch-first + objective routing. Never bypass. Never auto-send.
- **Confidence:** high.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** blast; forgot routine; plan-roaster YAML quotes; roast-skill collision
- **Speech ≠ behavior:** “I’ve never had bypass be an issue” vs he tells the blast and the rogue DMs

### Constraint + North Star before the build; simplest machine
- **Claim:** Five mindsets: work compounds only at the constraint; automating a broken process scales the break; AI-native ≠ AI-for-everything; you can’t automate what you can’t map; pick one number *before* you build. Pipe: 5× customers tomorrow, what breaks first? Then the next clog appears. Vending vs slot. Pyramid: chatbot/skill (you trigger) → deterministic workflow → AI-in-the-middle workflow → agent (unknown tool order). Default down. Jarvis/unattended only when airtight. Ads $200k→$1M is visible; constraint work needs the number or nobody sees ROI.
- **Reasoning:** Sexy sales-agent demos are not the clog. Productivity without a metric looks like no ROI.
- **Mechanism:** Map the pipe. Name the number. Build the lowest layer that works. Stop when the number moves.
- **Evidence:** Five-leads→fifteen example. Stripe-to-Slack at 9am does not need an agent.
- **Conditions:** You can walk the day-to-day. Process exists (or you map it first).
- **Exceptions:** Front-of-pipe “no water” is a lead problem, not an agent problem.
- **Action:** Steal pipe question + North Star + default-simplest. Do not hunt HVAC/inbox/e-com. Tape $ UNVERIFIED.
- **Confidence:** high as method.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** consulting jobs with no metric
- **Speech ≠ behavior:** “Jarvis” branding vs most asks should not be agents

### Context rot, skills, specialists, cache
- **Claim:** ~1M window; he resets ~250–300k with session-handoff (shift doc: locked decisions, key files, running state, verification, deferred, open questions) then `/clear` + paste. Autocompact too late. Progressive disclosure so 40 skills don’t all load. Skill = recipe in the main thread; sub-agent = clean context + optional cheaper model + parallel. Description is the trigger; misfires get rewritten; unclosed YAML is mechanical. Specialist assembly line > one mega-assistant. Open-source agent markdown: read-only verify (injection). Cache: 1h TTL on subscription; API/sub-agents 5 min; don’t pause >1h; model switch / opus-plan **reaches** everything; `CLAUDE.md` edit mid-session waits for restart. Tokens in < out; subscription ≠ API; $200 ≈ $8k inference UNVERIFIED.
- **Reasoning:** Truck-driver hours. Blank session already 30–40k. Cache hit rate is how limits feel generous.
- **Mechanism:** Handoff before dumb zone. YAML short. Sub-agent for research/roast. Don’t switch model mid-task.
- **Evidence:** 112k → 0 → 55k after handoff. Plan-roaster 22.8k off-main. 91M cached ≈ 9M billed UNVERIFIED.
- **Conditions:** Long sessions. Many skills. Paid sub.
- **Exceptions:** Terminal has `/memory` the desktop may not.
- **Action:** Steal handoff + YAML + specialist + cache habits. Do not quote 250k/1M/$8k as FACT. Do not install his token dashboard.
- **Confidence:** high as practice; numbers UNVERIFIED.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** roast vs plan-roaster collision
- **Speech ≠ behavior:** opus-plan as a “token hack” vs he now says it breaks cache

### Taste, slop, career: stay, document, don’t quit
- **Claim:** Writer→reviewer. Name on it = credit and blame. Kill-list. IBM 85% UNVERIFIED = every function becomes tech-in-domain. One tool + one weekly workflow + before/after + **ask permission**. If banned, map the policy. Job-stack = insurance (one passion, many packages); **do not quit**; contract/non-compete; don’t burn the day job. Building in public as default discoverability (AI search). Don’t quit in the dip. AI-native = what the hand reaches for.
- **Reasoning:** Relative “AI person” gets pulled into the task force before a title exists. Excel analog.
- **Mechanism:** Document the weekly win. Review everything that leaves with your name. Branch after momentum, not five domains at once.
- **Evidence:** Bullet↔email joke. Em-dash tell. His streams all off one north star.
- **Conditions:** Employment exists. Policy allows *something*.
- **Exceptions:** Quiet consulting / faceless product if public isn’t you.
- **Action:** Steal permission-first + reviewer + one-workflow. Do not hunt job-stacks. Do not quote 85% as FACT.
- **Confidence:** high as advice; study UNVERIFIED.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** slop trust leak
- **Speech ≠ behavior:** “don’t quit” vs he sells the course/community as the path

### Wiki at human scale; localhost is not live; 4Cs is another tape
- **Claim:** Karpathy wiki: ingest → organize → index/links → query. Obsidian is a viewer. Hundreds of pages + lint + hot cache (EA) beats RAG; millions of docs → traditional RAG. Five levels; pick the lowest that fits; no pain = don’t upgrade. Reverse-engineer how you will ask (hoop/ball). `CLAUDE.md` as router or it will not search the tree. Localhost ≠ friend’s laptop. GitHub+Vercel only after “push”; test local first. Cloud routines clone the repo without `.env` — keys go in the cloud environment; trusted network blocked ClickUp until **full**. 4Cs AIOS (context/connections/capabilities/cadence) = **2h Skool sequel**, not this finish.
- **Reasoning:** Chats are ephemeral; files compound. Fancy architecture without a recall question is a square ball.
- **Mechanism:** Markdown + index + “don’t read the wiki unless needed.” Name screenshot files. Don’t push until told.
- **Evidence:** 36 YT ingest ~14 min; AI 2027 ~10 min / 23 pages. Token drop when EA pointed at wiki vs stuffing context files.
- **Conditions:** Small-to-medium personal/business corpus.
- **Exceptions:** He is not on level 5 and argues against it.
- **Action:** Steal router + lowest-level + localhost≠live + don’t-push. Do not take the Skool sequel as SOP. Deploy HITL.
- **Confidence:** high as method; timings UNVERIFIED.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** trusted network vs ClickUp; localhost-share meme
- **Speech ≠ behavior:** “5 minutes to set up” vs 10–14 min ingests and a 6h course

## C. Mental Models
Harness = car; you drive. Intern, not magic. Manager of agents. Prompt is a suggestion; tools are the wall. Touch-assume. Vending vs slot. Pyramid of autonomy = pyramid of risk/cost. Constraint/pipe; 5× what breaks. North Star before build. Map or you cannot automate. AI-native ≠ AI-everywhere. Recipe skills; specialist subs. Progressive disclosure. Context rot / truck-driver. Session = shift handoff. Reviewer, not writer; name on it. Bike/training wheels; do not nap. Safe failure. Files+folders survive products. Connectors are a trap. Lowest second-brain level that fits. Hoop before ball. Cache is a habit. Localhost ≠ live. 4Cs is another course.

## D. Procedures
1. Do not install Claude Code / Cowork / Codex / Hermes / OpenClaw / Skool / Modal / GWS CLI. Cursor + Grok.
2. Do not hunt job-stacks, inboxes, HVAC, or mid-market. Clients parked. No new `icp_id`.
3. Workplace: permission first. If banned, map the policy. Do not chuck legal/PII.
4. One tool + one weekly workflow. Write before/after. Stop when the North Star moves.
5. Before a build: can you map it? Is the process already broken? 5× volume, what breaks first? One number.
6. Default the lowest pyramid layer. No agent if a vending machine works. No send tool if you only want drafts.
7. Watch the first N runs. Brief-only before labels/sends. Human sends / pays / deploys.
8. Skills: do the work once, then extract — or plan 1-2-3-4, then write. YAML short. Tune misfires.
9. ~250–300k or task-switch: handoff doc → clear → paste. Do not wait for autocompact. Do not pause >1h. Do not switch model mid-task.
10. `CLAUDE.md` / AGENTS.md is a router. Update it when output is bad. Don’t read the whole wiki unless needed.
11. Localhost first. Do not push / deploy until Evens says. Cloud clones have no `.env`.
12. Reviewer pass + kill-list before anything leaves with a name. Tape $ UNVERIFIED. HITL.

## E. Examples
**Situation:** Agent had CRM/list send.  
**Action:** It emailed a discount to ~150k.  
**Reasoning:** Tool existed; prompt did not hold.  
**Outcome:** Blast. Case study internally.  
**Lesson:** Prompt ≠ permission.

**Situation:** Weekly YouTube/Skool stats routine, “DM me.”  
**Action:** He forgot it for two months.  
**Reasoning:** Prompt-only wall; every ClickUp channel in scope.  
**Outcome:** Weeks 1–5 DM; then public.  
**Lesson:** Bike/nap. Unattended needs a tool wall.

**Situation:** Inbox “agent” with a massive rule prompt.  
**Action:** Rebuilt as objective routes (contacts / domain / billing / VIP) + small AI extract/summarize.  
**Outcome:** Almost no failures; cheaper; visible trail.  
**Lesson:** Decisions on facts, generation in a box.

**Situation:** Inbox skill first run, 201 unread.  
**Action:** Last 25–30; brief only; no labels until 10–30 watched runs.  
**Lesson:** Autonomy is earned.

**Situation:** “Roast my ice-cream stand.”  
**Action:** Roast *skill* fired (5 general subs); then YAML quotes unclosed so plan-roaster missed; then explicit invoke.  
**Outcome:** 22.8k stayed off-main.  
**Lesson:** Collision + mechanical YAML. Tune the trigger.

**Situation:** Google Slides via CLI.  
**Action:** Could build, could not see; ChromeDev screenshots; skill updated.  
**Lesson:** Prove-it needs eyes. Small window = worse shots.

**Situation:** Clone a live site + frontend-design + brand_assets.  
**Action:** Full-page shot + copied styles; screenshot loop; then brand; GitHub→Vercel after “push.”  
**Lesson:** Localhost first. Don’t push until told.

**Situation:** Cloud routine to ClickUp.  
**Action:** Repo clone had no `.env`; trusted network blocked; full + env vars worked.  
**Lesson:** Unattended ≠ local secrets.

**Situation:** EA stuffed with context files → pointed at wiki + hot.md.  
**Action:** “Don’t read wiki unless needed.”  
**Outcome:** Token use dropped (his claim).  
**Lesson:** Router + progressive read.

## F. Decision Rules
- IF no permission / policy unknown → do not automate the workplace.
- IF you cannot map the process → do not automate it.
- IF the process is broken → fix, don’t scale.
- IF 5× volume wouldn’t hit this step first → not the constraint.
- IF no North Star number → do not start the build.
- IF a vending machine works → no agent.
- IF draft-only → no send/delete tool.
- IF unattended (routine/schedule/cloud) → tool wall + watch first N + you still own send/deploy.
- IF output is slop / em-dashes / wrong facts → your name is on it; update the router + kill-list.
- IF context ~250–300k or task change or idle >1h → handoff + clear.
- IF model switch / opus-plan → assume cache is dead.
- IF desktop connector is the easy path → files+keys instead (portable).
- IF downloading someone else’s agent markdown → read-only verify first.
- IF localhost URL → not shipped.
- IF no pain in the second brain → do not climb a level.
- IF they want Jarvis overnight → bike, not nap.
- IF job-stack urge → do not quit; one passion; contract first. Not a hive hunt.

## G. Contrarian
Field sells “prompt engineering” and blank chats. He says context engineering + intern onboarding. Field wants Jarvis/agents. He wants the simplest layer and says most Jarvis asks are Stripe-to-Slack. Field uses in-app connectors. He says they trap you. Field trusts “never send” in the prompt. He had a 150k blast and a rogue ClickUp routine. Field waits for autocompact. He handoffs at ~25% of the window. Field climbs to RAG/level-5 autonomy. He stays at markdown+index until it hurts. Field says quit and stack five gigs. He says don’t quit, one north star. Field treats Claude Design / 4Cs / Skool as this course. He parks them as sequels.

## H. Assumptions
IBM 85%, $100/$200, $8k inference, 428k/1M, 250k reset, 5-hour/weekly, 15 cloud routines/day, 150k/100k blast, 91M cached, 36 videos / 14 min, AI 2027 10 min / 23 pages, $0.63 Excel, model $ / 1M, Oct 8 training toggle, Cowork “one week” = **UNVERIFIED**. Caption-only: UI paths UNKNOWN. WAT named, not defined. Clients parked. On-tape vendors stay on-tape.

## I. Questions
Which one weekly workflow already has permission and a before/after clock?  
What is the pipe clog if volume went 5× tomorrow (no new hunt)?  
Where do we still treat a prompt as a wall?  
Which send/delete tools exist that should not?  
When do we handoff vs keep talking?  
Is the second brain in pain, or are we climbing levels for sport?

## J. Connections
SYSTEM SYNTHESIS: `RzLV8sfFdMM` director sandwich / dumb zone ~250k / touch-assume / list blast. `8QQ_INxAhRs` discount-list family. `8MEJen0nblQ` jobs ≠ tasks; last mile; HVAC phone. `U6k4MeVks_Y` plan+dump; folder survives `/clear`; talk-to-deploy operate-never. `3TdD8Qv5Tk8` persist misses; localhost ≠ Vercel; watch first cron. `0YXjEzFfft8` SOP=prompt; amplification ≠ automation. `gb5TlGw6Uks` named keys; intern not your Gmail; one container per job. `8ktcSaSTvxk` constraint/pipe; relief not RAG; T&M→0. Hive: `ask-principal`, folder-as-memory, HITL send/pay/deploy. Cursor + Grok only.

## K. Future-Use
Harness-you. Tool-layer > prompt. Touch-assume. Constraint/pipe + North Star. Default-simplest / vending. Session-handoff. Progressive disclosure. Specialist subs. Portable files not connectors. Reviewer + kill-list. Permission-first at work. Wiki at human scale. Localhost≠live. Cache habits. Safe-failure language. Unassigned. No new `icp_id`.

## Steal / Operate-never

### Machine: map → number → lowest layer → tool wall → watch → handoff → rewrite the router
- **Epistemic:** SOURCE
- **Workflow / loop:** permission → map the process → name the North Star → pick chatbot/skill vs deterministic route vs AI-in-the-middle vs agent → remove send/delete if not needed → brief-only / human-trigger until N clean runs → prove-it (eyes, tests, golden set) → when context rots or task changes, shift-handoff → write the miss into `CLAUDE.md` / skill YAML / never-do settings
- **Questions / signals:** Would I give an intern this key? Can I map it? Is the process already broken? 5× what breaks? Does a vending machine work? Is this unattended? Is localhost the live URL?
- **Qualify / frame / objections:** “Jarvis” usually isn’t. Connectors are a trap. Prompt “never” is not a wall. Autocompact is late. 4Cs/Skool is another tape. Job-stack is insurance, not a quit plan.
- **Procedure:** D 3–12.
- **Example that proves it:** 150k blast; forgot ClickUp routine; routing inbox vs agent inbox; plan-roaster YAML; Slides need eyes; trusted-network ClickUp; localhost meme.
- **Why it works:** Models forget; tools don’t. Files compound; chats don’t. Constraints compound; sexy demos don’t.
- **Conditions / exceptions:** Tape $ / IBM / limits UNVERIFIED. Caption-only. He still bypasses on camera.
- **Operate-never payload:** Claude Code; bypass; Gmail/Slack/ClickUp auto-send; Modal/Vercel push; Skool 4Cs as SOP; job-stack hunt; quote $200 / $8k / 85% / 250k as FACT.
- **Hive run:** `ask-principal` before send/pay/deploy. Cursor + Grok. Clients parked.
- **Source:** `jdbOVepEtUE` @ UNKNOWN

### Operate-never
- Do not install Claude Code / Cowork / Codex / Hermes / OpenClaw / Skool / Modal / GWS CLI / 11 Labs.
- Do not enable bypass / dangerously-skip. Do not auto-send email/Slack/ClickUp.
- Do not hunt job-stacks, inboxes, or “become the AI person” as a client motion. No new `icp_id`.
- Do not quote IBM 85% / $200 / $8k inference / 5-hour / 1M / 250k / 150k / 91M as FACT.
- Do not treat the 2h 4Cs Skool course as this tape’s finish or as hive SOP.
- Send / pay / deploy / publish HITL. Cursor + Grok only.

## L. Role-Specific Applications
Forge steals **you-are-the-harness**, **tool-layer > prompt**, **touch-assume**, **constraint/pipe + North Star**, **default-simplest**, **watch-first / bike-not-nap**, **session-handoff**, **portable files**, **reviewer + kill-list**, **localhost≠live**, **lowest wiki level**. Does not steal Claude Code, bypass, Gmail/Slack MCP, job-stack hunting, Skool 4Cs, or tape dollars as FACT. Cursor + Grok.
