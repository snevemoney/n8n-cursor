# Lead Hunter — oWdJMJp2HgM
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/oWdJMJp2HgM/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/oWdJMJp2HgM/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** n8n JUST Leveled Up AI Agents With Guardrails
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Long of `NQhsLVmuItA`: need 1.119. Check-text (AI) vs sanitize (no AI). Types: keywords; jailbreak (Arena sad clip); NSFW; PII; secret keys (caught API key, passed “password password” — customize); topical alignment (n8n scope fails NBA); URLs allow-list + HTTPS schema (upai pass, others fail, HTTP fail); custom prompt+threshold. Sanitize: PII (real phone still in a log field), keys, URLs→“visit URL”, custom regex. Pass→email/CRM; fail→Slack or hard error. School template. Thresholds 0.8–0.95 on tape UNVERIFIED.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### Sanitize before the model; fail can hard-stop; secret-key node misses the word password
- **Claim:** Put non-AI sanitize in front of any LLM; AI-check outputs before client/DB; fail may error the workflow — and the stock secret guard missed “password” while catching a key-shaped string.
- **Reasoning:** Arena needed jailbreak detect. Topical alignment is a Slack-scope analog.
- **Mechanism:** Sanitize inbound → model → check outbound → pass-only side effects. Customize password if needed.
- **Evidence:** SOURCE: “sanitize text… doesn't use AI.” “it passed use my password password for the database… failed the API key.” “make the whole workflow stop and just throw an error.” School capture.
- **Conditions:** You know what must never proceed.
- **Exceptions:** upai allow-list is his site. Thresholds UNVERIFIED. Slack-on-fail can spam.
- **Action:** Steal sanitize-first + pass-only + customize-password. Do not hunt guardrail buyers. Do not auto-send on pass.
- **Confidence:** high
- **Source:** `oWdJMJp2HgM` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Keyword match is faster than AI-check.
- SOURCE: You can log the real phone while sending a redacted copy to the model.

## D. Procedures
- Sanitize PII/keys before the model. Check jailbreak/NSFW/topic/URL on the way out. Fail = stop or HITL notify. Test a password row.

## E. Examples
- Situation: password vs API key. Action: stock secret guard. Reasoning: looks for key shape. Outcome: password passed. Lesson: customize; don’t trust the default.

## F. Decision Rules
- If pass can send, HITL.
- If password isn’t in the secret list, add it.
- If HTTP is allowed, phishing path is open.

## G. Contrarian
- Rejects prompt-the-agent-to-be-careful as enough. Hive agrees on fail-closed.

## H. Assumptions
- Arena / upai / School on-tape. Jailbreak examples are tests, not tutorials to operate.

## I. Questions
- What threshold does he run in production? Demo only.

## J. Connections
- SYSTEM SYNTHESIS: `NQhsLVmuItA` short.
- SYSTEM SYNTHESIS: Watchdog / `send-removed`.

## K. Future-Use
- Unassigned: password-miss as a default test row on any secret guard.

## Steal / Operate-never

### Machine: Sanitize-in, check-out, fail-stop; test the password row
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** Inbound sanitize → model → outbound check → checkable stop = fail did not send and password row was tested.
- **Questions / signals:** Did password pass? Is HTTP blocked? Who gets the Slack?
- **Qualify / frame / objections:** Frame: fail-closed. Objection: “native guardrails so we’re safe” → password miss.
- **Procedure:** Do not auto-send on pass. Do not enter School. Do not hunt Arena.
- **Example that proves it:** Situation: keyword/jailbreak/PII/key/topic/URL. Action: pass/fail demos. Reasoning: before/after the model. Outcome: template CTA. Lesson: steal sanitize+fail-stop; park the node as a SKU.
- **Why it works:** From B: password miss and hard-error are explicit.
- **Conditions / exceptions:** Works as a checklist. Exception: paraphrase attacks remain.
- **Operate-never payload:** Auto-send. School. Quote 0.95 as FACT.
- **Hive run (existing skills only):** `send-removed`. Watchdog. `ask-principal`.
- **Source:** `oWdJMJp2HgM` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Auto-send on pass. Trust default secret guard. Hunt guardrail setups. Enter School.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal sanitize-first + password-test + fail-stop. No hunt. No auto-send.
- Hard step stays HITL: this desk drafts; Evens sends.
