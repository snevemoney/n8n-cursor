# Forge — eRS3CmvrOvA
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/eRS3CmvrOvA/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/eRS3CmvrOvA/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate Herk “**400 hours** in Claude Code” → six **boring** skills businesses pay for (time / money / fewer mistakes), not fancy-for-YouTube. Beats: same pains across RE / HVAC / coaches / marketing agencies → (1) **Skill Creator** (Anthropic official): talk like a coworker / drop an SOP → it drafts, tests, packages; factory for every other skill; `/plugin install`; he installs **user-global**; skill vs **plugin** (hooks/MCP) — he’ll still say “skill”; “I’m not an AI avatar” aside → (2) **Superpowers**: plan first, isolated env, tests before code, two-stage review (spec + quality); kills rush-to-code; HVAC dispatch / agency reporting; first pass **80% vs 60%** UNVERIFIED; **150k** GitHub stars UNVERIFIED; QA is still your job → (3) **GSD** (“get done”): **context rot** mid-window (sloppy, forgets, fake-done); fresh **sub-agent per task**; gates: dropped-requirement detector, security vs threat model; **autonomous** plan/execute/**commit**/walk-away; **not** a token saver (subs cost $) — saves redo hours → (4) built-in `/review` (local, fast) + `/ultra-review` (Opus 4.7+): upload branch, parallel reviewers (logic/security/perf/edges), bugs must be **reproduced** before they list; **10–20 min** background; need **2.1.86+** and a Claude **account** (API key alone no); use `/review` always, ultra before merge on **payments / auth / migrations**; Pro/Max **3 free** then **$5–20**/run UNVERIFIED → (5) **Context Mode**: raw tool dumps (Playwright **56kb**, GH issues **59kb**) → **~40%** window garbage in **30 min** UNVERIFIED; sandbox → only the needed bytes (**56kb→299b**, **315kb→5kb** claimed); local SQL of edits/tasks/decisions/errors; after compact, reinject snapshot; sessions **30 min → 3h** claimed → (6) **ClaudeMem**: cross-session; hooks capture edits/decisions/fixes; SDK compress → local SQLite + vectors; auto folder CLAUDE.md; **three-layer** retrieve (index → timeline → details); repo **~10×** retrieval save UNVERIFIED; local web viewer; **don’t** `npm install` the SDK-only path (hooks never register) → bonus (7) Anthropic **frontend-design** (less “AI slop”; also Claude Design) → sell **outcomes** (10h/week, fewer admin errors, more leads), not the workflow; if new, **pick one** skill, demo; Skool guide + “how I make money in 2026.” Timestamp UNKNOWN. Claude / Skool on-tape.

## B. Atomic Knowledge

### Plan-test-review + fresh context + don’t dump raw tool output
- **Claim:** The six are a factory (create skills), a senior loop (plan/test/review), a clean window per task, a real review before money-merge, a garbage filter, and a cross-session handoff.
- **Reasoning:** Fancy skills are YouTube. Clients pay for systems that work. Context rot and raw snapshots are why mid-session lies about done.
- **Mechanism:** Creator → Superpowers loop → GSD fresh workers → `/review` always, ultra on auth/pay/migrate → Context Mode bytes → Mem index-then-details.
- **Evidence:** His 400h / cross-industry pattern (on-tape). Stars / kb / 10× UNVERIFIED.
- **Conditions:** Claude Code plugin universe. We do not run it.
- **Exceptions:** GSD autonomous commit is a walk-away he offers — operate-never for us. Ultra is not free.
- **Action:** Steal the loop and the handoff shape. Do not `/plugin install` any of the six. Do not auto-write SKILL.md from this tape.
- **Confidence:** high on the loop; 400h / 150k / 56kb / $5–20 UNVERIFIED.
- **Source:** `eRS3CmvrOvA` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Boring sells. Skill ≠ plugin. 80% first pass is a win vs 60%. Tokens spent on clean context beat tokens spent on redo. Review that must reproduce a bug is different from style nits. CLAUDE.md by hand still matters; Mem is his automation of it. Outcome sentence is the pitch (same as `w9-gfaV5vlM`).

## D. Procedures
1. Do not install Skill Creator / Superpowers / GSD / Context Mode / ClaudeMem / frontend-design. 2. Plan → tests → two-stage review. 3. New worker when the window rots. 4. Review before merge; extra eyes on pay/auth/migrate. 5. Don’t paste raw snapshots into the thread. 6. Handoff = index → timeline → details (`6cEQEba0i2A`). 7. Sell (if ever) hours/errors, not the plugin name.

## E. Examples
**Situation:** RE agency wants property descriptions.  
**Action:** Skill Creator from an SOP vs hand-rolled skill.md.  
**Reasoning:** Factory, not the SKU.  
**Outcome:** Compresses the curve (claimed).  
**Lesson:** We already write skills by hand in Cursor — no Claude factory.

**Situation:** Mid-session fake-done.  
**Action:** GSD fresh sub-agent + gates.  
**Reasoning:** Context rot.  
**Outcome:** Redo hours saved, tokens not saved.  
**Lesson:** New thread + handoff, not a plugin.

**Situation:** About to merge payments.  
**Action:** `/ultra-review` 10–20 min.  
**Reasoning:** A prod bug costs more than the run.  
**Outcome:** Reproduced bugs only (pitch).  
**Lesson:** Extra review on money paths — HITL, not a $20 Claude fleet.

## F. Decision Rules
- If the skill is for a cool video → skip.
- If the window is rotting → new worker + card, don’t pile on.
- If pay/auth/migrate → extra review, human merge.
- If 400h / 150k / 56kb / $5–20 / 10× appear → UNVERIFIED.
- If the CTA is install-these-six + sell HVAC → park.

## G. Contrarian
Field installs a zoo of skills. He says six boring ones. Field treats GSD as a token win; he says it costs more tokens. Field one-shots prod; Superpowers is “slow down.”

## H. Assumptions
400h and cross-industry clients as told. Falsifier: community plugins rot faster than they help. We do not run Claude. Autonomous commit is a loaded gun.

## I. Questions
Is `golden-test-loop` already Superpowers-without-the-plugin? Do we dump raw browser snapshots into threads?

## J. Connections
SYSTEM SYNTHESIS: `6cEQEba0i2A` / `kB9iMD0EjT8` handoff. `EuzYhzB0vbI` checkable done. `XNQBCRcwXV4` don’t hobble with too many skills — tension: this tape *adds* six. Keep both: factory+loop, don’t zoo. `w9-gfaV5vlM` outcome pitch. No Claude plugins. No new SKILL.md.

## K. Future-Use
Two-stage review (spec + quality). Index-then-details memory. Extra review on pay/auth/migrate.

## Steal / Operate-never

### Machine: factory one skill → plan/test/review → fresh window → review money paths → don’t dump raw I/O → handoff index
- **Epistemic:** SOURCE
- **Workflow / loop:** describe the job → plan+tests → isolated build → spec review + quality review → new worker if rot → extra review on pay/auth/migrate → handoff card
- **Questions / signals:** Is this fancy-for-tape? Is the window rotting? Is this a money path?
- **Qualify / frame / objections:** Boring wins. GSD is not cheaper tokens. Ultra is not free.
- **Procedure:** No `/plugin install`. No autonomous commit. No HVAC hunt.
- **Example that proves it:** Property-description factory; mid-session fake-done; payments ultra-review.
- **Why it works:** Clients buy working systems. Rot and raw dumps make the model lie about done.
- **Conditions / exceptions:** Claude-specific installs. Tape counts UNVERIFIED.
- **Operate-never payload:** Claude plugin zoo; auto-commit walk-away; quote 150k/$5–20 as FACT; sell the six as a SKU.
- **Hive run:** `golden-test-loop` + `session-bootstrap` + `ask-principal` on merge. Deploy HITL.
- **Source:** `eRS3CmvrOvA` @ UNKNOWN

### Operate-never
- Install Claude / Superpowers / GSD / Context Mode / ClaudeMem.
- Auto-commit / walk-away prod.
- New hunt (HVAC/RE/coaches). Quote tape $ / stars as FACT.
- Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md.

## L. Role-Specific Applications
I will not install the six. I will plan/test/review and hand off when the window rots. Money paths stay HITL.
