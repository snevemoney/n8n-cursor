# Communications Manager — jZgcWCzxh1I
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jZgcWCzxh1I/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jZgcWCzxh1I/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
**Title (PACKET):** Claude Code Dynamic Workflows Clearly Explained
**Speaker / channel:** Nate Herk | AI Automation
**Kind:** tutorial · 3948 words · captions in `full.txt` only · timestamps **UNKNOWN** (no VTT used).
**Gaps:** Caption ingest; ASR errors possible (Naden/Nitn = n8n). Visual UI not fully narrated. Timestamps UNKNOWN.

Beats, in order:
- Open: a dynamic workflow audits his skills — 41 Haiku scorers in parallel, one Opus synthesizer, ~5M input tokens, HTML ranked worst→best plus patterns. Token-heavy, not output-heavy.
- Opus 4.8 shipped dynamic workflows. He was confused vs skills / sub-agents / teams / /goal. One test prompt burned ~half of a $200/mo plan in ~30+ minutes — UNVERIFIED. Ladder: more agents = more function = more risk and money.
- Skill = reusable recipe (can nest anywhere). Sub-agent = parallel, own window, talks only to main. Agent team = small crew, shared task list, they talk (war room). Workflow = Claude writes a JavaScript file that fans out many lone sub-agents; results merge back. The plan lives in the script, not the chat.
- Lovable mid-roll: one-click publish, connectors, MCP — operate-never for us (publish / n8n-cloud). He started sites in Lovable then moved to Code.
- He mostly will not use workflows: his work is automations + knowledge work, not giant PR sweeps. Desktop-crawl example ate the tokens. Know the what; you do not always need the how. Pain-point test: does it solve a problem you have today?
- Width vs depth: /goal loops until done=true (can run long). Workflow is width — 50+ agents execute a plan, no endless done-check. Combining them is a money burn. Bound the scope, name the deliverable, put workers on Haiku. /deep-research is a workflow that votes on claims.
- Use width when the job breaks into independent pieces (400-file migration). Single edit / a letter / general knowledge work → do not. Invoke: he had to say yes; can view the raw script; /workflows to watch/stop. Default save was global; he moved files into the project. Ultra = X-high + default workflows, bypasses a lot of permission friction — expensive. ‘Workflow’ in speech only rainbow-highlights; say ‘set me up a dynamic workflow.’ Slide: ask → skill → sub-agent → team → /goal → workflow. School CTA.

## B. Atomic Knowledge

### Width is not depth — a letter is not a 41-agent workflow
- **Claim:** Workflows fan out many silent workers and merge. /goal keeps looping until a criterion is true. A draft does not break into 41 independent files.
- **Reasoning:** Each worker is a full model call with its own window. Vague scope + width = half a plan gone.
- **Mechanism:** Bound the job. Name the deliverable. Confirm before any fan-out. Do not run Ultra on a sentence.
- **Evidence:** 41 Haiku + Opus; half of $200; desktop crawl.
- **Conditions:** A job that truly shards.
- **Exceptions:** $200 / 5M / 30 min UNVERIFIED. Claude workflows as ours is never. Auto-write SKILL.md from an audit is never.
- **Action:** Steal: confirm + bound + Haiku workers. Do not invoke a workflow to write mail.
- **Confidence:** high
- **Source:** `jZgcWCzxh1I` @ UNKNOWN
- **Epistemic:** SOURCE

### The plan can live in a script you must say yes to — Ultra tries to skip the yes
- **Claim:** He had to explicitly allow the run and could read the JS. Ultra defaults every prompt toward workflows and ‘bypasses a lot of those permissions.’
- **Reasoning:** Accidental orchestration is the cost center. The word ‘workflow’ in speech is not enough — good. Ultra is the opposite.
- **Mechanism:** Never enable a mode that auto-fans-out. Evens is the yes.
- **Evidence:** Yes-to-run; Ultra = X-high + workflows.
- **Conditions:** Any harness with an ‘ultra’ slider.
- **Exceptions:** We do not run Claude Ultra. We do not publish from Lovable.
- **Action:** If a tool offers ‘just orchestrate’ → refuse. Dual gate empty.
- **Confidence:** high
- **Source:** `jZgcWCzxh1I` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- Skill = how; workflow = how many. **SOURCE**
- Just because a feature exists does not mean you are behind if you skip it. **SOURCE**
- Workers on Haiku; one synthesizer on the expensive model. **SOURCE**

## D. Procedures
- Ask → skill if repeat → sub-agent if messy side task → team if they must talk → /goal if done=true → workflow if giant parallel. **SOURCE**
- This desk: one Grok thread. No JS fan-out. No send. **SYSTEM SYNTHESIS**

## E. Examples
- **Situation:** Audit 41 skills. → **Action:** Yes to workflow; Haiku workers; Opus rank. → **Reasoning:** Independent files. → **Outcome:** HTML + 5M input. → **Lesson:** Width fits a corpus, not a letter. Implicit rule: confirm is the product.

## F. Decision Rules
- If it does not shard → do not workflow.
- If Ultra is on → turn it off; we do not have Ultra anyway.
- If an audit wants to rewrite hive skills → do not auto-write SKILL.md.
- Refuse: half-plan $ as FACT. Lovable publish. Claude as ours.
- Optimize: bound + named deliverable.

## G. Contrarian
- Field will run Ultra on everything. He says he mostly will not use workflows. **SOURCE**

## H. Assumptions
- Half-plan burn is one anecdote. Falsifier: a workflow that sends.

## I. Questions
- Are we loading unused width on a one-page draft?

## J. Connections
- **SYSTEM SYNTHESIS:** `e18sdZLwP7o` (sub-agents). `XNQBCRcwXV4` (thin skills). `ask-principal`.

## K. Future-Use
- Width-vs-depth as an ops note. Skill-audit HTML is not a send.

## Steal / Operate-never

### Machine: Bound + confirm before width; never Ultra a letter; never auto-write skills
- **Epistemic:** SOURCE + SYSTEM SYNTHESIS
- **Workflow / loop:** Does it shard? → if no, one thread → if yes, Evens still confirms → no send.
- **Questions / signals:** Are we about to fan out? Is Ultra on?
- **Qualify / frame / objections:** Qualify: corpus audit vs a sentence. Frame: confirm is the product. Objection: ‘41 agents ranked my skills’ → not a mailer.
- **Procedure:** 1) No Claude workflow. 2) No Ultra. 3) No SKILL.md rewrite. 4) No send.
- **Example that proves it:** Desktop crawl ate half the plan.
- **Why it works:** Width without a bound is a bill.
- **Conditions / exceptions:** Claude Code tapes. Exception: Cursor + Grok.
- **Operate-never payload:** Quote $200 half-burn as FACT. Lovable one-click publish.
- **Hive run (existing skills only):** `ask-principal`. Do not auto-write SKILL.md.
- **Source:** `jZgcWCzxh1I` @ UNKNOWN


### Operate-never (this desk will not operate)
- Run a Claude dynamic workflow on hive mail. Enable Ultra. Auto-write SKILL.md from a 41-skill audit. Quote half-of-$200 as FACT.
- Gmail **send**. Connector write. Scheduled send. Auto-send. Ack-reply. Mass-DM. Postcard. Waitlist blast.
- Quote tape $ / student counts / job-loss % / ROI hours as FACT in a letter.
- New hunt ICP. Unpark Normand. Clients parked — no Path A this week.
- Install on-tape vendors (Vapi, Claude, Codex, ChatGPT, Gemini, Coda, Abacus, Skool, n8n-cloud). Stack stays Cursor + Grok. On-tape names stay on-tape.
- Grok Bot / `sendPrompt`. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md. New `icp_id`.

## L. Role-Specific Applications
- I do not fan-out a letter. I do not send. Clients parked.
