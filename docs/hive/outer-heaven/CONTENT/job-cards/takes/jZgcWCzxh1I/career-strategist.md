# Career Strategist — jZgcWCzxh1I
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jZgcWCzxh1I/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jZgcWCzxh1I/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Caption ingest (~3948 words). Beats in order: (1) dynamic workflow: 41 Haiku scorers on 41 skills → Opus synthesis → HTML worst-to-best + fix patterns; ~5M input tokens, “not that expensive” on output; one other prompt ate **half of a $200/mo** sub in ~30 min (2) ladder: skill = reusable recipe; sub-agent = clean window, talks only to main; agent team = crew that talks (war room, expensive); workflow = Claude writes a JS file that fans out many lone agents and merges; `/goal` = keep going to a criterion (3) who holds the plan: normal Claude vs the JS file (4) up the ladder = more function, more risk, more $ (5) Lovable sponsor (he started sites there, later moved to Claude Code) (6) save workflows in-project (`dot Claude/workflows`) not a global dump (7) “ultra code” = extra-high effort + every prompt a workflow, **bypasses a lot of permissions**, most expensive (8) say “set me up a dynamic workflow” — the word “workflow” rainbow-highlights and is not always an invoke (9) close ladder: quick ask → skill if repeat → sub-agent if messy side task → team if they must talk → `/goal` until criteria → dynamic workflow if giant parallel. Visual-only: HTML ranking, rainbow — unobserved.

## B. Atomic Knowledge

### Match the primitive to the job; price and blast radius go up together
- **Claim:** Skills / subs / teams / workflows / `/goal` feel similar and are not. One workflow prompt burned ~$100 of a $200 plan. 41-way skill audit was token-heavy on input. Ultra-code defaults every turn to a workflow and loosens permissions.
- **Reasoning:** Parallel fan-out is a cost and a permission event, not a flex.
- **Mechanism:** Pick the lowest rung that works (`3XIGcM7VICc` vending default).
- **Evidence:** “if you’re not being careful, you will obviously eat up your session limit”; ultra “bypass a lot of those permissions.”
- **Conditions:** You can name “repeat / side task / debate / until-done / giant parallel.”
- **Exceptions:** Saved workflows are for reruns you already liked.
- **Action:** Do not turn on ultra-code. Do not run 41-way audits for fun. $ UNVERIFIED.
- **Confidence:** high as a warning.
- **Source:** `jZgcWCzxh1I`
- **Epistemic:** SOURCE (ladder) + UNVERIFIED ($)

### The workflow is a file; keep it in the project
- **Claim:** Dynamic workflow = generated JS that is the plan. Default save can be global; he forced `project/.claude/workflows` so Herc 2 owns it. Re-run by pointing at the file.
- **Reasoning:** Same directory-outlives-harness.
- **Mechanism:** Explicit “set up a dynamic workflow”; save in-repo.
- **Evidence:** he had to tell it where to save.
- **Conditions:** You want replay.
- **Exceptions:** Rainbow “workflow” in speech ≠ invoke.
- **Action:** If hive ever uses a similar primitive, keep the artifact in the repo; ask-every-time on permissions.
- **Confidence:** high.
- **Source:** `jZgcWCzxh1I`
- **Epistemic:** SOURCE

## C. Mental Models
More agents ≠ more career. Ladder is cost/risk. Ultra-code is a spend + permission trap. Files are the workflow. Lovable is a beginner on-ramp he left — on-tape.

## D. Procedures
His close: ask → skill → sub → team → goal → dynamic. Check tokens. Save in-project. Never ultra as default.

Questions: Is this a repeat recipe or a 41-way fan-out? Where did the JS land? What did ultra just auto-allow?

Signals: half the month gone; permission bypass.

## E. Examples
**Situation:** 41 skills scored in parallel.  
**Action:** Haiku army + Opus merge → HTML.  
**Reasoning:** Giant parallel job.  
**Outcome:** Useful ranking; 5M input tokens.  
**Lesson:** Cool ≠ cheap. Do not copy as a weekly habit.

## F. Decision Rules
- If a skill would do, do not workflow.
- If ultra bypasses permissions, do not use it.
- If the JS landed outside the project, move it.
- If one prompt can eat half the plan, it is not a background job.

## G. Contrarian
Rejects “Anthropic shipped this to burn tokens” as the only read — he still uses it carefully. Rejects treating all multi-agent features as one thing.

## H. Assumptions
**Theirs:** $200 plan, half-burn, 5M tokens “not super expensive.” **Ours:** $ UNVERIFIED. Claude/Lovable on-tape. Falsifier: a workflow that sends because ultra skipped ask.

## I. Questions
- What did the skill ranking change in his OS?
- How often does “workflow” false-invoke?

## J. Connections
- SYSTEM SYNTHESIS → `iTY8Q449YNQ` (parallel + cost).
- SYSTEM SYNTHESIS → `5p5cV0yVDvQ` (permissions).
- Stack Cursor + Grok. No Claude Code ultra.

## K. Future-Use
Unassigned: lowest-primitive-that-works as a Forge check. Not a 41-agent audit.

## Steal / Operate-never

### Machine: lowest rung; never ultra-by-default
- **Epistemic:** SOURCE
- **Workflow / loop:** name the job → pick skill/sub/team/goal/workflow → save artifacts in-repo → watch spend → never permission-bypass mode
- **Questions / signals:** Repeat or giant parallel? Where is the file? What auto-allowed?
- **Qualify / frame / objections:** Token theater is not leverage.
- **Procedure:** No ultra. No send inside a workflow.
- **Example that proves it:** 41-skill audit (E); half-plan burn (B).
- **Why it works:** Blast radius scales with the primitive (B/C).
- **Conditions / exceptions:** Saved replay is the point of a workflow file.
- **Operate-never payload:** Ultra-code; quoting $200/half as FACT; Lovable publish; installing Claude workflows as hive SOP.
- **Hive run (existing skills only):** `ask-principal`
- **Source:** `jZgcWCzxh1I`

### Operate-never
- Enable permission-bypass modes.
- Burn a month of tokens on a demo.
- Install on-tape vendors. Publish from Lovable.
- Auto-write `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
Employment covers baseline. Career steal is “pick the cheapest primitive” and treat ultra/permission-bypass as operate-never. Token bills UNVERIFIED. Clients parked. Cursor + Grok.
