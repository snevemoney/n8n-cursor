# Librarian — jZgcWCzxh1I
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jZgcWCzxh1I/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jZgcWCzxh1I/LEARNED.md`
**ICP:** parked unless Evens named one.
**Title:** Claude Code Dynamic Workflows Clearly Explained
**Channel:** Nate Herk | AI Automation
**Kind:** video (~3948 words)
**Captions:** yt-dlp — timestamp UNKNOWN (no VTT unless noted)
**Walked:** 2026-08-14 librarian re-walk. Did not merge `LESSONS-FROM-TAPE.md`. Clients parked. Did not overwrite `takes/librarian.md` (18-tape SSOT).

## A. Source Map
1. Demo: dynamic workflow audits 41 skills → 41 **Haiku** scorers in parallel → **Opus** synthesis → HTML worst-to-best + fixes/patterns. ~**5M input** tokens, little output (he says not that expensive). Feature vs skills / sub-agents / teams / `/goal`. Cost aside: one workflow prompt burned **half of a $200/mo** plan in ~30+ min (UNVERIFIED) — desktop crawl.
2. Ladder: **skill** = reusable recipe (can nest anywhere). **Sub-agent** = own window, talks only to main, not to peers. **Agent team** = crew, shared task list, debate, expensive. **Workflow** = Code writes a **JS script** that spins many lone workers; results merge to main. Plan lives in the script, not the chat. Confirm before run; hard to invoke by accident. He will barely use it — knowledge-work/automations, not giant PR/codebase sweeps.
3. Lovable sponsor: NL apps, one-click connectors, publish without GitHub/Vercel; he started there then moved to Code. MCP to n8n/Claude/Cursor/Codex/Hermes. On-tape.
4. Width vs depth: `/goal` = loop until `done=true` (can run 24h+; he never has). Workflow = horizontal 50+ agents, no done-loop. Combining them = money fire. Bound scope, name the deliverable, put workers on Haiku. `/deep-research` auto-invokes a workflow (parallel research, vote on claims, cited report).
5. When: many independent pieces (400-file migration, every-file review). Not: single edit, quick Q, one deliverable, general knowledge work. Ask: does it break into parallel pieces?
6. Invoke: “run a workflow that…” + explicit yes; can view raw script. `/workflows` to watch/stop. Default save is **global**, not the project — he forced `.claude/workflows` inside Herc 2. Re-run later (skill audit, feature ranking).
7. **Ultra** effort = extra-high reasoning **and** defaults to workflows; bypasses a lot of permissions; expensive. Typing “workflow” rainbow-highlights; not every use of the word invokes. Slide: ask → skill → sub-agent → team → `/goal` → dynamic workflow. Free Skool guide.
Gap: script body, 41-agent UI. Timestamp UNKNOWN. Claude/Lovable/Skool on-tape.

## B. Atomic Knowledge

### Workflows are width (a saved JS plan); /goal is depth (done=true)
- **Claim:** A dynamic workflow is Code writing a script that fans out mute workers and merges. Teams talk; workflow workers do not. Ultra = xhigh + workflows. Confirm-to-run is the safety. Half a Max plan is the cost tell.
- **Reasoning:** Each worker is a full call with its own window; input tokens dominate. Vague scope loops (goal) or explodes width (workflow).
- **Mechanism:** Confirm → JS in `.claude/workflows` → Haiku workers → one synthesizer → HTML/report.
- **Evidence:** 41-skill audit; 5M input; desktop-crawl half-plan; `/deep-research`.
- **Conditions:** $200 / 5M / 30 min UNVERIFIED. Default save is global.
- **Exceptions:** He says he will barely use it for his work.
- **Action:** File width-vs-depth + confirm-to-run + pin-the-script-in-repo. Do not install Claude workflows as hive. Ultra is a blast-radius mode.
- **Confidence:** high as a routing ladder
- **Source:** `jZgcWCzxh1I` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared + demonstrated (transcript-implied)
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** half-plan desktop crawl
- **Speech ≠ behavior:** “not that expensive” (5M in) vs half a $200 plan on another prompt

## C. Mental Models
How many agents, and can they talk. Skill = how; workflow = how many. What/when ≠ you must use it daily. Confirm is a feature.

## D. Procedures
1. Climb only if the job splits into independent parallel pieces.
2. Name the deliverable; bound the corpus; put workers on the cheap model.
3. Say “set up a dynamic workflow…” and confirm; do not rely on the word “workflow.”
4. Force-save the JS into the project, not the global dir.
5. Never leave Ultra on for a vague crawl.
Avoid: Claude as hive; Lovable as hive; Skool guide as SSOT; half-plan as a flex.

## E. Examples
**41-skill audit:** Situation — global + project skills. Action — Haiku×41 + Opus rank. Outcome — HTML worst-to-best. Lesson — width with a cheap worker and one expensive merge.

**Desktop crawl:** Situation — “search my whole desktop.” Action — workflow. Outcome — half a Max month. Lesson — the bill is the size of the job you point it at.

## F. Decision Rules
- IF pieces cannot run independently → not a workflow (`/goal` or a skill).
- IF you need debate → team, not workflow.
- IF Ultra is on → assume it will orchestrate and spend.
- Refuse: Claude as hive; $200-half as FACT; Lovable publish as hive.

## G. Contrarian
Against “new feature = you must use it or fall behind.” Against “they shipped this only to burn tokens” (he doubts it, still warns).

## H. Assumptions
Complements `EuzYhzB0vbI` (loop=done+check+cap) and `4OOS96i2gfI` (climb-only). Caption-only.

## I. Questions
What is in the unread JS? Did `/deep-research` vote quality match STORM?

## J. Connections
SYSTEM SYNTHESIS → `EuzYhzB0vbI`; `4OOS96i2gfI`; `Tj3018n5MVg`; `eRS3CmvrOvA`.

## K. Future-Use
Width-vs-depth + confirm-to-run + pin-script-in-repo as atoms.

## Steal / Operate-never

### Machine: climb the ladder; workflow only for bounded parallel width
- **Epistemic:** SOURCE
- **Workflow / loop:** ask if it splits → confirm script → cheap workers + one merge → save in-repo → checkable stop = ranked HTML or a cited report, not an open crawl
- **Questions / signals:** Talk to each other? Done=true or fan-out? Where did the JS land?
- **Qualify / frame / objections:** Knowledge work rarely needs this.
- **Procedure:** D above.
- **Example that proves it:** 41-skill audit vs desktop half-plan.
- **Why it works:** Width is a scripted plan; depth is a loop; mixing them is a money fire.
- **Conditions / exceptions:** $ UNVERIFIED; Ultra bypasses permissions.
- **Operate-never payload:** Claude workflows as hive; Ultra always-on; Lovable; Skool.
- **Hive run:** Same ladder in Cursor/Grok. Do not install Claude dynamic workflows.
- **Source:** `jZgcWCzxh1I` @ UNKNOWN

### Operate-never
- Claude/Lovable as hive. Ultra for a vague crawl. Quote half-$200 as FACT. Merge LESSONS. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
File width-vs-depth next to `/goal`. Do not add a hive “dynamic workflow” product.
