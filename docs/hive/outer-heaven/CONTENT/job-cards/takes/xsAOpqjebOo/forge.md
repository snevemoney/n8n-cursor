# Forge — xsAOpqjebOo
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xsAOpqjebOo/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xsAOpqjebOo/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
“Watch my agents work” teaser (Claude Code). Beats: sidebar agents running the business → terminal is the Claude Code agent → ask to spin two researcher agents (Google vs OpenAI) + a visual diagram → three in parallel (two researchers + delegator) → right-hand shows current run → extra terminal = another conversation (YouTube comments last couple videos) → notification = sub-agent → **needs approval** → visual terminals/sub-agents + notifications → left-side agents disappear when done → first done, OpenAI still running → both idle → diagram of strengths/weaknesses → second terminal: 2-week comment analysis (Claude API cost pain, first client with zero social proof) → play-button to `62Rfe1w9NBc`. Timestamp UNKNOWN. Claude Code = on-tape only.

## B. Atomic Knowledge

### Observe + approve, not fire-and-forget
- **Claim:** He can see each terminal/sub-agent; a comment-analysis sub-agent **needs approval**; idle means done.
- **Reasoning:** Manager view, not a hidden loop.
- **Mechanism:** Delegate → parallel → notify → approve → idle.
- **Evidence:** Approval line + idle state + diagram output.
- **Conditions:** Claude Code desktop/terminal UI.
- **Exceptions:** He does not show a rejected approval.
- **Action:** Steal observe + approve. Do not install Claude Code. Do not hide work in a black box.
- **Confidence:** high.
- **Source:** `xsAOpqjebOo` @ UNKNOWN
- **Epistemic:** SOURCE

### One extra terminal is a second conversation
- **Claim:** Adding a terminal adds an agent you can brief separately (comments vs research).
- **Reasoning:** Parallel jobs, separate threads.
- **Mechanism:** New terminal → new message.
- **Evidence:** Comment-section ask.
- **Conditions:** He watches both.
- **Exceptions:** No merge of the two jobs until he looks.
- **Action:** One job per thread. Don’t one-shot “run the business.”
- **Confidence:** high.
- **Source:** `xsAOpqjebOo` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
The operator is a manager of visible workers. Approval is a first-class event. Idle is the checkable stop. “Running my business” is the hook, not the procedure.

## D. Procedures
On tape: brief the main agent to spawn two researchers + a diagram; open a second terminal for a different ask; wait for approval; wait for idle; open the artifact. Hive: same shape in Cursor Task / worktrees — still HITL.

## E. Examples
**Situation:** Google vs OpenAI strengths + a diagram.  
**Action:** Two researchers + delegator; watch until idle; open the viz.  
**Reasoning:** Parallel + visible.  
**Outcome:** Strengths/weaknesses diagram.  
**Lesson:** Done = idle + an openable file.

## F. Decision Rules
- If a sub-agent needs approval → don’t always-allow the estate.
- If you cannot see the worker → not this machine.
- If the hook is “run my business” → one job, not the business.

## G. Contrarian
Field hides agents in a service. He wants to watch.

## H. Assumptions
UI is accurate. Falsifier: idle with a wrong diagram. Claude Code not ours.

## I. Questions
What does approval actually gate? Long-tape dashboard (`ZAaxx3qyT8g`)?

## J. Connections
SYSTEM SYNTHESIS: `62Rfe1w9NBc` sibling. `ZAaxx3qyT8g` dashboard. `e18sdZLwP7o` / `vDVSGVpB2vc` gated subagents. `hive-spawn-desks` (parent spawns, desks visible).

## K. Future-Use
Idle + openable artifact as Forge done-when for parallel Tasks.

## Steal / Operate-never

### Machine: visible workers + approve + idle + open the file
- **Epistemic:** SOURCE
- **Workflow / loop:** brief → spawn named jobs → watch → approve gated steps → idle → open artifact
- **Questions / signals:** Can I see each worker? What needs approval?
- **Qualify / frame / objections:** “Running my business” is the never-scope.
- **Procedure:** One extra thread = one extra job. No Claude Code install.
- **Example that proves it:** Two researchers + diagram; comments thread needs approval.
- **Why it works:** A manager who cannot see the floor cannot stop a bad write.
- **Conditions / exceptions:** Vendor UI on-tape.
- **Operate-never payload:** Claude Code / always-allow / 18th vibe desk.
- **Hive run:** `hive-spawn-desks` (parent) + `ask-principal` + `slice-build`.
- **Source:** `xsAOpqjebOo` @ UNKNOWN

### Operate-never
- Install Claude Code. Always-allow subagents.
- Quote comment-pain as a hunt. New `icp_id`.
- Send / pay / deploy / book / publish. Merge `LESSONS-FROM-TAPE.md`.

## L. Role-Specific Applications
I will not install Claude Code. Parallel work stays visible. Approval stays on. Done = idle + I opened the file. Deploy HITL.
