# Day Planner — xsAOpqjebOo
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xsAOpqjebOo/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/xsAOpqjebOo/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Short: watch Claude Code agents work. Beats: terminal agent is “my Claude Code agent”; ask to spin up two researchers (Google vs OpenAI) + a visual diagram → three agents in parallel (two researchers + delegator); right-hand panel shows what is running; second terminal = YouTube comment analysis last couple videos → notification → sub-agent needs **approval**; visual terminals/sub-agents; left-side agents disappear when done; first research done, OpenAI still going, then both idle; diagram = strengths/weaknesses Google vs OpenAI; second terminal = 2-week comment pain (Claude API costs, first client with zero social proof). CTA to full (`62Rfe1w9NBc` / `ZAaxx3qyT8g`). Timestamp UNKNOWN. Vendor: Claude Code — on-tape.

## B. Atomic Knowledge
### Parallel researchers + a visible idle state
- **Claim:** One delegator can run two researchers in parallel; you know they are done when they go idle and the left chips disappear.
- **Reasoning:** Watch, don’t babysit forever.
- **Mechanism:** NL → spin two + diagram → panel + idle.
- **Evidence:** “both of these agents are done. They’re idle.”
- **Conditions:** A headed look at the panel.
- **Exceptions:** Headless “just run” has no idle signal he is showing.
- **Action:** Idle/done is the checkable stop. Do not install Claude Code.
- **Confidence:** high as the demo.
- **Source:** `xsAOpqjebOo` @ UNKNOWN
- **Epistemic:** SOURCE

### Sub-agent that needs approval is a HITL beat
- **Claim:** Comment analysis spun a sub-agent that “needs approval.”
- **Reasoning:** Some tool calls stop for a human.
- **Mechanism:** Notification → approval gate.
- **Evidence:** “now it needs approval.”
- **Conditions:** The sub-agent hit a gated tool.
- **Exceptions:** A read-only research may not gate — still watch.
- **Action:** Approval stays Evens. A second terminal is a conflict flag if it claims the same CUT hour (`CB5bG4mvnS0` parallel-chat).
- **Confidence:** high.
- **Source:** `xsAOpqjebOo` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Seeing the workers is the product (“I can actually watch”). Delegation is a first-class move. A second terminal is another conversation, not free capacity. Priority: show parallel + a diagram. Comment pain points are his audience, not a hunt list. Uncertainty: how long OpenAI research ran.

## D. Procedures
1. One delegator, named sub-jobs.
2. Watch the panel until idle.
3. If approval fires → Evens.
4. A second terminal is a second block — name the collision.
Avoid: Claude Code install; treating parallel as 2× CUT; auto-reply to YouTube comments.

## E. Examples
**Google vs OpenAI + diagram:** Situation → compare two labs. Action → two researcher agents + delegator; wait to idle; open the diagram. Reasoning → parallel watch. Outcome → strengths/weaknesses viz. Lesson → idle is the stop.

**Comment analysis approval:** Situation → second terminal, last videos’ comments. Action → sub-agent, notification, needs approval. Reasoning → gated tool. Outcome → 2-week pain list (API cost, first client/no proof). Lesson → approval is HITL; do not auto-reply.

## F. Decision Rules
- If they are not idle → the block is not done.
- If a second terminal opens during CUT → conflict flag.
- If comments would get auto-replies → never (see `gb5TlGw6Uks`).

## G. Contrarian
Rejects “agents work in the black box.” Field assumption: fire and forget. He wants a panel.

## H. Assumptions
Theirs: Claude Code panel is the way to see work. Ours: Cursor + Grok; hive-spawn-desks is our analog, not Claude. Falsifier: idle with empty output. Survivorship: one pretty diagram.

## I. Questions
Full `62Rfe1w9NBc` / `ZAaxx3qyT8g`? What tool needed approval? Same “watch agents” as the title twin?

## J. Connections
- SYSTEM SYNTHESIS → `62Rfe1w9NBc` · `ZAaxx3qyT8g` · `hive-spawn-desks` (parallel desks, not Claude) · `CB5bG4mvnS0` (parallel chat = collision) · `ask-principal`.

## K. Future-Use
Idle-as-stop + approval chip. Unassigned panel.

## Steal / Operate-never

### Machine: named parallel jobs → watch to idle → approval HITL
- **Epistemic:** SOURCE
- **Workflow / loop:** name two research jobs + a deliverable → run → wait idle → if approval, Evens → open the artifact
- **Questions / signals:** Are they idle? Did a second terminal steal the hour? Does a sub-agent need approval?
- **Qualify / frame / objections:** Black-box fire is the fail. A second silent terminal is a collision.
- **Procedure:** One headed watch. No Claude Code. No comment auto-reply.
- **Example that proves it:** Situation → Google vs OpenAI. Action → two researchers + diagram; idle; viz. Reasoning → watch the workers. Outcome → strengths/weaknesses. Lesson → steal idle+approval; not the vendor.
- **Why it works:** Idle is a checkable stop; approval keeps a tool from sending.
- **Conditions / exceptions:** Our analog is Cursor Task desks, not Claude Code.
- **Operate-never payload:** Claude Code as stack; auto-approve; YouTube auto-replies; quote comment pain as a hunt.
- **Hive run (existing skills only):** `hive-spawn-desks` · `ask-principal` · `morning-day-plan` (second terminal = conflict).
- **Source:** `xsAOpqjebOo` @ UNKNOWN

### Operate-never
- Install Claude Code / switch stack.
- Auto-approve / auto-reply comments.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as idle-as-stop (no Claude panel). Clients parked.
