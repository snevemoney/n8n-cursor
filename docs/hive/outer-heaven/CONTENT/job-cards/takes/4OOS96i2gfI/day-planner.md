# Day Planner — 4OOS96i2gfI
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Nate: agents are overused — four-layer pyramid. Beats: custom GPT/project/Gem (reactive intern) → no-AI workflow (deterministic if/then) → AI workflow (fixed path, AI at a decision) → agent (goal + tools + order unknown); cost/complexity/maintenance rise; be a **problem solver**, not an agent builder; often the answer is a simple automation, a project, or an existing SaaS; layer 1 example: YouTube setup-guide sticky (revisions in-chat beat a brittle automation); layer 2: trigger/sleep/6am, poll success/error only; layer 3: email classifier (support/finance/priority/promotion) then fixed path; **~50% of business automations need no AI** (UNVERIFIED); do not start a client on an agent; layer 4: Telegram marketing agent whose **tools are themselves fixed workflows**; decision tree: in the loop every time? → GPT. 100% logic? → workflow. Fixed order? → AI workflow. Else agent; support exception: skip KB vs search 5×; you will scale up or down after runs; Plus/Skool CTA (3,000 members — UNVERIFIED). Timestamp UNKNOWN.

## B. Atomic Knowledge
### Climb the pyramid only when the question forces it
- **Claim:** Start at the cheapest layer that solves the root problem; most first automations are no-AI; agents are last.
- **Reasoning:** Complexity, cost, maintenance go up.
- **Mechanism:** Three questions (in-loop / 100% logic / fixed order).
- **Evidence:** “the AI agent is almost never the right call to start off with right away.”
- **Conditions:** A named problem, not “we need AI.”
- **Exceptions:** After runs, you may scale up or down — he says you don’t know yet.
- **Action:** Put the three questions on the card before any agent block.
- **Confidence:** high as doctrine; 50% UNVERIFIED.
- **Source:** `4OOS96i2gfI` @ UNKNOWN
- **Epistemic:** SOURCE

### If you do use an agent, make its tools fixed workflows
- **Claim:** Autonomy to pick a tool is enough; once picked, the path should stay predictable.
- **Reasoning:** Performance got better when image tools were workflows.
- **Mechanism:** Agent chooses → workflow executes a fixed sequence (may loop back).
- **Evidence:** “once it chose the right path we basically wanted it to stay super predictable.”
- **Conditions:** Multiple tools.
- **Exceptions:** Support may need variable KB search count — then agent, not a single-pass workflow.
- **Action:** Tools = workflows. Agent is the router, not the whole graph.
- **Confidence:** high.
- **Source:** `4OOS96i2gfI` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Problem first. Quick win, low cost, simple. Reactive intern (GPT) is valid. Sleeping deterministic crons are valid. He sells Plus after teaching the pyramid. Priority: the tree. Uncertainty: 50% / 3,000 UNVERIFIED.

## D. Procedures
1. What is the root problem?
2. In the loop every time? → conversational project, not a cron.
3. 100% logic? → no-AI workflow.
4. Fixed order with one smart step? → AI workflow.
5. Else agent, tools = workflows.
6. After runs, be willing to scale down.
Avoid: agent-first; Plus/Skool; auto-reply support.

## E. Examples
**Setup-guide GPT:** Situation → every YouTube template needs a sticky. Action → custom GPT, revise in chat. Reasoning → automation would still need revisions. Outcome → faster. Lesson → in-loop → don’t automate.

**Email classifier workflow:** Situation → inbox paths. Action → AI classifies, then fixed branches. Reasoning → keywords aren’t enough; full autonomy isn’t needed. Outcome → still a workflow. Lesson → one smart step ≠ agent.

## F. Decision Rules
- If they said “we need an agent” → run the tree anyway.
- If 50% no-AI is the excuse to skip thinking → still name the problem.
- If support would auto-send → HITL regardless of layer.

## G. Contrarian
Rejects agent-first. Field assumption: more autonomy = more professional. He starts at GPT/cron.

## H. Assumptions
Theirs: 50% no-AI is what he sees in coaching. Ours: UNVERIFIED; Plus is operate-never; clients parked so the tree is doctrine only. Falsifier: a first problem that truly needs an agent. Survivorship: hundreds of builds.

## I. Questions
Same classifier as `9mqsVK6Iqoc`? Same media agent as `jBanaNBY-sM`? When does he scale an agent *down*?

## J. Connections
- SYSTEM SYNTHESIS → `wk8KV280fbg` / `w9-gfaV5vlM` (solution not agent) · `tNOk29fs_aY` (it was a workflow) · `9mqsVK6Iqoc` · `outcome-offer-funnel`.

## K. Future-Use
Three-question tree as the default planning gate. Unassigned.

## Steal / Operate-never

### Machine: problem → three questions → cheapest layer; agent tools = workflows
- **Epistemic:** SOURCE
- **Workflow / loop:** name the problem → in-loop? logic? fixed order? → pick layer → if agent, wrap tools as workflows → review after runs
- **Questions / signals:** Do I need to be in the loop every time? Are steps 100% logic? Is order fixed?
- **Qualify / frame / objections:** “Build an agent” is the fail frame. Existing SaaS / no-AI cron may be the pass.
- **Procedure:** Tree on the card. No Plus. No agent-first. Support send = Evens.
- **Example that proves it:** Situation → setup guides. Action → GPT not cron. Reasoning → revisions. Outcome → faster. Lesson → steal the tree.
- **Why it works:** Wrong layer is how cost and maintenance explode.
- **Conditions / exceptions:** 50% UNVERIFIED. Clients parked.
- **Operate-never payload:** Nate Plus/Skool; agent-first; auto-reply; quote 50%/3,000 as FACT.
- **Hive run (existing skills only):** `outcome-offer-funnel` · `slice-build` (one layer).
- **Source:** `4OOS96i2gfI` @ UNKNOWN

### Operate-never
- Agent-first / Plus community as a SKU.
- Auto-send support.
- Send / pay / deploy / book / publish.

## L. Role-Specific Applications
**One next:** `coverage-loop` score this packet as the three-question pyramid gate. Clients parked.
