# HITL Operator — 4OOS96i2gfI
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/LEARNED.md`
**ICP:** parked unless Evens named one.

Evens is the visionary. Operate ≠ learn. Role did not filter what was learned. Stack stays Cursor + Grok. Clients parked. No send / pay / deploy / book / publish. Tape $ UNVERIFIED.

## A. Source Map

**Title (PACKET):** AI Agents Are Overused. Here’s What to Build Instead
**Speaker / channel:** Nate Herk | AI Automation
**Kind / words:** framework · 2461 words
**Gaps:** No VTT cited in this take. Timestamps UNKNOWN. Visual-only UI clicks inferred only as INFERENCE.

Beats in order:

- Hundreds of agents/workflows = hundreds of mistakes. Four-layer pyramid: (1) custom GPT / Claude project / Gem — reactive intern, you must be in the loop; his setup-guide sticky after each n8n tutorial. (2) no-AI workflow — deterministic if/then, can run while you sleep (meeting done, 6am). (3) AI workflow — fixed order, AI for write/classify (email → support/finance/priority/promo). (4) AI agent — goal + tools + exceptions; cost/complexity/maintenance rise as you climb.
- Be a problem solver, not an agent builder. Often the answer is a simple automation, a project, or an existing SaaS. ~50% of business automations need no AI (UNVERIFIED). Don't start a first business install at the agent layer.
- Marketing Telegram agent: autonomy to pick tools, but each tool is a fixed workflow — hybrid. Decision tree: in the loop every time? → GPT. 100% logic? → workflow. Fixed order? → AI workflow. Else agent.
- Support exception: read → KB → reply looks like a workflow, but sometimes 0 KB searches or 5 — then agent. He still ends at 'respond to the email.' Plus community 3,000 + courses CTA.

## B. Atomic Knowledge

### Lowest layer that works; reply still HITL
- **Claim:** Start at the lowest layer that solves the problem. Agents are the expensive top.
- **Reasoning:** His own support example still replies — that payload is operate-never even when the tree says agent.
- **Mechanism:** Three questions → pick a layer. Climb only with evidence.
- **Evidence:** On-tape tree + 50% no-AI + support reply path.
- **Conditions:** On-tape demo / short captions.
- **Exceptions:** Tape $ and vendor names stay on-tape.
- **Action:** Steal the pyramid and the three questions. Operate-never auto-reply, Plus/Skool, start-at-agent.
- **Confidence:** medium — caption ingest, timestamp UNKNOWN
- **Source:** `4OOS96i2gfI` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models

- People ask for AI; the job is the root problem and a quick cheap win.
- You don't know what you don't know — layers can move after runs.
- Autonomy over a set of fixed tools is often enough.

## D. Procedures

- Ask: must a human be in every time? Are steps 100% logic? Is order fixed?
- Default no-AI. Add a classify step only if keywords fail.
- If the path replies to a customer → send-removed regardless of layer.
- Do not join Plus. Do not start a Path A at the agent layer.

## E. Examples

- **On-tape run** — Situation: Setup guide after every YouTube template → Action: Custom GPT he iterates in-chat vs a brittle automation → Reasoning: Needs revision loop → Outcome: Faster in the GPT → Lesson: In-the-loop is a feature

## F. Decision Rules

- If they ask for an agent first → walk the tree.
- If 50% no-AI → try a filter before a model.
- If support 'respond' is on the canvas → card.
- If Plus 3,000 is the close → ignore.

## G. Contrarian

- Field starts at agents. He says that is the common issue he coaches.

## H. Assumptions

- Hundreds / thousands / 50% / 3,000 UNVERIFIED.
- Plus/Skool on-tape.

## I. Questions

- When does he demote an agent after feedback?
- Does the support graph actually send?

## J. Connections

- SYSTEM SYNTHESIS → `slice-build` · `inbox-to-task-routing` · `outcome-offer-funnel` · `send-removed`.
- Sibling classify: `9mqsVK6Iqoc`.

## K. Future-Use

- Pyramid as a default scoping card. Keep.

## Steal / Operate-never

Informed by A–K. Auto-send / auto-book stay operate-never. The machine is still stolen.

### Machine: Three questions → lowest layer (send still a card)
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** name the problem → in-loop? logic? fixed order? → pick GPT/workflow/AI-workflow/agent → if outbound, strip Send → Evens
- **Questions / signals:** Must we be in the loop? Is it black-and-white? Does it email the customer?
- **Qualify / frame / objections:** I need an agent is not a spec. Existing SaaS may win.
- **Procedure:** Start cheap. No Plus. No auto-reply.
- **Example that proves it:** **On-tape run** — Situation: Email classify → Action: AI workflow not a free agent → Reasoning: Keywords fail → Outcome: Four buckets → Lesson: Router stolen; reply never
- **Why it works:** Cost and maintenance climb the pyramid. Most first installs don't need the top.
- **Conditions / exceptions:** Hard steps stay HITL.
- **Operate-never payload:** Start-at-agent. Auto-reply support. Quote 50%/3,000 as FACT. Join Plus.
- **Hive run (existing skills only):** `slice-build` · `inbox-to-task-routing` · `send-removed` · `ask-principal`
- **Source:** `4OOS96i2gfI` @ UNKNOWN

### Operate-never (this desk will not operate)

- Auto-send / auto-book / auto-voice-book / auto-publish / auto-pay / auto-deploy.
- Quote tape $ / student counts / job-loss % / token burns as FACT.
- Install on-tape vendors (Claude Code, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool) as our stack. Cursor + Grok only.
- New `icp_id`. Unpark Normand. Outreach / hunt because a tape was interesting.
- Always-allow MCP / classifier / guardrail-pass as Evens.
- Merge `LESSONS-FROM-TAPE.md`. Send / pay / deploy / book / publish.

## L. Role-Specific Applications

Put the three questions on scoping cards. Support reply remains REJECT. Plus CTA is never. Clients parked.
