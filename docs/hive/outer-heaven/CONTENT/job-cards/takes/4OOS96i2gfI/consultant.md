# Consultant — 4OOS96i2gfI
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map

Four-layer systems pyramid. Beats: hundreds of agents = hundreds of mistakes; he coaches thousands (UNVERIFIED). Pyramid bottom→top: custom GPT / Claude project / Gem (reactive intern, only when you start the chat) → simple no-AI workflow (if-then, meeting-done or 6am, predictable) → AI-inside-a-fixed-path (same order, AI classifies or writes) → AI agent (goal, pick tools/order, exceptions). Up = complexity, cost, maintenance. Job: **problem solver, not agent builder**. Client “needs AI” → root problem, cheapest quick win, low cost/simple — sometimes a SaaS that exists. Layer 1 example: YouTube setup-guide sticky; automation would still need revision, so a GPT he can iterate in is faster. Layer 2: deterministic, can run while you sleep. Layer 3: email classifier to support/finance/priority/promotion — need AI to read, then back to a fixed path. He sees **~50%** of automations you could build right away need no AI; agent is **almost never** the start; even Gems in daily work drive efficiency. Agent example: Telegram marketing request, content/image/DB tools; once a path is chosen, tools are themselves fixed workflows — that predictability helped. Decision tree: (1) in the loop every time? → GPT. (2) 100% logic? → no-AI workflow. (3) order fixed? → AI workflow; else agent. Exception: support mail that sometimes skips KB or searches 5× — agent may fit even if the happy path is 1-2-3. You don’t know what you don’t know: start GPT, scale up; or start agent, scale down after runs/feedback. Close: Plus, 3,000 members, Agent Zero 10h→10s, one-person agency, weekly live. No VTT. UNKNOWN. ~2461 words.

## B. Atomic Knowledge

### Cheapest layer that hits the win, agent last
- **Claim:** Most first builds should be a reactive GPT, a no-AI workflow, or a fixed-path AI step — not an autonomous agent. ~50% of immediate business automations need no AI.
- **Reasoning:** Up the pyramid is cost and maintenance. “I need AI” is not a layer pick.
- **Mechanism:** Ask: in the loop every time? 100% logic? order fixed? Pick the lowest yes.
- **Evidence:** “your job isn't to say, Here's what I will do with AI.” / “the AI agent is almost never the right call to start”
- **Conditions:** His coaching/build history. 50% / thousands / 3,000 UNVERIFIED.
- **Exceptions:** Support-email exception: variable KB loops can justify an agent. Scale up/down after runs.
- **Action:** Steal the three questions. Do not sell agent-first. Do not join Plus.
- **Confidence:** high as a decision tree; low as the 50%
- **Source:** `4OOS96i2gfI` @ UNKNOWN — “problem solver, not an AI agent builder”
- **Epistemic:** SOURCE
### If the agent’s tools are fixed workflows, you already cheated toward a workflow
- **Claim:** His Telegram marketing agent got better when each tool was a predictable workflow; after the choose-path moment it was almost an AI workflow that can loop.
- **Reasoning:** Autonomy over a pile of sloppy tools is how agents fail.
- **Mechanism:** If you must have an agent, make tools boring and fixed.
- **Evidence:** “once it chose the right path we basically wanted it to stay super predictable”
- **Conditions:** Marketing-team demo. Telegram on tape.
- **Exceptions:** Still an agent at the router. Not a reason to start there.
- **Action:** Predictable tools. No Telegram content-agent SKU.
- **Confidence:** high
- **Source:** `4OOS96i2gfI` @ UNKNOWN — “its tools are workflows or AI workflows”
- **Epistemic:** SOURCE


## C. Mental Models

He is tired of agent-first students. He wants quick wins and low maintenance. He still closes Plus/agency courses. He admits the flowchart is not always black and white. He likes iteration: scale up or down after feedback.

## D. Procedures

1. Name the root problem. 2. Ask the three yes/no. 3. Pick the lowest layer. 4. After runs, scale up or down. 5. If an agent, fix the tools. Avoid: agent as the default SKU. Avoid: Plus.

## E. Examples

**Situation:** Client says they need AI. **Action:** Find the root; pick GPT vs no-AI vs AI-path vs agent. **Outcome:** He claims most first work is not an agent. **Lesson:** Layer is a constraint answer. Implicit rule: you may start wrong and scale — if you are a problem solver that is fine.

## F. Decision Rules

If both “in the loop” and “100% logic” are yes, you do not get an agent. If 50% is the pitch, mark UNVERIFIED. If the next sentence is Plus, stop.

## G. Contrarian

Field default: they asked for an agent so build one. He refuses. Field default: more AI is more value. He says half the jobs need none.

## H. Assumptions

50% / 3,000 / 10h→10s / hundreds UNVERIFIED. GPT/Gemini/Claude/n8n/Telegram on-tape. Plus close. Pair `wk8KV280fbg` / `w9-gfaV5vlM`.

## I. Questions

What is a real scale-down story? How does he count the 50%?

## J. Connections

**SYSTEM SYNTHESIS:** Maps to `discovery-spiced-constraint` + `outcome-offer-funnel` + `slice-build` (one layer) + `golden-test-loop` (then scale). Agent-last = same physics as API→macro→vision on `CB5bG4mvnS0`.

## K. Future-Use

Unassigned: three-question card as a scope artifact; scale-up/down after runs as a four-blank review.

## Steal / Operate-never

### Machine: Three yes/no → cheapest layer; agent last; tools stay boring
- **Epistemic:** SOURCE
- **Workflow / loop:** Hear “I need AI” → name the root → (1) in the loop every time? (2) 100% logic? (3) order fixed? → pick the lowest layer → after runs, scale up or down → if agent, tools are fixed workflows
- **Questions / signals:** What is the root? Do they need to be in the chat? Is it if-then? Is the order fixed?
- **Qualify / frame / objections:** Qualify: they asked for an agent. Frame: problem solver. Objection: “build me an agent” — almost never the start.
- **Procedure:** Write the three answers. Do not sell agent-first. Do not join Plus.
- **Example that proves it:** Setup-guide GPT vs 6am no-AI vs email classifier vs Telegram agent whose tools are workflows.
- **Why it works:** Complexity/cost/maintenance rise with the pyramid. Quick wins live at the bottom.
- **Conditions / exceptions:** 50% UNVERIFIED. Vendor close. Exceptions exist.
- **Operate-never payload:** Sell agent-first. Quote 50%/3,000/10h→10s as FACT. Join Plus. Install GPT/Gem/n8n as ours.
- **Hive run (existing skills only):** `discovery-spiced-constraint` · `outcome-offer-funnel` · `slice-build` · `golden-test-loop` · `solo-then-consult`
- **Source:** `4OOS96i2gfI` @ UNKNOWN


### Operate-never
- Sell an agent as the default SKU.
- Quote 50% / 3,000 / 10h→10s as FACT.
- Join Plus / Agent Zero.
- Skip the three yes/no.
- Unpark a client / new `icp_id` / new `business-lanes.json` row. Learning ≠ hunt.
- Quote tape $ / student counts / job-loss % / hours×rate as FACT.
- Send / pay / deploy / book / publish. Approve draft ≠ send.
- Install on-tape vendors (Claude, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus, n8n-cloud, Skool). Stack stays Cursor + Grok.
- Grok Bot / `sendPrompt`. Merge `LESSONS-FROM-TAPE.md`. Auto-write SKILL.md. Overwrite `takes/consultant.md` or another desk's take.

## L. Role-Specific Applications

**Constraint first:** The stated ask is “build an agent / which layer?” Felt problem is the leak. The pyramid is how we refuse pharmacist-mode — only in this section, after A–K.

**Four-blank after constraint:** Toddler stop = we can point at the layer and the three answers. No community counts.

**Skeptical-customer:** Agent-at-the-top is smash. Clients parked.
