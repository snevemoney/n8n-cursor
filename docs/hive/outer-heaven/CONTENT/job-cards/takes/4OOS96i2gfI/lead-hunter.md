# Lead Hunter — 4OOS96i2gfI
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/LEARNED.md`
**ICP:** parked unless Evens named one.
**Tape:** AI Agents Are Overused. Here’s What to Build Instead
**Walk:** full.txt entire. Role did not filter A–K. Hunt skipped. No clients. No new `icp_id`. No `HUNT_LOG`. No Normand.

## A. Source Map
- Pyramid: (1) custom GPTs/Claude projects/Gems — reactive intern, you trigger it (YouTube setup-guide sticky). (2) no-AI workflows — deterministic if/then, runs while you sleep. (3) AI workflows — fixed order, AI only at a narrow choice (email classifier). (4) agents — goal, tools, exceptions. Up = complexity/cost/maintenance.
- Job: problem solver, not agent builder. Root problem, cheapest quick win — sometimes a SaaS that exists. Majority of business automations need no AI; agent is almost never the first call.
- His “agent” example: autonomy to pick a tool, then the tool is a fixed workflow — more workflow than agent, and that predictability helped.
- Questions: In the loop every time? If no → steps 100% logic? If yes, workflow. If no → order fixed? If yes, AI workflow; if no, agent. Then he undercuts: support email could be either; not black-and-white; you don’t know what you don’t know — scale up or down after runs.
- Plus community 3k members CTA (Agent Zero, one-person agency, live n8n, weekly call). Like. Counts UNVERIFIED.
- **Gaps:** Captions only in full.txt; visual UI / audio demos not fully described. Timestamps UNKNOWN unless a quote locus is marked. CTA “play button / full breakdown” is capture, not a second source.

## B. Atomic Knowledge
### Cheapest layer that solves the root; agent last
- **Claim:** Most “we need an agent” asks are a GPT, a no-AI cron, or a one-narrow-AI workflow; agents cost more to maintain; even a so-called agent should call fixed workflows.
- **Reasoning:** People name AI; the job is the root and a quick win.
- **Mechanism:** Loop? → logic? → fixed order? → pick layer. Revisit after runs.
- **Evidence:** SOURCE: “becoming a problem solver, not an AI agent builder.” “the AI agent is almost never the right call to start off with.” “the majority of automations can be simple and they don't even need any AI.”
- **Conditions:** You heard a root problem, not a requested SKU.
- **Exceptions:** Plus community is capture. 3k UNVERIFIED. Classifier example pairs with `9mqsVK6Iqoc`.
- **Action:** Steal the three questions + agent-last. Do not hunt agent-builders. Do not enter plus.
- **Confidence:** high
- **Source:** `4OOS96i2gfI` @ UNKNOWN
- **Epistemic:** SOURCE


## C. Mental Models
- SOURCE: Reactive GPT is an intern you must poke.
- SOURCE: Predictable tools behind an agent improve performance.

## D. Procedures
- Ask the three questions before a build. Prefer existing SaaS if it wins. Schedule a revisit to scale up/down.

## E. Examples
- Situation: they “need AI.” Action: root + cheapest layer; classifier as AI-workflow not agent. Reasoning: cost/maintenance. Outcome: plus CTA. Lesson: steal the questions; park the community.

## F. Decision Rules
- If they asked for an agent, still run the questions.
- If you’re in the loop every time, you may want a GPT, not a cron.
- If order is fixed, don’t sell autonomy.

## G. Contrarian
- Rejects agent-first. Rejects black-and-white — then still offers the flowchart.

## H. Assumptions
- Thousands coached UNVERIFIED. n8n-on-tape.

## I. Questions
- When does he scale an agent down? No story, only the pill.

## J. Connections
- SYSTEM SYNTHESIS: `tNOk29fs_aY` relabel agent→workflow.
- SYSTEM SYNTHESIS: `wk8KV280fbg` outcome not SKU.

## K. Future-Use
- Unassigned: three questions as a Lead Hunter qualify on any “we need an agent” inbound — still no hunt this walk.

## Steal / Operate-never

### Machine: Three questions → cheapest layer; agent last
- **Epistemic:** SYSTEM SYNTHESIS
- **Workflow / loop:** Hear “agent” → loop/logic/order questions → checkable stop = we did not start at the top of the pyramid.
- **Questions / signals:** Must a human poke it? Is it pure if/then? Is order fixed?
- **Qualify / frame / objections:** Frame: problem solver. Objection: “build us an agent” → cheapest layer.
- **Procedure:** Do not enter plus. Do not hunt agent-overuse victims as an ICP.
- **Example that proves it:** Situation: pyramid lesson. Action: four layers + questions. Reasoning: cost/maintenance. Outcome: community CTA. Lesson: steal questions; park capture.
- **Why it works:** From B: agent almost never first; majority need no AI.
- **Conditions / exceptions:** Works on a stated problem. Exception: no root → don’t build.
- **Operate-never payload:** Plus community. Quote 3k as FACT. Hunt “pyramid” clients.
- **Hive run (existing skills only):** `outcome-offer-funnel`. `list-anneal-funnel` (exclude agent-SKU rows).
- **Source:** `4OOS96i2gfI` @ UNKNOWN


### Operate-never
- New `icp_id`, named client, `HUNT_LOG` row, or unpark Normand. Learning ≠ hunt.
- Auto-dial, OTP / Instagram farms, fake identity, mass-DM, betting, OFM.
- MUST-score a raw 50. Send / pay / deploy / book / publish without HITL.
- Quote on-tape $ / student counts / job-loss % as FACT. Tape money stays UNVERIFIED.
- Install on-tape vendors (Vapi, Claude Code, Codex, ChatGPT, Gemini, Hostinger, School, n8n-cloud). Stack stays Cursor + Grok.
- Auto-write a new `SKILL.md`. Merge `LESSONS-FROM-TAPE.md`. Grok Bot / `sendPrompt`.
- Enter plus. Hunt agent-builders. Start every inbound at layer 4.

## L. Role-Specific Applications
- Hunt is skipped this walk. Do not open a 50, a named URL, or a Path A from this tape.
- Keep A–K global. Do not hide the stolen machine in L.
- Steal three questions + agent-last. No plus. No hunt.
- Hard step stays HITL: this desk drafts; Evens sends.
