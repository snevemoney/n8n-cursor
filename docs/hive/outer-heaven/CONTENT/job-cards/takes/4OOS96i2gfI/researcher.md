# Researcher — 4OOS96i2gfI
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/4OOS96i2gfI/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Four-layer pyramid (agents overused). Beats: (1) Hundreds of builds/mistakes; coaches “thousands” (UNVERIFIED). (2) Pyramid bottom→top: custom GPT/Claude project/Gem → no-AI workflow → AI workflow → AI agent; up = complexity/cost/maintenance. (3) Be a **problem solver**, not an agent builder; root + quick win + low cost; answer may be automation, a project, or **existing SaaS**. Plus community CTA. (4) **L1 GPT/project:** preconfigured intern; **reactive** — only when you start the chat. His setup-guide sticky for every n8n YT template: automation would still need revision; GPT lets him iterate in-place. (5) **L2 no-AI workflow:** if-then, deterministic, same trigger/sequence/output; can poll (success/error only); runs while you sleep (meeting done, 6am). (6) **L3 AI workflow:** same fixed order, AI for write/plan/classify (email → support/finance/priority/promotion — keywords wouldn’t suffice). Fixed path, intelligent decisions. **~50% of business automations need no AI** (UNVERIFIED); start-up with a business: **agent is almost never first**; even Gems drive efficiency. (7) **L4 agent:** goal, perceive, decide, tools, exceptions, adapt. Marketing/Telegram example: 3 content + 2 image + DB tools; interesting: image tool **is a workflow** — once chosen, path is fixed (helps performance); unlike a one-shot workflow it can loop. (8) Decision tree: In the loop every time? → GPT. Steps 100% logic? → no-AI workflow. Order fixed? → AI workflow. Else agent. (9) Exception: support email *looks* 1-2-3 (read, KB, reply) so could be a workflow; but sometimes 0 KB searches, sometimes 5 — agent wins. Never fully black-and-white; scale up/down after runs. (10) Plus community: 3,000 members, classroom, weekly live (UNVERIFIED). Timestamp UNKNOWN.

## B. Atomic Knowledge

### Climb only when the question forces it
- **Claim:** Most first builds should be L1–L3; agents are last; ~50% of automations need zero AI; tools-as-workflows make agents safer.
- **Reasoning:** Cost/maintenance rise; buyers ask for “AI” when a filter or a GPT would do; classify is a narrow AI step inside a fixed path.
- **Mechanism:** Three questions (loop? logic? fixed order?) + the support exception (variable KB hits).
- **Evidence:** Setup-guide GPT; email classifier; Telegram marketer whose tools are workflows.
- **Conditions:** You can name the manual process.
- **Exceptions:** After months you may promote or demote a layer.
- **Action:** Run the three questions before any “agent.” Hive: still HITL send.
- **Confidence:** high as doctrine; 50%/thousands UNVERIFIED.
- **Source:** `4OOS96i2gfI` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Problem-solver identity. Predictable beats autonomous. An agent whose tools are workflows is a hybrid he likes. You don’t know until it runs.

## D. Procedures
1. Name the root problem and the quick win.
2. Q1: must a human start every time? → GPT/project.
3. Q2: 100% black-and-white? → no-AI workflow.
4. Q3: fixed order? → AI workflow (classify/write inside).
5. Else agent; prefer tools that are themselves workflows.
6. Revisit layer after real runs.
7. Check existing SaaS before building.

## E. Examples
- **Situation:** Setup-guide sticky every video. **Action:** GPT, not a cron. **Reasoning:** Revisions in-chat. **Lesson:** Reactive is a feature.
- **Situation:** Inbox categories. **Action:** AI classify then fixed branches. **Lesson:** One narrow brain, then deterministic.
- **Situation:** Support. **Action:** Could be 1-2-3 workflow; variable KB hits → agent. **Lesson:** Tree is a start, not a law.

## F. Decision Rules
- If they asked for an agent → still run the tree.
- If 50% guess says no AI → try L2 first.
- Refuse: Plus/Skool; quote 50%/3000 as FACT; agent-first on a new client (clients parked anyway).

## G. Contrarian
Title: agents overused. He still sells agents in other tapes — this tape is the brake. Do not flatten with “build anything with agents” shorts.

## H. Assumptions
50% is coaching folklore. Telegram marketer is a YT build. Community numbers UNVERIFIED.
**Desk dissent:** none yet. Aligns with `wk8KV280fbg` (solutions not tools) and `tNOk29fs_aY` (it wasn’t an agent).

## I. Questions
- What SaaS has he actually substituted for a build?
- When he demotes an agent, what signal?

## J. Connections
- **SYSTEM SYNTHESIS:** `9mqsVK6Iqoc` (classifier). `wk8KV280fbg`. `HbsbqMQE-lI` (overkill). `slice-build`. `outcome-offer-funnel`.

## K. Future-Use
Three-question tree as unassigned build-intake.

## Steal / Operate-never

### Machine: three-questions-then-layer
- **Epistemic:** SOURCE
- **Workflow / loop:** root problem → in-loop? → pure logic? → fixed order? → pick L1–L4 → prefer workflow-shaped tools if L4 → revisit after runs
- **Questions / signals:** The three Qs + “is there already a SaaS?”
- **Qualify / frame / objections:** “We need an AI agent” → problem-solver, not agent-builder.
- **Procedure:** D.
- **Example that proves it:** Setup GPT; email AI-workflow; support maybe-agent; marketer tools=workflows.
- **Why it works:** Matches complexity to the job; keeps cost/maintenance down.
- **Conditions / exceptions:** 50% UNVERIFIED. Tree has a known exception (variable tool counts).
- **Operate-never payload:** Plus community; agent-first SKU; quote 50%/3000 as FACT; new ICP.
- **Hive run:** `slice-build` · `inbox-to-task-routing` · `outcome-offer-funnel` · `4OOS96i2gfI` as doctrine pointer
- **Source:** `4OOS96i2gfI` @ UNKNOWN

**Operate-never**
- Agent-first for parked clients. Join Plus. New `icp_id`. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
This is the architecture brake for the rest of the corpus. Put the tree in LEARNED so builder tapes cannot silently overwrite it.
