# Researcher — KVFfApQZhE4
Status: filled
Protocol: deep-video-learning
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/KVFfApQZhE4/full.txt`
**Packet LEARNED:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/KVFfApQZhE4/LEARNED.md`
**ICP:** parked unless Evens named one.

## A. Source Map
Gemini file-search short. Beats: (1) Drop a doc → embeddings → chat; skip a “huge massive” RAG pipeline. (2) Four HTTP steps: create store (“folder”), upload file to Google Cloud (not yet in folder), move file into the store, query. (3) Agent + knowledge tool chats the file in the folder. (4) Demo: 22-page golf-rules PDF. (5) Gold Q: club breaks during the round → short answer (continue / repair / replace if normal play) + source “knowledge base” + rule 4 clubs. (6) Play-button. Timestamp UNKNOWN. Long: `irg-2IfAjpo`. Gemini on-tape. “10x cheaper” is title-only here.

## B. Atomic Knowledge

### Four-step store, not one drop
- **Claim:** “Just drop a file” still is create-store → upload-to-cloud → move-into-store → query.
- **Reasoning:** Upload ≠ in the folder.
- **Mechanism:** Four HTTP requests he will “walk through.”
- **Evidence:** Explicit four-step list + golf PDF moved after upload.
- **Conditions:** Gemini file-search API + n8n HTTP.
- **Exceptions:** Title sells “no pipeline”; body is a four-call pipeline. Do not flatten.
- **Action:** Steal the upload-vs-in-store distinction + gold Q with source+rule number.
- **Confidence:** high.
- **Source:** `KVFfApQZhE4` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Vendor RAG is still a pipeline; he just outsourced chunking. Source+rule-id is the prove (same sport-rules motif as `QrJhdTbK3TU`).

## D. Procedures
1. Create store. 2. Upload. 3. Move into store (do not query after upload only). 4. Ask a gold Q; require short answer + source locator. 5. Hive: do not stand up Gemini HTTP.

## E. Examples
- **Situation:** 22-page golf PDF. **Action:** Four HTTP; ask broken-club. **Reasoning:** Can it cite a rule? **Outcome:** Continue/repair/replace + rule 4. **Lesson:** Source locator is the stop. Implicit rule: upload-only is not retrievable.

## F. Decision Rules
- If upload succeeded but move did not → do not query.
- If answer has no rule/source → fail.
- Refuse: Gemini RAG as hive; quote 10x/22 pages as FACT.

## G. Contrarian
Sells “no massive pipeline,” then draws four HTTP boxes.

## H. Assumptions
22 pages / rule text correct. HTTP is the only n8n path (no native node on this clip).
**Desk dissent:** none yet. Do not merge with OpenAI file search (`QrJhdTbK3TU`) or Drive→Supabase (`Fu6vOfzFmcw`).

## I. Questions
- Cleanup/delete store? Cost? (title 10x not explained here)

## J. Connections
- **SYSTEM SYNTHESIS:** `irg-2IfAjpo` long. `QrJhdTbK3TU`, `Fu6vOfzFmcw`. `golden-test-loop`. Three RAG paths stay separate.

## K. Future-Use
Upload-vs-enrolled-in-store as unassigned ingest check.

## Steal / Operate-never

### Machine: enroll-then-gold-q-with-locator
- **Epistemic:** SOURCE
- **Workflow / loop:** create store → upload → move/enroll → gold Q → require answer + source locator
- **Questions / signals:** Is the file in the store or only in the bucket? Rule number present?
- **Qualify / frame / objections:** “No pipeline” → four calls.
- **Procedure:** D.
- **Example that proves it:** Golf PDF; broken club; rule 4 cited.
- **Why it works:** Enroll is the real ingest; locator proves retrieval.
- **Conditions / exceptions:** Gemini HTTP on-tape. Other RAG stacks exist on other tapes.
- **Operate-never payload:** Gemini file search as hive; new ICP; quote 10x as FACT.
- **Hive run:** `golden-test-loop` · `wiki-ingest` (our corpus only if Evens points)
- **Source:** `KVFfApQZhE4` @ UNKNOWN

**Operate-never**
- Gemini HTTP RAG. New `icp_id`. Send / pay / deploy / book / publish.

## L. Role-Specific Applications
Keep three RAG paths unmerged. No vendor add.
