# Librarian — independent transcript walk

**Status:** filled — 18 takes + roll-up. Evens is the visionary. Steal the machine even from ugly tapes. Do not operate the payload. Do not copy `LESSONS-FROM-TAPE.cursor-draft.md`.
**Lens:** `docs/hive/outer-heaven/CONTENT/job-cards/librarian.md`
**Correlation:** `tape-self-teach-20260814`

---

## 1. `13eo8dWa1Gw` — Manila OFM

**Transcript said:** The speaker reloads a creator dashboard and narrates money arriving in the notification feed, then says the strategy is unlimited Instagram accounts used for mass actions that drive traffic to one OnlyFans page. Device Farm is the factory: cloud phones with unique IP and fingerprint so accounts look unlinked; he creates a phone on 4G/5G proxies, then runs an automation that opens Gmail and pulls the verification code from a paid API (Instacaster) so account creation can batch five-at-a-time across many phones. After creation he moves accounts into a web-automation panel on a VPS, where he can remote-control each icon, run follow/warmup/story/reel jobs, and attach a chatbot that replies in voice-note style and redirects the person to OnlyFans. He shows a live chat where the bot flirts, handles nonsense, and steers to the destination; he notes a RAM cap per VPS so one panel cannot hold an unbounded icon count. He says new accounts still need profile pictures and fake followers, claims the whole loop is how the dashboard fills, and closes by promising a free YouTube course instead of a paid one. Income figures and “unlimited” claims are the speaker’s; they are not ours.

**Librarian take:** The machine to file is many isolated surfaces → one destination, a live dashboard as the receipt, parallel workers that do not share fingerprints, and a panel that turns a follow into a redirect. Persist that architecture with provenance to this `full.txt`; do not persist OTP farms, fake identity, or mass-DM seduction as something we operate. Outer Heaven already has a place for ugly tapes — CHRONICLE and this take — so the job is to keep the pattern and the never in the same room, not to quarantine the video because Cursor once wrote CUT.

## 2. `TL8V41Ea6oM` — Swadia 4 Cs

**Transcript said:** A former CEO walks four agents under a 4 Cs frame: coordination, creativity, clarity, coaching. Coordination starts as Gmail-read plus draft, not send; the prompt has five parts — job, tool, categories, output, boundary — then calendar is added only after the email agent has earned trust, and nothing that moves a meeting goes out without approval. Creativity takes a messy folder of notes and a target (who, length, format, voice) and returns a real file you can judge; he says you stay the director. Clarity is telescope (pull scattered sources into one cited picture) versus microscope (do not “summarize this contract”; extract terms, plain English, risk, questions). Coaching is rehearsal out loud, then break character and ask for the fumbles, then a one-page prep card — first time you say the important thing should not be in the important room. He says promote the agent the way a person gets promoted: visible work, then efficient, then automatic, then handoff. Vendor names on this tape (Cowork, Coda, ChatGPT, Gemini) are on-tape only.

**Librarian take:** This tape is a job-card schema, not a stack change. Persist the five-part prompt and the “earn the next permission” ladder as how we write agent cards and OPERATOR_MEMORY DON’Ts. Coordination’s real keep is architectural: read/classify/draft, send removed — that is already Communications + HITL, and Librarian should refuse to file “never send” as prose when the connector still has send. Clarity’s microscope table is the capture format for contracts and policies we actually ingest; a short summary is receipt noise.

## 3. `sboNwYmH3AY` — Nate wiki

**Transcript said:** Nate shows 36 of his YouTube videos turned into a graph of pages with tags, links, raw files, and takeaways, plus a smaller personal “second brain” for life, businesses, and Q2 initiatives. He cites Karpathy: ingest source documents, let the model maintain indexes and brief summaries, query the wiki; at ~100 articles he says fancy RAG was unnecessary. The folder shape is vault → `raw/` (drop sources) → `wiki/` (pages the model writes) plus `index` and `log`; a CLAUDE.md / claw.md tells the agent how to search and update. He clips a page into `raw`, says ingest, answers granularity questions, and watches pages and backlinks appear; he adds a `hot.md` cache for the executive-assistant vault and skips it on the YouTube vault. Lint is an LLM health check for inconsistency, missing data, and new article candidates. He compares wiki-graph (follow indexes and links, markdown only) to semantic RAG (embeddings, re-embed on change) and says hundreds of well-indexed pages are fine; millions of documents are not. Obsidian is a viewer. Token-drop anecdotes on the tape stay UNVERIFIED.

**Librarian take:** This is our lane, already built: Outer Heaven is the wiki — do not stand up a second wiki app and do not build 8k-node Obsidian theater. Persist the cycle we already own: raw drop → pages + index + log + provenance, lint on a schedule, `hot.md` only where a desk needs a 500-word working set, CLAUDE.md as the schema. Nate’s two-vault split (public corpus vs personal brain) is a keep: tape takes live in `CONTENT/job-cards/takes/`, operator keeps go to OPERATOR_MEMORY / CHRONICLE, and Cursor’s CUT draft is not the scoreboard.

## 4. `eMPWBunaOic` — packet 4

**Transcript said:** The speaker walks Karpathy’s “prompting 2.0”: when the model needs more bits than you will type, declare you are switching to speech, lean back, and ramble a long stream of consciousness — sometimes a 10-minute dump, sometimes a short interview — because the model reconstructs the tangle cleaner than you started. He contrasts a short task prompt plus many correction loops against one long initial ramble plus a short polish, and runs both in parallel on the same job: scrape a creator’s early-channel stats and build a comparison dashboard. Short prompt returns a dark, number-heavy board he scores as usable but hard to read; long ramble returns a lighter, more interactive board plus an “evidence file” assembled from transcripts, Social Blade, Wayback, and community pages. Rules he states: declare the style up top, use any voice-to-text, actually go long, do this at session/project start not on every tweak. Community upsell on the tape is not a lane.

**Librarian take:** File this as `session-bootstrap`, not as a new prompting religion. The keep is one long operator dump (goal, constraints, taste, done-when) into a fresh session, then short loops — that is how Evens already starts projects, and it is how a wiki page should be born: ramble in `raw/`, model writes the clean page, log the ingest. Do not persist the YouTube-race dashboard or the comparison-to-Nick SKU; persist the evidence-file habit (sources listed, not vibes) as the provenance block on anything we keep.

## 5. `kwSVtQ7dziU` — packet 5 — chunk

**Transcript said:** Karpathy on No Priors: since a capability jump he mostly expresses will to agents instead of typing code; the psychosis is “everything that fails is a skill issue” — instructions, memory, parallel sessions. Mastery he describes is not one chat but many isolated workers on non-overlapping slices, reviewed at macro-action size, plus claw-like persistence that loops when you are not looking. Auto-research is the implication: remove yourself as the next-prompt bottleneck; give an objective, a metric, and boundaries, then let it run — he says it found tunings he had missed on a repo he thought was already tight. Caveat he is explicit about: if you cannot evaluate, you cannot auto-research; models are jagged (brilliant on verifiable rails, stuck on soft intent and jokes). He wants research orgs described as markdown (`program.md`) that can themselves be optimized; education shifts from lectures for humans to skills that teach agents, who then teach the person. Home-automation “Dobby,” WhatsApp-as-portal, and untrusted-swarm / blockchain analogies stay on tape. Job-market percentages on the tape are not FACT.

**Librarian take:** Persist three objects, dated to this transcript: (1) isolated workers + a shared plan/index so they do not overwrite, (2) no autonomous loop without a metric we can check, (3) role files (`program.md` / job cards) as the thing we lint — not a second personality app. “Explain it to the agent; the agent explains to the human” is already how job cards and wiki pages should be written. Do not persist home-LAN hacks or a WhatsApp god-object as operate; file them as a unification pattern (many apps → one natural-language door) with the security hold he already states.

## 6. `IVx8OSMbTss` — packet 6 — chunk

**Transcript said:** Nate builds a SaaS in one sitting under six P’s: pain, promise, product, plumbing, packaging, proof. Pain is scraped from his own comments/community plus public threads by parallel research agents; he picks a client-proposal/handoff pack, says there is no real moat if someone can rebuild it, and that primary data plus subject-matter expertise is the only honest idea source. He runs Claude as thought-partner / Fable as project manager (delegate, do not burn the expensive model on execution) and Codex as packager and later as computer-use breaker; they share one folder, use session-handoff when context rots, and write progress back to a plan doc. Packaging (name, logo, waitlist page, email capture, admin CSV) ships and gets a real domain before the product is done — sell-before-build. V1 turns discovery transcripts into a branded 10-slide deck (pain, reframe, metrics, math, why-us, scope, next step); he clicks the customer journey, then a different model finds launch blockers and a security review finds release blockers he would not have seen. He closes on person + pain + promise, waitlist signal, do-things-that-don’t-scale, proof as the unstealable, one free result then charge, and do not scale invites faster than you can absorb churn. Dollar prices, comment counts, and “million-dollar SaaS in a day” are speaker claims, not ours.

**Librarian take:** Persist the six-P packet as an ingest schema for build tapes, and persist session-handoff as a skill: summary + key files + “do nothing until I say” so the next session does not invent a second history. The waitlist + admin CSV is a proof artifact to file, not a second wiki. The sentence to keep is his: outsource collection, not understanding — Librarian’s version is outsource the write, never the keep-gate. Client Pack is parked; the moat he names (operator IP baked into the analyze prompt) is exactly what job cards and LESSONS are for after Evens keeps, not a SKU we launch from this tape.

## 7. `EzQAgnjTq2k` — packet 7

**Transcript said:** Alli Miller tells Greg the phrase “managing agents” is already wrong: she wants infrastructure and escalations, not Susie-go-there. Best prompt she names is three words — “do smart things” — after the workforce can see goals, transcripts, mail, calendar, Notion, Stripe, GitHub; risk tier stays the same (she still checks every email) while breadth expands. She distinguishes trigger automations (drop a recording → transcript + social drafts) from undefined proactive work, which needs a north star, tools, and a sense of trigger. Uncodified judgment gets a daily diary prompt so agents are not inventing from iMessage gaps; a named “Toby” watches the workforce for friction and missing context. She starts founders on one agent → proactive one agent → two agents passing work → a workforce, traditional titles first, then AI-native roles (including a “weirdo in the corner” who only 10xes). Arbitrage she names: AI as watchdog with anomaly-to-action, and build the factory for the next product (login, pay, share, newsletter primitives) instead of one-off apps. Headcount and “company of a thousand” talk on the tape is not a hive org chart.

**Librarian take:** Persist the diary → wiki loop: anything that is not already in mail/meetings/Slack gets dictated once and ingested, or the desks will hallucinate a confirmation that lived on iMessage. Toby-as-friction-watcher is our lint — session-review of what Evens kept correcting, then one DON’T on the card. “Do smart things” is not a keep until goals docs exist; without them it is receipt noise. Do not persist a 34-agent Friends-cast as theater; persist watchdog-over-the-workforce and factory-behind-the-task as how we file break/fix.

## 8. `hGdG-04TkDs` — packet 8

**Transcript said:** The speaker says the one-person company is becoming the default because models can do multi-step work, agents can take actions in tools, and the cost of generation is collapsing — scale unit moves from employees to agents. He is explicit that this is not get-rich-quick: cheap output floods the world with mediocre supply; winners pick a niche with an outcome, package it, and deliver it consistently. Bad positioning is “I do marketing with AI”; good is a named pain for a named group (his example: dental clinics turning reviews into booked appointments). Orchestration is the skill: break a goal, assign steps to workers, review until it matches. Failure mode is generating plans, sites, and brands for weeks with no demand conversations. Playbook: pick a problem people already pay for, build a repeatable AI delivery system, make proof (Loom / screenshot / testimonial), pick one distribution channel, keep human edge on taste and accountability. Prices and “20K/month” on the tape are not FACT.

**Librarian take:** Persist the paradox as a keep-gate: if everyone can generate it, we do not file the output as a lesson — we file the direction, the proof artifact, and the exclusion. The dental review→book loop is a named parked machine (already in the one-person usecase pile), not a new `icp_id` and not a hunt rotation. Librarian’s proof standard stays receipts we measured, not walkthroughs of other people’s Looms.

## 9. `IWdvG9Up8Mc` — packet 9

**Transcript said:** Same operator as the 4 Cs tape, now on a “billion-dollar solo” path that is explicitly not another automation-tour video. Step one is a founders triangle: domain (years in a niche), depth (craft that feels like play), distribution (unfair path to customers) — Harvey is the case study (litigator + modeler + law-firm pilot). Step two is machinery he calls DREAM: demand, revenue, engine, admin, marketing — automate one recurring task in one box this week, not the whole mountain. Step three is moats: counterpositioning, switching-cost habits, proprietary data / learning loops (Cursor keystroke loop is the example). Step four is mindset: deathbed-regret over the inner critic; bring taste, purpose, relationships, judgment. Valuation and “58% of small businesses” claims stay UNVERIFIED. Cursor / Harvey / ChatGPT are on-tape names.

**Librarian take:** Persist DREAM as a catalog rubric for what we bother to ingest: if a tape has no demand, no engine, and no admin receipt, it is a bookmark, not a FACT. Founders triangle is a keep-gate for new lanes — zero of three means we do not open a wiki section. Do not persist Harvey or Cursor as ours; persist “one recurring task in one DREAM box” as the capture-cycle default so the vault does not grow theater.

## 10. `whIp1SOahOM` — packet 10

**Transcript said:** Zubair demos “Jarvis” with a real phone number: the agent calls a restaurant, hears 7pm is full, checks with him, calls back and books 8pm; then calls a mechanic, quotes an oil-change price and a slot, books only after he says yes, and writes the drop-off on his calendar. He can call a hotline; Jarvis demands a code, answers from the local second-brain (client notes, calendar), and hangs up + alerts him if the code is wrong twice. Telegram is a third door (text / voice note) that can pull a lead row, draft a quote in the house format, and write John Doe back into long-term memory. Stack on tape is a voice platform plus Twilio plus a local brain plus Gmail/calendar/sheets; he gates credit-card and other sensitive fields in instructions. Free community gets a prompt pack; paid community gets the finished zip. Auto-dial and voice-agent-to-strangers are the payload.

**Librarian take:** Steal the memory shape: one second brain, tools listed, write-back after every real job, a code gate on the public door, and a notify-on-failed-auth so the operator sees the probe. Do not operate the phone — auto-dial, restaurant/mechanic calling, and “Retail/Vapi” are on-tape only and sit on the operate-never list. Persist the security pattern (code, two strikes, hang up, status) as how we treat any surface that can read the vault; do not file the prompt pack as a hive SKU.

## 11. `nS2FrgXN-EY` — packet 11

**Transcript said:** Mike at Glenn Coco says the best phone reps are not more talented — they have a system around the call. Claude does not place the dial; humans still pick up. Five layers: (1) an AI knowledge builder that turns a client’s site/PDFs/blogs into overview, ICP, competitors, objections, scripts, and a training path so fractional reps ramp in days; (2) in-CRM email draft from that knowledge plus the live call; (3) Playbook Studio — talk tracks as cards (intro, pitch, objections, loops) not a linear Google Doc, drafted inside their screenplay, with top-rep recordings on the card; (4) Claude-helped list build (plant-level manufacturing example) then cheap waterfall enrich, suppress already-pulled contacts, spot-check LinkedIn; (5) every call scored on the same rubric (intro, pitch, qualify, objections, close) so coaching is same-day, not one Friday listen. He says AI-SDR voice agents that try to be the conversation churn; the moat is the human conversation. Dial counts and “99% of teams” are not FACT.

**Librarian take:** Persist “one knowledge spine, many surfaces” — that is wiki-ingest, not a new sales OS. Persist cards-not-linear-docs: LESSONS and job cards should be flip-to-the-objection, not a novel. The scoring rubric is a capture cycle we may run on calls we actually recorded; do not import their floor’s percentages as FACT. Do not operate the dialer or an AI SDR. List-anneal + suppress-already-pulled is already a hive skill — file the exclusion-upgrade loop, do not rotate the live hunt because a manufacturing plant list looked clever.

## 12. `mjg_JUMar04` — packet 12

**Transcript said:** A builder shows a Godot snow scene and refuses the one-prompt game. Four models split jobs: Fable decides what and why, Opus writes mechanics, GPT/Soul and Kimi handle Blender aesthetics. He starts from a Pinterest still, writes a color bible (daylight reference translated to night), and nothing enters the game off-bible. Rule: best model writes the plan; every job is a fresh chat; every job ends in a test; if it fails, that job goes back, not the whole game. Ground is a sliding height field, snow depth slows the walk, footprints paint one fixed-size image the wind can erase. Placeholders first, then Blender scripts (fix a line, re-run — do not ask for realistic meshes). Lighting is a slider panel of presets. Enemies are few and lethal; interior is the same house with roof/wall culled. He says earlier attempts looked right and had no loop.

**Librarian take:** Persist the ingest rule this desk already needed: one job, one fresh session, one test — do not pour eighteen tapes into one wiki page and call it a graph. Color bible = the STYLE_BIBLE / job-card “nothing enters off-schema” rule. Failed job goes back to `raw/` or the take file; we do not rewrite the whole vault because one mechanic failed. Park the game itself; keep the loop.

## 13. `ESIxitOLYoQ` — packet 13

**Transcript said:** A GTM operator builds a lead list inside Claude Code with a short chain: Disco-like CLI for accounts/lookalikes, an auto-prompt creator that anneals an ICP scorer against a seed batch, then AI Arc for contacts. He works a B2B robotics slice because he can write the message, not because a tool picked it. Fit target he states is about 60–70 percent — enough signal, not so tight you delete valid companies — then he tightens employee caps and exclusion strings (SaaS-not-robots, marketing agencies, misclassified domains) in conversation with the tool. Human-in-the-loop until a campaign is winning; YOLO only after that. He uses a session-review skill to mine logs and upgrade skills; he wants arguments/pointers to ICP files so a changed ICP does not leave a stale copy baked into the skill. Recap on tape: hundreds of domains in, a qualified subset, then contacts; extra enrich is optional. LeadGrow / Disco / AI Arc are vendors on tape. Do not treat his counts as our list.

**Librarian take:** This is `list-anneal-funnel` spoken in a terminal: 50-ish batch, score, rewrite exclusions, then more. Persist session-review → skill upgrade as Librarian’s lint (the exclusion file is a LESSON with provenance, not a vibe). Persist arguments-as-pointers: ICP lives in one file we update; skills read it — that stops wiki rot. Do not add an `icp_id` or a `business-lanes.json` row from robotics-on-tape. Do not YOLO the live hunt.

## 14. `I7mpF7_pnPM` — packet 14

**Transcript said:** A 16-year-old films a 10-hour clock: sell first, then build. Idea is an AI analyzer for prediction-market bets (Kalshi / Polymarket), name Polymind, Canva logo, site+MVP+Stripe on Abacus ChatLLM, domain on GoDaddy, $1 week trial with a “refund the dollar if not profitable” line. He copies a competitor’s page structure, wires checkout, tests analyze-from-screenshot, posts on X, messages a personal/business list, posts a School community, makes an Instagram reel. After a pause he reports two people paid the dollar (one from the business network who already trades these markets, traffic mostly from X); the reel flopped. He kills the product: opportunity cost versus his real AI-implementation business; two curious dollars is a signal, not a company. He says if he had no other machine he would chase ten more by Friday; he will not. Abacus is on the never-install list. Betting is the payload. Dollar and view counts are not FACT for us.

**Librarian take:** Persist the kill-with-receipt: shipped a live checkout, got two cards, wrote why it dies, moved on. That is a CHRONICLE-shaped object — do not delete it to keep the vault pretty. Do not operate a betting SKU, do not install Abacus, do not quote the dollar as revenue. The keep is his last line: optimize for a shipped test, not a perfect site — and opportunity is good or bad only against the cost of pursuing it. Park Polymind.

## 15. `f4mI3d-nTrI` — packet 15

**Transcript said:** An MCP spec update makes the protocol stateless: SCP 2575 drops the initialize handshake; SCP 2567 drops the session-ID header. Old pain: session pinned you to the instance that minted it, so load-balanced or restarted pods returned session-not-found unless you added sticky sessions or Redis. Now each request is independent; meta carries protocol/capability; optional `server/discover` replaces the handshake dump; gateways can route on `MCP-Method` / `MCP-Name` headers; list calls get TTL and cache-scope hints. Want state? Mint your own handle (basket id) and pass it as an argument — HTTP-style. Server follow-ups become `input required` plus a serialized request-state the client echoes, so any instance can resume. Long work becomes official tasks (write working, return, poll or subscribe). Authorization was hardened. Deprecations get at least twelve months; SDKs major-bump (TypeScript split server/client + a codemod). He frames it as MCP on normal HTTP, scale-to-zero friendly.

**Librarian take:** Persist a dated wiki note: protocol-stateless, we keep state in files we own (index, log, handles), not in a session the host can lose. That matches Karpathy-wiki and our vault — memory is markdown with provenance, not a pin to one process. File the deprecation window as a Watchdog/Forge calendar fact when we next touch MCP clients; do not stand up a second “MCP wiki.” Breaking-change lists belong in the log, not in LESSONS as folklore.

## 16. `Ums8suyAG1A` — Nate agentic manager

**Transcript said:** Nate opens with job-loss and “Claude Code does most tasks” claims, then quotes a CEO: you lose to a person who uses AI, not to AI. Agentic means you name the outcome and the model takes steps. Three demos in Claude Code on local files: a `/goal` Q2 YouTube digest into a local Excel (pull, scorecard, pillars, missing CTR called out); a cleaning-business job tracker as HTML with memory; a Clay pull of 50 avatar leads enriched with reviews/pain and outreach columns. Common loop: reason, act, verify (screenshot the sheet) before handoff. The one skill he names is being an AI manager: onboard like a new hire (goal, good/bad, avoid), pick one real recurring task and correct until you would use it, then connect tools you already use and keep score (hours or leads vs a baseline). He says Claude asks before it touches, you set permissions. Community/course CTA at the end. Job-loss percentages and “thousands of members” are not FACT. Claude Code as a vendor is on-tape only.

**Librarian take:** This is `agent-as-hire`. Persist the loop onto job cards: talk → one SOP → review → gated connectors → score a number we already track. Do not persist replacement percentages. The YouTube digest and the 50-lead sheet are demo shapes we already route (Publishing / list-anneal + Comms draft, HITL send) — file the shapes, not Nate’s OS as a second brain. Herc 2 is his vault; ours is Outer Heaven.

## 17. `kpMreA9ATOo` — Julian Grok SEO

**Transcript said:** Julian says Grok is now an answer engine that searches the web and cites sites. Method one: hunt a keyword that just appeared on X (his example: Grokbot vs Hermes), ship several formats (two sites, two videos, a Reddit thread), pay an indexer (Indexceptional / Indexational on tape), and claim first-page Google plus Grok citations inside a day. Method two: ask Grok the same question and see if your URLs are sources. Four steps: find a fresh question, write across surfaces, index fast, check Google and Grok, iterate. He says the pages Grok cited were honest, hands-on, specific — case studies, actual use, information gain — not a generic SERP rewrite. He notes Grok picked up new URLs faster than ChatGPT in their test, and that a weaker domain can still get cited inside Grok. Course, “AI boardroom,” and “3,700 businesses” are the CTA. 24-hour #1 and those counts are not FACT.

**Librarian take:** This is `info-gain-cite`. Persist one loop we will actually run: one fresh question in a lane we already operate → one honest page on a surface we already own, built from work we ran → HITL publish → ask Grok the same question → note cite or miss. Do not operate a rank-everywhere farm, a paid indexer SKU, or a second site for the same keyword. Do not rotate the live hunt because a trending pair looked empty. Claude-as-writer stays on tape.

## 18. `QIsJe-nZ5XE` — Nate sell workflows

**Transcript said:** Nate argues that if you have never sold a workflow, starting an agency is the trap: you under-scope, then a hired builder or setter multiplies the bad price and the brand takes the hit. He wants clients who treat AI as investment, not a sinking-ship patch; a discovery question he used was what they think about AI (we do not have to copy that gate). Path: freelance until you have a handful of paid proofs, then consult (“let me figure out what you need”), only then consider a team. Sell steps: one person + one pain (not “I automate anything”); a short Loom that shows destination not the car; outreach in time and money; after go-live, re-measure the same numbers and keep the case study. Consultant frame is systems and where AI should not go, then optional retainers. He says his first paid end-to-end came inbound from something he had already built. Accenture / enterprise roadmap prices on the tape are not our rate card. Plus/School are on-tape communities.

**Librarian take:** This is `solo-then-consult`. Persist: 17 named desks are the bench — no nameless delivery hire until Evens has kept a few Path A proofs. Persist destination-first proof (Loom / walkthrough) and metrics-after-live as what we file; do not persist hours×rate fanfic or AI-excitement as a MUST score. Pain dollars stay UNVERIFIED until the owner says them. Do not add an `icp_id` from this video; Normand stays the live hunt.

---

## Roll-up

- **Skills:**
  - `wiki-ingest` — raw drop → pages + index + log + provenance; Outer Heaven stays the only wiki.
  - `tape-self-teach` — each desk writes `takes/{slug}.md` from `full.txt`; Librarian persists what Evens keeps, not the cursor-draft.
  - `session-bootstrap` — one long operator dump, then short loops; ramble belongs in `raw/`.
  - `agent-as-hire` — onboard like a new hire: one SOP, review, gated tools, score a real baseline.
  - `info-gain-cite` — one honest page from work we ran, then check whether Grok cites it.
  - `list-anneal-funnel` — batch → 60–70% fit → rewrite exclusions; session-review upgrades the exclusion file.
  - `solo-then-consult` — Path A install ourselves; consultant frame after receipts.
  - `agent-job-card` — five-part prompt + earn-the-next-permission; cards, not linear novels.
  - `steal-sheet` — park machines after research; pick `icp_id` later, never from the tape walk.
  - (proposed) `session-review-lint` — mine session logs / desk takes for repeated corrections; one DON’T + instead on the card.
  - (proposed) `capture-cycle-hot` — maintain a short `hot.md` of the last operator keep; not a second-brain app.
  - (proposed) `provenance-gate` — every FACT needs source path + date; tape dollars and job-loss percents stay UNVERIFIED.
  - (proposed) `six-p-packet` — pain / promise / product / plumbing / packaging / proof as the ingest schema for build tapes.
  - (proposed) `program-md-lint` — treat job cards + CLAUDE.md as the org; lint inconsistency the way Karpathy lints a wiki.

- **Other business (parked — not a hunt):** OFM / device-farm / OTP redirect factory; Client Pack proposal-deck SaaS; Jarvis + Retail/Twilio voice assistant; Glenn Coco outbound marketplace; Polymind prediction-market analyzer; Julian rank-everywhere + paid indexer; Nate Plus / School as a lane; dental-clinic review→book; Harvey-style legal AI; LeadGrow / Disco-like list shop; Dobby home-automation claw; YouTube-vs-Nick dashboard.

- **Leverage:** One wiki, one ingest cycle, desk takes as provenance — Evens keeps, Librarian files. Isolated workers writing into a shared index/log without overwriting. Cards and lint beat an 8k-node graph. Proof we measured beats a walkthrough we watched.

- **QoL / values:** Evens is the visionary. Ugly tapes stay in the room. Operate ≠ learn. Persist what Evens keeps — Cursor quarantine is not the scoreboard. Outsource collection, not the keep-gate.

- **Never:** This desk will not operate farms, OTP, fake identity, mass-DM, betting, or auto-dial — and will not stand up a second wiki or delete a CHRONICLE entry to “clean” an ugly tape.
