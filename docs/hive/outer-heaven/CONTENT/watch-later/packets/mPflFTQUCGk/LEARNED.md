# LEARNED — mPflFTQUCGk
Protocol: deep-video-learning
Status: filled
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/mPflFTQUCGk/full.txt`
**Desks merged:** Researcher 2026-08-14. Librarian not yet. Keep later dissent as labeled rows. Do not flatten.
**ICP:** parked. Tape $ UNVERIFIED. No new `icp_id`.
**Note:** Derived from Researcher A–K + Steal after a full `full.txt` walk. Other desks add labeled rows; do not overwrite dissent.

## A. Source Map
Instance-MCP how-to short (sibling of `9IzGe0BBj_c`). Beats: (1) n8n settings → MCP access → enable. (2) Claude: add connectors; n8n native; connect; first time paste server URL. (3) Instead of write-email-and-copy, “send that off now” **if** an n8n send-email workflow is available over MCP. (4) “use n8n to send… michael@dundermifflin.com” → search workflows → get details (always allow) → three body fields + webhook → always allow → confirmation; email visible. (5) Busy day: ClickUp “email Michael about PTO” urgent due today → “use n8n to move… to complete” via ClickUp task-manager agent → found, done; ClickUp shows complete. (6) Play-button. Timestamp UNKNOWN. Long: `5p5cV0yVDvQ`.

## B. Atomic Knowledge

### Search-then-always-allow-then-send
- **Claim:** Claude can find a send-email workflow, read its schema, and fire it, if you always-allow the tools.
- **Reasoning:** You stay in chat; no manual POST.
- **Mechanism:** Enable instance MCP → connect → NL “send” → search → details → execute.
- **Evidence:** Dunder Mifflin email appears.
- **Conditions:** A send workflow already exists; you click always-allow.
- **Exceptions:** Always-allow is the opposite of hive HITL. Instance-wide search is the blast radius from `9IzGe0BBj_c`.
- **Action:** Learn the loop; operate-never always-allow send.
- **Confidence:** high as a demo.
- **Source:** `mPflFTQUCGk` @ UNKNOWN
- **Epistemic:** SOURCE

### Chat as a remote for existing ops
- **Claim:** ClickUp complete can be done from Claude via an n8n task-manager workflow so you do not context-switch.
- **Reasoning:** “I’m right here.”
- **Mechanism:** NL → find ClickUp workflow → complete.
- **Evidence:** PTO task moves to complete.
- **Conditions:** That workflow exists and is callable.
- **Exceptions:** Completing “email Michael” without proving the email was the same send is a possible desync (INFERENCE).
- **Action:** Steal “remote the existing workflow”; do not mark done without the artifact.
- **Confidence:** high he marked complete; medium that it matched the send.
- **Source:** `mPflFTQUCGk` @ UNKNOWN
- **Epistemic:** SOURCE + INFERENCE (desync)

## C. Mental Models
MCP makes the instance a toolbelt (same metaphor as `9IzGe0BBj_c`). Always-allow is convenience. He shows send, then a separate complete — two workflows.

## D. Procedures
1. Know MCP is on and which client is connected.
2. Only call a send workflow if a human already liked the draft (he says this) — hive: still HITL send.
3. Do not always-allow.
4. Do not mark a task complete unless the artifact exists.

## E. Examples
- **Situation:** Draft email he likes. **Action:** “Use n8n to send to michael@…” **Reasoning:** Workflow already sends. **Outcome:** Mail in inbox. Then PTO task completed from chat. **Lesson:** Two calls (send, complete). Implicit rule: always-allow is how the demo stays fast — and how blast radius grows.

## F. Decision Rules
- If send → HITL, never always-allow.
- If complete → require the artifact.
- Refuse: instance MCP on hive; Claude as operator.

## G. Contrarian
`9IzGe0BBj_c` celebrated instance MCP; this clip shows why (send + complete without leaving chat). Researcher still operate-never the combo.

## H. Assumptions
Always-allow is safe in his instance. Dummy recipient. Native Claude connector exists.
**Desk dissent:** Same as `9IzGe0BBj_c` — learn, do not enable. Do not flatten “he liked the email so send is fine” into hive auto-send.

## I. Questions
- Can it activate workflows that are off?
- Scope of “always allow this tool”?

## J. Connections
- **SYSTEM SYNTHESIS:** `9IzGe0BBj_c`, `5p5cV0yVDvQ`. `send-removed`. `nQtogLs_dlg` (also emails Michael Scott).

## K. Future-Use
“Liked draft ≠ always-allow” as an unassigned MCP policy line.

## Stolen machines

### Machine: find-schema-then-hitl-exec
- **Epistemic:** SOURCE (find+schema+exec) + INFERENCE (HITL, no always-allow)
- **Workflow / loop:** NL intent → search workflows → read schema → human allow once → exec → verify artifact → only then complete the task
- **Questions / signals:** Is send in the tool list? Did we always-allow? Is the mail actually there?
- **Qualify / frame / objections:** “I’m busy / stay in chat” → still HITL on send.
- **Procedure:** D.
- **Example that proves it:** Search send-email → three fields → mail appears; later ClickUp complete.
- **Why it works:** Schema discovery beats hand-built POSTs; verification beats “it said sent.”
- **Conditions / exceptions:** Instance MCP on. Speaker uses always-allow; hive does not.
- **Operate-never payload:** Instance MCP; always-allow send; Claude connector; new ICP.
- **Hive run:** `send-removed` · `ask-principal`
- **Source:** `mPflFTQUCGk` @ UNKNOWN

**Operate-never**
- Enable instance MCP. Always-allow. Auto-send. New `icp_id`. Send / pay / deploy / book / publish.

## THINK / BEHAVE / TRICKS / USE
**Added:** 2026-08-14 last-mile. Caption-only. Visual/click UNKNOWN unless `watch.json`. Do not flatten this speaker into a hive personality.

### THINK
Decision order, what they ask before they build, what they ignore, how they choose tools, when they kill vs continue — see §C Mental Models and §F Decision Rules above. Desk that must think this way: see TAPE-WIRE-NOTES.

### BEHAVE
What they repeatedly check, skip, retry, and speech≠behavior — see §A / §E / speech≠behavior rows. Sequence-from-speech only. `multimodal-youtube-learning`: no invented clicks.

### TRICKS
Do / don’t and implicit shortcuts — see §D Procedures and Stolen machines. Shown system (files, loops, UI, offer, CTA) mapped to Cursor+Grok primitives on the named workflow. Caption-only = transcript-implied / unobserved.

### USE
Each trick lands as a desk **action** on Cursor + Grok Bot (not a quote). Operate-never on their vendors. Reproduce card: `job-cards/takes/_knowledge-use/{{slug}}.md`.
