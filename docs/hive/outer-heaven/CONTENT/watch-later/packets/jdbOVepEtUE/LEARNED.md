# LEARNED — jdbOVepEtUE
Protocol: deep-video-learning
Status: filled
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/jdbOVepEtUE/full.txt`
**Desks merged:** Researcher 2026-08-14. Librarian not yet. Keep later dissent as labeled rows. Do not flatten.
**ICP:** parked. Tape $ UNVERIFIED. No new `icp_id`.
**Note:** Derived from Researcher A–K + Steal after a full `full.txt` walk. Other desks add labeled rows; do not overwrite dissent.

## A. Source Map
Caption-only (`full.txt`, ~10121 lines). Title: Claude Code for Non-Coders (6h). Nate Herk. Visual/click **UNKNOWN**. Timestamp **UNKNOWN**. Beats: (1) Chat / Cowork / Code. Cowork built in ~a week with Code. Code = local files + web; same models (Opus/Sonnet/Haiku/Fable). Harness = car; you drive. Six skills: **AI person** (relative; one tool + one weekly workflow; IBM 2026 **85%** CEOs want functional leaders as domain-tech **UNVERIFIED**) → **taste** (your name is on it; em-dash tell; feed corrections) → **context engineer** (Karpathy; intern onboard; AIOS) → **iteration** (bike; ugly PC; north-star metric then maintain) → **Jarvis** (trigger audit; vending vs slot; Stripe→Slack = no agent) → **unemployment insurance** (one passion, many branches; check contract). AI-native = what your hand reaches for. Don’t quit in the dip. You are a **manager**. (2) Install desktop (or VS Code). Pro then **$100/$200** Max. $200 vs **$100k** hire; Max ≈ **$8k** inference **UNVERIFIED**. Find file + vision. `/goal` Q2 YT Excel ~**10 min / 75 videos / ~$0.63 / 25k out / 428k of 1M** **UNVERIFIED**. Prompt: role + context + **negative** + **prove its work** (60%→80% first pass). Tokens: Haiku $1/$5, Sonnet $3/$15, Opus $5/$25 per M; Fable ~2× Opus; output dearer; 5h + weekly + Fable meters. (3) `CLAUDE.md` = router (role + where things live). Global kill-list vs project. New folder → trust → init `.claude` (settings / agents / skills). API = password in `.env` + gitignore. Tavily: works → clear → forgets → write endpoint into CLAUDE.md. MCP = another plug; CLI later. (4) Modes: auto / manual / accept-edits / plan / **bypass**. Horror: DB wipe; **150k-list discount** (prompt ≠ lock). Scope keys (ElevenLabs sound-effects only). Deny delete in settings, *then* bypass. Fast mode almost never. Effort: stay **high**; GPT 5.5 effort ≠ score; Fable high overthinks; Opus extra-high slower. Privacy: closed models leave the box; Oct 8 train = forced opt-in; still have the data. Don’t chuck company docs. (5) Tier-1 connect: revenue / customers / calendar / comms / tasks / meetings / knowledge (Stripe, Skool, ClickUp, Slack, Fireflies, Drive). Habit: % of day from one window. **GWS CLI** > MCP (one tool, 100 recipes, visual-validate slides). Desktop **connectors** easy but **not portable** — `.env` so Codex/Hermes/OpenClaw/VS Code can sit on the same folder (operate-never those as hive). (6) Skills = recipes + YAML front matter + progressive disclosure. Proactive vs after-the-fact. Inbox triage: brief-only first; confirm then mark-read. Complex skills call files/sub-agents. Steal markdown (Matt Pocock grill). Re-run on new models. Skills transfer if the folder name matches the harness. (7) Dumb zone / rot. Reset ~**250–300k** (Nate+Cole). **Session handoff > `/compact`** (auto-compact too late). Memory: automemory + `memory.md` + CLAUDE.md + HerkBrain wiki. Slop = trust tax; draft≠send; “outsource thinking, not understanding.” (8) Pick-to-build: constraint pipe (5× tomorrow what breaks); north star before build; can’t automate what you can’t map; Lincoln 4/6 sharpen. Sub-agents = fresh window + cheaper model; custom = same as skill.md; global vs project; roast/plan-roaster; misfire → fix description. Agent teams = talk-to-each-other (experimental env); war-room/debate; token-heavy; he had to **stop** a fake team of six parallels. IBM PDF debate: **10% vs 72% / 25% vs 86% / +17% overrated** **UNVERIFIED** — **do not flatten** vs `8MEJen0nblQ` 76% · `LVAHYV4Xrto` 86%. Artifacts = hosted HTML. (9) Websites: localhost ≠ share. `CLAUDE.md` + frontend-design skill + GitHub + **Vercel** HITL. Pyramid: skill (human send) → workflow (no AI) → AI workflow (fixed order) → agent (unknown tools). **No send tool if you don’t send.** Prompt is a suggestion. ClickUp routine went rogue into public channels (forgot it existed 2 months). Bike: watch, then maybe remove hands. Evals = golden set before prod. Modal: “what could go wrong / fail safe.” (10) Karpathy LLM wiki: raw→wiki+index+log; Obsidian optional visual (he rarely opens it). **383 files / 100 transcripts / −95% tokens** **UNVERIFIED**. Five levels: exact word → topic → semantic → relationship → autonomous; he **stays L2**; no pain → don’t climb. Codex twin = `AGENTS.md`. Hermes/GBrain named — operate-never. (11) Routines: local needs PC on; cloud = clone repo, **no `.env`** → cloud-env secrets; trusted vs **full** network; stateless (no cookies); Playwright/Skool failed remote; **15/day** Max, Pro ~3–5 **UNVERIFIED**; one-shot prompt; dedicated small repo > fat Herk 2. Prefer **script on Modal/Trigger.dev** when the path is linear. Daily brief: Tavily+OpenRouter+hardcoded ClickUp DM (“boring is beautiful”). Webhook form = **zero AI**. `/goal` used for the webhook build (keep vs overnight-warn tapes). Remote-control phone; `/clear` bug on desktop. (12) Token hacks: reread compounds (**98.5%** history **UNVERIFIED**); `/clear` between tasks; drop unused MCP; CLI>MCP; batch; plan first; `/context` `/cost`; status line; watch it; lean CLAUDE.md **<200 lines**; `@file`; compact ~**60%** then handoff after 3–4; 5-min cache break; command-output bloat; Sonnet default / Haiku subs / Opus <20%; teams 7–10×; peak 8a–2p ET; cache 10¢, sub TTL 1h / API+subs 5m; model-switch breaks cache (opus-plan setting). Close: 4 Cs — context / connections / capabilities / cadence. Next course = AIOS (Skool). **Clients parked.** Hive Cursor+Grok. Operate-never: OpenClaw/Hermes/Vapi/Higgsfield/auto-send/auto-post.

## B. Atomic Knowledge

### Manager sandwich: map → simplest machine → prove → scar
- **Claim:** You manage, you don’t vibe. Taste signs your name. Context > prompt. Iterate to a north-star then maintain. Jarvis only after the bike is watched. Vending (script) before slot (agent). Constraint in the pipe, not a sexy demo. Lincoln: map before build.
- **Reasoning:** Slot machines fail in unexpected ways; a clog you didn’t name cannot be automated.
- **Mechanism:** Write the process; pick one metric; default no-AI; prove-work (browser/100-form/screenshot); feed the miss into CLAUDE.md/skill.
- **Evidence:** Inbox brief-only then confirm; ClickUp stats routine went public; 150k-list send; webhook form with zero AI.
- **Conditions:** Claude Code on-tape. Hive: `golden-test-loop` / `context-docs`. No Claude switch.
- **Exceptions:** `/goal` used for a short webhook — keep vs overnight-warn tapes.
- **Action:** Steal manager-loop + vending-first. No auto-send.
- **Confidence:** high as the loop.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** Tavily forget-after-clear; fake agent-team; `/clear` on phone remote
- **Speech ≠ behavior:** “always-on Jarvis” vs he forgot a routine for 2 months.

### Folder OS + portable secrets; handoff before dumb zone
- **Claim:** The product is folders. Global vs project CLAUDE.md. `.env`+gitignore, not desktop connectors (those die on harness switch). Skills = markdown recipes (front matter). Reset ~250–300k via **session handoff**, not late compact. Wiki (Karpathy) compounds; stay at the lowest level that doesn’t hurt (he stays L2). Codex twin file = `AGENTS.md`.
- **Reasoning:** 1M is a false ceiling; connectors lock you to one app.
- **Mechanism:** Router CLAUDE.md; progressive disclosure; GWS CLI over MCP dump; copy handoff → `/clear` → paste.
- **Evidence:** Tavily scar written to CLAUDE.md; 20-MCP dump named earlier in corpus; 250k = Cole’s number too.
- **Conditions:** Keep vs `iTY8Q449YNQ` handoff · `3GAxd90fEE4` compact · `RzLV8sfFdMM` 250k · `3TdD8Qv5Tk8` Codex. Hive Cursor+Grok.
- **Exceptions:** Compact at ~60% if you must; after 3–4, handoff.
- **Action:** Steal folder-OS + handoff + CLI. No Hermes/OpenClaw port.
- **Confidence:** high as the hygiene.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** YouTube routine looked in `.env` on the clone
- **Speech ≠ behavior:** “tool-agnostic” vs the course is Claude-first.

### Tool-layer lock + linear deploy; routines are a last resort
- **Claim:** If it must not send/delete, **do not give the tool**. Prompt is a suggestion (101st time). Local cron dies if the PC is off; cloud routine = clone without secrets → put keys in cloud-env; full network if the host isn’t “trusted.” Stateless = no cookies. Prefer a **hardcoded Python on Modal** when the path is 1-2-3. Hardcode the DM. Watch `run now` before schedule. 15 cloud runs/day on Max.
- **Reasoning:** A forgotten routine with a send tool is a blast waiting.
- **Mechanism:** Deny-list then maybe bypass; Modal secrets; webhook not poll; fail-safe + logs.
- **Evidence:** 150k email; ClickUp public-channel drift; Skool Playwright no cookies; form webhook zero-AI.
- **Conditions:** Hive: `send-removed` / `ask-principal`. Deploy/publish HITL. Same assume-touch as `RzLV8sfFdMM`.
- **Exceptions:** Cloud routine only when you truly need the agentic loop.
- **Action:** Steal no-send-tool + boring-script. No Vercel/Modal without HITL.
- **Confidence:** high as the lock.
- **Source:** `jdbOVepEtUE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** trusted-network ClickUp fail; YouTube env miss
- **Speech ≠ behavior:** “15/day” vs he was at 0/15 while testing.

## C. Mental Models
You drive the car. Taste is the signature. Context is the intern packet. Vending before slot. Constraint before demo. Folder is the OS. Prompt ≠ lock. Handoff before rot. Lowest wiki level that works. Boring deploy. Cache is a habit. Hive stays Cursor+Grok.

## D. Procedures
1. One tool, one weekly workflow, one north-star number.
2. New folder. Lean CLAUDE.md (router, <200 lines). `.env`+gitignore.
3. Connect only tier-1 tools via API/CLI, not desktop connectors.
4. Liked run → skill. Brief-only until confirmed.
5. Prove-work. No send/delete tool if you don’t mean it.
6. ~250k or task-switch → session handoff, not a fat compact.
7. Linear job → Modal/script. Agentic leftover → watched routine, small repo, cloud-env keys.
8. Hive: Cursor+Grok. Clients parked. Deploy/send HITL.

## E. Examples
- **Situation:** Tavily after `/clear`. **Action:** write endpoint into CLAUDE.md. **Outcome:** next chat uses it. **Lesson:** scar the recipe.
- **Situation:** ClickUp weekly stats. **Action:** prompt-only DM. **Outcome:** public channel. **Lesson:** tool-layer.
- **Situation:** Form lead. **Action:** template, no LLM. **Outcome:** works. **Lesson:** simplest.
- **Situation:** “Agent team.” **Action:** six parallels. **Outcome:** stop + skill. **Lesson:** watch + name the function.

## F. Decision Rules
- IF no north star → do not build.
- IF if-this-then-that → script, not agent, not routine.
- IF it can send → assume it will; remove the tool.
- IF context ~250k or new topic → handoff/`/clear`.
- IF cloud routine → secrets in cloud-env; `run now` first; small repo.
- Refuse: OpenClaw/Hermes/Vapi; auto-send; new ICP; treat IBM/tape $ as fact.

## G. Contrarian
Unemployment-insurance / job-stack. He stays wiki L2. Obsidian optional (vs anti-Obsidian tapes). Hitting the limit = power-user, not failure. Opus-plan setting **breaks cache**. IBM numbers on this tape ≠ other IBM tapes. `/goal` used casually.

## H. Assumptions
$100/$200, $8k inference, $0.63/25k, 75 videos/10 min, 85%, 150k/100k list, 15/day, 98.5%, 91M/300M cache, 383/100/−95%, 10%/72%/25%/86%, 5× pipe, $200k ads → $1M = **UNVERIFIED**.
**Desk dissent:** vs `iTY8Q449YNQ` handoff · `3GAxd90fEE4` compact · `RzLV8sfFdMM` 250k/hooks · `8MEJen0nblQ` IBM 76% · `LVAHYV4Xrto` 86% · `3TdD8Qv5Tk8` Codex · Serop anti-Obsidian. Hive Cursor+Grok. Clients parked.

## I. Questions
- Did the 15/day cloud-routine cap hold?
- Is 250k still the feel on current Opus?
- Did `/clear` on desktop remote-control get fixed?

## J. Connections
- **SYSTEM SYNTHESIS:** `RzLV8sfFdMM` · `iTY8Q449YNQ` · `3TdD8Qv5Tk8` · `8ktcSaSTvxk` KPI. Skills: `golden-test-loop` · `context-docs` · `workflow-compiler` · `ask-principal` · `send-removed`.

## K. Future-Use
Manager-loop. Vending-first. Folder-OS. Portable `.env`. Prove-work. No-send-tool. Handoff@250k. Lean router. CLI>MCP. Linear→Modal. Watch-first routine. Lowest wiki level.

## Stolen machines

### Machine: map-vending-prove-handoff
- **Epistemic:** SOURCE
- **Workflow / loop:** north-star + map → simplest (script/skill) → `.env`+lean CLAUDE.md → prove → scar → handoff before rot → deploy only if linear+hardcoded+watched
- **Questions / signals:** What’s the clog number? Does this need an LLM? Can it send? Are we in the dumb zone? Is the routine a clone without secrets?
- **Qualify / frame / objections:** Six-hour Claude course ≠ hive stack. Hermes/OpenClaw named as portable *targets* — hive does not switch. IBM % stay on this tape.
- **Procedure:** D.
- **Example that proves it:** Tavily scar; 150k send; ClickUp rogue; zero-AI webhook; fake team stopped.
- **Why it works:** A manager with a folder, a lock, and a watched first run beats a 24/7 agent with a send tool.
- **Conditions / exceptions:** Claude Code on-tape. Hive Cursor+Grok, HITL send/deploy, no new ICP.
- **Operate-never payload:** OpenClaw/Hermes/Vapi/Higgsfield; auto-send/auto-post; Claude Code as hive; unattended Vercel/Modal; new `icp_id`.
- **Hive run (existing skills only):** `golden-test-loop` · `context-docs` · `workflow-compiler` · `ask-principal` · `send-removed`
- **Source:** `jdbOVepEtUE` @ UNKNOWN

**Operate-never**
- OpenClaw / Hermes / Vapi / Higgsfield. Auto-send / auto-post. Claude Code as hive default. Publish/deploy without HITL. New ICP.

## THINK / BEHAVE / TRICKS / USE
**Added:** 2026-08-14 last-mile. Caption-only. Visual/click UNKNOWN unless `watch.json`. Do not flatten this speaker into a hive personality.

### THINK
Decision order, what they ask before they build, what they ignore, how they choose tools, when they kill vs continue — see §C Mental Models and §F Decision Rules above. Desk that must think this way: see TAPE-WIRE-NOTES.

### BEHAVE
What they repeatedly check, skip, retry, and speech≠behavior — see §A / §E / speech≠behavior rows. Sequence-from-speech only. `multimodal-youtube-learning`: no invented clicks.

### TRICKS
Do / don’t and implicit shortcuts — see §D Procedures and Stolen machines. Shown system (files, loops, UI, offer, CTA) mapped to Cursor+Grok primitives on the named workflow. Caption-only = transcript-implied / unobserved.

### USE
Each trick lands as a desk **action** on Cursor + Grok Bot (not a quote). Operate-never on their vendors. Reproduce card: `job-cards/takes/_knowledge-use/{{slug}}.md`.
