# LEARNED — pxzo2lXhWJE
Protocol: deep-video-learning
Status: filled
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/pxzo2lXhWJE/full.txt`
**Desks merged:** Researcher 2026-08-14. Librarian not yet. Keep later dissent as labeled rows. Do not flatten.
**ICP:** parked. Tape $ UNVERIFIED. No new `icp_id`.
**Note:** Derived from Researcher A–K + Steal after a full `full.txt` walk. Other desks add labeled rows; do not overwrite dissent.

## A. Source Map
Caption-only (`full.txt`, ~909 lines). Title: Watch Me Build a Multi-Agent Newsletter System in n8n (step-by-step). Visual/click **UNKNOWN**. Timestamp **UNKNOWN**. Beats: (1) Linear n8n: week research → planner (title+topics) → per-topic deep research → section writers → aggregate → editor HTML → **you** for approval. Wireframe first (Excal/Miro): weekly trigger, green=AI, three topics, aggregate, editor. Stack: n8n + Tavily×2 + Gmail + OpenRouter (GPT-5 family “new/fun”). (2) Schedule weeks / Sunday midnight — leave **inactive**. Tavily search: niche “AI adoption for SMBs,” topic=news, past week, max 3 (demo). n8n ≥1.101 + community nodes. Tavily **1,000** free searches/mo. Rename + pin (`P`). Planner: GPT-5 Mini; format-array helper from School pack (vs drag three articles). Specialist system: title + 3–5 word topics. First run dumps one blob → **structured output parser** (JSON schema) so title/topics are fields. Split Out → three items. (3) Second Tavily: query=topic, general, past month, **include raw content** (first pass was summaries only). Hostinger self-host sponsor (KVM2, code Nate Herk 10% — on-tape). Section writer: same Mini, topic+formatted results; “one section, cite, no intro/outro.” No parser. Aggregate `output` 3→1. Editor: **GPT-5** (not Mini) for HTML voice; `join` with `\n\n\n`; must rename planner or `$()` refs break. Long system: intro/outro/sources/HTML. Parser → `subject` + `content`. Run ~4 min (he expected 30–90s). Gmail **create draft** (he also “send to test”); type=HTML or you see raw tags. (4) Draft: Gap-to-Growth; Aug 17 2025 brief; per-sentence Source; clickable list. Tune labels in prompt (Forbes vs “source”). Unpin, activate weekly. Next: log sheet + feedback, error handling, HITL revision, extra research (Perplexity/SERP). School JSON/PDF + Plus. **Do not flatten** vs `vFepZE_wrfg` Claude newsletter · hive no Tavily/Hostinger. All $ UNVERIFIED.

## B. Atomic Knowledge

### Wireframe + specialist agents + structured fields
- **Claim:** One mega-agent “write a newsletter” overwhelms. Planner / writer / editor each own one job. Parser turns a blob into drag-able fields (subject vs body).
- **Reasoning:** Downstream nodes need names, not a paragraph.
- **Mechanism:** School format-array + JSON schema parser; Split Out; Aggregate; join with newlines; rename nodes so refs stay green.
- **Evidence:** First planner dump; after parser, three topic fields; `$()` broke until “planning agent.”
- **Conditions:** n8n/OpenRouter/Tavily on-tape. Hive no vendor.
- **Exceptions:** Section writer skipped parser because aggregate eats `output`.
- **Action:** Steal specialist+parser+rename. `workflow-compiler`.
- **Confidence:** high as the graph machine.
- **Source:** `pxzo2lXhWJE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** blob output; broken `$()`; 4-min editor
- **Speech ≠ behavior:** “send it off” vs draft-first (and a test send).

### Two Tavily modes; pin; don’t activate the Sunday gun
- **Claim:** Pass 1 = news + week + no raw (headlines for topics). Pass 2 = raw content so writers can cite. Pin so you don’t re-bill. Schedule stays off until you mean it.
- **Reasoning:** Topic discovery ≠ draft research. Accidental weekly fire = spend + a public draft.
- **Mechanism:** Options: topic/time/max/raw. Hygiene: spacebar rename, `P` pin.
- **Evidence:** Three dated SMB articles; three raw topic packs.
- **Conditions:** 1,000 free searches UNVERIFIED. Community-node version gate.
- **Exceptions:** He says you can add Perplexity/SERP later.
- **Action:** Steal two-pass research + pin + inactive trigger. `warm-draft-hitl`.
- **Confidence:** high as the research split.
- **Source:** `pxzo2lXhWJE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** none on Tavily itself
- **Speech ≠ behavior:** “once a week in production” vs he tells you to leave it off.

### Draft + cite; Mini plans, full model edits; log before you scale
- **Claim:** Gmail draft (HTML). Cite per claim. Editor is the expensive brain. After v1: sheet log + prompt edits + error handling — not more agents first.
- **Reasoning:** Uncited newsletters are unverifiable. Mini is enough to pick topics.
- **Mechanism:** Parser subject/content; email type HTML; prompt-tune source labels.
- **Evidence:** Per-sentence Source; 4-min editor; he likes Claude-for-prose historically, GPT-5 “impressed.”
- **Conditions:** Keep vs `vFepZE_wrfg` (Claude WAT newsletter, Key images).
- **Exceptions:** He sent a test copy — hive still draft-only.
- **Action:** Steal draft+cite+log. `send-removed` · `info-gain-cite`.
- **Confidence:** high as the publish gate.
- **Source:** `pxzo2lXhWJE` @ UNKNOWN
- **Epistemic:** SOURCE
- **Knowledge type:** declared
- **Modality:** speech
- **Evidence status:** transcript-implied
- **Shown / clicked / sequenced:** UNKNOWN
- **Failed / retried:** 4-min surprise
- **Speech ≠ behavior:** Hostinger “unlimited executions” mid-build.

## C. Mental Models
Whiteboard before nodes. One job per agent. Parser is the contract. Discovery search ≠ write search. Pin is money. Trigger off until trusted. Draft is the product. Cite or don’t ship. Log is how prompts evolve.

## D. Procedures
1. Wireframe trigger → research → plan → split → deep research → write → aggregate → edit → draft.
2. Tavily-1 news/week; pin. Planner + schema. Split.
3. Tavily-2 raw; pin. Writer (no intro). Aggregate.
4. Editor full model + join + parser subject/content.
5. Gmail **draft** HTML. Human edits. Then maybe activate.
6. Add a log sheet before a second source.
7. Hive: no Tavily/Hostinger/School JSON. `send-removed`.

## E. Examples
- **Situation:** Planner one field. **Action:** JSON parser. **Outcome:** drag-able topics. **Lesson:** schema or you can’t split.
- **Situation:** `$()` red. **Action:** rename planner. **Outcome:** title flows. **Lesson:** names are APIs.
- **Situation:** First Tavily summaries. **Action:** raw on pass 2. **Outcome:** citable sections. **Lesson:** two research modes.
- **Situation:** Text email. **Action:** he warns tags. **Outcome:** set HTML. **Lesson:** type must match the editor.

## F. Decision Rules
- IF one agent would write the whole letter → split jobs.
- IF you need fields downstream → parser.
- IF trigger is weekly → keep inactive until a logged good draft.
- IF email type=text → you’ll ship markup.
- IF you dislike “Source” labels → prompt, don’t rebuild the graph.
- Refuse: Tavily/Hostinger as hive; auto-send list; new newsletter ICP.

## G. Contrarian
Three agents in n8n, not a Claude WAT project (`vFepZE_wrfg`). GPT-5 Mini for plan/write, full GPT-5 only to style — opposite of “always the smartest model.”

## H. Assumptions
1,000 Tavily, Hostinger 10%, 4 min, GPT-5 “new” = **UNVERIFIED**.
**Desk dissent:** vs `vFepZE_wrfg` · hive Cursor+Grok · no community-node chase.

## I. Questions
- Same School format-array as other n8n packs?
- Draft vs send — which Gmail op is in the JSON?

## J. Connections
- **SYSTEM SYNTHESIS:** `vFepZE_wrfg` · `tFFKuq2t0rI` split-brain. Skills: `workflow-compiler` · `warm-draft-hitl` · `send-removed` · `info-gain-cite` · `golden-test-loop`.

## K. Future-Use
Wireframe-first. Specialist+parser. Two-pass research. Pin. Inactive schedule. Draft+cite. Log-then-tune.

## Stolen machines

### Machine: wireframe → specialist-parser → two-pass research → draft-cite
- **Epistemic:** SOURCE
- **Workflow / loop:** board → schedule-off → news pass → plan+schema → split → raw pass → write → aggregate → edit+parser → Gmail draft → human → log
- **Questions / signals:** Raw on? Parser on? Trigger active? HTML type?
- **Qualify / frame / objections:** Template ≠ a list business. Hostinger is a mid-roll.
- **Procedure:** D.
- **Example that proves it:** blob→schema; `$()` rename; 4-min editor; per-sentence Source.
- **Why it works:** Contracts between nodes beat one chat that “writes the newsletter.”
- **Conditions / exceptions:** n8n on-tape. Hive compiler + HITL.
- **Operate-never payload:** Auto-send; Tavily/Hostinger SKU; School JSON as the offer.
- **Hive run (existing skills only):** `workflow-compiler` · `warm-draft-hitl` · `send-removed` · `info-gain-cite`
- **Source:** `pxzo2lXhWJE` @ UNKNOWN

**Operate-never**
- Tavily / Hostinger / OpenRouter. Activate Sunday send. New `icp_id`. Quote 1,000 searches as FACT.

## THINK / BEHAVE / TRICKS / USE
**Added:** 2026-08-14 last-mile. Caption-only. Visual/click UNKNOWN unless `watch.json`. Do not flatten this speaker into a hive personality.

### THINK
Decision order, what they ask before they build, what they ignore, how they choose tools, when they kill vs continue — see §C Mental Models and §F Decision Rules above. Desk that must think this way: see TAPE-WIRE-NOTES.

### BEHAVE
What they repeatedly check, skip, retry, and speech≠behavior — see §A / §E / speech≠behavior rows. Sequence-from-speech only. `multimodal-youtube-learning`: no invented clicks.

### TRICKS
Do / don’t and implicit shortcuts — see §D Procedures and Stolen machines. Shown system (files, loops, UI, offer, CTA) mapped to Cursor+Grok primitives on the named workflow. Caption-only = transcript-implied / unobserved.

### USE
Each trick lands as a desk **action** on Cursor + Grok Bot (not a quote). Operate-never on their vendors. Reproduce card: `job-cards/takes/_knowledge-use/{{slug}}.md`.
