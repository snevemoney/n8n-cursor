# LEARNED — kB9iMD0EjT8
Protocol: deep-video-learning
Status: filled
**Source:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/kB9iMD0EjT8/full.txt`
**Desks merged:** Researcher 2026-08-14. Librarian not yet. Keep later dissent as labeled rows. Do not flatten.
**ICP:** parked. Tape $ UNVERIFIED. No new `icp_id`.
**Note:** Derived from Researcher A–K + Steal after a full `full.txt` walk. Other desks add labeled rows; do not overwrite dissent.

## A. Source Map
Port a Claude Code project to Codex in-place. Beats: (1) Stuck in Claude → hand to Codex without a new project; split pane. (2) Problem: Claude wants `CLAUDE.md` + `.claude`; Codex wants `AGENTS.md` + `.codex` + `.agents`. (3) Shared knowledge (docs/refs/scripts) is the same; names differ. (4) Herc 2: `CLAUDE.md` = exec assistant + wiki; `AGENTS.md` ≈ same inject. `.claude` = memory/agents/rules/skills/settings; `.codex` = agents + config. (5) HTML cheat sheet → free Skool (operate-never). Agents can read their own docs. (6) Global vs project: `~/.claude` vs project `.claude`; Codex same idea. (7) Skills = same markdown+YAML, different folders (`.claude` vs `.agents`). Agent defs: Claude markdown vs Codex **TOML** (ClickUp searcher both sides). (8) Three layers: shared knowledge / skills / tool-specific config (`settings.local.json` vs `config`). (9) NL convert: “I built this in Claude; make `AGENTS.md` from `CLAUDE.md`; `.codex`; skills→`.agents`; agents→`.codex`; research both docs.” (10) Maintenance: if `CLAUDE.md` changes, update `AGENTS.md` too. Codex sub-agents **do not auto-invoke** — must call explicitly. (11) Mastered Claude ≈ mastered Codex; compare video coming (`RLjaUES9P8A`). (12) Dual-build HTML: Claude styled, Codex restored lost value — **overwrite risk** on the same file. (13) Skill **session handoff**: summary, active files, decisions, next steps → paste into Codex; sometimes unsticks in ~10s. (14) Tool-agnostic; if Claude is down, can you still move? Two subs if you can afford. He is “terminal boy” now. Timestamp UNKNOWN. Hermes named on-tape only. Hive: Cursor+Grok; do not add Codex.

## B. Atomic Knowledge

### Three layers + a handoff note
- **Claim:** Portability is shared knowledge (untouched) + skills copied to the other folder + a thin tool-specific config; plus a session-handoff blob when you switch mid-problem.
- **Reasoning:** Agents share files; they disagree on filenames and auto-invoke.
- **Mechanism:** NL convert using both vendors’ docs; keep `AGENTS.md` in sync; explicit Codex sub-agent calls; don’t dual-write one file blindly.
- **Evidence:** ClickUp searcher md vs TOML; HTML dual-edit overwrite; 10s unstick claim (UNVERIFIED).
- **Conditions:** You already have a Claude-shaped project (we do not run Claude).
- **Exceptions:** Hive portability target is Cursor, not Codex. `-nG-9vlSkho` hour-to-port is the doctrine; this is a vendor-specific recipe.
- **Action:** Steal three-layer split + handoff note + overwrite warning. Do not create `.codex`.
- **Confidence:** high as a map.
- **Source:** `kB9iMD0EjT8` @ UNKNOWN
- **Epistemic:** SOURCE

## C. Mental Models
Tool-agnostic > married. Shared wiki is the moat (`-nG-9vlSkho`). He wants you on two paid CLIs. Terminal > extensions.

## D. Procedures
1. Keep truth in shared docs/scripts, not in vendor folders.
2. If you must dual-harness (we must not): generate the other sidecar from the first; research both official docs.
3. After a major instruction change, sync the twin file.
4. Before switching mid-task: write handoff (talked / files / decisions / next).
5. Never two writers on one file without a lock.
6. Assume the other harness will not auto-call your sub-agents.

## E. Examples
- **Situation:** Stuck in Claude. **Action:** Session handoff → Codex. **Reasoning:** Different strengths. **Outcome:** Sometimes 10s fix. **Lesson:** The blob is the product. Implicit rule: convert is NL + vendor docs, not memorizing the cheat sheet.

## F. Decision Rules
- If knowledge is only in `CLAUDE.md` → not portable.
- If two agents edit one file → expect clobber.
- Refuse: Codex/Claude/Hermes; Skool HTML; two hive subscriptions; quote 10s as FACT.

## G. Contrarian
You do not need to memorize the map — the agent reads its own docs. Also: “mastered Claude = mastered Codex” vs his promised “different strengths” compare — keep both.

## H. Assumptions
Folders are as he draws. Auto-invoke difference is current Codex behavior.
**Desk dissent:** Portability yes; destination Codex no.

## I. Questions
- What are the “five things that trip beginners” (not read aloud)?
- Handoff skill text?

## J. Connections
- **SYSTEM SYNTHESIS:** `-nG-9vlSkho` (hour-to-port). `RLjaUES9P8A`. `context-docs`. `hive-spawn-desks` (don’t dual-write).

## K. Future-Use
Session-handoff schema (talked/files/decisions/next) as unassigned desk-switch note.

## Stolen machines

### Machine: shared-truth-plus-handoff-blob
- **Epistemic:** SOURCE
- **Workflow / loop:** keep wiki/scripts shared → vendor folders are adapters → before switch, write handoff → one writer per file → sync twin instruction files after big edits
- **Questions / signals:** Is the truth only in a vendor file? Will Codex auto-call this agent? Two writers?
- **Qualify / frame / objections:** “Duplicate the project” → no, adapter folders.
- **Procedure:** D.
- **Example that proves it:** `CLAUDE.md`/`AGENTS.md` twins; HTML clobber; handoff unstick.
- **Why it works:** Knowledge is harness-agnostic; config is not.
- **Conditions / exceptions:** His dual-CLI life. Hive stays Cursor+Grok.
- **Operate-never payload:** Codex/Claude/Hermes; Skool; two paid CLIs; new ICP.
- **Hive run:** `context-docs` · `ask-principal`
- **Source:** `kB9iMD0EjT8` @ UNKNOWN

**Operate-never**
- Add Codex. Join Skool. New `icp_id`. Send / pay / deploy / book / publish.

## THINK / BEHAVE / TRICKS / USE
**Added:** 2026-08-14 last-mile. Caption-only. Visual/click UNKNOWN unless `watch.json`. Do not flatten this speaker into a hive personality.

### THINK
Decision order, what they ask before they build, what they ignore, how they choose tools, when they kill vs continue — see §C Mental Models and §F Decision Rules above. Desk that must think this way: see TAPE-WIRE-NOTES.

### BEHAVE
What they repeatedly check, skip, retry, and speech≠behavior — see §A / §E / speech≠behavior rows. Sequence-from-speech only. `multimodal-youtube-learning`: no invented clicks.

### TRICKS
Do / don’t and implicit shortcuts — see §D Procedures and Stolen machines. Shown system (files, loops, UI, offer, CTA) mapped to Cursor+Grok primitives on the named workflow. Caption-only = transcript-implied / unobserved.

### USE
Each trick lands as a desk **action** on Cursor + Grok Bot (not a quote). Operate-never on their vendors. Reproduce card: `job-cards/takes/_knowledge-use/{{slug}}.md`.
