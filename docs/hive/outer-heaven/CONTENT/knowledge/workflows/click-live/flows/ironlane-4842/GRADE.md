# ironlane-4842 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, PR #323, branch cursor/ironlane-product-match-capex-3fe7)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4842 the shell's See schedule / Book a session land on a calendar (not 404); a slot can be selected and confirmed and ends green with "Confirmed · on the calendar"; ?state=confirmed shows the full-week picture; check.html returns Hit a wall for dm-only-gym.example and Path works for ironlane.example with no outbound request; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/ironlane-product/ + flows/ironlane-4842.yaml + 11 cards + RUN.md
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Keyboard only: Tab from the hero to a slot, Space to select, Enter on Confirm — does the toast fire and focus land on the green slot?
- `check.html?url=https://evenslouis.ca` — must read "Not checked", and the Network panel must show zero requests to evenslouis.ca.
- Reset to empty after a confirm — pill back to "empty", inbox un-muted.
- `prefers-reduced-motion` — toast still appears, no transition.
- Compare against the seven `ui-*.jpg` stills in the Forge handoff folder (not in repo).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
