# Factory rules (stricter — this bite)

- One spec = one ticket. This sitting’s ticket is the scroll-scrub wrap.
- Shapiro level 3: Forge writes; Watchdog validates; Evens plans/flips.
- Loop: prime → plan → implement → validate. No auto-commit. No auto-PR.
- Hold-outs live in `watchdog/HOLD-OUTS.md`. Forge does not copy them into the page.
- Bite-size: one HTML wrap + stills copy + `SEEDANCE-NEXT.md`. Not a site system.
- Priority: fix open misses → review → next accepted → triage.
- Cadence: this sitting. `hosted-neq-scheduled` — no new host.
- Deploy named: local folder preview. Evens flips. Merge ≠ ship.
