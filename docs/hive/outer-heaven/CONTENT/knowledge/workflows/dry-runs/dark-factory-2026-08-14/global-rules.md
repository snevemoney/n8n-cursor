# Global rules (always — even outside this factory)

- Stack = Cursor + Grok Bot. On-tape vendor names stay on-tape.
- Hard step: send / pay / deploy / book / publish = Evens.
- Prompt “don’t” is not a lock. Tools are the lock (`assume-it-will-touch`).
- Builder does not fill GRADE (`separate-verifier`).
- Pass ≠ send (`sanitize-in-check-out`).
- Caption-only tapes do not invent clicks.
- Path A / Normand stay parked unless Evens names a client.
- No secrets in chat. Higgsfield stays on Creative.
