# Watchdog hold-outs — dark-factory-2026-08-14

**Desk:** Watchdog only.  
**Builder:** Forge must not read this file while building. Do not paste these IDs or pass/fail lines into the HTML.  
**Written:** 2026-08-14 · **before** the bite.  
**Subject:** scroll-scrub wrap at `CONTENT/creative/seedance-site-2026-08-14/index.html`

GRADE is a later write. This file is the exam.

---

## H-CTA-FOLD

At `scrollY = 0`, the primary CTA is visible in the first viewport. The button’s bottom edge sits above the fold (inside the initial window height). Fail if the only CTA appears after ~100vh or is footer-only.

## H-REDUCED-MOTION

When `prefers-reduced-motion: reduce` is true, the plate region shows one static poster and does not advance frames on scroll. Fail if the scrub script still swaps frames under reduce.

## H-ONE-PLATE

The wrap is one plate, not stitched shorts. This sitting: stand-in stills from the existing Higgsfield AE proof pack are allowed. A later Seedance plate must be one continuous file, ≤30s, 720p. Fail if the page presents five unrelated clips as the wrap, or claims a billed Seedance generate happened this sitting.

## H-CTA-COLOR

The primary CTA wears a color nothing else on the page may wear (body copy, nav, secondary links, chrome). Fail if that fill/text color is reused on non-CTA chrome.

## H-AWARD-REFS

The live page names 1–3 award refs in visible copy (Awwwards / igloo / Hubtown or equivalent named motion). Fail if the names exist only in this factory pack or in HTML comments.
