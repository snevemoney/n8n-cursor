# Watchdog GRADE — adopt-then-bus-then-mouth-or-face 2026-09-04

Status: **pending**

Builder must not self-PROVEN.

Check:

- `apps/agent-stack` is original MIT, not an AGPL copy
- adopt writes vault path + `bus_dir` / `bus/state.json`
- `permission_mode=ask` · hosts cursor+grok · mouth/face/hands parked
- no `CLAUDE.md` / no `~/my-agent` / no vendor trees in `apps/`
- `start.sh` skips missing face/mouth and never starts hands
- skill is `adopt-then-bus-then-mouth-or-face` (not a remap-as-done of session-bootstrap)
