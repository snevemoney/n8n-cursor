# Dry-run TEST — adopt-then-bus-then-mouth-or-face 2026-09-04

```
DONE-CHECK: apps/agent-stack npm test exits 0 · adopt writes .hive/agent-stack.json pointing at existing vault + bus dir · start.sh skips missing face/mouth · no CLAUDE.md
CAP: no mic · no face server · no billed ElevenLabs · hands parked
COST: local tsc
STOP-KIND: cap + done-check
```

Builder runs `cd apps/agent-stack && npm test && npm run adopt && ./start.sh`.
Watchdog fills GRADE. Builder does not self-PROVEN.
