# Dry-run TEST — voice-os hands 2026-09-04

```
DONE-CHECK: hands wired; classify/arm/click dry-run tests pass; pane can POST /api/hands; live events only when approved+armed
CAP: this Mac only · 127.0.0.1 · no inferred hard-step clicks
COST: ctypes CoreGraphics, no new vendor
STOP-KIND: cap + done-check
```

Builder ran unit tests with `VOICE_OS_DRY_HANDS=1`. No live CGEvents in tests.
Headed share-screen + Accessibility grant needs Evens's Mac. Watchdog fills GRADE.
