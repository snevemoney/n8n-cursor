# Watchdog GRADE — adopt-then-bus-then-mouth-or-face sittings 2–3 2026-09-04

Status: **pending**

Builder must not self-PROVEN.

Check:

- `apps/agent-stack` is original MIT, not an AGPL copy
- mouth `turn.py --self-test` refuses send/pay/deploy and queues a desk-turn only after spoken yes
- face `serve.py --self-test` binds `127.0.0.1` and `/healthz` is ok
- `start.sh check` exits 0; `start.sh` execs face; mouth is not a second OS
- pieces: mouth=wired · face=wired · hands=parked
- PTT is hold Home / hold Talk, not open-mic
- no ElevenLabs · no `0.0.0.0` · no Claude Code · no mouse
- skill is still `adopt-then-bus-then-mouth-or-face` (not a remap-as-done of session-bootstrap or observe-pane)
- cinematic-recipe loop is on disk at `apps/agent-stack/face/DESIGN-FACE.md` (two distinct then pick; pick = cloud glass)
