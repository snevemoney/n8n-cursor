# Dry-run TEST — talk-then-see-then-bus 2026-09-04

```
DONE-CHECK: python3 scripts/hive/tests/test_voice_os.py exits 0
CAP: no mic · no 0.0.0.0 · no mouse takeover
COST: local only
```

Builder ran unit tests (`test_voice_os.py` 5/5, `test_agent_stack.py` 6/6).
Curl on this host: `/healthz` ok · `/voice.html` 200 · `list skills` speaks hive slugs · `send this email` → refuse.
Headed share-screen + hold-to-talk needs Evens's browser. Watchdog fills GRADE.
