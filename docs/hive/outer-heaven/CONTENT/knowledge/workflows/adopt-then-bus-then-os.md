# Workflow — adopt-then-bus-then-os
Status: compiled
Protocol: workflow-compiler
**Provenance:** WORKFLOW → PATTERN → ATOMS → TRANSCRIPT
**Title:** Adopt the hive as the agentic OS, then attach mouth/face to one file bus
**Tape:** `github-jaredrhod-fullstack-agent` · tour `FiOTrxq9ckM` (caption-only / README)
**Compiled:** 2026-09-04
**Owners:** forge, watchdog, librarian
**Hive skill:** `adopt-then-bus-then-os`

## Classify
- **Who / ICP:** `us` · hive-os. Not a public SKU. Not Path A.
- **Outcome:** Existing vault + repo are the brain. A file bus exists. Mouth/face can attach later without a second home.
- **Stage:** dry-run (adopt + validate). Mouth/face parked.
- **Operate-never:** Claude Code install · AGPL vendor into `apps/` · second vault · live `/` · auto-approve · Serena-style extra dashboard

## Decompose
1. Confirm one brain = `HIVE_OBSIDIAN_VAULT` + this git repo (`internal-host-os` + session-matrix sitting)
2. Adopt — `agent-stack.py adopt` writes `.hive/agent-stack.json`
3. Idle bus — `.hive/bus/state.json` phase=idle job_status=done
4. Validate — kind=agentic-os · hosts=cursor,grok · permission_mode=ask · memory=adopted
5. Watchdog GRADE the files. Builder does not self-PROVEN
6. Park mouth / face / hands. Next sittings write/read the same bus

## Coverage map
| id | task | coverage | pointer |
|----|------|----------|---------|
| T1 | Adopt existing vault | have | `vault-config.py --resolve` |
| T2 | Write stack + bus | have | `scripts/hive/os/agent-stack.py` |
| T3 | Refuse second home | have | validate never[] + no CLAUDE.md |
| T4 | Watchdog GRADE | HITL / desk | `separate-verifier` |
| T5 | Mouth (PTT → Cursor/Grok → TTS) | parked | sitting 2 |
| T6 | Face (localhost pane reads bus) | parked | sitting 3 · cinematic-recipe then |

## Steps
1. **Do:** Resolve vault. Do not create `~/my-agent`.
2. **Do:** `python3 scripts/hive/os/agent-stack.py adopt && python3 scripts/hive/os/agent-stack.py validate`
3. **Don't:** `git clone` the four AGPL repos into `apps/`. Don't install Claude Code.
4. **Don't:** Start a face server or download voice models this sitting.

## Stop
`DONE-CHECK:` validate exits 0 and `kind=agentic-os` · `CAP:` no mic / no billed TTS / no headed face · `COST:` local disk only
