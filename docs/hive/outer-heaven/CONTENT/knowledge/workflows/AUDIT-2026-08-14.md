# Knowledge-audit — 2026-08-14 compile

Three passes on the new graph / patterns / workflows. Findings written. **Not** silently fixed by merging atoms.

**Verifier:** Watchdog take `job-cards/takes/_knowledge-use/watchdog.md`  
**Builder:** `scripts/hive/knowledge-compile-layers.py`  
**Protocol:** `knowledge-audit` + `separate-verifier`

## Pass 1 — Coverage

| object | result | note |
|--------|--------|------|
| Graph | pass | 2609 edges from 849 atoms; explicit fields + same-video + speech≠behavior condition-conflict |
| Patterns | pass | 111 versioned objects; each has support_ids; seeded machines keep dissent_ids |
| `us` | pass | hive skill / sourced / HITL |
| `coverage-loop` | pass | this compile is the execute |
| `local-clinic` | pass-with-gap | T6 explicit gap: no review-mining atom |
| `local-pro` | pass | book/HITL transferable + hive skills |
| `restaurant` | pass-with-gap | 0 restaurant atoms; gap task + card |
| `exec-coach` | pass-with-gap | 0 exec-coach atoms; four-blanks transferable |
| `creator-longform` | pass | clip-factory pattern |
| `agency-delivery` | pass | log-first-approve-deck |
| `industrial-smb` | pass-with-gap | list-anneal is hive skill, not atom cluster |
| `mktg-software` | pass-with-gap | same |
| `owner-coach-fitness` | pass-with-gap | book transferable only |
| `law-adj` | pass-with-gap | book + sanitize transferable only |

**Finding:** Caption-only corpus cannot cover click/UI procedures. Those stay gaps, not invented clicks.

## Pass 2 — Context-misuse

**Pass-with-notes.**

- On-tape vendors (Vapi, ElevenLabs, Higgsfield, Claude Code, Firecrawl) kept as operate-never / steal-the-split.
- Dentist/HVAC examples on tape are scrape/outreach — not used as a hunt list.
- VIEW B (`P-dont-lead-with-word-AI`) is operator-heard, low as SOURCE — labeled.
- `us` retrieve is wider than the five seed patterns (HITL keyword). Patterns remain the primary retrieve; extra atoms are declared support, not a blend.
- Parked Path A workflows do not call `lead-web-find`.

**Finding:** Do not treat a keyword hit as demonstrated sequence. knowledge_type stays declared.

## Pass 3 — Contradiction

**Pass.**

Dissent kept visible:

- VIEW A `P-category-pitch-fails` ⊥ VIEW B `P-dont-lead-with-word-AI` (not merged).
- Inbound-from-proof vs playbook-before-send / outbound-first (labeled, not averaged).
- 114 speech≠behavior mismatches (`do_not_resolve: true`).
- zWLZ3bVVwD8 native-calendar ≠ y-cq_Qo4zVo n8n-MCP.
- LVAHYV4Xrto $ rows ≠ Pi-m8R068r4 $ rows.
- Tape $ UNVERIFIED on every workflow.

**Finding:** First local-clinic compile was `audit-fail` (gap job missing explicit gap task). Fixed by adding T6 gap — not by inventing a review-mining atom.

## Ship

All 12 workflows `Status: compiled` after the gap-task fix. Path A hunts remain parked. Hard steps HITL.

## GRADE (Watchdog)

pass-with-notes
