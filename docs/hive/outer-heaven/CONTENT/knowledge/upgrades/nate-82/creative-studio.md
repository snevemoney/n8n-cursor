# Creative Studio — Nate 82 UNTESTED upgrades

**Desk:** `creative-studio` · **Cluster:** BUILD  
**Status:** UNTESTED · `do_not_auto_install: true`  
**Sources:** 82 takes `job-cards/takes/{id}/creative-studio.md` + LEARNED packets. Caption-only. No transcript re-walk.  
**Stack:** Cursor + Grok. On-tape vendors stay on-tape (Sora / Key / FAL / Blotato / Nano Banana stay there).  
**Clients:** parked. Tape $ UNVERIFIED. No LESSONS merge. No atom dump of 146.

Parts, not a Nate media-army clone. Style/motion bible first. Taste stays on a strong model. Publish HITL.

## Desk job (unchanged)

`cinematic-recipe` · `motion-pipeline`. Never fake 3D. Never cheap-model taste. Never NSFW / watermark / jailbreak patterns.

## Upgrades

### 1. Still-lock, then motion, then last-frame compare

**One-line:** Lock the product still (do not mutate the SKU), run motion, put still beside last frame, human picks.

- **Capability:** Image-prompt “do not alter product” → generate → describe → video-prompt. Bare image-to-video mutated jar→bag and painted HDR orange (speech). Sora hallucinated an extra object; blocks realistic-human stills.
- **Implementation fragment:** Speech names poll loops and quote-stripping; HTTP bodies / Key settings = `unobserved`. 32¢ / 15¢ UNVERIFIED.
- **Primitive:** PRODUCT-LOCK STILL → MOTION → FIDELITY CHECK.
- **Leverage:** You cannot sell a bag when the listing is a jar. First frame = thumbnail trap if you auto-post.
- **HITL:** Licensed spend. Client-facing publish. Evens picks the frame.
- **Operate-never:** Auto-post. Key/Sora/FAL as hive spend. Fake product mill.
- **Evidence:** `AYsg5gAMWyo` · `uC5tDwGhyVA` · `Vm8QOo9MiC4` (SOURCE). Creative L on `uC5tDwGhyVA`: “the real plate is still beside last frame.”
- **Contradiction (labeled):** Nano+V3 “king” vs Sora cheaper — he will not resolve. Keep both. Quality ≠ cost trophy.

### 2. N proofs, then pick, then optional final

**One-line:** Three (or N) small proofs → human confirm → then a larger final. Log the extras. Never post the runner-up.

- **Capability:** 1024 proofs before 2048. Name → N variants → pick → motion. Manager delegates; binary lives in a child flow.
- **Implementation fragment:** Speech: confirm before finals; leftover text-to-video had no branding. JSON V3 prompt-agent stuffed the reference image — he *removed* it. Clicks = `unobserved`.
- **Primitive:** PROOFS-THEN-PICK.
- **Leverage:** “Full autonomy” on tape still confirms proofs and picks #1 (speech ≠ behavior).
- **HITL:** Post / email / share-anyone-with-link. Public-share is a post precondition on his tape — hive still does not auto-post.
- **Operate-never:** Blotato / Apify scrape. Auto-post JBL/TikTok path. Nine-JSON import as ours.
- **Evidence:** `jBanaNBY-sM` · `IlNwjnIzrOo` (SOURCE).
- **Contradiction (labeled):** Structured JSON video-prompt (removed) vs lock-still structured path (`AYsg5gAMWyo`). Autonomy won *in the binary subflow*; still-lock won *for SKU fidelity*. Do not flatten into “always freehand” or “always JSON.”

### 3. Checkable-done, then human taste

**One-line:** Loops get a metric or a hard cap. Taste is not a number. Evens is the scorer.

- **Capability:** Cake-fork on creative runs. “Until satisfied” + Beast rubric was a soft stop. Abbey Road ≥9 / cap 8 still failed likeness — wrong medium.
- **Implementation fragment:** Speech only. Thumbnail / plane minutes UNVERIFIED.
- **Primitive:** CHECKABLE-STOP + HUMAN TASTE (two brakes).
- **Leverage:** A dedicated scorer if scores are mushy; still not a publish.
- **HITL:** Publish. Overnight creative loops only if Evens names `/loop`.
- **Operate-never:** Mark taste-done from a self-score. Claude `/goal` mill of 10 thumbnails as SOP.
- **Evidence:** `EuzYhzB0vbI` · `CB5bG4mvnS0` (annotate-one, not 100-scenario mill) (SOURCE).

### 4. Poll the ticket, sanitize the body, crop frame-0

**One-line:** Fire-and-forget APIs need wait → GET until `success`. Strip newlines and curly quotes or the JSON dies. Watch the first frame before anyone sees it.

- **Capability:** Ready-row queue + match-key write-back (unique `number`, not “last row”). Six ready rows = six paid gens you didn’t mean.
- **Implementation fragment:** Speech: 5s image / 10s video polls; curly-quote replace after chat models leaked them. Exact APIs = `unobserved`.
- **Primitive:** TICKET-POLL + BODY-SANITIZE + FRAME-0 GATE.
- **Leverage:** First-frame clones the thumbnail if you auto-post. Sanitize is the actual build (he said he wouldn’t deep-dive the API).
- **HITL:** Spend / post. One row at a time unless Evens says otherwise.
- **Operate-never:** Key AI spend. Auto-post. Quote 32¢ as ours.
- **Evidence:** `AYsg5gAMWyo` · `Vm8QOo9MiC4` (SOURCE).
- **Contradiction (labeled):** “Every day create tons” vs one-row-at-a-time demo. Keep both.

### 5. Readable RAW, then named-file combine

**One-line:** Resolve the source file, name it (human), then edit. Do not public-host the raw. Spelling / type of output is the stop.

- **Capability:** Ingest → human name → named tool → open the file → read the type. Thin parent + child that polls.
- **Implementation fragment:** Speech: Nano Banana / Photoshop-agent path. Public-host raw = never. Clicks = `unobserved`.
- **Primitive:** NAME-THEN-COMBINE.
- **Leverage:** Unnamed binaries cannot be re-found (media army forced a Drive re-search).
- **HITL:** Publish. Licensed asset spend.
- **Operate-never:** Public raw. Game-studio SKU. Cheap taste model.
- **Evidence:** `UT_Ek_tmeVA` · `TWvjqpk3uSQ` · `7UNsK9LoORo` (SOURCE).

### 6. Pin the overlap, then try to break it

**One-line:** Annotate one real visual break (overlap, Y-axis, type) on a surface we already have, then headed QA. Not a 100-scenario mill.

- **Capability:** Same headed break-it as Forge, on creative surfaces (deck chart, still, frame). Locked sections then fix the chart (`KGXFkUlBHxw`).
- **Implementation fragment:** Speech: planted UI bugs for the annotate demo — survivorship. Clicks = `unobserved`.
- **Primitive:** ANNOTATE-ONE THEN NAMED FAILS.
- **Leverage:** Taste work still needs a pass/fail list for *layout*, then a human for *taste*.
- **HITL:** Publish the deck. Human yes before the chart ships.
- **Operate-never:** Codex annotate as hive. Slash-goal 100.
- **Evidence:** `CB5bG4mvnS0` · `KGXFkUlBHxw` · `-Q_P7HFydZk` (log-always, deck-sometimes) (SOURCE).

### 7. Brand kit + WAT before the PDF draft

**One-line:** Plan + brand kit + saved profile → draft. First PDF will miss. Heal in the room. Human ships.

- **Capability:** Newsletter / proposal path ends at HTML or deck draft. Frozen niche → research → plan → pin.
- **Implementation fragment:** Speech sequences. Gamma / Poppy / Base44 stay on-tape. Tape $ UNVERIFIED.
- **Primitive:** KIT-THEN-DRAFT.
- **Leverage:** Month-two questions and must-nevers belong in the brief (`AO5aW01DKHo`).
- **HITL:** Send / publish. No list-send.
- **Operate-never:** Auto-send newsletter. New ICP from inbound-similar-build tapes.
- **Evidence:** `tDGiWn0flK8` · `vFepZE_wrfg` · `pxzo2lXhWJE` · `0Ujdys4LqNs` · `KGXFkUlBHxw` (SOURCE).

### 8. Sanitize inputs; refuse a send-tool on a broken wrapper

**One-line:** Creative prompts get the same inbound-clean / outbound-check. A/B the still; do not attach a send-tool to a wrapper that cannot ack.

- **Capability:** Guardrail fragment on image/video prompts (PII, jailbreak, NSFW — Creative already nevers NSFW). Fail is a path.
- **Implementation fragment:** Speech: send can succeed while the run fails (`Vb1SwBAn9cQ`). Sanitize before model (`oWdJMJp2HgM`).
- **Primitive:** INBOUND-CLEAN / OUTBOUND-CHECK (shared).
- **Leverage:** A pretty still is not a send.
- **HITL:** Send / post. Pass ≠ publish.
- **Operate-never:** Jailbreak / watermark patterns. Always-allow execute.
- **Evidence:** `oWdJMJp2HgM` · `Vb1SwBAn9cQ` · `NQhsLVmuItA` (SOURCE).

## Labeled contradictions (do not flatten)

| Keep both | Why |
|-----------|-----|
| Still-lock structured path vs JSON prompt-agent removed | `AYsg5gAMWyo` · `jBanaNBY-sM` |
| “Full autonomy” vs proofs-then-pick | `jBanaNBY-sM` speech≠behavior |
| Nano+V3 quality vs Sora cheaper | `AYsg5gAMWyo` — unresolved on tape |
| “Tons a day” vs one ready row | `AYsg5gAMWyo` |
| Checkable metric vs taste-cannot-be-a-number | `EuzYhzB0vbI` — two brakes, not one |
| Log-always vs deck-sometimes | `KGXFkUlBHxw` · `-Q_P7HFydZk` |
| Agent-last vs UGC/Sora/media-army builder tapes | `4OOS96i2gfI` is the brake — climb only if the still/motion job needs a loop |

## System-upgrade flags (not a Creative SKILL.md)

See `CLUSTER-build.md`. Creative spotted: still-beside-last-frame as a visual verify, proofs-then-pick, ticket-poll, frame-0 gate, annotate-one, inbound-clean on prompts.

## Operate-never (desk)

Sora / Key / FAL / Blotato / Apify as hive. Auto-post. Fake 3D / game studio. Cheap taste model. NSFW / watermark / jailbreak. Public-host raw. Quote ¢ / “create anything” as FACT. New hunt. Merge `LESSONS-FROM-TAPE.md`.
