# Product GTM — Nate 82 upgrades (UNTESTED)

**Cluster:** GTM  
**Desk:** `product-gtm`  
**Source IDs:** `SHORTLIST-year-agents.md` (82 only).  
**Honesty:** Existing `takes/{id}/product-gtm.md` (all 82) + packet LEARNED where takes already dissent. Caption-only. Timestamps UNKNOWN. No invented clicks.  
**Status:** UNTESTED. Parts, not a Nate agency clone. Do not auto-install `SKILL.md`. No LESSONS merge.  
**Clients:** parked. No new `icp_id`. Path A / Path B / Path C stay doctrine, not a hunt.  
**Tape $:** UNVERIFIED.  
**Stack:** Cursor + Grok. Talk track stays **one person + 17 named desks**, not True Horizon / Milan / Tyler.

## Operate-never (this desk)

- Publish, ads spend, pricing commits on a custom domain stay Evens.
- Sell “I do AI,” agent libraries, $200 template packs, Skool/Plus as our room.
- Quote $23k / $30k / $1,650 / $12k / 60% / 2,000 members / 20–30% close as FACT.
- Productize Codex browser, Vapi receptionist, n8n builder, UGC factory, newsletter mill.
- Launch spend before a checkable eval. Fork Client Pack SaaS.

## Upgrades (UNTESTED)

### UPG-gtm-01 Diagnose → solve → value → price

**What changed:** The offer sentence names a numbered leak (time / money / focus), not the agent. Price is a fraction of *their* annualized save — after they give hours.  
**HITL:** Evens commits price. Do not run his 5–10 talks unless he names them.

**Capability**
```
CAPABILITY: Outcome-offer spine
GOAL: leak → fix → hours/$ → fraction price
OBSERVED WORKFLOW (speech): questions not workflow → LRP numbers → one Tuesday bottleneck → write in/out/done
```

**Implementation (caption-only)**  
- Tools: talk track + a calculator (not the LLM — `NWbh5ZoEHkA`).  
- Trigger: a named conversation Evens opens.  
- State: whose hours, rate, frequency, mistake rate, one weekly fire.  
- Guardrails: if you cannot get hours, you cannot price. Pedagogy 5h / 80% / $10k / $3k UNVERIFIED.

**Primitive:** observe pain → classify T/M/F → compute with their numbers → draft offer → wait.  
**Business leverage:** Stops the commodity “I sell agents” SKU. Hive already: `outcome-offer-funnel` · `pricing-margin-roi-guardrails` · `four-blank-sku`.  
**Evidence:** `w9-gfaV5vlM` · `YF0XPMXLHOA` · `wk8KV280fbg` · `D-OOsl8C4Ns`  
**Dissent:** VIEW A — first $1,200 / $1,650 were hours-talk or a random bump, not this SOP (`ySl-SyboPa4`, LEARNED `HNKlFTd1maM`). VIEW B — later he teaches the four-move every time. Do not flatten into one rate card.  
**Status:** UNTESTED

### UPG-gtm-02 90-minute one-bottleneck + camera-on Loom

**What changed:** Proof is a 15/60/15 walkthrough (map / rough build / 3-min Loom: before, solution, result, face on). Not a five-vendor platform for Tuesday’s bottleneck.  
**HITL:** Evens publishes the Loom. Faceless farm never.

**Capability**
```
CAPABILITY: Faced walkthrough POC
GOAL: ranked pain → one drawing with done → one rough path → one Loom
OBSERVED WORKFLOW (speech): 15 min trigger/steps/sources/outputs/done → 60 min rough → 15 min camera-on Loom
```

**Implementation (caption-only)**  
- Tools: drawing + one platform + camera. His n8n click-through `unobserved`.  
- Trigger: after LRP ranked a Tuesday bottleneck.  
- State: definition of done on the drawing.  
- Guardrails: no multi-agent fantasy, no five vendors, no platform. Sometimes their CRM already does it.

**Primitive:** diagnose → one act → show → wait for Evens.  
**Business leverage:** Walkthrough beats screenshot (already on the job card). People hire people.  
**Evidence:** `w9-gfaV5vlM`  
**Dissent:** VIEW A — flashy multi-agent YouTube wins views. VIEW B — highest-ROI client work was low-view / un-fancy. Content algorithm ≠ buyer algorithm.  
**Status:** UNTESTED

### UPG-gtm-03 Three-question pyramid (agent last)

**What changed:** “Needs AI” is a symptom. Climb L1 GPT → L2 no-AI cron → L3 fixed-path + one judgment → L4 agent only if order is not fixed.  
**HITL:** Do not ship Path C as an agent-first SKU.

**Capability**
```
CAPABILITY: Complexity ladder
GOAL: named problem → cheapest layer that hits done
OBSERVED WORKFLOW (speech): in-loop every time? 100% logic? order fixed? else agent. Revisit after real runs (scale up or down).
```

**Implementation (caption-only)**  
- Tools: none. Tree is speech. Exception: support that skips/repeats KB may need L4 even if it *looks* 1-2-3.  
- Trigger: anyone says “we need an agent.”  
- State: chosen layer + why.  
- Guardrails: ~50% / “thousands coached” UNVERIFIED. Plus/3,000 stay never.

**Primitive:** classify job → pick layer → ship smallest → observe runs → reclassify.  
**Business leverage:** Same physics as API → macro → vision (`CB5bG4mvnS0`). Hive `slice-build` already prefers one system.  
**Evidence:** `4OOS96i2gfI` · `-6yUeJ3rkvg` (audience-split before “X killed Y”)  
**Dissent:** VIEW A — year catalog is agent-titled. VIEW B — this tape: agent-first is almost never the start. Keep both.  
**Status:** UNTESTED

### UPG-gtm-04 Flywheel price vs complexity price

**What changed:** He later says they priced the Slack PA ($6k) above the sales agent ($4k) because it was complex — and that was backwards. Sales usage compounds with leads; admin may not.  
**HITL:** No Path A price card. All $ UNVERIFIED.

**Capability**
```
CAPABILITY: Flywheel-aware package
GOAL: two job types → ask “does more volume make this more valuable?” → price that, not node-count
```

**Implementation (caption-only)**  
- Tools: discovery + a written in/out/change-request.  
- Trigger: enough jobs to compare.  
- State: baseline hours (he forgot on #2 = weak case study).  
- Guardrails: 1 week internal QA + 1 week client QA (speech). Tiers + high-anchor are a *shape*; do not publish $25k / $12k as ours.

**Primitive:** observe usage curve → classify flywheel vs admin → draft package → Evens prices.  
**Business leverage:** Stops complexity theater. Protects margin via change-request.  
**Evidence:** `HNKlFTd1maM`  
**Dissent — do not merge:**  
- VIEW A — early: random bump from last close (`ySl-SyboPa4`).  
- VIEW B — mid: sit-down hours × rate (`YF0XPMXLHOA`).  
- VIEW C — later: flywheel > complexity; close >50% / no pushback = raise; B2B strong 20–30% (UNVERIFIED).  
- VIEW D — `w9-gfaV5vlM`: free/cheap or money-back for first 3–5 to get proof.  
Phase-condition them. Do not average into one price.  
**Status:** UNTESTED

### UPG-gtm-05 Similar-build inbound as the GTM motion

**What changed:** First deals: public similar-build + email in the description; not positioned as freelancer. Later: diagnose/prescribe, not “chatbot.”  
**HITL:** Ads spend stays Evens. No Path B from his niche slide.

**Capability**
```
CAPABILITY: Proof-then-inbound
GOAL: showable analog → they email → scope → yes
OBSERVED WORKFLOW (speech): excited post → inbound → couple of calls → (later team) discovery ×3 → close
```

**Implementation (caption-only)**  
- Tools: one public artifact + reachable contact.  
- Trigger: stranger already has the analog in mind.  
- State: inbound + scoped in/out.  
- Guardrails: “you don’t need a team for $4k” (his words). Do not spawn True Horizon.

**Primitive:** HITL publish proof → wait → classify inbound → draft → Evens sends.  
**Business leverage:** Path C waitlist + camera-on walkthrough before Stripe (job card).  
**Evidence:** `5IM27lbCwjM` · `8C6iCpJ9HPo` · `HNKlFTd1maM`  
**Dissent:** VIEW A — later he uses MCP “bleeding edge” as a close lever. VIEW B — hive will not switch stack to close. VIEW C — `8C6iCpJ9HPo` also “throw a number” after they were already excited. Keep all three.  
**Status:** UNTESTED

### UPG-gtm-06 Always-log the call; deck only on a yes

**What changed:** Meeting-end writes a thin row. A human decides whether a client-facing deck is needed. Generate is not send.  
**HITL:** Slack-yes = generate. Mailing the deck is a **second** box. Evens sends.

**Capability**
```
CAPABILITY: Approve-or-skip artifact
GOAL: call ends → log always → deck only if yes
OBSERVED WORKFLOW (speech): Fireflies/summary wait → sheet row → human yes/no → (only if yes) Gamma
```

**Implementation (caption-only)**  
- Tools named: meeting log + Slack/approve node + Gamma. Gamma stay on-tape.  
- Trigger: meeting end. Collision guard: last item only if two end together (`-Q_P7HFydZk`).  
- State: thin log vs generated deck (split so you can route).  
- Guardrails: do not hide “system-produced” as a style rule (`KGXFkUlBHxw`).

**Primitive:** event → log → classify need → (maybe) generate → wait → Evens sends.  
**Business leverage:** Stops auto-proposal mills. Hive `ask-principal`.  
**Evidence:** `KGXFkUlBHxw` · `-Q_P7HFydZk`  
**Status:** UNTESTED

### UPG-gtm-07 Eval gate before launch spend

**What changed:** Evaluation = hypothesis + labeled expecteds + per-row right/wrong. A green average is not a sitting.  
**HITL:** No launch spend before the sheet. “Passed eval” is not a send.

**Capability**
```
CAPABILITY: Labeled-set gate
GOAL: “it works” → six (or N) rows with expected category/priority → scores written back
OBSERVED WORKFLOW (speech): write expecteds → run → read misses → new hypothesis
```

**Implementation (caption-only)**  
- Tools: eval tab / table of expecteds. n8n eval UI `unobserved`.  
- Trigger: before anyone treats a graph as proof.  
- State: per-row correctness + token/time (he shows averages; steal the misses).  
- Guardrails: n=10 Apple 10-K is too small — he wants 100–200 (`X80ljdCPM_U`). Counts UNVERIFIED.

**Primitive:** hypothesize → run labeled set → observe misses → patch → wait.  
**Business leverage:** Job card already: no launch spend before KPI + baseline. This is the sheet.  
**Evidence:** `8IUWeF3B-hk` · `X80ljdCPM_U` · `lcNN3X9gXls` (one knob per run)  
**Dissent:** VIEW A — title “build anything” + 77–82% slides. VIEW B — speech: human approval; demo still sends (`nQtogLs_dlg` Michael Scott). Store the mismatch.  
**Status:** UNTESTED

### UPG-gtm-08 Wireframe four boxes; AI only on the last mile

**What changed:** The $2,600 “AI agent” (UNVERIFIED) was four onboarding flows with AI only on the last welcome. They paid for onboarding that does not leak.  
**HITL:** Auto-welcome / reminder email is still a send. Day-N escalate is human Slack.

**Capability**
```
CAPABILITY: Picture-first, AI-last
GOAL: leaky onboard → four named boxes → AI only where judgment is the last mile
OBSERVED WORKFLOW (speech): show the four boxes → get paid → AI on flow 4 only
```

**Implementation (caption-only)**  
- Tools: wireframe then (his) n8n. Clicks `unobserved`.  
- Trigger: a repeating onboard leak.  
- State: statuses + human escalate.  
- Guardrails: title bait ≠ the machine. Do not mail “I’ll build this in 2 hours.”

**Primitive:** draw boxes → deterministic rails → one judgment step → human escalate.  
**Business leverage:** Same as pyramid L2/L3. Fast because most of it is not an agent.  
**Evidence:** `bxGE_LXPyAU`  
**Status:** UNTESTED

## SYSTEM_UPGRADE_CANDIDATE (not a Product GTM skill)

| id | type | discovery | evidence |
|----|------|-----------|----------|
| `SU-nate82-api-macro-vision` | Agent infrastructure | API first → deterministic macro → headed vision last; watch ≥10 on money | `CB5bG4mvnS0` |
| `SU-nate82-checkable-stop` | Agent infrastructure | Loop = trigger + action + X=Y; most tasks no fleet | `EuzYhzB0vbI` |
| `SU-nate82-one-knob-eval` | Agent infrastructure | Log intermediates; change one variable per run | `lcNN3X9gXls` · `8IUWeF3B-hk` |

## Reproduce / generalize / improve (desk)

1. **Reproduce:** Four-blank sentence + faced Loom script + pyramid card on paper. No Path A.  
2. **Generalize:** Same diagnose-value spine already is `outcome-offer-funnel`. Do not fork a second offer OS.  
3. **Improve:** Do not improve by auto-send decks or quoting his $ as ours.
