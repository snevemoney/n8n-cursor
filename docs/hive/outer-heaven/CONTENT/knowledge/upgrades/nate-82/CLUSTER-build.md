# BUILD cluster — Nate 82

**Desks:** `forge` · `researcher` · `librarian` · `creative-studio`  
**Status:** UNTESTED · `do_not_auto_install: true`  
**Synthesized from:** 82 takes (all four slugs present) + `packets/{id}/LEARNED.md`. No transcript re-walk. No LESSONS merge. No atom dump of 146.  
**Stack:** Cursor + Grok. Clients parked. Tape $ UNVERIFIED. Caption-only — no invented clicks / APIs / prompts.  
**Parts, not clones.** One capability → many desks. Infra is not a marketing SKILL.md.

Desk files: [forge.md](forge.md) · [researcher.md](researcher.md) · [librarian.md](librarian.md) · [creative-studio.md](creative-studio.md)

## Per desk — upgrades (name · one-line · HITL)

### Forge

| # | Name | One-line | HITL |
|---|------|----------|------|
| 1 | API → macro → headed vision | API first, then repeatable macro, then headed vision + named fail list | 2FA / admin / deploy / publish |
| 2 | Cake-fork before the run | Done = X metric Y + check tool + hard cap | Overnight only if Evens names `/loop`; deploy never from a self-score |
| 3 | Pile-to-specialist, deny tools | Fresh window + short trigger + allow-list; permissions ≠ “please don’t” | No uncapped ultra; no unverified agent md |
| 4 | WAT, heal, freeze W+T | Sit the first run, patch in-session, freeze write+test | Deploy / activate |
| 5 | Write local, prove idempotency | Unique key is a test; secrets never in chat/git | Deploy; keys in vault |
| 6 | Sanitize in, check out | Redact before the model; check after; pass ≠ send | Send / pay / deploy / book / publish |
| 7 | Pin the skeleton | Text-to-workflow is a 70% graph; re-read every “fix” | Activate / deploy; approve ≠ send |
| 8 | One board: status / kill | Glance idle, approve, or stop a bad path; open the file | Kill / approve / deploy |

### Researcher

| # | Name | One-line | HITL |
|---|------|----------|------|
| 1 | Human-gesture retrieval | Filter / SQL / full-read / vector — never vector-first | No secrets into a managed store; no new vector SKU |
| 2 | Ingest-count, then gold Q | Chunk count, then one known fact, then pipeline 2 | Do not publish the smoke answer as FACT |
| 3 | Cite, then Ctrl+F | Demand a span you can find; summary ≠ quote | Do not publish unverified cites |
| 4 | Bake-off, one knob | Same brief; change model *or* prompt *or* tools | Do not crown a vendor / release |
| 5 | Docs-curl, assume side effect | A send can succeed while the run fails | Send / book; pass ≠ send |
| 6 | Split research, watch idle | Territory + kill switch; inspect ≠ activity-log skin | Approve / kill subs |
| 7 | Research → lookup → draft | Machine ends at draft + log | Send / publish |
| 8 | Three questions, then layer | Agent almost never first; variable KB-pass is the exception | Do not spawn an agent SKU for a parked client |

### Librarian

| # | Name | One-line | HITL |
|---|------|----------|------|
| 1 | Four retrieval doors in the vault | File the chooser next to cite-the-page; do not default to vectors | Destructive memory edits; no second vector app |
| 2 | Filter the row, allow-list keys | Deterministic narrow, then LLM; never dump the table | Lane registry; do not migrate to n8n-cloud tables |
| 3 | Ticket write-back | Next hop reads the card, not the chat | Destructive edits; no auto-send from a log row |
| 4 | Prompt/model as keyed config | IP off the canvas; dropdowns beat free-type slugs | Secrets out of the config table |
| 5 | Description is the contract | Curated 1–3 node catalog vs execute-any; file the jump | Execute / send; never always-allow send |
| 6 | Designed-fail vs golden-test | Label fail type; do not average planted bugs with goldens | Do not pick a winner on speech≠behavior |
| 7 | Highlights required | Cite without a Ctrl+F span is not a FACT | — (publish stays other desks) |
| 8 | One-knob eval store | Hypothesis + labeled expected + miss row | Do not auto-install a skill from an average |

### Creative Studio

| # | Name | One-line | HITL |
|---|------|----------|------|
| 1 | Still-lock → last-frame compare | Do not mutate the SKU; still beside last frame; human picks | Spend / publish; Evens picks the frame |
| 2 | N proofs, then pick | Small proofs → confirm → optional final; log extras | Post / email; never auto-post |
| 3 | Checkable-done, then taste | Metric or cap, then Evens as scorer | Publish; no overnight mill unless named |
| 4 | Poll, sanitize, crop frame-0 | Wait until `success`; strip quotes; first frame is a trap | Spend / post; one ready row |
| 5 | Readable RAW, named combine | Human names the file; do not public-host raw | Publish / licensed spend |
| 6 | Pin the overlap, break it | Annotate one visual break; named fails; not 100 scenarios | Human yes before the deck/chart ships |
| 7 | Brand kit + WAT before draft | First PDF will miss; heal; human ships | Send / publish |
| 8 | Sanitize; refuse broken send-tool | Inbound-clean on prompts; pretty still ≠ send | Send / post; pass ≠ publish |

## Hive-wide system-upgrade flags

Infrastructural. **Not** a desk SKILL.md. Hive-wide rows mapped to WIRED ops skills (do not install twice).

| Flag | Discovery | Applicability | Proposed change | Evidence |
|------|-----------|---------------|-----------------|----------|
| **SU-1 Checkable-stop + hard cap** | A loop without X=Y + cap is a hope. “Until satisfied” is a weak stop. | ALL looping agents | Write goal + check method + cap *before* run. Kill on cap/cost. | `EuzYhzB0vbI` · `ZAaxx3qyT8g` |
| **SU-2 Actuate → observe → compare** | Do not assume the click/API worked. Open the file / URL / inbox. **WIRED** as `verify-after-browser` (ops). | ALL browser- or side-effect agents | Action → observe state → compare expected → retry/escalate | `CB5bG4mvnS0` · `xsAOpqjebOo` · `Vb1SwBAn9cQ` |
| **SU-3 Sanitize-in / check-out / pass≠send** | Redact before the model; check after. Named guardrails miss (passwords). | ALL agents that touch text in or out | Fail branch required. Pass is not send. Test the miss. | `oWdJMJp2HgM` · `NQhsLVmuItA` |
| **SU-4 Allow-list tools; description is the contract** | Permissions ≠ vibes. Execute-any + always-allow send is the demo and the never. | ALL tool-using agents | Curated catalog. Manual enable. Refuse execute-any. | `e18sdZLwP7o` · `5p5cV0yVDvQ` · `9IzGe0BBj_c` |
| **SU-5 Secrets in vault, never chat/git** | `.env` does not push. Two places or it is not there. | ALL hosted / off-laptop runs | Vault + host env. No keys in prompts. | `UGIZnh6HNLc` · `27Y44JYXZJ8` · `3GAxd90fEE4` |
| **SU-6 Visible idle + kill + approve** | Activity-log skin ≠ seeing the work. Hosted ≠ always-on. | ALL parallel / long runs | Glance pane: idle / approve / kill. Open the artifact. | `ZAaxx3qyT8g` · `xsAOpqjebOo` · `62Rfe1w9NBc` · `27Y44JYXZJ8` |
| **SU-7 Filter-then-LLM; don’t dump tables** | Hundreds of rows in context = tokens + hallucination. | ALL agents with a store | Deterministic get-where, then model. Allow-list keys. | `QCjMBOEhpLE` · `lcNN3X9gXls` |
| **SU-8 Hypothesis + labeled-set + one knob** | “Looks better” is not eval. Inspect the miss row. | ALL desks before a fragment is Keep | Hypothesis → expected → run → writeback → one knob next | `8IUWeF3B-hk` · `lcNN3X9gXls` |
| **SU-9 State on the card** | Follow-up must read the ticket. Spawn-prompt-only memory is a warning. | ALL multi-hop agents | Write-back required. Intermediate + error log. | `hN58VkYLie4` · `lcNN3X9gXls` |
| **SU-10 Prove idempotency** | “Auto-dedupe” duplicated in prod. | ALL off-laptop / scheduled jobs | Unique key is a re-test, not a claim. | `UGIZnh6HNLc` |
| **SU-11 Start-low layer** | Agent almost never first. Builder tapes will overwrite this if unfiled. | ALL intake / Forge + Researcher | Three questions → climb only when forced. Variable KB-pass stays an exception. | `4OOS96i2gfI` |
| **SU-12 Cite = Ctrl+F-able span** | Vendor “content” is often a summary. | ALL retrieve-and-claim agents | Highlights / locator required. Else `unobserved`. | `QojPKL96Dx4` · `KVFfApQZhE4` |

## Cross-cluster contradictions (labeled — do not flatten)

| Pair | Tapes | Do not do |
|------|-------|-----------|
| Agent-first builder corpus vs start-low pyramid | most of 82 vs `4OOS96i2gfI` | Average into “we build agents” |
| 24/7 host vs don’t-scale-bugs vs unsupervised `/goal` warn | `ehg4fhydTgs` · `EuzYhzB0vbI` · `ZAaxx3qyT8g` | “Overnight is the brand” |
| Silent specialists vs talking teams | `e18sdZLwP7o` · `vDVSGVpB2vc` | One mega-crew |
| Four-door chooser vs managed “drop file and chat” | `kOKavHnlPik` · `QojPKL96Dx4` | Crown Pinecone or vectors |
| Folder gold-Q vs managed File Search (no lifecycle) | `Fu6vOfzFmcw` · `irg-2IfAjpo` | One RAG SOP |
| Assigned 7-verb MCP vs instance execute-any | `glM8godEcic` · `5p5cV0yVDvQ` | Always-allow send |
| API-first vs browser-cheaper-than-metered-API | `CB5bG4mvnS0` | Quote ~95% / X fees as FACT |
| Idempotency claimed vs dupes proven | `UGIZnh6HNLc` | Trust the host brochure |
| Secret-keys miss passwords | `oWdJMJp2HgM` | One node = all secrets |
| Table faster vs 400-row lose | `QCjMBOEhpLE` | “Native table always wins” |
| Still-lock vs JSON prompt-agent removed | `AYsg5gAMWyo` · `jBanaNBY-sM` | Always-structured or always-freehand |
| “Full autonomy” vs proofs-then-pick | `jBanaNBY-sM` | Clone the media army |
| Chat-refine can lie vs guided-edit writes the prompt | `27Y44JYXZJ8` | Trust the chat summary of the prompt |

## Reproduce / generalize / improve (cluster)

1. **Reproduce (dry-run only):** Cake-fork on one existing Forge run; four-door chooser on one hive file set; still-beside-last-frame on one existing preview. Draft only.
2. **Generalize:** Same primitives → other events (do not wire): PR fail → Forge; new packet → Researcher gold-Q; log row → Librarian write-back; still → Creative fidelity check.
3. **Improve:** Do not “improve” by auto-send. Hive already has `send-removed` · `confirm-then-actuate` · `input-required-gate` · `ask-principal`.

## What this turn did not do

- Re-walk transcripts or `full.txt`
- Merge `LESSONS-FROM-TAPE.md`
- Emit atoms / dump 146
- Auto-install `.cursor/skills/`
- Compile a Frankenstein workflow
- Unpark a client / new `icp_id`
- Send / pay / deploy / book / publish
- Install on-tape vendors (Codex, Claude, Vapi, Trigger, Pinecone, Key, Sora, n8n-cloud)
