# Big Boss — Nate 82 upgrades (OPS/GOVERNANCE)

Status: **UNTESTED** · do not auto-install · do not wire  
Cluster: ops/governance  
Corpus: `SHORTLIST-year-agents.md` (82) · takes `{id}/big-boss.md` · packets `{id}/LEARNED.md`  
Stack: Cursor + Grok only. Clients parked. No LESSONS merge. No atom dump. No vendor install.

I manage. I do not clone Nate’s OS, Claude teams, Codex browser, Hermes pocket, or instance-MCP execute.

## Constraints

- Operate-never: send / pay / deploy / book / publish stay HITL (`ask-principal`).
- Parts, not clones. 17 desks stay. No 18th without founders triangle.
- Tape $ / 99% / 85 checks / 15-run caps / 210-spawn = **UNVERIFIED**.
- Caption-only: clicks/APIs/prompts not in speech = `unobserved`.

## Upgrades (UNTESTED)

### 1. Checkable-stop loop
- **One-line:** A job is trigger + action + how it knows to stop (metric = Y, or a hard cap) — not “until satisfied.”
- **HITL:** Overnight / 24/7 default is never. Soft-done still needs Evens on send/pay/deploy/book/publish.
- **Kind:** primitive · `reason-act-observe-until-metric-or-cap`
- **Hive-wide:** WIRED as `checkable-stop` (coverage-loop job + spawn card). This desk file stays local doctrine.
- **Sources:** `EuzYhzB0vbI` · `ZAaxx3qyT8g`
- **Contradiction (keep labeled):** Same tape says write loops *and* most tasks are one terminal + verify. Overnight-as-experiment vs `ZAaxx3qyT8g` unsupervised-`/goal` warn — do not flatten.

### 2. Named specialists, fresh window
- **One-line:** Lead stays the CEO seat; pile/parallel/independent work goes to a named desk with a thin trigger and a short report back — not a 210-wide herd.
- **HITL:** Spawn only when Evens orders. Ultra/dynamic trigger words are a kill. No mesh that can send.
- **Kind:** capability part · `hive-spawn-desks` fragment
- **Sources:** `e18sdZLwP7o` · `vDVSGVpB2vc` · `xsAOpqjebOo`
- **Contradiction (keep labeled):** Teams exist for peer bounce; subs exist for return-only. He still used a “research team” on sequential work. 3–5 is his band, not a hive law.

### 3. Climb only when forced
- **One-line:** GPT/project → no-AI workflow → AI-in-a-fixed-path → agent. Agent is almost never first.
- **HITL:** Promoting a path to an agent that can leave the building is Evens.
- **Kind:** primitive · `four-layer-tree`
- **Sources:** `4OOS96i2gfI` · `wk8KV280fbg`
- **Contradiction (keep labeled):** Variable KB-pass count (0 vs 5 searches) is his agent exception. ~50% “no AI” is **UNVERIFIED**.

### 4. One board, cwd, needs-input
- **One-line:** Many sessions are not managed unless statuses, idle, cwd, and yellow needs-input are visible on one board.
- **HITL:** Unsupervised `/goal` without a metric. Kill/spawn that can deploy.
- **Kind:** capability part · session board
- **Sources:** `ZAaxx3qyT8g` · `xsAOpqjebOo` · `62Rfe1w9NBc`
- **Contradiction (keep labeled):** Visibility is incomplete on tape (“I don’t know where she’s off to”).

### 5. Interview + never-rules before code
- **One-line:** If you cannot brief a human, do not brief the agent. Outcome, never-rules, and keys-are-human come before WAT folders.
- **HITL:** Auto-accept of a plan. Keys in chat. Outreach/scrape (dentist/Places) stay operate-never.
- **Kind:** capability part · `interview-never-rules-wat`
- **Sources:** `AO5aW01DKHo` · `tDGiWn0flK8`
- **Contradiction (keep labeled):** He says you approve; he auto-accepted on tape. “Never send” as the only lock is false security.

### 6. Hosted ≠ scheduled
- **One-line:** A cloud sandbox or pretty session UI is not always-on. No cron / no heartbeat = a fancy run button. Run-now until one-shot, then maybe cadence.
- **HITL:** Deploy / schedule / API-wake. Hive does not install Anthropic routines, managed agents, or Trigger.dev.
- **Kind:** primitive · `hosted-vs-scheduled`
- **Sources:** `27Y44JYXZJ8` · `ehg4fhydTgs` · `hN58VkYLie4`
- **Contradiction (keep labeled):** Routines *have* a ≥1h schedule; managed agents as taped do *not*. Do not merge into “cloud = 24/7.”

### 7. API → macro → vision last
- **One-line:** First call is an API/CLI. Static pixels get a dead macro. Vision browser is last, headed, with a written SOP.
- **HITL:** Bank / 2FA / admin / publish. No password CSV. No Codex install.
- **Kind:** primitive · `api-then-macro-then-vision`
- **Sources:** `CB5bG4mvnS0`
- **Contradiction (keep labeled):** He used browser *despite* an X API (format/$). Ladder invert stays labeled, not doctrine.

### 8. Assume-it-will-touch as spawn policy
- **One-line:** If a specialist can read it or has the tool, assume it will. Allow-list tools/MCP. Bypass on the lead flows downhill.
- **HITL:** No Send/Pay/Deploy tools on workers. File territory or they overwrite.
- **Kind:** primitive · `assume-it-will-touch` (hive-wide candidate — see CLUSTER-ops)
- **Sources:** `e18sdZLwP7o` · `vDVSGVpB2vc`
- **Contradiction (keep labeled):** He prefers main-as-approver; hive keeps Evens on hard steps.

## Primitives (this desk)

| Primitive | Shape | Not |
|-----------|-------|-----|
| Checkable-stop loop | trigger → act → observe → metric or cap | “until satisfied” |
| Climb-when-forced | three questions → name layer → promote after runs | agent-first |
| Fresh-window specialist | pile? → named desk + allow-list + short report | persona swarm |
| Hosted≠scheduled | classify wake primitive before approving “always-on” | laptop-off as default |

## Capability parts (not clones)

- Orchestrator pattern we already have (`hive-spawn-desks` · `interview-to-desk`) — steal description-tune + allow-list, not `.claude/agents/`.
- WAT = SOP md + one-job tools + brain — map to `workflow-compiler` / `slice-build`, not Claude Code folders.
- Context-switch KPI (shoot the skill, return) — Day Planner owns the calendar; I own “one system this session.”

## Operate-never

- Install Claude Code / Codex / Hermes / OpenClaw / Trigger.dev / n8n-cloud / Vapi / Skool as stack.
- 18th desk / nameless swarm / 210-spawn / bypass teams.
- Quote tape $ / 99% / 85 / 15-run / 10× as FACT.
- New `icp_id`. Unpark clients. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.
