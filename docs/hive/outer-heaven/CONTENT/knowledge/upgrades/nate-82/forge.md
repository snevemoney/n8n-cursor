# Forge — Nate 82 UNTESTED upgrades

**Desk:** `forge` · **Cluster:** BUILD  
**Status:** UNTESTED · `do_not_auto_install: true`  
**Sources:** 82 takes `job-cards/takes/{id}/forge.md` + LEARNED packets. Caption-only. No transcript re-walk.  
**Stack:** Cursor + Grok. On-tape vendors stay on-tape.  
**Clients:** parked. Tape $ UNVERIFIED. No LESSONS merge. No atom dump of 146.

Parts, not a Nate clone. Each row is a fragment Forge could gain. Do not auto-write `SKILL.md`. Deploy / domain / Stripe stay HITL.

## Desk job (unchanged)

`slice-build` · `click-live-site` · staging smoke. Never one-shot the whole site. Never “looks good” without opening the URL.

## Upgrades

### 1. API → macro → headed vision

**One-line:** Ask API first, then a repeatable pixel/macro, then headed vision with a named fail list — not “automate anything.”

- **Capability:** Take a URL we already have → annotate one real break → headed try-to-break → pass/fail list → fix those. Vision last.
- **Implementation fragment:** Speech names the ladder after a no-API demo; session-save; 2FA from the phone stays manual. Clicks / Codex plugin settings = `unobserved`. Do not invent a password-CSV import.
- **Primitive:** ACTUATION LADDER — cheapest guaranteed path first; vision is a pocket tool.
- **Leverage:** Most “browser” jobs are an API or a script. Headless only after a watched path.
- **HITL:** 2FA / admin / security the agent cannot interpret / deploy / publish. Draft ≠ ship.
- **Operate-never:** Codex / Glydo / computer-use plugin; password CSV; bank-session bots; slash-goal 100 UI scenarios as a mill.
- **Evidence:** `CB5bG4mvnS0` (SOURCE). Hive already has `click-live-site` + `golden-test-loop` — this is the intake order, not a second mouse.
- **Contradiction (labeled):** Same tape says API ~95% UNVERIFIED *and* “browser can be cheaper than a metered API (X)” UNVERIFIED. Keep both. Do not average.

### 2. Cake-fork before the run

**One-line:** Write done = X metric Y + the check tool + a hard cap before any loop starts.

- **Capability:** Reason → act → observe until the fork comes out clean, or the cap kills it.
- **Implementation fragment:** Speech: trigger + action + checkable stop. “Until satisfied” is a weak stop. Abbey Road ≥9 / cap 8 still failed likeness. Minutes / 12h / V7 = UNVERIFIED.
- **Primitive:** CHECKABLE-STOP LOOP — verify is the loop.
- **Leverage:** A swarm without a fork scales bugs. Most jobs = one session + verify, not a fleet.
- **HITL:** Overnight / 12h only if Evens names `/loop`. Deploy never from a self-score. Closer-looking HTML is not ship.
- **Operate-never:** Claude `/goal` as hive stack; vibe meta-agent; mark done from “satisfied.”
- **Evidence:** `EuzYhzB0vbI` (SOURCE). Do not rebuild `coverage-loop`.
- **Contradiction (labeled):** Same tape: overnight-before-bed experiment vs “12h+ not useful.” `ZAaxx3qyT8g` warns unsupervised `/goal`. Do not flatten into “overnight is fine.”

### 3. Pile-to-specialist, deny tools

**One-line:** Fresh window + short trigger description + allow-list. Permissions are tools, not “please don’t.”

- **Capability:** If it is a pile you will never reread → specialist. If 1-2-3 depends or must ask Evens → stay main.
- **Implementation fragment:** Speech: YAML description is the trigger; misfire → trim, don’t scold; unclosed quote = no fire; colliding skill/agent steals invoke. Ultra/dynamic herds (41 / 210) UNVERIFIED. Clicks = `unobserved`.
- **Primitive:** CLEAN-WINDOW SPECIALIST — description is the API.
- **Leverage:** Main context is the scarce resource. Cheap model eats the pile.
- **HITL:** No uncapped ultra. No unverified GitHub agent md (injection). Spawn stays Cursor desks, not Claude `.claude/agents/`.
- **Operate-never:** Install Claude Code; copy awesome-list agents; 210-sub flex as SOP.
- **Evidence:** `e18sdZLwP7o` (SOURCE). Hive map: `hive-spawn-desks` + `agent-job-card`.
- **Contradiction (labeled):** Skip subs if they must talk (`e18sdZLwP7o`) vs teams-for-peer-talk (`vDVSGVpB2vc`). Keep both. Conditions: independent pile vs bounce.

### 4. WAT, heal in the room, freeze W+T

**One-line:** Plan the outcome and the never, sit the first run, patch the tool in-session, then freeze write+test before anyone deploys.

- **Capability:** Interview → WAT files / one-job tools → first PDF/run will miss → heal → own the KPI. Title $ is theater.
- **Implementation fragment:** Speech: plan-mode questions are the build; bypass is a footgun; self-heal is a *session* property. Clicks = `unobserved`.
- **Primitive:** SESSION-HEAL THEN FREEZE — do not ship the live patcher.
- **Leverage:** You are QA. One-shot skip-plan made search+dedupe weak on the host tape.
- **HITL:** Deploy / activate. Do not leave a watcher that writes prod.
- **Operate-never:** Quote tape $ as a quote; sell “the crew”; Claude/Trigger as hive host.
- **Evidence:** `vFepZE_wrfg` · `tDGiWn0flK8` · `3GAxd90fEE4` · `AO5aW01DKHo` (SOURCE).

### 5. Write local, run off-laptop, prove idempotency

**One-line:** Job is written here; runtime is elsewhere; unique key is a test, not a claim; secrets never in chat or git.

- **Capability:** Split find vs create so retry is one job. Dev env ≠ prod env. `.env` does not push — paste twice or it is not there.
- **Implementation fragment:** Speech: claimed auto-idempotency; prod test duplicated; fix = search + place ID. Yelp free-tier died mid-build (on-tape). Clicks = `unobserved`.
- **Primitive:** PROVE-DEDUPE — idempotency is a re-test.
- **Leverage:** Queue/retry is why a host exists. Local scheduled jobs die when the laptop sleeps.
- **HITL:** Deploy / GitHub-as-prod-wire. Keys in vault only.
- **Operate-never:** Trigger/Yelp/SER spend; keys in chat; lead-scrape hunt; quote “25 in 9s” as FACT.
- **Evidence:** `UGIZnh6HNLc` · `hN58VkYLie4` · `27Y44JYXZJ8` (SOURCE).
- **Contradiction (labeled):** “Automatically taken care of” vs “apparently it was happening” (dupes). Speech ≠ behavior. Keep the scar.

### 6. Sanitize in, check out, pass ≠ send

**One-line:** Redact before the model (no LLM); check after the model; a pass branch is not permission to send.

- **Capability:** Builder wires fail → notify or throw. Test each guard with known-good and known-bad, including a password line.
- **Implementation fragment:** Speech: sanitize ≠ check; secret-keys (even “strict”) passed “use my password password,” failed a key-shaped string. Thresholds / node IDs = `unobserved`.
- **Primitive:** INBOUND-CLEAN / OUTBOUND-CHECK.
- **Leverage:** Don’t leak secrets into the model; still inspect outbound.
- **HITL:** Send / pay / deploy / book / publish. Guardrail pass ≠ auto-send (`send-removed`).
- **Operate-never:** Treat secret-keys as a password wall; Skool guardrail template as hive install.
- **Evidence:** `oWdJMJp2HgM` · `NQhsLVmuItA` (SOURCE). Infra — also a system-upgrade flag.

### 7. Pin the skeleton, re-read every “fix”

**One-line:** Text-to-workflow is a 70% graph. Name trigger/source/dest, pin each node, open the node the chat said it fixed.

- **Capability:** Generate a skeleton → punch-list the red → never ship unread. One item on one line.
- **Implementation fragment:** Speech: named tools in the words → inspect prompt → approve ≠ send. Clicks = `unobserved`.
- **Primitive:** GENERATED-GRAPH IS A DRAFT.
- **Leverage:** Crew one-shots hide leftover nodes. “Fixed” in chat ≠ fixed in the node.
- **HITL:** Activate / deploy. Approve the plan is not send.
- **Operate-never:** n8n AI Builder as hive SOP; quote “10x faster” as FACT.
- **Evidence:** `a5sJNwfZ528` · `rXpHzWXjHrw` · `TDHFkKSTJ30` (SOURCE).

### 8. One board: status, enter, reply, kill

**One-line:** Loops need a metric *and* a pane you can glance — idle, approve, or stop a bad path. Do not count sprites.

- **Capability:** Visible workers + approve gate + idle detect. Open the file the worker claims it wrote.
- **Implementation fragment:** Speech: `/goal` needs a metric; cwd identity; activity-log skin ≠ seeing the work. Extension security checklist is his, not a blessing. Clicks = `unobserved`.
- **Primitive:** GLANCE-THEN-STOP — manager loop.
- **Leverage:** Parallel researchers without a kill switch run past the miss.
- **HITL:** Kill / approve / deploy. Hosted agent ≠ always-on (`27Y44JYXZJ8`).
- **Operate-never:** Rent their sandbox; Claude agent dashboard as ours; 24/7 spend because Twitter said so.
- **Evidence:** `ZAaxx3qyT8g` · `xsAOpqjebOo` · `62Rfe1w9NBc` (SOURCE). Infra — also a system-upgrade flag.

## Labeled contradictions (do not flatten)

| Keep both | Why |
|-----------|-----|
| API-first vs browser-vs-metered-API | `CB5bG4mvnS0` — $ UNVERIFIED |
| Overnight experiment vs 12h-not-useful vs unsupervised `/goal` warn | `EuzYhzB0vbI` · `ZAaxx3qyT8g` |
| Subs if pile / teams if they must talk | `e18sdZLwP7o` · `vDVSGVpB2vc` |
| Agent-last pyramid vs builder-agent tapes | `4OOS96i2gfI` vs the rest of the 82 — pyramid is the brake |
| Hosted agent ≠ always-on | `27Y44JYXZJ8` |
| Idempotency claimed vs dupes proven | `UGIZnh6HNLc` |
| Chat-refine can lie; guided-edit writes the prompt | `27Y44JYXZJ8` |
| Instance execute-any vs assigned 1–3 node tools | `5p5cV0yVDvQ` · `9IzGe0BBj_c` · `glM8godEcic` — Forge refuses execute-any |

## System-upgrade flags (not a Forge SKILL.md)

See `CLUSTER-build.md`. Forge spotted: checkable-stop (WIRED), actuation-observe-compare (WIRED as `verify-after-browser`), sanitize/check, allow-list tools, vaulted secrets, idle-kill, prove-dedupe, generated-graph-is-draft.

## Operate-never (desk)

Install Codex / Claude / ChatGPT / Gemini / Trigger / Glydo. Password CSV. Bank / 2FA computer-use. Auto-send / auto-publish / auto-deploy. Execute-any MCP. Quote tape $ / 85 / 100 / ~95% / minutes as FACT. New hunt. Merge `LESSONS-FROM-TAPE.md`.
