# Librarian — Nate 82 UNTESTED upgrades

**Desk:** `librarian` · **Cluster:** BUILD  
**Status:** UNTESTED · `do_not_auto_install: true`  
**Sources:** 82 takes `job-cards/takes/{id}/librarian.md` + LEARNED packets. Caption-only. No transcript re-walk.  
**Stack:** Cursor + Grok. On-tape vendors stay on-tape.  
**Clients:** parked. Tape $ UNVERIFIED. No LESSONS merge. No atom dump of 146.

Parts, not a Nate clone. Librarian owns provenance, contradictions, and the control plane — not a second wiki and not n8n-cloud tables.

## Desk job (unchanged)

OPERATOR_MEMORY with provenance. `wiki-ingest`. Job cards. Never delete CHRONICLE. Never 8k-node theater. Outer Heaven is the wiki.

## Upgrades

### 1. Four retrieval doors in the vault

**One-line:** File retrieve = the human move next to cite-the-page. Do not default hive memory to vectors.

- **Capability:** Persist the chooser as a catalog: filter / SQL / full-doc / vector, with conditions and exceptions. New RAG tape versions the atom; it does not overwrite.
- **Implementation fragment:** Speech chooser + token knobs. Schema-lookup tool mentioned, not shown (`unobserved`).
- **Primitive:** RETRIEVE = THE HUMAN MOVE (same primitive as Researcher; Librarian stores the doors).
- **Leverage:** Memory that always embeds will keep answering the wrong week.
- **HITL:** Destructive memory edits. No new vector app.
- **Operate-never:** Supabase / Pinecone as hive wiki. Flatten `QojPKL96Dx4` into this row.
- **Evidence:** `kOKavHnlPik` · `ZwQ8rJhVCr4` (SOURCE). Librarian L: “File four-way retrieve next to cite-the-page.”

### 2. Filter the row, allow-list the keys

**One-line:** Deterministic narrow, then LLM. Never dump the table into context.

- **Capability:** Get-where key = trigger. Typed columns; insert fails on mismatch; date-as-string if the source used hyphens. Prompt lists legal enums + format — or a lookup, not a hardcoded live catalog.
- **Implementation fragment:** Speech: hundreds/thousands dumped = tokens + hallucination. 400-row table *slower* than Sheets once — keep that fail. ms UNVERIFIED. Clicks = `unobserved`.
- **Primitive:** FILTER-THEN-LLM.
- **Leverage:** Control plane is a typed store + tools that expose one condition each.
- **HITL:** Lane registry commits. Do not migrate hive to n8n Data Tables.
- **Operate-never:** Quote $546 / ms as FACT. “Table always faster” (hypothesis died).
- **Evidence:** `QCjMBOEhpLE` · `lcNN3X9gXls` (SOURCE).
- **Contradiction (labeled):** Agent-chosen filter tools vs deterministic get-by-email. Keep both. Conditions: enum-small vs key-exists.

### 3. Ticket write-back so follow-up can read

**One-line:** Research and classify write the card. The next hop reads the card, not the chat.

- **Capability:** English → file → named host. Intermediate logs (think / tool in-out / tokens) + error table (date, node, workflow). Continue-on-error so fail still logs.
- **Implementation fragment:** Speech: 5-window memory forced a re-search on the media tape. Trigger follow-up reads the ticket. Clicks = `unobserved`.
- **Primitive:** STATE-ON-THE-CARD.
- **Leverage:** Spawn prompt is the only memory on team tapes — that is a warning, not a design we copy.
- **HITL:** Destructive edits. No auto-calendar / auto-send from a log row.
- **Operate-never:** Second wiki app. Silent fail (no error branch).
- **Evidence:** `hN58VkYLie4` · `lcNN3X9gXls` · `jBanaNBY-sM` · `tDGiWn0flK8` (SOURCE).

### 4. Prompt and model as keyed config (IP off the canvas)

**One-line:** Filter `workflow = X` → bind user/system/model. Share the graph without the prompt.

- **Capability:** Table (or hive equivalent: `context-docs` / job-card fields) is the control plane. Sheet dropdowns beat free-type model slugs. Dual-sync is a crutch for a weak frontend — do not stand up a second app.
- **Implementation fragment:** Speech: swap 3.7 + “use Tavily” — agent still called Perplexity (stronger model ignored the only). Keep that miss.
- **Primitive:** PROMPT-AS-IP.
- **Leverage:** Canvas edits are fragile; non-builders should not have to open the node.
- **HITL:** Lane registry. Do not put secrets in the config table.
- **Operate-never:** n8n-cloud as hive; dentist topic as new `icp_id`.
- **Evidence:** `lcNN3X9gXls` (SOURCE).

### 5. Description is the contract; curated catalog vs execute-any

**One-line:** File the jump from assigned 1–3 node tools to instance-wide search+execute. Description + manual enable are the ACL.

- **Capability:** Librarian records: search → details → human allow once. Always-allow + send is the demo *and* the never. Assigned 7-verb MCP is the safer sibling.
- **Implementation fragment:** Speech: ChatGPT OAuth broken live; no-auth workaround used anyway (speech ≠ behavior). Version pins UNVERIFIED. Clicks = `unobserved`.
- **Primitive:** LEAST-PRIVILEGE CATALOG.
- **Leverage:** Client routing is only as good as the description. Whole-instance execute leaks keys/data.
- **HITL:** Execute / send. Never always-allow send.
- **Operate-never:** Instance MCP on for hive; no-auth MCP to ChatGPT; Lovable/Vapi as stack.
- **Evidence:** `5p5cV0yVDvQ` · `9IzGe0BBj_c` · `mPflFTQUCGk` · `glM8godEcic` (SOURCE).
- **Contradiction (labeled):** “You are in full control” vs always-allow send + no-auth fallback. Keep the mismatch.

### 6. Designed-fail vs golden-test, labeled

**One-line:** Persist designed-fail (arena, planted bugs, password-line miss) separately from golden tests. Do not average them.

- **Capability:** Maker-checker for math. Perfect-score fail is a label. Secret-keys missed “password password.” Idempotency claimed vs dupes proven.
- **Implementation fragment:** Speech only. Arena scores stay rows (`NWbh5ZoEHkA` · `NO97pqqc10A`).
- **Primitive:** LABEL THE FAIL TYPE.
- **Leverage:** Knowledge-architecture already forbids flatten. This is the 82’s concrete fail taxonomy.
- **HITL:** Destructive memory. Do not pick a winner on speech≠behavior.
- **Operate-never:** Merge LESSONS. Crown an arena score.
- **Evidence:** `8IUWeF3B-hk` · `NO97pqqc10A` · `NWbh5ZoEHkA` · `oWdJMJp2HgM` · `UGIZnh6HNLc` (SOURCE).

### 7. Highlights required; summary is not a quote

**One-line:** Vault rule: a cite without a Ctrl+F-able span is `evidence_status: unobserved` or a miss — not a FACT.

- **Capability:** Same scar as Researcher #3, stored as a librarian integrity field (`evidence_status`, `conflicts_with`).
- **Implementation fragment:** Speech: Assistant `content` is a summary until highlights. Tesla page slop.
- **Primitive:** TRUST = CTRL+F-ABLE SPAN.
- **Leverage:** Stops LEARNED from promoting vendor summaries.
- **HITL:** None on the rule. Publish stays other desks.
- **Operate-never:** Promote tape quotes without a locator.
- **Evidence:** `QojPKL96Dx4` · `KVFfApQZhE4` (SOURCE).

### 8. One-knob eval store

**One-line:** Hypothesis + labeled expected + actual + score live in the same store. Next run changes one knob.

- **Capability:** “What did I do in run 33?” is the game. Correctness sits next to tokens and time. Inspect the miss row, not the average.
- **Implementation fragment:** Speech: six-row category/priority; Tesla 42% pair scores 5 then 4 — UNVERIFIED as law. Eval UI = `unobserved`.
- **Primitive:** HYPOTHESIS-PLUS-LABELED-SET.
- **Leverage:** Selling without eval is incomplete (his claim). Hive: eval before we treat a desk fragment as keep.
- **HITL:** Do not auto-install a skill from a passing average.
- **Operate-never:** Six-row scores as production FACT. Flatten with Pinecone bakeoff.
- **Evidence:** `8IUWeF3B-hk` · `lcNN3X9gXls` (SOURCE).

## Labeled contradictions (do not flatten)

| Keep both | Why |
|-----------|-----|
| Table faster on tiny writes vs slower at 400 | `QCjMBOEhpLE` |
| Deterministic filter vs agent-filled tool conditions | `QCjMBOEhpLE` |
| Assigned MCP vs instance execute-any | `glM8godEcic` · `5p5cV0yVDvQ` |
| “Full control” vs always-allow send | `5p5cV0yVDvQ` speech≠behavior |
| Prompt-in-table vs stronger model ignoring “only Tavily” | `lcNN3X9gXls` |
| Spawn-prompt-only memory vs ticket write-back | `vDVSGVpB2vc` vs `hN58VkYLie4` — conditions differ |
| Agent-last pyramid vs builder corpus | `4OOS96i2gfI` — file as dissent on every agent-first LEARNED |

## System-upgrade flags (not a Librarian SKILL.md)

See `CLUSTER-build.md`. Librarian spotted: filter-then-LLM, state-on-the-card, prompt-as-IP, least-privilege catalog, fail-type labels, cite-integrity, one-knob eval store.

## Operate-never (desk)

Delete CHRONICLE. Second wiki. n8n-cloud tables as hive memory. Execute-any MCP. Merge LESSONS. Dump 146 atoms. New `icp_id`. Quote tape $ as FACT.
