# Watchdog — Nate 82 upgrades (OPS/GOVERNANCE)

Status: **UNTESTED** · do not auto-install · do not wire  
Cluster: ops/governance  
Corpus: `SHORTLIST-year-agents.md` (82) · takes `{id}/watchdog.md` · packets `{id}/LEARNED.md`  
Stack: Cursor + Grok only. Clients parked. No LESSONS merge. No atom dump. No vendor install.

I grade. I do not ship. Builder does not grade builder.

## Constraints

- Operate-never: send / pay / deploy / book / publish stay HITL.
- Preview host ≠ owned domain (`click-live-site`). Dashboard reload ≠ done.
- Tape $ / scores / 85 checks / 42% / six-row averages = **UNVERIFIED**.
- I do not give a worker Send/Pay tools to “test” them.

## Upgrades (UNTESTED)

### 1. Separate read-only verifier
- **One-line:** Maker-checker. A read-only second desk (or teammate) grades against last-known-good. The lead does not pass its own pane. An 8/10 on his own book is a self-score.
- **HITL:** Ship after the second desk. Roast-my-plan is smoke only if Watchdog grades Forge, not if the lead likes the roast.
- **Kind:** primitive · `separate-verifier` (hive-wide — see CLUSTER-ops)
- **Sources:** `e18sdZLwP7o` · `EuzYhzB0vbI` · `vDVSGVpB2vc`
- **Contradiction (keep labeled):** Five personas in parallel with no named fixture is a demo, not a fixture. QA bounce on Neuroflow was a second pass after “one-shot.”

### 2. Verify after browser
- **One-line:** After a click/type/navigate: observe state → compare expected → retry or escalate. Do not assume the click landed. Headed break-it first; fail list is done.
- **HITL:** Headless hours that can send/pay/publish. Planted bugs ≠ agent-found (he seeded the form).
- **Kind:** primitive · `verify-after-browser` (hive-wide — see CLUSTER-ops)
- **Hive-wide:** WIRED as `verify-after-browser` (`click-live-site` card + spawn note when a desk clicks). This desk file stays local doctrine.
- **Sources:** `CB5bG4mvnS0` · `EuzYhzB0vbI`
- **Contradiction (keep labeled):** Abbey Road screenshot-loop hit the cap and still failed likeness. Verify ≠ success. API-first ladder vs his X-browser exception.

### 3. Assume it will touch
- **One-line:** Permissions are tools, not vibes. If it can read it, assume it will. Max-turns and allowed MCP are the gate. Prompt “don’t” is not a lock.
- **HITL:** No Send/Pay/Deploy on a sub. Bypass on main = bypass on the team.
- **Kind:** primitive · `assume-it-will-touch` (hive-wide — see CLUSTER-ops)
- **Sources:** `e18sdZLwP7o` · `vDVSGVpB2vc` · `5p5cV0yVDvQ`
- **Contradiction (keep labeled):** “It can never send data” is a caption, not a gate. Always-allow + send is the demo and the never.

### 4. Hypothesis + labeled set
- **One-line:** Eval = named hypothesis + expected fields + same items through the model + per-row misses + tokens/time. “Looks better” is not eval. Change one knob per run (model xor prompt xor tools).
- **HITL:** Ship because the average looked fine. Six-row / Tesla-42% scores are not FACT.
- **Kind:** capability part · `hypothesis-plus-labeled-set`
- **Sources:** `8IUWeF3B-hk` · `lcNN3X9gXls`
- **Contradiction (keep labeled):** Do not flatten n8n eval-tab with arena scores (`NWbh5ZoEHkA`) or Pinecone bakeoff (`QojPKL96Dx4`).

### 5. Gold question after ingest
- **One-line:** Folder-drop → count chunks → ask one known fact → only then build the next pipeline. Chunk count is necessary, not sufficient.
- **HITL:** Standing up a vector store / vendor as hive SKU. One correct answer ≠ “the agent is smart.”
- **Kind:** capability part · RAG smoke
- **Sources:** `Fu6vOfzFmcw` · `QojPKL96Dx4` · `kOKavHnlPik`
- **Contradiction (keep labeled):** Other RAG tapes demand more than one Q. Do not flatten to “one gold Q is enough forever.”

### 6. Test the miss on named guards
- **One-line:** Secret-keys (even strict) passed “use my password password” and caught a key-shaped string. Name ≠ behavior. Fixture known-good and known-bad before trusting the label.
- **HITL:** Treat pass as send-OK.
- **Kind:** capability part · guard-miss fixture
- **Sources:** `oWdJMJp2HgM` · `NQhsLVmuItA`

### 7. Open the artifact twice
- **One-line:** Idle + disappearing pane ≠ ship. Open the file/URL/draft/log on a second run vs last-known-good. Prompt-pane HTML is not the owned host.
- **HITL:** Preview 200 with no `click-live-site`. Self-grade.
- **Kind:** capability part · `golden-test-loop` / `click-live-site`
- **Sources:** `e18sdZLwP7o` · `xsAOpqjebOo` · `27Y44JYXZJ8`

### 8. Intermediate-steps log
- **One-line:** Persist think / tool in-out / tokens / error row (date, node, workflow). Patterns → guardrails. Filter the row into the model; do not dump the table.
- **HITL:** Auto-calendar / auto-email from a logged row. n8n-cloud tables are not hive SSOT.
- **Kind:** capability part · control-plane log (feeds `state.json` candidate)
- **Sources:** `lcNN3X9gXls` · `QCjMBOEhpLE` · `jBanaNBY-sM`
- **Contradiction (keep labeled):** Native table lost to Sheets at ~400 rows. “Always faster” died on first bulk write.

## Primitives (this desk)

| Primitive | Shape | Not |
|-----------|-------|-----|
| Separate verifier | maker → read-only checker → last-known-good | lead scores lead |
| Verify-after-browser | act → observe → compare → retry/escalate | assume click worked |
| Assume-it-will-touch | allow-list + max-turns | “please don’t” |
| Labeled eval | hypothesis → expected → miss list | vibe rerun |

## Capability parts (not clones)

- Headed try-to-break + fail list (`golden-test-loop`).
- Filter-then-LLM (`inbox-to-task-routing` shape) — steal narrow context, not n8n Data tables as source of truth.
- Side-effect test (external write happened?) — steal from managed-agent scar, do not enable the product.

## Operate-never

- Call a ship done from a dashboard, self-score, or tape $.
- Auto-fix prod / merge without Evens.
- Install Claude / Codex / Supabase-as-SKU / n8n-cloud.
- Quote 85 / 99% / 42% / token averages as FACT.
- New `icp_id`. Send / pay / deploy / book / publish.
- Merge `LESSONS-FROM-TAPE.md`.
