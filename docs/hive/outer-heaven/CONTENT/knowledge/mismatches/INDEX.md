# Mismatches (stated vs observed)

First-class objects. Compare speech vs behavior. Do not assume either is correct. **The discrepancy is knowledge.**

**Schema:** [schema.json](schema.json)

`do_not_resolve: true` always. Do not flatten into the nicer sentence.

Caption-only: only flag when speech contradicts speech-reported behavior. Do not invent an on-screen contradiction.

**114 mismatches this turn** from `speech-ne-behavior` atoms. File: [by-atom.jsonl](by-atom.jsonl).

**+12** from YT WL AI 2026-08-14 (`gt8k4bA01Mo` · `RDytbVDzMF4` · `vLlIBT0HSSc` · `eecUhBpTz_g` · `iRBs8PCBCaA` · `lRUpu2-KtGQ`). Caption-only. `do_not_resolve: true`.
