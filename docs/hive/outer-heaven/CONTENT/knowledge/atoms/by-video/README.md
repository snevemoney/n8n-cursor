# Atoms by video

One jsonl per packet: `{video_id}.jsonl`.

Empty until Researcher emits after a named tape’s A–K + LEARNED. Do not pre-create 146 files. Do not invent clicks.

Named clip emitted: `x-2088007687149601254.jsonl` (5 atoms). VIEW A `category-pitch-fails` ≠ VIEW B `buyers-dislike-term-AI`.
