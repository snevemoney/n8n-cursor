# Skill candidate — adopt-then-bus-then-os

**candidate_id:** `SC-fullstack-agent-adopt-then-bus-then-os`  
**desk:** `forge` (run) · `watchdog` (GRADE) · `librarian` (index)  
**fragment:** adopt existing vault + write file bus. Not a Jarvis clone.  
**status:** WIRED  
**do_not_auto_install:** true

Minted as `adopt-then-bus-then-os`. Labeled merge on `internal-host-os` (also proven from fullstack-agent). Do not remint `session-bootstrap`.
