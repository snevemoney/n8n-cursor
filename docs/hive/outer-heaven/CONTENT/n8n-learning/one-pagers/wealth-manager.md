# Wealth Manager — n8n legacy one-pager
**Labels:** FACT = inventory name · UNVERIFIED nodes

## Role in estate
Personal markets/analysis. No exclusive hive JSON row. Personal CFO templates inactive until operator scopes + HITL.

## Workflows you own (table)

| Live name | JSON file | Trigger | HITL | When to use | Status |
|-----------|-----------|---------|------|-------------|--------|
| Automated Stock Analysis Reports with Technical & News Sentiment using GPT-4o | *(no hive JSON)* | UNVERIFIED | caution | Scheduled/analysis reports | ACTIVE **FACT** name |
| Automate Financial Operations… | *(no hive JSON)* | — | **yes** if revived | Personal CFO lane | INACTIVE **FACT** name |

## How they work (nodes — from JSON)
**None in `workflows/hive/`.** Nodes **UNVERIFIED** — Visual SOP before any call. Prefer Grok/broker plugins for live decisions.

## Call recipe
Prefer Grok market tools + vault notes. If using Stock Analysis workflow: read-only Executions check first; no autonomous trading.

## Failures → visual SOP
**FACT (estate ritual):** On fail/off/drift — open https://evenslouis.ca/n8n (read-only) → screenshot (a) full canvas, (b) failing node params+error, (c) Executions detail → attach to operator / Forge. Login/2FA → `request_box_help`. Never wipe `n8n_data`, never n8ncloud, no activate without Tier 3.

## Do NOT
- Trade / transfer / connect bank flows without Tier 3 HITL.
- Activate Personal CFO templates casually.
## Do NOT
- Invent nodes/paths not in JSON or estate map (**FACT** estate: 177 / 69 active / 108 inactive).
- Paste `X-Hive-Secret` / Telegram / bearer values in chat.
- Auto-approve money, client send, merge main, activate inactive, wipe volumes.
- Treat archived rows as callable even if `active=true`.
