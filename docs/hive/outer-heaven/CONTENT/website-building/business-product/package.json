{
  "name": "business-product-preview",
  "private": true,
  "version": "0.0.0",
  "description": "Evens Louis SAMPLE \u2014 soft /work proofs door (building mode) behind the brand shell. CapEx / preview only. Not a pnpm workspace member.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "bash serve.sh"
  }
}
