# business-4868 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/work-verticals-product-match-capex-dc1b)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4868 the shell's See the proofs lands on proofs.html (not 404); the door starts closed with no cards and the filter chips disabled; Open renders the sample proof cards and the pill reads n proofs · building mode; filter chips narrow the set and the count updates; tapping a card shows proven / not proven / grade with UNFILLED for non-graded; Close the door empties the grid; ?state=open&filter=graded lands on the graded set; no deck or call-booking language outside the struck card; no outbound request; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/business-product/ (shell + proofs.html + assets + tests) · batch critic: website-building/product-match-critic/BATCH.md
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Filter to Held then Close then Open — filter must reset to All, not remember Held.
- Search the DOM for slide, deck, investor, discovery call — none outside the struck friction card.
- Confirm no card links off-origin (all hrefs local or absent).
- `prefers-reduced-motion` — state changes still land, no transition.
- Compare against the launch-video beats for Evens Louis (Creative Studio packet, not in repo).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live `/work` case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
