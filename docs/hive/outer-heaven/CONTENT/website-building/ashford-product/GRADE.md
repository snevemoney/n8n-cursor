# ashford-4843 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/book-path-product-match-capex-9b2e)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4843 the shell has no public Book door and every Apply CTA lands on apply.html (not 404); the three-step form refuses Continue until a step is complete; Submit privately ends with an outcome card in one of three lanes (Referred / Under private review / Not now), reference AV-SAMPLE-0001, toast "Received · private review", Loud Book card muted; ?state=received shows the same picture; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/ashford-product/
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Keyboard only: Tab through step 1, Continue via Enter, does focus land on the first field of step 2?
- Pick "Exploring" on Fit — outcome must read "Not now" with the brand (hold) tone, not green.
- Reset to empty after a submit — pill back to "empty", Loud Book un-muted, step 1 visible.
- `prefers-reduced-motion` — toast still appears, no transition.
- Network panel: zero requests after load (no fetch, no beacon).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
