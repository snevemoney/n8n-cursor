{
  "name": "ledgerline-product-preview",
  "private": true,
  "version": "0.0.0",
  "description": "Ledgerline SAMPLE — demo book + 10s clarity UI behind the ledger-blue shell. CapEx / preview only. Not a pnpm workspace member.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "bash serve.sh"
  }
}
