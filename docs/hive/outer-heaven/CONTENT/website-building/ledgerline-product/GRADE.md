# ledgerline-4846 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/book-path-product-match-capex-9b2e)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4846 the shell opens on exactly three clarity lines with a measured pill (lines · words · under 40) and the toggle swaps to 24 feature tabs and back; ?mode=fog opens on the fog; every Book-a-demo CTA lands on demo.html (not 404); Book demo is disabled until a slot AND a role are picked; booking turns exactly one slot green with "✓ Sample finance lead", pill "demo booked · on the calendar", toast; the feature-fog card mutes; ?state=booked shows the same picture; no page carries %, $, Nx, faster, save; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/ledgerline-product/
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Read the three lines aloud with a stopwatch — does it land under ten seconds at a normal pace? (The page never claims this; the story does.)
- Pick a role first, then a slot — Book demo must enable in either order.
- Reset to empty after booking — pill "empty", fog card un-muted, role select cleared.
- `prefers-reduced-motion` — toast still appears, no transition.
- Network panel: zero requests after load.

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
