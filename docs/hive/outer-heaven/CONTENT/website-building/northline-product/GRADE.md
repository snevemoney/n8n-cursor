# northline-4848 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/book-path-product-match-capex-9b2e)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4848 every Book-privately CTA lands on book.html (not 404, no tel:); Submit privately stays disabled until slot + visit type + name + consent are all set; submitting marks the slot dashed "Requested", pill "form received · SLA 1 business day · sample", stage 1 lit on the track, toast; the clinic-side button advances to In clinical review then Slot confirmed, at which point exactly one slot is green "✓ held privately" and the outcome card shows; the waitlist-chaos card mutes on submit; ?state=received / review / confirmed land deterministically; no email / tel / date inputs exist; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/northline-product/
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Tick consent, then untick it — Submit privately must disable again.
- Keyboard only: Space on the consent checkbox, Enter on Submit — does focus land on the clinic-side button?
- Reset to empty after confirmed — pill "empty · private", track cleared (no stage lit), waitlist card un-muted.
- `prefers-reduced-motion` — toast still appears, no transition.
- Network panel: zero requests after load; Application panel: no storage keys written.

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
