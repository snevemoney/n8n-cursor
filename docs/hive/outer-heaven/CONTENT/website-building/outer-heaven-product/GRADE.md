# outer-heaven-4863 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/work-verticals-product-match-capex-dc1b)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4863 the shell's Walk the line lands on walkthrough.html (not 404); with no spec loaded every bench is dark and Advance is disabled; Load example spec lights station 1 and reveals the spec excerpt; Advance walks Hold-outs (count only, items hidden) → Build → Grade (hold-outs revealed, sample stamp labelled sample) → HITL hold, where the line reads complete and Ship stays locked; ?state=complete shows the finished run; the hero contains no AI-agency phrasing; no outbound request; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/outer-heaven-product/ (shell + walkthrough.html + assets + tests) · batch critic: website-building/product-match-critic/BATCH.md
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Back from HITL hold to Grade — hold-outs stay revealed, completion flag drops.
- Try to advance past HITL hold — Ship must stay locked with the HITL copy visible.
- Confirm the sample GRADE stamp says sample and never reads as this slice's real grade.
- `prefers-reduced-motion` — state changes still land, no transition.
- Compare against the launch-video beats for Outer Heaven (Creative Studio packet, not in repo).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live `/work` case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
