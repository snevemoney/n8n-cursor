# betawise-earth-4865 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/work-verticals-product-match-capex-dc1b)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4865 the shell's Enter the stage lands on stage.html (not 404); the stage starts dark with the canvas idle and Pause disabled; Start turns the globe (rotation readout increases) and the pill reads live · 0 stock assets; density and style controls change the drawing without a reload; Pause freezes the readout; with prefers-reduced-motion the globe renders one still frame; DevTools Network shows only local files; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/betawise-earth-product/ (shell + stage.html + assets + tests) · batch critic: website-building/product-match-critic/BATCH.md
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Toggle density while paused — the still frame must redraw with the new count.
- Resize the window — canvas must stay square and sharp (devicePixelRatio handled).
- Grep the served folder for .mp4 / .webm / .json / .glb — must find none.
- `prefers-reduced-motion` — state changes still land, no transition.
- Compare against the launch-video beats for Betawise Earth (Creative Studio packet, not in repo).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live `/work` case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
