# atelier-4845 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/book-path-product-match-capex-9b2e)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4845 the shell has no opt-in field or lead-magnet copy and every Request-a-seat CTA lands on seat.html (not 404); Autumn shows 9 struck-through and 3 open chairs; a taken chair cannot be picked; Request this seat turns exactly one chair green with "✓ Sample maker", pill "seat N held", toast; switching to Winter (full) relabels the button "Join the waitlist" and the outcome reads "Waitlist · place 3" in brand tone; the funnel-spam card mutes; ?state=held / ?state=waitlisted / ?cohort=winter land deterministically; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/atelier-product/
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- After a held seat, cohort chips must be disabled (no switching a held request to another cohort).
- Keyboard only: Tab to Seat 7, Space, Enter on Request — does focus land on the outcome card?
- Reset to empty after waitlist — pill back to "3 seats open" on Autumn, funnel card un-muted.
- `prefers-reduced-motion` — toast still appears, no transition.
- Network panel: zero requests after load; no timers running (Performance panel).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
