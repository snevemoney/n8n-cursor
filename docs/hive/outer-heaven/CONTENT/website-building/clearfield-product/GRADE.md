# clearfield-4861 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/work-verticals-product-match-capex-dc1b)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4861 the shell's Open a casefile lands on workbench.html (not 404); empty casefile shows no claims and Attach disabled; adding a claim, selecting it, choosing evidence + relation and tapping Attach increments that claim's count; attaching a supports and a contradicts to the same claim shows the contested badge; the summary strip reads '0 verdicts' in every state and no True/False/Verdict control exists; ?state=linked shows the sorted casefile; no outbound request; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/clearfield-product/ (shell + workbench.html + assets + tests) · batch critic: website-building/product-match-critic/BATCH.md
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Attach the same evidence twice to one claim — must not double-count.
- Detach the only contradicts item from a contested claim — badge must drop.
- Search the DOM for the strings True, False, Verdict, Debunked — must find none outside the struck friction card.
- `prefers-reduced-motion` — state changes still land, no transition.
- Compare against the launch-video beats for Clearfield (Creative Studio packet, not in repo).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live `/work` case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
