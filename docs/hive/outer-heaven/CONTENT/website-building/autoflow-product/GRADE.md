# autoflow-4862 — Watchdog GRADE

UNFILLED. Forge does not self-grade.

```
BUILDER: Forge (Cursor cloud, branch cursor/work-verticals-product-match-capex-dc1b)
VERIFIER: Watchdog — new session, read-only, no Send / Pay tools
HYPOTHESIS: on 127.0.0.1:4862 the shell's Open the editor lands on editor.html (not 404); the canvas starts blank with Connect and Cite disabled; adding two ideas places two nodes; selecting both enables Connect and a line appears; selecting one node, entering a source and tapping Cite flips its badge from uncited to cited; footer counts cited/total and the stage reads cited only at all-cited; ?state=cited shows the ready screen; no finance word appears outside the struck card; no outbound request; layout holds at 390px; every page says SAMPLE / CapEx / invent_kpi=0.
LABELED: docs/hive/outer-heaven/CONTENT/website-building/autoflow-product/ (shell + editor.html + assets + tests) · batch critic: website-building/product-match-critic/BATCH.md
MISS:
GRADE:
```

## Hold-outs (Watchdog picks; builder did not script these)
- Connect a node to itself — must be refused.
- Cite with a blank source — must stay uncited.
- Add nine ideas — grid must wrap without overlap at 390px.
- `prefers-reduced-motion` — state changes still land, no transition.
- Compare against the launch-video beats for Autoflow (Creative Studio packet, not in repo).

## Hard step
`GRADE: pass` is not deploy / publish / wiring the live `/work` case page. Those stay Evens. Merge ≠ ship. Live `/` HOLD.
