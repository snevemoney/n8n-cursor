# ICP runbook: `us`

**Who:** Evens / hive OS — internal desk, proofs, paid slices  
**Pain:** Agents guess jobs; chat ephemeral; preview ≠ prod  
**Path:** C  
**Machine:** internal catalog (pick one per session)  
**Owners:** Big Boss · Forge · Librarian · Watchdog · Creative  
**Default city:** N/A (internal)

## Route here / not here

**Route here:** Hive desk, wiki, proof pages, paid slice, agent routines — Evens-owned work.  
**Not here:** Any client ICP · second wiki app · game studio SKU this cycle.

## Internal machines (pick one today)

| Machine | When |
|---------|------|
| `morning-ceo-desk` | Visible → efficient → automatic digest |
| `wiki-ingest` / `agent-job-card` | Memory + owns/never |
| `slice-build` + `cinematic-recipe` | Our proof page |
| `paid-slice` | Our Stripe/checkout thin V1 |
| `click-live-site` | After any ship |
| `session-bootstrap` | New dump → short loops |
| `checkout-proof` | Warm network pay test |
| `motion-pipeline` | Creative proof clip |
| `demand-validate` | Pre-hunt: public gigs >$500 — Path B gate, not a client type |

## Disqualify

- Second wiki app · 8k-node theater · rebuild Claude inside Grok
- Game studio SKU · quote tweet income
- Both click-live **and** paid-slice in one session (pick one)

## Landmines

- One system per session (`slice-build`)
- Preview ≠ prod domain
- Public proof URLs for anything client-adjacent

## Proof URLs (internal)

| Demo | Port / path |
|------|-------------|
| Cinematic | `:3005` · public Vercel when deployed |
| MCP connector | `:3006` |
| Speed-to-lead / Intake→Book | `:3007` · [speed-to-lead-demo](../speed-to-lead-demo/INDEX.md) |

## Path C — skill chain

1. `session-bootstrap` if new dump
2. One machine only (`slice-build` rule)
3. `click-live-site` if URL shipped (public)
4. Librarian: `wiki-ingest` promote if durable
5. Append internal note to [HUNT_LOG.md](./HUNT_LOG.md) if hunt-adjacent (optional)

## Hard step (HITL)

Send · pay · deploy prod domain · publish

## Today (session 1) — pick ONE

- [ ] **Proof:** `click-live-site` on **public** cinematic or MCP URL (not localhost in anything client-facing)
- [ ] **Build:** next `paid-slice` thin V1 **or** speed-to-lead public deploy — not both

Job cards + brief inject are **done** — do not re-check those boxes.

## Session 2

1. If proof: fix any click-live failure · log in Watchdog note.
2. If build: margin/checklist for paid-slice · smoke checkout.
3. Librarian: promote one durable FACT to OPERATOR_MEMORY if earned.

## Done when (≤60d)

One internal machine shipped with public URL · `click-live-site` PASS · or paid-slice smoke PASS with HITL Stripe/domain.

## Handoff

Log → [HUNT_LOG.md](./HUNT_LOG.md) (icp_id=`us`) · Load: `CONTENT/job-cards/` · STEAL_SHEET Path C machines.
