# Phase 1 immutable snapshot — Git evidence only

Full packet remains on the shared box at:

`/workspace/research/packets/fleet-reentry-20260916-IMMUTABLE-SNAPSHOT/`

This folder contains **only**:
- `SANITIZED_MANIFEST.json` — file inventory + SHA-256 tree hash
- `SHA256SUMS.txt` — per-file checksums

Payload files (agents-raw, shared-surface, fleet report, etc.) are **not** published here.
