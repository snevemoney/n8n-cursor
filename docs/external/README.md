# External context (no secret values)

## Domains

- Canonical host: `https://evenslouis.ca`
- Legacy: `n8ncloud.tech`, `evenslouis.pro`, `lightningflow.online`

## Env var names (examples)

- `HIVE_MACHINE_TOKEN` / `CE_HIVE_TOKEN`
- `N8N_API_KEY`
- OpenClaw hook token

Values only on VPS / local `.env` (gitignored).

## Operator access

- Caddy basic_auth username `evens` (password on VPS only)

## VPS

- Hostinger `69.62.66.78` / `srv765579`
