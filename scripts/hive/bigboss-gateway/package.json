{
  "name": "bigboss-gateway",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "BigBoss Context Gateway slice 1 — organizational memory API between ElevenLabs and GitHub (hive process, not a SKU)",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
