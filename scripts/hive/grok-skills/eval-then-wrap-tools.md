---
name: eval-then-wrap-tools
description: >-
  eval the job → wrap existing tools (not install a new stack). Remap Cursor + Grok. Not a landing. Not Claude.
---

# Eval then wrap tools

**Owner:** Forge (run) · Watchdog (already GRADE'd — do not self-PROVEN) · Librarian (index). **Stack:** Cursor + Grok Bot.
**Cursor copy:** `.cursor/skills/eval-then-wrap-tools/SKILL.md`
**Grok `/` copy:** `~/.grokbot/skills/eval-then-wrap-tools/SKILL.md`
**Status:** WIRED 2026-08-17 auto-mint after PROVEN. Not accepted forever.
**Parents:** `golden-test-loop` · `send-removed` · `ask-principal`
**Source:** packet `X80ljdCPM_U` · capability `CAP-X80ljdCPM_U-eval-then-wrap-tools` (PROVEN). Caption-only unless `watch.json` exists.

**Primitive:** eval-then-wrap-tools

## When

packet X80ljdCPM_U in steal/learn queue

Not: a `website-assets` landing · a Claude install · a copy of `session-bootstrap` · STEAL_SHEET.

## Card

```
GOAL: eval the job → wrap existing tools (not install a new stack)
REMAP: Cursor + Grok
HITL: send / pay / deploy / book / publish stay Evens
DONE: smallest checkable dry-run on disk — not a vendor clone
```

## 3 ICPs (Q8)

| icp | Who | What they run |
|-----|-----|----------------|
| **hive-os** | Evens / desks when this trigger fires | Run the card on Cursor + Grok. Do not clone the YouTuber. |
| **golden-test-loop** | Desk that owns `golden-test-loop` | Same steps as a fragment — parts, not a vendor clone. |
| **watchdog-hitl** | Watchdog / HITL Operator | Hard steps stay Evens. This skill is post-PROVEN; do not self-GRADE. |

## Steps

1. Hold the primitive: eval-then-wrap-tools
2. IF class is not website → do not invent landings
3. IF fragment matches existing slug → merge
4. IF ask is vendor install / $ as FACT → refuse
5. eval-then-wrap-tools

## Stop

Send / pay / deploy / book / publish = Evens. Install / stack flip = ASK.

## Never

send / pay / deploy / book / publish stay HITL · do not file as website-assets · do not mint a duplicate skill · Claude Sonnet install · n8n-cloud flip · Build ANYTHING as a hive SKU · new icp · Claude Cowork/Code, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus · website-assets dump / landing from a non-landing machine · hand-edit ~/.grokbot · send / pay / deploy / book / publish
