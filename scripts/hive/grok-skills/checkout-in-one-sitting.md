---
name: checkout-in-one-sitting
description: >-
  Distro on the clock → thin offer → site+Stripe → self-test pay
  path → 48h card-test → opportunity-cost kill. $1 ≠ PMF. Path C
  only. Cursor plus Grok Bot. Status WIRED.
---

# Checkout in one sitting

**Owner:** Product GTM (clock + offer) · Forge (thin V1) · Money Desk (card-test vs revenue) · Publishing (effort launch). **Stack:** Cursor + Grok Bot.  
**Cursor copy:** `.cursor/skills/checkout-in-one-sitting/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/checkout-in-one-sitting/SKILL.md`  
**Status:** WIRED 2026-08-14 from `I7mpF7_pnPM` (Ty Chen). Not accepted forever.  
**Workflow:** `knowledge/workflows/checkout-in-one-sitting.md`

**Source (caption-only):** `I7mpF7_pnPM` — 10h = product + brand + site + checkout + marketing, not waitlist; sell/audience before build; $1 / 7 days; self-test pay→login; 48h: 2 paid $1 (not “revenue”); kill vs Autoplay opportunity cost. Visual/click **UNKNOWN**. Abacus / betting SKU operate-never. Tape $ UNVERIFIED.

**Dissent (do not flatten):**
- `paid-slice-funnel` = pain → thin V1 → checkout HITL → dual smoke. This skill adds **distro on the clock**, **$1 card-test ≠ PMF**, **kill on opportunity cost**. Same family, extra steps. Labeled merge is not enough — Evens un-hid this machine.
- “Strangers pay in 10 hours” ⊥ 2 network/$1 after 48h. Keep both.
- No new `icp_id`. Path A parked. Our Stripe only if Evens names Path C.

## When

Path C / our paid surface. Evens wants a **same-sitting** checkout proof with distribution on the clock — not a waitlist-only demo. Not a client hunt.

## Card

```
CLOCK: includes tease + site + checkout + launch (not “market later”)
OFFER: one promise · narrow span (≤10y if age) · $ UNVERIFIED
PAY PATH: self-test pay → login → core action
LAUNCH: effort post (copy what already works) · network drafts HITL
48H: count cards, not stories
KILL: ask-principal if main machine is dearer
```

## Steps

1. Tease first (draft only). Name/logo fast. Narrow the ICP span — do **not** mint a new `icp_id`.
2. Thin site + checkout via `slice-build` + `paid-slice-funnel`. Stripe/domain HITL.
3. Self-test the pay→login→core path on preview **and** domain when Evens has them. Watchdog lists blockers.
4. One overnight proof if the product claims accuracy (draft metric). Not PMF.
5. Effort launch: copy a working post structure + face/proof. URL in comments not body is his scar. Network templates = draft (`send-removed`).
6. 48h hands-off: count **cards**, not “revenue.” $1 = someone typed a card.
7. Kill vs pursue: `ask-principal` if a main machine (Path C hive-os) is dearer. Same data can be a go if we have zero.

## Stop

Stripe live · DNS · publish · send network blasts · new ICP · pay = Evens.

## Never

Abacus/Claude install · pred-market SKU · quote $100k-mo kids / 2×$1 as FACT · auto-DM · unpark Path A
