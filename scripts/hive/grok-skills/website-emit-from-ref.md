---
name: website-emit-from-ref
description: >-
  Run the emit walk. Q4 detector decides clone / reconstruct-from-trace /
  file-a-reference — 1, 2, or all 3 can fire in one sitting for different
  jobs. Website emits land in website-assets. Tools → tool-captures.
  Skills → skill-candidates. Same spine is a system-upgrade. Not any-repo
  / any-skill → landing. Cursor plus Grok Bot. Status WIRED.
---

# Website emit from ref

**Owner:** Forge (clone + file) · Creative Studio (identity + verticals). **Stack:** Cursor + Grok Bot.  
**Cursor copy:** `.cursor/skills/website-emit-from-ref/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/website-emit-from-ref/SKILL.md`  
**Status:** WIRED 2026-08-17 from `86HM0RUWhCk` sitting + grill Q1–Q9. Q4 detector locked same day (`Tliio-33w4g` non-website proof). Not accepted forever.  
**Landing repo:** `docs/hive/outer-heaven/CONTENT/creative/website-assets/` (monorepo folder — no nested `git init`).  
**Parents:** `cinematic-recipe` (always-on) · `slice-build` · `ingest-is-steps` · `skill-from-session` · `capability-acquisition` (both mint gates).

**Source:** today = ingest `86HM0RUWhCk` → `4822` HIS living/ref → `4821` ours file-clone → `website-assets` `00–07` + `4831` canonical. Headed shots UNKNOWN. Registry TESTING ≠ PROVEN.

**Dissent (do not flatten):**
- Q1 misread ⊥ this skill. Every GitHub repo / every skill zip as a `website-assets` landing would mess up everything. This skill is **this website walk only**.
- `url-habit-router` still classifies Friday’s *other* signal. First file for a non-website signal is **not** `website-assets/README`.
- Nate Claude Code / Claude Design = operate-never. Remap first. Install X only if ASK + necessary-to-implement; record if you install.
- Glow on HIS chrome stays HIS. Glow banned on *new* ours unless Evens orders exact-copy chrome.
- Watchdog GRADE is a **later sitting**. Builder does not fill GRADE (`separate-verifier`).
- `screenshot-brand-loop` is TESTING. This skill does not promote it to PROVEN.
- `FFWtxjvW2ts` clone-competitor-feel (Glido from Lemon) = **HOLD**. Path A parked until Evens names the client. Do not fire a client clone from this tape. Brand-onto-their-feel stays a method, not a sitting.

## When

Evens names a **website emit** (living URL, local ref, or “run the website emit walk again”) and the job is to file a **buyable option** in `website-assets`.

Not when the signal is a tool, a Claude skill zip, a GitHub repo of a procedure, or “file this as a landing.” Those go to `tool-captures/` or `skill-candidates/`. Q1 stays closed. The **Q4 detector** still runs on those classes (1, 2, or all 3 methods) — it does not route them into `website-assets`.

## Card

```
DETECTOR: LIVING_URL? · EMIT_CLASS? · PROCEDURE? · COPYABLE? · ONLY_NEED_SOURCE?
METHODS: clone | reconstruct-from-trace | file-a-reference  (1, 2, or all 3)
REF: living URL · public repo · or ACTION TRACE
OURS: emit into the typed folder for this class
LANDING: CONTENT/creative/website-assets/  (website emits only · buyable options)
TOOL: CONTENT/knowledge/tool-captures/     (not website-assets)
SKILL: CONTENT/knowledge/skill-candidates/ (3 ICPs on the card · not STEAL_SHEET)
SERVE: bash serve.sh · 127.0.0.1 only
VERTICALS: 8 Path C options · never 08-repo / 09-skill
VERIFY: later sitting · Watchdog GRADE later · TESTING ≠ PROVEN
```

## Rules from grill Q3–Q9 (2026-08-17)

- **Q3 system-upgrade.** One spine. The point of the walk is a **system-upgrade** (website / tool / skill are inputs that can upgrade the system). Not four snowflake machines. Do not invent a new desk.
- **Q4 detector (locked 2026-08-17 · replaces ASK-then-pick-ONE).** Clone, reconstruct-from-trace, and file-a-reference can all fire in **one sitting for different jobs**. Run the table. Do not ask Evens to pick a single method and stop. ASK only when the table is ambiguous (two living URLs, or clone would install a vendor).

```
LIVING_URL?        yes → REFERENCE (file the living emit into refs/ or the typed folder)
EMIT_CLASS?        website / tool / skill / system-upgrade (can be more than one)
PROCEDURE?         yes → TRACE (LEARNED + HOW THEY BUILT + ACTION TRACE)
COPYABLE?          public template / living HTML → REFERENCE + optional CLONE into the typed folder
ONLY_NEED_SOURCE?  yes → stop after REFERENCE · no → also TRACE and/or CLONE
```

  Website clone still needs a living URL. Tool/skill clone = file-clone of *shape* into `tool-captures/` or `skill-candidates/`, never `website-assets`. Write `DETECT.md` on the packet.
- **Q5 install.** Not never and not casual. Remap first. Install X only if ASK + necessary-to-implement. Record if you install.
- **Q6 folders.** Website emits stay here. Tools → `CONTENT/knowledge/tool-captures/`. Skills → `CONTENT/knowledge/skill-candidates/`. Desk proposes the slug; Evens can override.
- **Q7 done-check (“we can do this again”).** (1) we can **use the website** (on a port with all its variations); (2) tested skills or tools **upgrade the system or the businesses or my business**; (3) workflows are **tested and can finish a job/task**. UNKNOWN stays first-class. Do not mark `screenshot-brand-loop` PROVEN from this walk.
- **Q8 variations.** Website → 8 Path C options (done: `00–07`). Tool capture → 3 wrappers (hosts/adapters) when Evens is selling that emit. Skill → 3 ICPs on the skill-candidate card. Do not edit STEAL_SHEET.
- **Q9 mint gates.** Both `skill-from-session` and `capability-acquisition` last-mile can apply. Do not pretend one killed the other. **Mint law (2026-08-17):** after PROVEN, auto-mint the distinct slug — class=system-upgrade no longer skips. This walk still does not drive-by mint TESTING cards (`screenshot-brand-loop`). No website-assets skill dump.

## Steps

1. **Run the Q4 detector.** Fill `LIVING_URL?` · `EMIT_CLASS?` · `PROCEDURE?` · `COPYABLE?` · `ONLY_NEED_SOURCE?`. Fire 1, 2, or all 3 methods for *different jobs*. Write `DETECT.md`. ASK Evens only if the table is ambiguous or a clone would install a vendor.
2. **`cinematic-recipe` always-on** when the emit is a website / webapp bite. Webapp bite (public / Path A / Path C / local) → that skill or the sitting is not done. seedance / slop roast / worlds stay available. Today’s shader bgs do not waive this. Do not fire cinematic-recipe on a tool or skill zip.
3. **Run every method the detector fired.**
   - Clone: living URL or public template. Website = file-clone chrome (HIS glow stays on the HIS copy). Tool/skill = file-clone *shape* into the typed folder — not a vendor drop-in.
   - Reconstruct-from-trace: timestamps / state transitions (`ingest-is-steps` + ACTION TRACE). Do not invent a page or clicks.
   - File-a-reference: store the living/ref copy as a dated reference. That is not “learned.”
4. **Whitelabel identity** (website emit). Mark, name, two-path copy, sample stats. Navy chassis stays unless he orders a different chassis. Glow banned on *new* ours unless he orders exact-copy chrome.
5. **File in the typed folder.** Website → `website-assets/` (buyable options a client can buy / we customize). Tool → `tool-captures/` (desk proposes slug). Skill → `skill-candidates/` (desk proposes slug). Never any-signal → website-assets. Never `variations/08-repo` or `09-skill`.
6. **Variations (Q8).** Website: optional Path C options — copy `_base` or add a `_build.py` row. Same chrome, new identity + one background. Tool (when Evens is selling that emit): 3 wrappers (hosts/adapters). Skill: 3 ICPs on the candidate card — not STEAL_SHEET.
7. **Serve 127.0.0.1** (website). `cd …/website-assets && bash serve.sh`. Gallery `4823`. Verticals `4824–4830` + `4831`. Never bind 0.0.0.0. Never kill `4017` / `4821` / `4822`. `4821` / `4822` are dated dry-run refs. `4831` is asset-folder canonical for `00-outer-heaven`.
8. **Install X?** Remap first. Only if ASK + necessary-to-implement. Record the install. Not casual. Not never.
9. **Stop.** Clock parked unless he names a client. No deploy. No GitHub create. No commit unless he asked. No STEAL_SHEET. Watchdog / GRADE = later sitting. Curl 200 is not `verify-after-browser`. Do not claim “we can do this again” unless Q7 holds.

## What I am not thinking (plan leftover)

This skill does not make TESTING into PROVEN (Q7). A second desk has not walked it. Folder survives the chat. A tool or skill zip is not a landing.

## Stop

Prod domain / Stripe / publish / deploy / book = Evens. Preview on 127.0.0.1 is not production. Client buy / deploy = HITL.

## Never

Any-signal → `website-assets` landing · `08-repo` / `09-skill` fake verticals · skip the Q4 detector / force a single method when the table fires more · Claude Cowork/Code, Codex, ChatGPT, Gemini, Coda, Vapi, Abacus · glow on new ours unless exact-copy ordered · nested `git init` / `gh repo create` / push · hand-edit `~/.grokbot` · builder fills GRADE · promote TESTING → PROVEN from this walk · edit STEAL_SHEET · drive-by mint · send / pay / deploy / book / publish

## Shard-2 labeled merges (2026-08-17 · shard 2)
Labeled merge, not a new skill. Caption-only. $ UNVERIFIED. Evens can override slugs (Q6).
- `tFFKuq2t0rI` — Base44 is a builder, not a landing machine we lack. One spine. Glow banned on new ours. n8n+Base44 operate-never as stack flip. Operate-never: Base44 install · 8-vertical farm from this tape · glow on new ours.

## Repeat-signal leftover sweep (2026-08-17)
Labeled merges, not new skills. Caption-only. $ UNVERIFIED. Claude/Vapi/n8n-cloud operate-never.

| tape | fragment |
|------|----------|
| `2073320835658772937` | `website-emit-from-ref` |
| `2075545587093807111` | `cinematic-recipe` |
| `2077199463278395732` | `cinematic-recipe` |
| `2082070276112564256` | `cinematic-recipe` |
