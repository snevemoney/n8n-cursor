---
name: tape-self-teach
description: >-
  After a new L2 video, each desk studies full.txt with Deep Video
  Learning (A–K globally, then steal the machine, then L). Evens skipped
  merge 2026-08-14; takes stay SSOT. Do not merge LESSONS-FROM-TAPE.md.
  Use when Researcher finishes a packet or Watch Later lands a new
  caption file. Not a morning re-read. Not Cursor writing 17 hats.
---

# Transcript self-teach

**Stack:** Cursor + Grok. Captions only — not CHAPTERS, not the steal sheet, not the cursor-draft.

**Study method:** `deep-video-learning` — this protocol is how you study a tape. Old short steal/never take is not enough.

**Scoreboard shell:** `docs/hive/outer-heaven/CONTENT/job-cards/LESSONS-FROM-TAPE.md`  
**Canonical takes:** `docs/hive/outer-heaven/CONTENT/job-cards/takes/{video_id}/{slug}.md`  
**Global store:** `docs/hive/outer-heaven/CONTENT/watch-later/packets/{id}/LEARNED.md`  
**Not canonical:** `LESSONS-FROM-TAPE.cursor-draft.md`  
**Captions:** packet `full.txt` (+ `.vtt`). Legacy: `~/.grokbot/research-packets/**/transcripts/{id}/full.txt`

Evens is the visionary. Desks cowork. Cursor does not pre-vote the never-list.

## When

One new `full.txt` exists. Do not re-walk the old 18 on a morning routine. Do not re-walk the Nate 82 unless Evens says.

**Fleet walk (operator-ordered):** Evens says spawn the desks / tape walk in Cursor **and names a `video_id`**. Each desk writes **only** `takes/{video_id}/{slug}.md`. Evens skipped merge 2026-08-14. Takes stay SSOT. Do not merge. Do not ask again. Do not Load the cursor-draft. Do not flatten to CUT.

**Default path (Cursor):** skill `hive-spawn-desks` — `python3 scripts/hive/cursor-spawn-desks.py --job tape-self-teach --video-id <id> --write` then 17 `Task`s in this chat. Never Grok Bot.

**Optional (Grok):** `python3 scripts/hive/grokbot-dispatch-tape-self-teach.py` — only if Evens names Grok Bot.

## Steps

1. **Read** the entire `full.txt` (chunk if huge). Fetch captions first if missing. Skip = fail. Ugly tapes stay in the room. Never steal first / skip the transcript.
2. **Load** `scripts/hive/grok-skills/deep-video-learning.md`. Reconstruct **A–K** globally. Role does not filter what you learn. Never skip a section because “not my job.”
3. **Steal / Operate-never** — after A–K, before L. Steal the machine (workflow, loop, checkable stop, questions, procedures, examples) informed by A–K. Operate ≠ learn: payload can stay operate-never; the machine still gets stolen. Never “understand only, don’t steal.” Never a one-line skim steal.
4. **L last** — role-specific only. Take file = header + A–L + Steal / Operate-never.
5. **Researcher + Librarian** merge A–K **and** stolen machines (SOURCE / INFERENCE tagged) into packet `LEARNED.md`. Do not flatten disagreements. Do not copy L.
6. **Researcher emits** layer-2 atoms, then `capability-acquisition` (six + upgrade/opportunity). Status UNTESTED. Do not auto-write SKILL.md. Do not compile a mega-workflow.
7. **Park** — learning ≠ hunt. Do not add an ICP or a `business-lanes.json` row. Pointer to `steal-usecases` / `steal-sheet` only after the Steal block exists.

## Never

Daily full-corpus re-read · re-walk the Nate 82 unless Evens says · brief VTT dump · understand-only · steal-first / skip transcript · one-line skim steal · auto-write every proposed `SKILL.md` · clone the YouTuber into a desk · dump 146 capabilities · quote transcript $ / job-loss % as FACT · rotate off the live hunt because a video was interesting · skip a tape because Cursor wrote CUT · operate farms / OTP / fake identity / mass-DM · merge `LESSONS-FROM-TAPE.md`
