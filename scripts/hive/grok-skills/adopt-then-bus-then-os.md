---
name: adopt-then-bus-then-os
description: >-
  Adopt the existing hive as the agentic OS: one interview, path
  configs, file signal bus. Mouth and face attach later. Use when
  Evens names fullstack-agent, Jarvis stack, or upgrades the OS
  with voice/face. Do not install Claude Code. Cursor plus Grok.
---

# Adopt, then bus, then OS

**Owner:** Forge (adopt) · Watchdog (GRADE) · Librarian (index).  
**Status:** WIRED 2026-09-04 from `github.com/jaredrhod/fullstack-agent` pattern port. Not accepted forever.  
**Cursor copy:** `.cursor/skills/adopt-then-bus-then-os/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/adopt-then-bus-then-os/SKILL.md`  
**CLI:** `python3 scripts/hive/os/agent-stack.py adopt|validate|bus-write|bus-read`  
**Parent:** `internal-host-os` (labeled merge — this is the conductor + bus, not a second vault).  
**Also:** `observe-pane` · `session-bootstrap` · `voice-four-pieces-three-doors` (mouth later).

**Source:** public repo + README (observed). Tour tape `FiOTrxq9ckM` caption-only / unwatched this sitting. AGPL upstream — pattern rewrite, no vendor.

## When

Evens names fullstack-agent / Jarvis / memory+voice+face / “make it the agentic OS.” Not a client SKU. Not a second dashboard (Serena already removed).

## Card

```
KIND: agentic-os
VAULT: adopt via vault-config.py (never create)
BUS: .hive/bus/state.json  phase=idle|listen|think|speak
JOB: observe-pane working|yellow|done
PERMISSION: ask
HOSTS: cursor + grok
PARK: mouth · face · hands
NEVER: claude-code · second-home · live-/
```

## Steps

1. Spectacle test — this buys a bus the existing OS can share. If you were about to clone four repos or write `~/my-agent/CLAUDE.md`, stop.
2. `python3 scripts/hive/os/vault-config.py --resolve` — one vault.
3. `python3 scripts/hive/os/agent-stack.py adopt` — writes `.hive/agent-stack.json` + idle bus.
4. `python3 scripts/hive/os/agent-stack.py validate` — must say `kind=agentic-os`, memory=adopted, permission_mode=ask.
5. Watchdog GRADE those two files. Builder does not self-PROVEN.
6. Mouth sitting (later): PTT → local STT → Cursor SDK or one Grok desk turn → TTS. Writes the bus. Auto-approve stays off unless Evens flips it.
7. Face sitting (later): localhost pane reads the bus. Prefer upgrading `obsidianOS` / observe-pane, not a circuit-board clone. `cinematic-recipe` applies to that webapp bite.

## Reproduce (Forge)

```
python3 scripts/hive/os/agent-stack.py self-test
python3 scripts/hive/os/agent-stack.py adopt
python3 scripts/hive/os/agent-stack.py validate
```

No mic. No face server. No billed ElevenLabs.

## THINK / BEHAVE / TRICKS / USE

- **THINK:** Adopt before install. Identity and vault are never rebuilt. One interview so later pieces never re-ask.
- **BEHAVE:** Wire by paths, not relocation. Replacements retire in place; nothing the person built is deleted.
- **TRICKS:** “The voice writes little status notes; the face reads them.” Permission default = ask out loud.
- **USE:** `agent-stack.py` is the conductor. Desks read `agent-stack.json` + bus the way they already read `jobs.json` and `hot.md`.

## Never

Claude Code / Cowork / Codex as host · vendor AGPL trees into `apps/` · second vault · extra dashboard · live `/` · send / pay / deploy / book / publish · self-PROVEN
