---
name: ingest-is-steps
description: >-
  Ingest (especially a build tape) is a numbered procedure to walk.
  If they built a UI and used design skills, run that process on
  Cursor + Grok. Use when studying a build tape, stealing a UI, or
  the operator says ingest is steps / steal their design process.
  Not a thesis-only steal. Not one-shot HTML.
---

# Ingest is steps

**Owner:** Researcher (classify + D) · Forge (walk D) · Creative Studio (design loop). **Stack:** Cursor + Grok Bot.  
**Cursor copy:** `.cursor/skills/ingest-is-steps/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/ingest-is-steps/SKILL.md`  
**Status:** WIRED 2026-08-15 from `njHuj8OxIVI` sitting (thesis steal, then folder browser — both failed). Not accepted forever.

**Source:** Evens rule after the command-center sitting. Tape `njHuj8OxIVI` proved the gap: LEARNED §D existed; implement did not walk it.

**Dissent (do not flatten):**
- `deep-video-learning` already says preserve order in D. This skill is **implement time**: walk those numbers. Not a second A–K.
- `slice-build` = one system per sitting. This skill runs **before** implement when the sitting is “build what this tape built.”
- `cinematic-recipe` = 3 motion refs + video hero. Command-center glass is a UI slice, not a cinematic landing. Same pick-one family as five mockups / three versions.
- `internal-host-os` = wrapper last. When Evens says build the wrapper, walk tape D — do not invent a folder browser.
- Caption-only: spoken steps only. Do not invent clicks or looks they did not say.

## When

A tape (or packet) **builds** something, or Evens says ingest is steps / they built a UI / steal their design process. Thesis tapes stay on the normal steal path.

## Card

```
TAPE-KIND: build | thesis
D-STEPS: numbered, in order (from LEARNED §D)
DESIGN: yes | no
HIVE-HOST: Cursor + Grok
CAPTION: spoken steps only
```

If `DESIGN: yes`: refs + N distinct variations + pick + double-down **before** implement. Floor after `FFWtxjvW2ts`: **two distinctly different** impressive variants, then pick. One HTML is a skip.

## Design lane (2026-08-16 · business-operations-and-beautiful-webapps)

When the signal is UI/design: same packet / atoms / LEARNED, plus a DESIGN block (refs → N mockups → pick → double-down). Same steal machine — add design. Not a second skill.

**Anti-slop seven (2026-09-02 · `FFWtxjvW2ts`):** if the tape (or Evens) is building a marketing site, D must include or cite: brand · Three Ps · scroll lock · named inspiration · grab components · mobile · screenshot-verify scroll. “Make it premium” with no living object is not a DESIGN ref. Two-distinct-then-pick is this loop (Ferna vs Lowkey on-tape). Clone-competitor-feel HOLD until Evens names a Path A client. No new Scrollcraft skill.

## How they built (2026-08-16 · website-selling brain)

Every build tape (product / tool / workflow / website — not only websites) gets a LEARNED section. UI/design still requires the DESIGN block. HOW THEY BUILT is the build-method steal. See `CONTENT/knowledge/training/SIGNAL-BUILDS.md`.

```
## HOW THEY BUILT
- product/tool/workflow/website:
- steps they showed:
- tools they used (on-tape vs we already have):
- what we can emit from our factory:
```

Demonstration tapes **require** (append; do not rewrite DESIGN / HOW THEY BUILT). UNKNOWN is a valid value. Do not invent clicks. Semantic goals + expected result, never x,y.

```
## ACTION TRACE
- timestamps (said / clicked / typed / changed) — or UNKNOWN
- state transitions (need → action → result) — or UNKNOWN
- UNKNOWN blockers (KNOWN / OBSERVED / UNKNOWN / HYPOTHESES / BLOCKING?)
```

```
## CAPABILITY CARD
CAPABILITY / SOURCE / DEMONSTRATED OUTCOME / PREREQUISITES / WORKFLOW
OBSERVED TRICKS / DECISION RULES / FAILURE MODES
SYSTEM PRIMITIVE / SKILL CANDIDATES / SYSTEM UPGRADE CANDIDATES
```

Write or update `knowledge/capabilities/{id}.json` at status **LEARNED** (knowledge exists) or **TESTING** (reproduced partly). Never PROVEN without a verify. See `SIGNAL-BUILDS.md`.

## Steps

1. After A–K, classify `TAPE-KIND`. If they assembled, installed, mocked, shipped, or walked a build — it is `build`.
2. Write `LEARNED.md` §D as a **numbered runbook**. Steal lists those same numbers. A one-line thesis (“vault is a map”) is not steal on a build tape.
3. If they used design skills (refs, N mockups, pick, double-down, motion, glass, tick): set `DESIGN: yes`. Remap on-tape Claude Design / Pinterest to Creative + `slice-build` bible + `cinematic-recipe` refs. Do not install the on-tape design tool.
4. Implement walks D in order. Vendors remapped to Cursor + Grok. Skip a spoken step only if Evens names the skip.
5. Caption-only: steal spoken design steps. Looks they named (glass, tick, stars) may land. Clicks / layouts they did not say stay UNKNOWN.
6. One system (`slice-build`). Watchdog grades. Builder does not fill GRADE.

## Stop

D numbered · steal cites D · design loop done if `DESIGN: yes` · one slice on disk. Voice / plugin / deploy stay Evens.

## Never

Thesis-only steal on a build tape · one-shot HTML when they showed a design loop · skip two-distinct-then-pick · invent a folder browser instead of walking D · install Claude Design / Claude Code / Fable / Scrollcraft · invent clicks from captions · sell the factory · send / pay / deploy / book / publish
