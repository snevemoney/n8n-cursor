---
name: specialist-handoff
description: >-
  Named specialists in a group chat: dump + reference, classify
  WAKE, written handoff, draft ≠ send. On-stack Grok Bot. Cursor
  plus Grok. Status WIRED.
---

# Specialist handoff (group chat)

**Owner:** Communications Manager (inbox/invoice drafts) · Day Planner (meeting→tasks) · HITL Operator (send gate) · Creative (photo→ad skill). **Stack:** Cursor + Grok Bot.  
**Cursor copy:** `.cursor/skills/specialist-handoff/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/specialist-handoff/SKILL.md`  
**Status:** WIRED 2026-08-14 from `lRUpu2-KtGQ` (Brock Mesarich). Not accepted forever.  
**Workflow:** `knowledge/workflows/specialist-handoff.md`

**Source (caption-only):** `lRUpu2-KtGQ` — specialists text each other; inbox ↔ chief of staff; invoice event trigger; “I send it myself”; skill zip = markdown recipe; YouTube scrape blocked → RSS; Zapier MCP when plugin missing. Visual/click **UNKNOWN**. On-stack tape (Grok Bot). Claude Cowork / Gmail send / new ClickUp operate-never.

**Dissent (do not flatten):**
- `inbox-to-task-routing` · `invoice-email-automation` · `product-ad-from-photo` · `hosted-neq-scheduled` · `send-removed` are **parts**. This skill is the **handoff machine** (two named desks + WAKE + written sentence). Merge list is not a compiled funnel — Evens un-hid this.
- “Send emails on my behalf” ⊥ invoice path “draft so I can send myself.” Hive = draft.
- Title “Cowork killer” ⊥ “I already do this in Claude Cowork.” Delta he names = group chat + event triggers.
- Firecrawl $19 / 86 Higgsfield tools UNVERIFIED. No new vendor from this tape.

## When

Two hive desks must hand off (inbox→invoice, meeting→day-plan, photo→ad) and Evens wants a **written** handoff, not one omni-agent.

## Card

```
SPECIALISTS: two slugs (not a new desk)
WAKE: scheduled | event (mail/Slack) — hosted-neq-scheduled
REFERENCE: one artifact (PDF, photo, dump)
HANDOFF: one sentence who owns what
OUT: draft only
SEND: Evens
```

## Steps

1. Name two existing specialists. One dump + one reference artifact.
2. Classify WAKE: daily schedule vs event. `hosted-neq-scheduled`. Default no new host.
3. Write the handoff sentence in the group/thread (who flags, who drafts).
4. Draft on disk. Outbound is **Gmail SMTP** from env (`SMTP_HOST` / `SMTP_USER` / `SMTP_PASS` in `.env.dev` — named, not pasted). Not Gmail MCP. **Send-removed.** Pass ≠ send. SMTP cannot leave a draft in Gmail; it only sends when Evens says send.
5. Product still: photo → `product-ad-from-photo` (Higgsfield already on Creative). Publish HITL.
6. If scrape needed: `filter-then-llm` first. New vendor = Evens.
7. Meeting notes → Day Planner tasks. No Granola SKU from this tape.
8. ClickUp / Zapier / Firecrawl subscribe = `ask-principal`.

## Stop

Gmail send · auto-charge · new ClickUp · publish · pay = Evens.

## Never

Claude Cowork · auto-send · clone five new desks · quote $19 · invent plugin clicks
