---
name: seedance-site
description: >-
  Award-site refs → one continuous ≤30s 720p Seedance plate →
  split frames → page scrubs on scroll → CTA-before-scroll roast.
  Use when wrapping a cinematic “3D” landing. Not muted-video-hero
  only. Cursor plus Grok Bot. Status WIRED.
---

# Seedance site (scroll-scrub plate)

**Owner:** Forge (wrap + roast) · Creative Studio (refs + plate). **Stack:** Cursor + Grok Bot.  
**Cursor copy:** `.cursor/skills/seedance-site/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/seedance-site/SKILL.md`  
**Status:** WIRED 2026-08-14 from `gt8k4bA01Mo` (Zubair Trabzada). Not accepted forever.  
**Workflow:** `knowledge/workflows/seedance-site.md`

**Source (caption-only):** `gt8k4bA01Mo` — Awwwards / igloo / Hubtown as refs; Seedance 2.5 continuous ≤30s 720p; “this is a video, just divided into multiple frames”; CTA visible before any scrolling; localhost:4995 narrated. Visual/click **UNKNOWN**.

**Dissent (do not flatten):**
- `cinematic-recipe` = 3-ref bible + muted **video element** hero. This skill = **one continuous plate split to frames the page scrubs**. Same family, different wrap.
- `motion-grade-pipeline` = still → video → upscale → color → timing. This skill consumes that plate; it does not replace the grade.
- “One single prompt” ⊥ prompt-pack + connector-on + model pick + minutes of generation. Keep both.
- Claude Code / desktop / Jarvis / paid community = operate-never. $10k / $28.5M / 153 mph UNVERIFIED.
- `iRBs8PCBCaA` world-engine ≠ this page wrap.

## When

Evens wants a cinematic landing that **feels 3D** via scroll-scrub of one plate. Path C preview or a named page. Not WebGL. Not Genie.

## Card

```
REFS: 1–3 award pages + signature motion named
PLATE: one continuous ≤30s 720p (not stitched shorts)
WRAP: frames + scroll-scrub · prefers-reduced-motion → poster
ROAST: CTA visible before scroll, repeated after proof, unique color
VERIFY: click-live-site → verify-after-browser
DEPLOY: HITL
```

## Steps

1. Creative: pick 1–3 Awwwards / award pages. Name the signature motion (infinite zoom, follow-cam, glass burst). Taste on a strong model. No code yet.
2. Connector already authorized (hive: Higgsfield on Creative). If off → do not prompt.
3. Generate **one continuous** Seedance plate (≤30s spoken; 720p load). Reject stitched shorts.
4. Split to frames **after** the plate exists. Forge wrap: scroll scrubs frames. `prefers-reduced-motion` → poster.
5. Roast live preview: CTA before scroll? Repeated after proof? Unique color? Then `verify-after-browser`.
6. Localhost / preview ≠ prod. Deploy / domain / Stripe = Evens.

## Stop

Prod domain / publish / pay = Evens.

## Never

Claude Code/Cowork · Jarvis · quote $10k/$28.5M as FACT · WebGL-as-default · stitched-shorts as “continuous” · invent Higgsfield OAuth clicks · send / pay / deploy / book / publish
