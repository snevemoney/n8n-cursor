---
name: skill-from-session
description: >-
  Winning Cursor/Grok session → named SKILL.md in three places.
  Plan-first, explain-don’t-recommend, folder survives new-chat.
  Not session-bootstrap. Cursor plus Grok Bot. Status WIRED.
---

# Skill from session

**Owner:** Forge (write the skill) · Librarian (index) · Big Boss (what becomes a skill). **Stack:** Cursor + Grok Bot.  
**Cursor copy:** `.cursor/skills/skill-from-session/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/skill-from-session/SKILL.md`  
**Status:** WIRED 2026-08-14 from `U6k4MeVks_Y` (Chase). Not accepted forever.  
**Workflow:** `knowledge/workflows/plan-mode-objective.md`

**Source (caption-only):** `U6k4MeVks_Y` — skill-creator is the meta-skill; “take a look at the entire message history… turn that into a skill”; skills = more deterministic; plan mode + “what am I not thinking?”; don’t smash Recommended; new chat in the **same folder** ~30%; `/goal` only with objective success; other model grades. Visual/click **UNKNOWN**. Claude Code operate-never.

**Dissent (do not flatten):**
- `session-bootstrap` = one long dump, then short loops. This skill = **after a winning run, codify it**. Not the same steps.
- `checkable-stop` = DONE-CHECK + CAP + COST. Chase `/goal` is stricter on **objective end-state**. Merge the stricter done-when; unsupervised `/goal` stays never.
- `separate-verifier` = Watchdog grades. Chase Codex-on-tape = other-model grade. Hive = Watchdog, not Codex.
- `desk-wiki-before-work` / `wiki-ingest` = raw → wiki → index.md. Same file-shape; 8k Obsidian already dissent.
- Nate terminal-for-status-line (`vfWTyEreOEc`) ⊥ desktop-first. Keep both.
- 1000h / $20/$100/$200 / 103 agents / 6M tokens UNVERIFIED.

## When

A Cursor or Grok session just produced a **repeatable** result (site wrap, research pass, roast, checkout dry-run) and Evens wants it as a named skill. Not every chat.

## Card

```
SESSION: what we did (files on disk)
SKILL: slug + one-sentence when
PLACES: grok-skills + .cursor/skills + ~/.grokbot/skills
FUNNEL: hive-funnels row
INVENTORY: desk Tools
DONE-WHEN: objective, not “cool”
GRADE: Watchdog, not the builder
```

## Steps

1. Plan first. End the dump with “what am I not thinking?” Do not smash Recommended — ask it to **explain** the options (`session-bootstrap` dump is allowed; this step is the unknown-unknowns ask).
2. Work in a **folder**. If context rot (~30% his rule, not a bench) → new chat in the **same folder**. Files outlive the thread.
3. When the result is good: write `scripts/hive/grok-skills/{slug}.md` from the session (steps, card, never). Copy stub to `.cursor/skills/{slug}/SKILL.md` and `~/.grokbot/skills/{slug}/SKILL.md`.
4. Add hive-funnels row + inventory/desk Tools. Status WIRED, not accepted-forever.
5. Long-horizon only if done-when is **objective** (`checkable-stop`). Eternal = skill + log + routine Evens names. Unsupervised `/goal` never.
6. Watchdog grades (`separate-verifier`). Builder does not fill GRADE.

## Stop

Install Claude/Codex · 6M-token burn · accept-forever · send / pay / deploy / book / publish.

## Never

Remap this to “already session-bootstrap” · Claude Code/Cowork/Codex · quote tape $ · flatten vs Nate terminal
