---
name: api-macro-vision
description: >-
  API first (fast/cheap/consistent) → deterministic pixel macro if the
  path never changes → headed vision last. Use before a vision or
  computer-use agent, or when Forge opens a browser. Cursor plus Grok Bot.
---

# API → macro → vision

**Stack:** Cursor + Grok Bot. Vision is last, headed, watched.  
IF Cursor → `cursor-ide-browser`. IF Grok Bot → Grok Bot’s own web browser (Grok Bot path; do not call Cursor MCP). Not Chrome / Playwright / browser-use.

**Upgrade:** `UPG-nate82-api-macro-vision`  
**Sources:** `CB5bG4mvnS0` (timestamps UNKNOWN, caption-only)  
**Cursor copy:** `.cursor/skills/api-macro-vision/SKILL.md`  
**Grok `/` copy:** `~/.grokbot/skills/api-macro-vision/SKILL.md`

**Contradiction (keep labeled):** X-browser-despite-API is his cost/format exception. Do not quote ~95% / X fees as FACT. No Codex.

## When

Computer-use / browser jobs. Forge intake. Big Boss when someone asks for a vision agent. After a page ship, `click-live-site` is vision-last only if API/macro cannot click the CTA.

## Card (required before a vision agent)

```
LADDER: api | macro | vision
WHY: <why this rung>
WATCH: headed if vision
```

Cannot name LADDER → no vision agent.

## Steps

1. Prefer an API or repo CLI (`hive-state.py`, Gmail read, GitHub). Fast / cheap / consistent.
2. If the pixel path never changes, a deterministic macro beats vision.
3. Vision browser last, headed, watched — on the host path (Cursor → `cursor-ide-browser`; Grok Bot → Grok Bot web browser). Then `verify-after-browser`.
4. Watch ≥10 on money surfaces — and still do not drive send/pay/publish headed.
5. Saved login = keys (`vault-not-prompt`).

## Forge intake (the wired instance)

`slice-build` / `click-live-site`: write LADDER before opening the host browser. Localhost forms only for break-it. No Codex computer-use plugin. No Playwright/Chrome for hive click-verify. Grok Bot desks keep Grok Bot’s browser.

## Stop

Send / pay / deploy / book / publish = operator. Headed watch is not a close.

## Never

Vision-first when an API exists · Codex · Playwright/Chrome for hive click-verify · headed send/pay/publish · quote fee % as FACT · flatten his X-browser exception · Claude Cowork/Code, ChatGPT, Gemini, Coda, Vapi, Abacus
